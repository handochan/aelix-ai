# Batch #234 · #226 · #231 — progress / measurements (2026-09-06)

The independent batch `handoff-222-2026-09-06.md` §2 named while #230/#232 wait on the owner
(files disjoint: `tools/bash.py` / `oauth/_resolve_config.py` / `tui/completion.py`). Rule 3:
one issue = one worktree = one branch = one commit = one windows leg. Designs of record:
`234-design-2026-09-06.md`, `226-design-2026-09-06.md`, `231-design-2026-09-06.md` (+ the
`*-critique-resolution-2026-09-06.md` tables).

## 0. Baseline

- `main` at session start **`b94a6db`** (= `origin/main`; `fc567ad` + the #222 handoff commit).
- Worktrees: `/tmp/wt-234` `fix/234-bash-watcher-teardown-cancellation`, `/tmp/wt-226`
  `fix/226-resolve-config-tty-stop`, `/tmp/wt-231` `fix/231-completion-enumerator-contract`,
  all at `b94a6db`, plain `uv sync` (installs `aelix_server`, #224) — `import aelix_server,
  aelix_coding_agent` ok in each.
- Board: #234, #226, #231 moved Backlog → In progress (`gh project item-edit`, ids in the
  board memory).
- Codex: `codex exec --skip-git-repo-check --sandbox read-only "Reply with the single word OK."`
  → `ERROR: You've hit your usage limit … try again at Sep 7th, 2026 2:29 PM.` Every lane in
  this batch is Claude (opus); commit bodies say so.
- Full-suite baseline on `main`: not re-run yet (handoff: `fc567ad` 10237 / 12 in the #222 tree,
  10212 / 12 plain-sync). Deferred until the design lanes' timing probes are done — CPU
  contention skews the drain measurements (#221 review).
- Facts measured before the design lanes started:
  - `which fd` → not found on this box; `~/.aelix/agent/bin/fd` exists (auto-downloaded by the
    `find` tool). `tui/completion.py::_fd_binary` is `shutil.which` on the process PATH only,
    so the owner's TUI uses the `os.walk` fallback today (#231).
  - Pi (`/tmp/pi-src` @ `da840b6`): `getFuzzyFileSuggestions` returns `[]` without `fdPath`;
    `fdPath` comes from `ensureTool("fd")` at TUI mount. Pi's `!command`
    (`executeWithDefaultShell`) is `execSync` with no `detached` — the helper stays in Pi's
    own foreground group (#226).
  - `tests/` has no pty-based case at all (`grep -rl 'pty\.(fork|openpty|spawn)|TIOCSCTTY'`
    → nothing) — #226's controlling-terminal case is new ground.
  - The completion menu overdrawing the statusline (#231's "seen alongside") is **#133** (OPEN,
    owner-triaged post-beta) — the orchestrator's first search missed it; the #231 design lane found
    it. Attach the §E capture there, no duplicate.

## 1. Design + critique (workflow `wf_66f5459b-1e5`)

43 agents, 4.91 M tokens, 64.5 min, 0 nulls. Per issue: design (opus) → 3–4 critique lenses →
one refuter per BLOCKING/MAJOR (measurement) → revision 2 + resolution table.

| issue | findings | serious | upheld / refuted / partial | design |
| --- | --- | --- | --- | --- |
| #234 | 13 | 7 | 7 / 0 / 0 (+6 MINOR applied) | `234-design-2026-09-06.md` rev 2 (477 lines) |
| #226 | 24 | 10 (cap) | 6 / 0 / 11, 7 handed to synthesis as upheld-pending | `226-design-2026-09-06.md` rev 2 (556 lines) |
| #231 | 9 | ≤10 | see `231-critique-resolution-2026-09-06.md` | `231-design-2026-09-06.md` rev 2 (505 lines) |

Decisions of record (owner questions kept OUT of the code, defaults stated in each §G):
- **#234** — `await asyncio.wait([watcher_task])` + retrieval in a `finally`; a cancel landing after a
  TIMEOUT kill now wins over the `timed_out=True` report (owner question, default "cancel wins").
  Reach: only a caller that supplies a signal (`rpc_mode._handle_bash`) arms the watcher — measured
  0/524 lost with `signal=None`, 14/388 with an `AbortSignal`.
- **#226** — keep `process_group=0` (rationale corrected: the tty stays the *controlling* terminal so
  a read is a detectable STOP, not the silent keystroke theft `setsid` gives a by-path reader);
  detect `WIFSTOPPED` + `SIGTTIN`/`SIGTTOU` in a 50 ms poll, kill, and name the cause. 10.0 s →
  0.052–0.059 s (darwin + linux/dash in docker, py3.11/3.12). Option 2 (`tcsetpgrp`) rejected by
  measurement (sub-ms race; abandoned prompt leaves echo off; in the TUI Aelix itself stops in 21 ms).
  gpg-agent-mediated prompts stay invisible (10 s) — documented. Three owner questions defaulted.
- **#231** — (a) truthful docstrings + (b′) `_fd_binary()` looks in `get_bin_dir()` FIRST then PATH
  (the order `find`/`grep` and Pi use) + two one-liners (`--type l`; the shared exclude predicate
  applied to the walk arm too). (b) `pathspec` rejected (fd's ignore family is five rules, three of
  them outside `.gitignore`). Owner question: the `@` menu narrows on the owner's own box (12618 →
  1482 candidates); default "no toggle", follow-up issue for a `/settings` toggle.

## 2. Implementation + pre-merge review (workflow `wf_652e320d-610`)

- Filed **#235** (hooks site: same SIGTTIN/SIGTTOU stop, `ThreadedChildWatcher` blocks in
  `waitpid(pid, 0)`, full `timeout_ms` with no named cause — default 60 000 / max 600 000 ms) at the
  #226 fix lane's request, so ADR-0238's "What stays open" bullet can name it. Added to project 1.

36 agents, 4.36 M tokens, 103 min, 0 nulls. Per issue: implement (opus, RED-first, §F gate, full
suite) → 3 review lenses → refuters → verdict → fix lane → re-gate.

| issue | review findings | verdict | fix items | full suite after fix | commit |
| --- | --- | --- | --- | --- | --- |
| #234 | 11 (3 BLOCKING/MAJOR upheld) | FIX_FIRST | 13 | 10242 / 12 (369 s) | `0732bb4` |
| #226 | 16 (7 BLOCKING/MAJOR upheld) | FIX_FIRST | 17 | 10243 / 12 (370 s) | `e3e1eb9` |
| #231 | 16 (8 BLOCKING/MAJOR upheld) | FIX_FIRST | 18 | 10247 / 12 (353 s) | `0f89988` |

What the review caught (all measured by refuters; per-issue tables in `<n>-progress-2026-09-06.md`
§7): #234 — a CHANGELOG sentence inverting its own measurement, a stale mechanism description in
`tests/test_abort_lane_ab.py`, and a docstring claiming a `_must_cancel` delivery path the shipped
code cannot reach; #226 — ADR-0238 stating a rule ADR-0235 does not contain (BLOCKING; it came
from a Korean line in the design doc, now corrected there too), the `if pid == 0` guard and the
SIGTTOU arm unpinned by any test (mutants survived), a `-1` returncode rendering as `SIGHUP` next
to `(SIGTTIN)`; #231 — `fd.EXE` vs `fd.exe` case mismatch red on the windows leg (shutil.which
uppercases PATHEXT), a new absolute ("can never resolve to different binaries") measured false
(mode 0644 copy, a directory named `fd`), a non-raw docstring carrying a real lone surrogate,
ADR-0193 contradicting itself 130 lines apart.

**Incident:** replying to the #226 fix lane's request for the hooks-issue number (via
`SendMessage`) RESUMED that agent as a second writer in `/tmp/wt-226` while the workflow's own
fix lane was still applying the same list. The resumed instance noticed (a file changed between
two of its own calls), halted, and was told to stand down; integrity verified afterwards (every
marker string ×1, `ast.parse` OK, index untouched). Rule for next time: never `SendMessage` a
workflow lane that has already returned — it resumes it.

### Live checks (rules 9/10, by the orchestrator, darwin)

- **#234** rule 10: `-p` with openrouter haiku, `--permission-mode yolo`, one bash call →
  `hello-aelix234-live`, rc 0, 3.82 s, `pgrep aelix234live` 0. Rule 9 (tmux, branch TUI):
  `!sleep 5; echo done5` + Esc at 1 s → nothing at 3 s, `done5` at 7 s (Esc has no effect on a
  `!command` — `handle_user_bash` passes `signal=None`; the design's corrected expectation);
  `!echo ok` → `ok`; strays 0.
- **#226** (tmux controlling terminal `/dev/ttys027`, `AELIX_CODING_AGENT_DIR=/tmp/live-226/agent`,
  provider `ttyprobe` with `api: openai-completions` — the design's snippet lacked `api`):
  `main` `-p hi` **10.56 s**, `Failed to resolve API key … from shell command: …` with no cause;
  branch **0.42 s** + ` — The command stopped reading the terminal (SIGTTIN): …`. `!stty -echo`
  → SIGTTOU named, echo stays on (0.42 s). `trap '' TTOU; stty -echo; echo x` → resolves to `x`
  (401 from openrouter), echo left OFF, `stty sane` restores — the documented residue. Cached
  path: `auth.json` **in the agent dir** (the design's `AELIX_AUTH_PATH` arm loads nothing — that
  variable is not the storage path) → branch `Command '[…]' died with <Signals.SIGKILL: 9>. The
  command stopped reading the terminal (SIGTTIN): …` 0.42 s vs `main` `died with
  <Signals.SIGHUP: 1>.` 10.38 s. Happy path `!sh -c 'printf %s "$OPENROUTER_API_KEY"'` → real
  haiku answer `OK` 1.79 s. Rule 9 TUI: branch shows the full ~250-char cause on screen 2.5 s
  after Enter, no stall; `main` frozen ~10 s (keys typed meanwhile surface afterwards), then the
  causeless line. Not measured: the `ps … T sh` snapshot (my 3 s poll missed the window),
  gpg-agent-mediated prompts through the product (no secret key on this box).
- **#231** rule 9 (tmux, cwd = the owner's main checkout, branch binary): arm 1 (managed fd)
  `@completionpy` → 7 rows ending `…/tui/completion.py`, footer `(1/7)`, no worktree row;
  `@settingsjson` → empty menu; `@src/` → `aelix/`. Arm 2 (`~/.aelix/agent/bin/fd` renamed
  away) `@completionpy` → three `.claude/worktrees/aelix-{137,site,phaseB}/…` rows,
  `tui/completion.py` absent; `@settingsjson` → `.claude/settings.local.json` + `.omc/state/
  sessions/**` rows. `diff` non-empty (PASS); fd restored, `fd --version` 10.4.2. Menu/statusline
  overlap visible on both arms (#133). Rule 10: headless `-p "reply with OK"` → `OK`, rc 0.

### CI
Pushed: #231 `0f89988` run 34020351754, #234 `0732bb4` run 34020357516, #226 `e3e1eb9` run
34020414737 — **18/18 green on the first push** (ubuntu py3.11/3.12, windows py3.11/3.12, install.ps1
e2e ×2 per branch). Windows py3.11 legs: #231 **10191 / 68 / 0** (job 101451657034; log carries
`the walk cap dropped 19 of 27 paths with no signal to the user`), #234 **10186 / 68 / 0** (job
101451675070; `watcher-teardown cancel [normal/inline] 0.344s`, `[timeout/inline] 0.375s`,
`[normal/after_completion] 0.328s`, `teardown cancel with a holder: 2.329s on win32`), #226
**10187 / 68 / 0** (job 101451835193; `#226 reap-repair through _run_shell_command: 0.515s`).

### Merge
ff in the order #231 → #234 → #226: `0f89988` (ff), `818400d` (#234 rebased onto it — git
auto-merged CHANGELOG and `decisions/README`), `bcf057c` (#226 rebased — three doc conflicts,
all "both sides added adjacent text": CHANGELOG entries kept in order #231/#226/#234's `Fixed`,
ADR-0238 Status line now carries both amendments, the "What stays open" bullets and the two
Consequences paragraphs concatenated, the `decisions/README` 0238 row = #226's narrowed row +
#234's landing clause). Post-rebase quick gate on `bcf057c`: citations 851 none drifted;
`tests/test_citation_drift.py tests/test_docs_bundle_sync.py tests/oauth/test_resolve_config.py
tests/docs` 69 passed; `ruff check` clean. `main` pushed at `bcf057c`; CI run 34021348001.
Issues closed with run/job IDs in their comments; board Done ×3; remote branches, local branches
and worktrees removed. Follow-ups filed: **#235** (hooks stop), **#236** (`@` completion residues:
`/settings` toggle, `ensure_tool("fd")` at mount, non-UTF-8 walk arm, cap truncation visibility),
**#237** (OAuth `/login` `suppress(CancelledError, Exception): await manual_task` ×4).
Local full suite on merged `main` `bcf057c` (main checkout venv, `-p no:cacheprovider -q`): **10258 passed / 12 skipped / 0 failed** in 364.50 s. `main` CI run 34021348001: **6/6 success** — windows py3.11 10202 / 68 / 0 (job 101454436324), py3.12 10202 / 68 / 0 (job 101454436238), ubuntu py3.11/3.12, install.ps1 e2e ×2.
