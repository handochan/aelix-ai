# #222 — progress / measurements (2026-09-05 → 06)

Design of record: `222-design-2026-09-05.md` (revision 2). Critique resolution:
`222-critique-resolution-2026-09-05.md`. Worktree `/tmp/wt-222`, branch
`fix/222-bash-tool-process-tree`, base `main` `92aba6d`.

## 0. Baseline

- `main` at session start `86c750d` (= `origin/main`); moved to `92aba6d` by this session's own
  #229 (`475e4f6`) and #224 (`92aba6d`) merges. `/tmp/wt-222` rebased onto `92aba6d` before any
  edit; plain `uv sync` there installs `aelix_server` (#224).
- Full suite on `main` `92aba6d` (lane #224, plain-sync venv): **10212 passed / 12 skipped**
  (349 s).
- Codex: usage limit until 2026-09-07 14:29 (probed at session start — `omc ask codex` exit 1,
  "You've hit your usage limit"). Every lane in this issue is Claude (opus).

## 1. `main` 86c750d, measured (darwin, py3.12, `/tmp/live-222-cwd/probe_main.py`)

| case | elapsed | result |
| --- | --- | --- |
| `sleep 30 \| cat & echo $!; wait`, timeout 1.0 | 1.01 s | `timed_out=True`, survivors `[]` |
| `python -c 'os.setsid(); print(pid); sleep(8)' & sleep 30`, timeout 1.0 | **8.02 s** | `timed_out=True`, holder held the drain 7 s past the kill |
| `python -c 'print(pid); sleep(4)' & echo started`, timeout 10 | **4.02 s** | `exit_code=0` — success path waited for the helper (→ #232) |

Critique lane measurements on the same `main` (all recorded in the resolution table): the
`setsid` holder hangs the abort-signal and `CancelledError` legs too (8.02 / 8.03 s); under
`start_new_session=True` a child reading the tty is `Ss`, not `T` — it competes with the TUI for
keystrokes (`'secret\n'` to the child, `b''` to the TUI) and can `tcsetattr` the terminal;
`call_soon_threadsafe(on_data)` + a polled `eof` loses the tail chunk in 0.3–6 % of runs while
`main`'s shape loses 0; applying the design's A.1/A.5 edits drifts 11 gated citations.

## 2. Design critique (workflow `wf_b771f4f0-8eb`)

5 lenses → 32 BLOCKING/MAJOR + 27 MINOR → 32 refuters (each opus) → synthesis. First attempt
died at the refute phase on a session limit (all 32 refuters + synthesis null; the 5 critiques
were cached); resumed with `resumeFromRunId` after the limit reset — 38 agents, 2.05 M tokens,
19 min live. Upheld: the three independently-measured tail-loss findings (one defect), the
`detach()` placement, double retention, `stdin` mechanism + win32 clause + rule-9 waiver,
citations inventory, C.2-5 helper stdio, the drop-the-twin answer to I.4, the constants answer
to I.3. Refuted (do not re-raise): SCOPE-4 (the drain bound belongs to #222 — ADR-0238 assigns
the *hang*), POSIX-2/WIN32-7/SCOPE-3/TESTS-8 (loop stall — measured 1.6 ms), POSIX-3 (belt),
ASYNC-2, ASYNC-3, POSIX-6, TESTS-2/3/5, WIN32-3/5/6, SCOPE-5/7.

## 3. Implementation (workflow `wf_8c7e8a46-6d4`, 5 lanes, 0.91 M tokens, 67 min)

Lane P (primitive) → parallel(B bash, S subprocess + shim + citing docstrings, D docs) → G gate.
Reports in `/tmp/222-lane-{1..5}.md` (session-local). 25 files: +2587 / −277.

- `_PipeReader(stream, state, *, on_chunk=None, on_eof=None)`; with a callback it retains
  nothing; `on_eof` once, from `run()`'s `finally` (one site that dominates the three exits the
  design enumerated — deviation of form, stated in the code).
- `bash.py` `exec`: 720 → 901 lines. Popen(`stdin=DEVNULL`, containment kwargs) → attach in the
  next statement inside the belt `try` → daemon reader with `_post` (suppress `RuntimeError`) →
  `to_thread(proc.wait, timeout)` → one `_end_the_tree` per leg (`REAP_GRACE` / `INTERRUPT_REAP`)
  → success drain `await eof.wait()`, kill drain `_drain_past_the_kill` (idle 0.1 s re-armed,
  cap 1.0 s, `is_alive`, post-cut `sleep(0)`) → finallys: detach+`delivering=False` → watcher →
  `close()`. `_kill_group` gone. Deviations recorded by the lane: attach belt has no reap; the
  site constructs `_PipeReader` directly (`_start_reader` not widened); `delivering` cleared in
  the detach `finally`; C.2-4 parametrised; C.2-6/10 are sync tests under `asyncio.run`.
- `_subprocess.py`: containment kwargs, attach before the first await, `hard_kill` + `proc.kill`
  under `suppress(ProcessLookupError)`, bounded wait, `close()` in `finally`. Shim deleted.
- Tests: `test_bash_tool_containment.py` (970 lines, 14 ids), `test_pipe_reader_callbacks.py`
  (307 lines, 7), C.4 ×2 in `test_bash_tool.py`, C.3 in `test_subprocess_helper.py` (+279),
  `test_process_tree_win32.py` rewritten, C.1 docstring flip.
- Red-on-main (main venv running the new file): 8 failed / 6 passed — C.2-4 ×3 (`exec did not
  return within 8.0 / 7.5 / 7.5 s`), C.2-6, C.2-11, C.2-7/10/12 (structural), C.4 ×2. C.2-1/2/3
  are green on main on POSIX (`killpg` reaches the leaf) and red against mutant (a); C.2-5/8/9
  pin behaviour that must not change.
- Mutations (lane B, each reverted, `diff` clean): (a) 2 red, (c) 4 red, (e) 2, (f) 2, (j) 1,
  (k) 1, (m) 1, (o) 2 (timeout leg green as designed), (p) 1. **Green, stated in the
  docstrings:** (l) polled-`eof` success drain — 0/40 rounds red (an awaited poll sits in the
  same FIFO; the critique's lossy shape was the non-awaiting one); (l2) drop the post-cut
  `sleep(0)` — 0/15; (n) `_detached` guard `continue` → `pass` — unkillable at the site by
  construction (`delivering` covers it); the primitive's own case kills it.
- Elapsed (darwin, `warnings.warn`): timeout leg 1.007 s · abort 0.002 s · cancel 0.002 s ·
  timeout+holder 0.613 s · abort+holder 0.102 s · cancel+holder 0.102 s · helper 0.021 s ·
  exit-path drain 0.541 s.

## 4. Gate (lane G) + orchestrator

| gate | result |
| --- | --- |
| `pytest tests/tools tests/process_tree` ×3 | 357 / 357 / 357 passed, 30.0–30.2 s, no flake; `pgrep -fl aelix222` empty |
| full suite (`/tmp/wt-222-full.txt`) | **10236 passed / 12 skipped / 0 failed**, 343 s (+24 vs main) |
| `ruff check .` | clean |
| pyright linux / windows | 0 errors, 1 pre-existing warning (`storage.py:72`), identical to main |
| citations | `--fix` relocated 6, 9 new anchors verified by hand (`bash.py:321`, `_subprocess.py:106`, `bash.py:436-443`), `--lock`; after staging the two new test files (orchestrator): **851 gated, none drifted**, `test_citation_drift.py` 22 passed |
| `tests/packaging_gate` | 77 passed |
| no-model A/B driver (`/tmp/live-222-cwd/probe_222.py`, wt vs main) | `setsid` holder: **1.106 / 0.131 / 0.132 s** vs 60.04 / 60.03 / 60.02 s (timeout / abort / cancel); stdin `b"''\n"` vs `b"'SHOULD-NOT-BE-READ…'"`; pipeline, exit-0 helper, `run_cancellable` ×3 unchanged |
| `ruff format --check` | 588 files would be reformatted on the branch vs 590 on main — pre-existing repo-wide drift (CI runs `ruff check` only) |

`ISSUE-EXIT-DRAIN` placeholder → **#232** (filed 2026-09-05) in CHANGELOG, ADR, decisions README.

## 5. Live (rules 9 and 10)

**Rule 9 — TUI, tmux, scratch cwd `/tmp/live-222-cwd`, haiku via OpenRouter.**

- `main`: `!python3 -c "import sys; print(repr(sys.stdin.readline()))"` → the TUI hung; the
  child was `Ss` on `tty ??` (no controlling terminal — not stopped); typing `secret` Enter
  `more` C-j into the TUI went to the **child**, which printed `'secret\rmore\n'` and only then
  exited. The user's keystrokes were eaten. (§1-6 reproduced in the real TUI.)
- `/tmp/wt-222`: the same command printed `''` at once, child gone, TUI responsive; `!cat`
  returned at once (`cat gone`); a model turn running `printf "one\ntwo\n"; sleep 1 | cat; echo
  done` through the permission prompt rendered `one two done` in the tool block exactly as
  before.

**Rule 10 — print mode, real model (`/tmp/live-222-cwd/run_live.sh`, before `before_main.txt`,
after `after_wt222.txt`).**

| prompt | `main` | `/tmp/wt-222` |
| --- | --- | --- |
| `sleep 30 \| cat`, timeout 2 | 5.84 s, "Command timed out after 2 seconds", survivors 0 | 5.08 s, same, 0 |
| `setsid` holder (`sleep 60`) & `sleep 60`, timeout 2 | **64.26 s** | **5.25 s**; the `setsid` holder survives (documented — the reaper's walk; the driver reaps it) |
| `echo hello…` | 2.88 s, verbatim | 2.80 s, verbatim |
| `python -c 'print(repr(sys.stdin.read()))'` | `''` (the driver's own stdin is `/dev/null` in the background — not discriminating here; the TUI check above is) | `''` |

## 6. Pre-merge review (workflow `wf_6aa83329-0b4`, 18 agents, 1.9 M tokens, 37 min) → fixes (`wf_0c042eda-ef1`, 3 lanes, 0.32 M, 37 min)

6 lenses (POSIX / WIN32 / ASYNC / TESTS / DOCS / REGRESS) → 6 BLOCKING/MAJOR refuted one each
→ **FIX_FIRST**. Resolution: `222-review-resolution-2026-09-06.md`.

- **F1 (BLOCKING, upheld, measured)** — the abort watcher stayed armed through the post-exit
  drain (the drain `finally` was innermost, the watcher's two levels out; `main` cancels the
  watcher before `await drain_task`). Abort at 1.0 s into an rc-0 command that backgrounded a
  20 s helper: `main` `exit_code=0`, 20.02 s, helper alive; branch `exit_code=None`, 1.01 s,
  helper **killed**, "Command aborted". Fixed by reordering (watcher → drain → detach → close);
  new case `test_an_abort_during_the_exit_path_drain_keeps_the_helper` — RED before
  (`ExecExitResult(exit_code=None…)`, helper gone), GREEN after (2.05 s). The review's "reuse
  `ROOT_THAT_BACKGROUNDS_A_HELPER`" could not measure it (its helper's pipe EOFs at root exit,
  drain ~1 ms wide) — the case has a 2 s pipe-holding `tail` child plus the all-DEVNULL helper.
- **F2 (MAJOR)** — three cases had 0.5 s budgets under pwsh's 0.5–0.57 s runner startup:
  reproduced red with a `sleep 0.5; exec bash` wrapper (`SHELL=/tmp/fix222/slowsh`: 3 failed),
  raised to 2.0 s / pid-file wait, 15 passed under the same emulation.
- **F3 (MAJOR)** — CHANGELOG `Removed` said "two importers"; `git grep` on `92aba6d` shows three
  (+ `tests/tools/test_process_tree_win32.py`). Rewritten.
- MINOR FIX-NOW (all done): `_mark_the_kill` after the ladder (M-2), `suppress(OSError)` in
  `run_cancellable` (M-8), 30 s → 2 s sleeper in `test_bash_tool.py` and case 12's 60 → 15 s
  (M-4; registering case 12 through `strays` measured impossible — killed inside the attach
  window before the interpreter announces, 5.05 s wasted), `_deliver`/Protocol contract for an
  `on_data` that raises (M-3), `_ReadState.exited_at` writers (M-6), `_end_the_tree`'s POSIX
  sentence → §A.4's invariant (M-9), `_taskkill_tree` five legs at two files (M-13),
  `_start_reader` out of `__all__` (M-14), case 9 docstring honest about (l2) (M-10),
  `+5.0` watchdogs in `test_subprocess_helper.py` (M-11), "before the first `await`" (M-12),
  ADR reap-constant sentence split per site (M-1), ADR `process_group=0` clause — `models.json`'s
  `!command` is `process_group=0`, the TUI's `!command` is this `exec` and never was (M-5),
  SLICE-STATUS "alone" (M-15), git's real string "Aborting commit due to empty commit message."
  (M-16), the design's stale C.5(d) parenthetical.
- LATER → filed: **#233** (`BaseException` belt at both async sites; M-7 + POSIX-1/ASYNC-2
  residue), **#234** (watcher teardown `suppress` swallows the turn's cancel ~0.5 %; M-17,
  `main`'s line), M-18 noted on #230.
- Refuted, do not re-raise: POSIX-1, ASYNC-1 (pre-existing, same on main), ASYNC-2 (a real
  SIGINT arrives as `CancelledError`), TESTS-1 (the windows leg gates `kill_on_close`), DOCS-3,
  REGRESS-2 (accepted by design).
- Gate after fixes: `tests/tools tests/process_tree` 358 ×2 (35 s), callers
  `tests/cli tests/rpc tests/agents_ext tests/tui …` 5332 passed, full suite **10237 / 12 / 0**
  (348 s), ruff clean, pyright 0/0 both models, citations 851 gated none drifted (two
  `_watch_signal` anchors re-derived by hand to `bash.py:472-487`), drift test 22 passed. The 5
  pyright errors in `tests/agents_ext/test_print_channel_spawn.py` the gate noticed are
  **identical on `main`** (checked) and outside the gate's `include`.
- Post-fix live: no-model A/B unchanged (`setsid` holder 1.116 / 0.134 / 0.134 s; stdin `''`;
  helper alive); real model `after_wt222_fix.txt` — holder prompt 5.22 s, pipeline 5.32 s,
  exit-0 3.01 s, survivors 0 except the documented holder.

Commit `fc567ad`, pushed; CI run **33979234515**.

## 7. Windows leg — CI run 33979234515 on `fc567ad` (first push, 6/6 green)

| job | result |
| --- | --- |
| ubuntu py3.11 (101341309871) | 10248 passed / 1 skipped, 504 s |
| ubuntu py3.12 (101341309826) | success |
| windows py3.11 (101341309867) | **10181 passed / 68 skipped / 0 failed**, 836 s |
| windows py3.12 (101341309901) | **10181 passed / 68 skipped / 0 failed**, 907 s |
| install.ps1 e2e ×2 | success |

`warnings.warn` measurements from the two windows jobs (py3.11 / py3.12), the §J table:

| case | win32 | linux (py3.11) |
| --- | --- | --- |
| C.2-1 timeout leg (#9129 tree, `timeout=1.0`) | 1.063 / 1.047 s | 1.003 s |
| C.2-2 abort leg | 0.063 / 0.063 s | 0.001 s |
| C.2-3 cancel leg | 0.047 / 0.047 s | 0.001 s |
| C.2-4 `setsid` holder, timeout (`timeout=2.0`) | 2.047 / 2.063 s — holder **DEAD** (the job reaches it) | 2.105 s — holder ALIVE, bound armed |
| C.2-4 abort / cancel with a holder | 0.062 / 0.047 s · 0.063 / 0.063 s | 0.104 / 0.104 s |
| C.2-5 backgrounded helper kept | 0.329 / 0.328 s (alive after `close()`, `kill_on_close=False` — §J-5 ✔) | 0.025 s |
| C.2-8 exit-path drain to EOF | 0.797 / 0.813 s | 0.533 s |
| F1 abort during the exit drain keeps the helper | 2.328 / 2.328 s | 2.035 s |
| C.2-9 delivery byte-equal (§J-1) | passed (0 loss on the proactor loop in the one round the leg runs) | passed |

§J answers: (1) delivery green on the proactor loop — one round per leg, not ≥50; (5) helper
alive after `close()` on win32 ✔ (mutation (d) is red there); (6) C.2-4 win32 arm 2.05 s, holder
DEAD ✔; (7) **the post-kill bound is never armed on win32** — the job ends the holder so the
drain ends at EOF ~50 ms after the kill; the bound is POSIX-exercised and a belt for the
failed-attach case on win32, as the design predicted; (8) pwsh chain: the C.2-1 timeout leg
returned 1.06 s against a 1.0 s timeout, so the root's tree was announced inside the budget
(the `_await_pids` vacuity guard would have failed otherwise); (2)/(3)/(4) not instrumented
(no stopwatch around the attach; no heartbeat task) — still open; (9) `CONIN$`/`Read-Host`
unverified, recorded as such in README/ADR.

Merged ff to `main` = `fc567ad`; branch and `/tmp/wt-222` removed; #222 closed with the
numbers above.
