# 다음 세션 핸드오프 — #209·#205 닫음, windows 141 → 109 (2026-09-04)

`handoff-windows-clusters-2026-09-04.md` §2("바로 시작할 것")의 두 항목을 끝냈다. 둘 다 windows 레그로 실측하고 닫았다.

## 0. 기준점

| | |
| --- | --- |
| `main` | `4f9b5f0` (push 됨, origin/main 동일) |
| 로컬 스위트 | `68bff1d`에서 `1 failed / 9370 passed / 12 skipped` — darwin `test_config_from_env_overrides`(`/tmp` realpath) 기존 1건. `4f9b5f0` 전체는 안 돌림(#205는 관련 파일 420 + 1661건만) |
| windows 실측 | #209 브랜치 `848bf6e`: run 33823781849 → **125 failed / 9200 passed** · #205 브랜치 `62fb09e`(272400c 기반): run 33824271543 → **125 failed / 9201 passed**. 각각 기준 141에서 자기 17건 제거 + 타이밍 flake 1건 |
| main `4f9b5f0` 합산 | run **33825294806** · job 100876580045 → **109 failed / 9220 passed / 61 skipped**. 기준 141 대비 34건 제거(#209 17 + #205 17, 1:1) + `test_tabbed_*` 2건 새로 빨강(둘 다, 아래 §4-2) |
| 보드 | #209 Done(자동), #205 닫음(보드 자동 이동 확인 안 함) |

## 1. 이번 세션이 한 것

- **#209** `68bff1d` + `848bf6e`: 5개(+win32-skip 1개) hand-built 자식 env dict → `tests/env_sandbox.py::child_env(home, /, **extra)`. `home_env()`(HOME+USERPROFILE/HOMEDRIVE/HOMEPATH/APPDATA/LOCALAPPDATA)와 부모에 **비어 있지 않게 있을 때만** 복사하는 `_CHILD_BOOT_KEYS`(SystemRoot, SystemDrive, windir, COMSPEC, PATHEXT, TEMP, TMP, TMPDIR). `sandbox_home`은 `home_env`를 monkeypatch로 적용하는 래퍼로 재작성(키·값 동일). dict 채널 가드 `test_no_test_hand_builds_a_child_home`(`"HOME": str(` grep) 추가.
- **#205** `4f9b5f0`: `_file_url_to_path` → `urllib.request.url2pathname` + `OSError`→`CatalogError`. `extension index` 안내 → `.as_uri()`. 테스트 4개(드라이브 복원 테스트는 non-win32에서 `url2pathname`을 `nturl2path`로 monkeypatch해 모든 레그에서 회귀 감지).

## 2. 바로 시작할 것

**#208 (cli, 한 줄 둘)** — `{target!r}` 백슬래시 2배, `sorted(Path)` case-fold로 README가 help 맨 앞을 벗어남. 이슈 본문에 사이트·수정안 있음.

잔여 109건의 기준 목록: `gh api repos/handochan/aelix-ai/actions/jobs/100876580045/logs`에서 `FAILED tests/` grep. #208은 이 세션 말미에 Workflow로 착수했다 — 브랜치 `fix/208-cli-repr-and-help-order`가 남아 있으면 그 상태부터 본다.

## 3. 이후 순서

#213 → #214 → #215 → #216 → #210 → #212 → #211(오너 결정 먼저) → #206 → #207(#202와 함께). 그리고 #205 코멘트에 적은 후속: **UNC `--out`이면 `as_uri()`가 `file://server/...`를 내고 fetcher가 의도적으로 거절** — 별도 이슈로 만들 것.

## 4. 이 레포에서 이번에 물린 것

1. **같은 브랜치에 push하면 진행 중인 windows 레그가 cancel된다**(`concurrency: cancel-in-progress: true`, ref 단위). 68bff1d의 첫 run은 7827/9383에서 잘렸고 `-q`라 어느 파일까지 돌았는지 알 수 없다 — 부분 로그로는 결론 못 냄. **리뷰 반영 커밋은 첫 run이 끝난 뒤에 push**하거나 처음부터 한 번에.
2. **`tests/tui/test_context.py::test_tabbed_*`는 windows에서 타이밍 flaky.** `asyncio.sleep(0.1)` 뒤 첫 paint 단언. #209 run에서 `test_tabbed_tab_key_advances_active_tab`, #205 run에서 `test_tabbed_tab_switch_works_while_filter_active`가 각각 새로 빨강(둘 다 기준 run엔 없음). 두 run 다 기준(532s)보다 느렸다(696s, 744s). 실패 목록 diff에서 이 파일의 +1은 이 변경 탓이 아니다. #206에 붙일 것.
3. **Codex는 9/7 14:29 리셋까지 usage limit.** #205 워크플로우의 Codex 단계가 두 번 그 메시지를 받았다. 그때까지 `/ccg`·`omc ask codex`는 안 된다 — 교차 리뷰 없이 넘긴 것은 이슈 코멘트에 적었다.
4. **Workflow 성공 패턴(이번엔 529 없었음).** `robust()`(같은 모델 2회 → sonnet 폴백, null이면 throw) + implement(opus) → parallel[refute(opus, schema), run-tests(sonnet), codex(sonnet)] → blocking 있으면 fix(opus)+재검증. 4 agents, 188k 토큰, 10분. 스크립트: `~/.claude/projects/-Users-handochan-dev-aelix-ai/*/workflows/scripts/fix-205-*.js`. **opus 반박 리뷰어가 실제로 실행해서 잡은 것**: nturl2path의 `OSError` 탈출(계약 위반), 드라이브 테스트가 POSIX에서 회귀 가치 0. 둘 다 반영.
5. **`ruff format`은 tests/에 강제되지 않는다.** 건드린 5개 파일이 이미 "Would reformat" 상태라 `ruff format`을 돌리면 무관한 95줄이 바뀐다. `ruff check --fix`만 쓰고 format은 새 파일/새 함수에만.
6. **worktree에서 메인 `.venv`로 테스트 돌리기**: `/Users/handochan/dev/aelix-ai/.venv/bin/python -m pytest` — tests/만 바뀐 경우 editable 설치가 메인 트리를 가리켜도 무방. `uv run`은 worktree에 새 venv를 만든다.
7. **pyright 게이트 범위는 `include = ["src", "packages/**/src", "scripts"]`** — tests/는 밖. `test_print_channel_spawn.py:1822`의 `reportOptionalMemberAccess`는 main에도 있는 기존 에러.
8. **오너 상시 규칙(재확인)**: "항상 dynamic workflow 적극 이용 단 opus5이하". 메모리에 저장됨.

## 5. 반증된 것 — 다시 믿지 말 것

- ❌ **"3.11의 `shutil.which`는 PATHEXT 기본값이 없다"** — 내가 68bff1d 주석에 썼고 리뷰어가 CPython 3.11.15 `shutil.py:51`(`_WIN_DEFAULT_PATHEXT`)로 반증. 848bf6e에서 이유를 고쳤다(부모 값이 더 넓다 — py launcher의 `.PY/.PYW`).
- ❌ "`test_print_mode_json_contract.py`의 자식은 hermetic이었다" — HOME이 아예 없어서 `posixpath.expanduser`가 `pwd`로 떨어져 **개발자의 실제 `~`**를 읽고 있었다. 68bff1d가 부수적으로 고쳤다.
- ❌ "SystemRoot 하나로 충분한가"(#209 본문 ⚠️) — **분리 측정 안 함.** 8개 키를 한꺼번에 넣어 17건이 사라진 것만 실측. 단독 효과는 여전히 모른다.
- ✅(측정됨) 합산 34건 감소는 main run 33825294806에서 확인. 109 = 141 − 34 + 2(tabbed flake 둘 다).

## 6. 변하지 않은 금지 사항

🔴 PUBLIC 저장소. `.omc/specs/recovery-report-dead-session-91.md`와 `docs/assets/*` 10건은 여전히 **의도적으로 미커밋**. `git add -A` 금지 — 이 세션은 파일을 명시해서 add 했다.
