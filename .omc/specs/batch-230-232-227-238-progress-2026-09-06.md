# Batch #230 · #232 · #227 · #238 — progress / measurements (2026-09-06, second batch of the session)

The batch that follows the owner's answers to `handoff-batch-234-226-231-2026-09-06.md` §2. One
issue = one worktree = one branch = one commit = one windows leg (rule 3). Files disjoint:
`utils/_process_tree.py` (#230) / `tools/bash.py` (#232) / `oauth/_resolve_config.py` (+ a
layering-clean shell primitive, #227) / `tui/completion.py` + settings (#238). Designs of record:
`230-design-2026-09-06.md`, `232-…`, `227-…`, `238-…` (+ `*-critique-resolution-2026-09-06.md`).

## 0. Baseline

- `main` at batch start **`7fa6796`** (= `origin/main`; `bcf057c` + the first batch's handoff
  commit). Worktrees `/tmp/wt-230` `fix/230-abort-handle-finishes-at-the-reap`, `/tmp/wt-232`
  `fix/232-bash-success-drain-idle-rule`, `/tmp/wt-227` `fix/227-command-configured-shell-win32`,
  `/tmp/wt-238` `feat/238-at-menu-gitignore-toggle`, all at `7fa6796`, plain `uv sync` ok.
- Board: #230, #232, #227, #238 → In progress. **#238** filed from the owner's decision (split
  out of #236 item 1; #236's body points at it).
- **Owner decisions (2026-09-06, in this session):**
  1. **#230 + #232 = A.** The bash tool's success path adopts Pi's idle rule (return once the
     pipe is idle 0.1 s after the root exit, re-armed per chunk; a cap to be decided against
     `run_contained`'s); the helper keeps running and its later output is not captured. An
     abort landing in the post-exit drain window kills nothing, at both sites (`run_contained`
     via `AbortHandle` finishing at the reap; the bash tool already disarms its watcher before
     the drain since #222 F1).
  2. **#234 = A.** A cancellation landing in the teardown after a timeout kill stays delivered
     as a cancellation (no change).
  3. **#231 = B.** A `/settings` toggle "`@` menu respects `.gitignore`", default on → #238.
  4. #226's three residues (cause on plain timeout, `waitid(WNOWAIT)` branch, termios restore)
     stay declined (no answer needed, none given).
- Codex: still at its usage limit (resets 2026-09-07 14:29) — every lane Claude (opus).
- Full-suite baseline on `main` `7fa6796` = the merged-main run of the first batch on `bcf057c`
  (same code): **10258 passed / 12 skipped** (364 s).
- Layering fact for #227, checked before the lanes started: `_resolve_config.py` lives in
  `aelix-ai` (lowest package), `tools/bash.py::_resolve_shell` in `aelix-coding-agent` (highest);
  the dependency direction forbids importing the latter from the former, so the design must move
  a primitive down, inject, or duplicate-and-pin.

## 1. Design + critique (workflow `wf_11f9c891-2fe`)

- First run (2026-09-06 20:xx KST): every design lane hit the **weekly** Claude limit (resets
  2026-09-07 21:00 KST) — 12 attempts (3 per issue incl. the sonnet fallback) all failed, 0
  results in the journal, 0.64 M tokens spent. The #238 lane had already written a complete
  revision-1 draft (532 lines) before dying; the #230 lane left probe pid files in scratch.
  Worktrees untouched (porcelain 0 ×4), `main` unmoved.
- Resumed 2026-09-07 21:2x KST with `resumeFromRunId` after patching the script: the #238 brief
  now says "reuse the draft, re-verify, fill in #238"; **Codex is back** (probe `OK` at 21:19)
  so a Codex cross-review lane (sonnet wrapper → `codex exec --sandbox read-only`, findings
  converted verbatim, lens `codex`) was added to every issue's critique stage (rule 8).

- **Codex post-hoc review of the first batch (workflow `wf_76ed443f-d82`, 21:2x–21:3x KST): no
  findings — all three `codex exec` runs (one per commit, launched in PARALLEL, default reasoning
  effort "ultra") hit the usage limit mid-investigation after ~92 k / 118 k / ~100 k tokens each
  and emitted no verdict. Limit resets **2026-09-08 02:19 KST**. Artifacts for a retry:
  `scratchpad/codex-posthoc/{231,234,226}/{diff.patch,prompt.md,out.md}`. Lesson: run ONE
  `codex exec` at a time with a lower reasoning effort; three parallel ultra runs exhaust the
  quota in ~10 min. The design workflow's Codex lanes launched after this will report
  "codex unavailable" — the Claude lenses carry the critique; the debt stays in the handoff.

- Resumed run: **65 agents, 8.07 M tokens, 78 min, 0 nulls** (the Codex lanes returned "unavailable").

| issue | findings | serious | upheld / refuted / partial / dead | design rev 2 |
| --- | --- | --- | --- | --- |
| #230 | 21 | 13 | 7 / 0 / 3 / 3 | 770 lines |
| #232 | 26 | 18 | 6 / 2 / 2 / 8 | 664 lines |
| #227 | 31 | 23 | 5 / 0 / 5 / 13 | 712 lines |
| #238 | 21 | 11 | 7 / 0 / 3 / 1 | 700 lines (rev-1 draft reused) |

Decisions of record: **#230** `abort._finish()` at the reap on both legs (timeout leg gated on
`returncode is not None` — 1 of 10 arrivals unreaped) + `_drain` clause (d) "the caller aborted"
ends the drain at once (helper alive, `abort()` → False, 0.002–0.008 s after the abort vs 1.5 s with
`_finish()` alone). **#232** `run_contained`'s rule whole (idle 0.1 s from max(last chunk, root
exit), cap 2.0 s, and the remaining timeout floored one grace past the exit) with the cap
arithmetic extracted to `_process_tree.py::_exit_drain_cap`, `_drain_past_the_kill` generalised,
the root-exit stamp separate from the kill stamp, and `_PipeReader` no longer pinning the
caller's callback (8 MiB → 0 retained); silent holder 4.04 s → 0.126 s, chatty holder 5.07 s →
2.03 s. **#227** a shared win32 chain (`$SHELL` → `sh` → `pwsh` → `powershell` → `%COMSPEC%` →
`cmd.exe`) whose primitive incl. `ShellConfig` moves DOWN to `aelix_ai/utils/_shell.py` (no
compat re-export), PowerShell `-NoProfile -NonInteractive` (a profile that prints prepends to
the value — measured), cmd family as a raw `/d /s /c` command line, fall-through classified by
errno (ENOEXEC/EINVAL have no OSError subclass), `CREATE_NO_WINDOW` at this site, and a second
bug fixed: CRLF (`rstrip("\n")` left a CR in the key) → `.strip()`. POSIX byte-identical.
**#238** global `respect_gitignore`/`respectGitignore` (absent = on), a live `/settings` row
"Gitignore in @ menu" (help fitted to a measured 78-cell ceiling), `--no-ignore` (≡ the walk's
set; `--no-ignore-vcs` is not) read through a callable on every enumeration with the flag in the
TTL cache key; three forwarding hops pinned by a new `tests/tui/test_completer_wiring.py`
because two lanes measured the row shipping inert-and-green otherwise.

Owner questions carried (not decided): #230 — should a ^C landing after the catalog clone's own
`git` exited still end that tree (the interrupt ladder also keeps `finally: rmtree` off a running
git)? #232 — say anything when a helper's output is cut on the cap (silent today, Pi silent)? #227
— a `!command` written for `sh` now runs under PowerShell on a stock box and can return a wrong
value with rc 0; alternative: stop the chain at `sh` with a named error. #238 — should the
drill-in listing (`@src/`) respect the toggle too (it never applied ignore rules)?

Follow-ups filed from the designs (2026-09-07 21:5x KST): **#240** (per-request `!command`
re-resolution ≈ 0.5 s PowerShell per turn), **#241** (win32 chain via `shutil.which` searches
CWD first), **#242** (`resolve_config_value` caches an empty value), **#243** (bash.py lets
WinError 193 escape), **#244** (`/settings` "Check for updates" row KeyError — README claim
false), **#245** (`/settings` help past 78 cells silently dropped, 14 of 22 rows), **#246**
(`run_cancellable` waits for a backgrounded helper's pipe). #239 was created by someone else
meanwhile.

## 2. Implementation + pre-merge review (workflow `wf_5513f9fb-701`)

- First run (2026-09-07 22:xx → 2026-09-08 00:xx KST): 65 agents scheduled, **44 done, 21 hit the
  session limit** (resets 02:10 KST) — the verdict lanes of #232 and #238, four #238 refuters and
  the #227 fix lane (which had applied FIX 1–13 of 14 and written §8 before dying in the re-gate).
  7.5 M tokens, 110 min. All four implement lanes DONE: #230 8 files, #232 16, #227 33, #238 27.
  Reviews: #230 15 findings (3 serious) → FIX_FIRST 10 → fix DONE (full suite 10269 / 12); #227
  16 (7) → FIX_FIRST 14; #232 19 (8); #238 18 (7). Codex lanes: 0 findings (quota).
- 🔴 `main` moved during the run: `7fa6796` → `fafabea` (the owner's other session: external PR
  #119 for #92, CONTRIBUTING.md/community files, `handoff-external-prs-2026-09-07.md`). Overlap
  with our staged files: `citations.lock.json` (#232/#227/#238 — re-run `check_citations.py --fix`
  after each rebase) and, for #238, `tui/shell.py` + `cli/agent_context.py` (hand-check).
- Resumed 2026-09-08 07:0x KST with `resumeFromRunId` after adding an audit clause to the fix
  prompt ("if §8 exists, a previous lane died — verify what is applied, apply only the missing").

### Live checks (rules 9/10, by the orchestrator, darwin)

- **#230** (2026-09-08 07:1x KST, tmux, `-e /tmp/live-230/ext_230.py`, haiku via openrouter, yolo;
  the extension's tool runs `api.exec` on a root that backgrounds a 5 s chatty pipe holder, prints
  `done`, writes a `leaving` timestamp and exits 0; Esc sent 0.3 s after that file appears):
  **branch** Esc landed at exit **+0.322 s** (inside the ~2 s drain window) → helper pid **ALIVE**
  afterwards, turn shows `✖ Operation aborted`; **`main`** Esc at exit **+0.323 s** → helper
  **gone**, same turn text. Helper ended on its own ~5 s later in both arms; strays 0. This is the
  contrast the design §E asked for, with the timing anchor that makes "alive" mean something.

- **Resumed run** (2026-09-08 07:0x–08:2x KST): 52 agents, 6.0 M tokens, 76 min, 0 errors. Verdicts:
  #230 MERGE (second review pass; the first pass's 10-item fix already applied), #232 FIX_FIRST 16
  → fix DONE (full 10262 / 12), #227 FIX_FIRST 28 → fix DONE (the dead lane's FIX 1–13 found
  applied, the re-gate finished; full 10285 / 12), #238 FIX_FIRST 21 → fix DONE (full 10282 / 12).
  Review BLOCKERs this round: #227 a `/usr/bin/bash` literal that EXISTS on usr-merged
  ubuntu-latest (red on both Linux legs, green on darwin — the impl gate was blind by
  construction) and a guide sentence about `Read-Host` measured false; #232 three user-facing
  claims false by measurement (README crediting #232 with the kill-path bound; "everywhere else";
  "never past the deadline") and the deadline term unpinned at the bash call site; #238 a false
  provenance date and split figures for one measurement; #230 an ADR sentence resting on a
  measurement taken at the wrong placement.
- **Live, #232** (branch, haiku via openrouter, yolo): `python3 -c "print('started');
  time.sleep(4)" & echo now` → `main` `now`+`started` (wall 7.27 s), branch `now` only (4.37 s —
  the model round trips are ~3 s of both); `(sleep 1; echo late) & echo now` → `main`
  `now`+`late` 4.26 s, branch `now` 3.36 s. TUI `!(sleep 3; echo late) & echo now`: branch —
  prompt back with `now` at 1 s, still no `late` at 4.5 s; `main` — nothing at 1 s, `now`+`late`
  at 4.5 s. Strays 0.
- **Live, #227** (rule 10, `models.json` `shellprobe` provider, `apiKey` a `!sh -c 'printf %s
  "$OPENROUTER_API_KEY"'`): `main` `OK` 1.76 s; branch `OK` 3.10 s, identical shape; a TUI turn
  with the same key answers `OK`. Windows: not measurable here — the branch's windows legs are
  the first evidence for the chain (two cases force a real pwsh / cmd.exe spawn there).
- **Live, #238** (rule 9, tmux, cwd = the owner's checkout, branch binary, the owner's
  `~/.aelix/agent/settings.json` backed up → restored byte-identical): `@settingsjson` on → empty
  menu; `/settings` filter `gitignore` → the row `Gitignore in @ menu on` with the whole help on
  screen (`grep -c 'needs fd; live'` = 1); Enter → `off` (transcript line `gitignore in @ menu →
  off`); `@settingsjson` without restart → `.claude/settings.local.json`,
  `.omc/restore-2026-09-02/…`, six `.omc/state/sessions/**` rows (diff vs the on capture non-
  empty); Enter again → `on` → empty menu (menu region identical to the first). `settings.json`
  ends `respectGitignore: true`. Rule 10 headless `-p` → `OK` 1.72 s. Strays 0.
- **Dates normalised before committing:** the landing/amendment/착지 literals the lanes wrote as
  2026-09-07 → 2026-09-08 (the actual commit date) in ADR-0238 (#230, #232), ADR-0193 (#238) and
  the two index rows; measurement dates and the owner-decision date 2026-09-06 untouched.
- **Commits** (all on `7fa6796`): #230 `77816c7`, #232 `9b72869`, #227 `14625bc`, #238
  `abd7400`. Pushed 2026-09-08 08:3x KST; CI runs 34169787993 / 34169795394 / 34169803230 /
  34169814402. _(results below)_

## 3. CI, merge, close

### Branch CI (base 7fa6796)
| issue | run | result | windows py3.11 | warned numbers (win32) |
| --- | --- | --- | --- | --- |
| #232 `9b72869` | 34169795394 | **6/6** | 10206 / 68 / 0 | `exit-path drain: 0.375s chunks=b'EARLY\n'`, `drain cap: timeout=None 2.656s ticks=41`, `timeout=1.0 1.000s ticks=13` |
| #227 `14625bc` | 34169803230 | **6/6** | 10229 / 68 / 0 | `!echo x resolved through 'C:\Program Files\Git\bin\sh.EXE': 0.125s`, `forced-PowerShell candidate ran '…\pwsh.EXE': 0.750s`, `cmd floor ran '"cmd.exe" /d /s /c "echo x"': 0.047s` — the first Windows evidence for the chain |
| #238 `abd7400` | 34169814402 | **6/6** | 10226 / 68 / 0 | (no new timing case) |
| #230 `77816c7` | 34169787993 | **5/6 — windows py3.12 FAILED** | py3.11 10213 / 68 / 0: `run_contained abort in the exit drain: 0.296s total, 0.015s past the abort on win32` | py3.12 (job 101887768083): 2 failed / 10211 passed |

**#230 py3.12 failures.** (1) `test_an_abort_in_the_exit_drain_keeps_the_backgrounded_helper` —
the vacuity guard `assert aborted_at[0] < returned` failed with **equal** stamps
(`1251.437 < 1251.437`): on Windows 3.11/3.12 `time.monotonic()` is `GetTickCount64` at ~15.6 ms
resolution, and the abort and the return landed in one tick (py3.11 passed by 0.015 s). Fixed by
stamping both with `time.perf_counter()` (test-only; the case still passes on darwin, citations
851 unchanged); #230 amended to **`dc48b83`** and force-pushed → run 34170874815. (2)
`tests/tui/test_run_tui_smoke.py::test_run_tui_builtin_wins_over_extension_command` — a 3 s
`_wait` predicate timed out on the same overloaded leg (905 s run); #230 touches nothing in
`tests/tui` and the other three branches' py3.12 legs passed it — treated as a runner-load flake,
to be confirmed by the amended run and the `main` run.

### Local merge (before any push of `main`)
ff order #230 → #232 → #227 → #238 on `fafabea`. #230 rebased clean; #232 hit the five predicted
hunks (1 `_process_tree.py`: #232's `_exit_drain_cap` call + #230's `abort=abort`; ADR-0238
Status line both amendments, "What stays open" both bullets, Consequences = #232's paragraph
which already carries #230's policy; `decisions/README` row = token-level three-way merge of the
#230 and #232 clauses); #227 hit eight (CHANGELOG both entries; ADR Status + both bullets;
`decisions/README` row three-way; `citations.lock.json`/`reaper.py`/`permission.py`/
`test_print_channel_spawn.py` taken from upstream and `tool.py` from #227 (its de-citation),
then `check_citations.py --fix` relocated the rest and locked 14 new anchors → 850 gated);
#238 rebased clean (git auto-merged `shell.py`/`agent_context.py`/the lock; citations 850 OK).
Post-rebase gates: #232 833 passed (`tests/tools tests/process_tree tests/pi_parity` + drift +
bundle), #227 3630 / 11 skipped (oauth, tools, process_tree, pi_parity, docs, bundle, builtin,
agents_ext — the first run's single failure was the drift test before `--fix`), #238 2189
(`tests/tui tests/settings_manager tests/pi_parity` + drift + the new #92 case); ruff clean on
all. Two lessons: a same-line index-row conflict needs a TOKEN-level three-way merge (prefix/
suffix splicing duplicated the #234 clause twice), and `if ! cmd | tail -1` tests `tail`'s exit
code, not `cmd`'s.

### After the #230 amendment
The merged tree BEFORE the amendment (`e52ff5a` = 0ac2431 → a2ee1f8 → 3205eec → e52ff5a) ran the
full suite: **10328 passed / 12 skipped** (362 s). Then `git rebase --onto
fix/230-abort-handle-finishes-at-the-reap 0ac2431 main` replayed the three commits onto the
amended `dc48b83` with no conflict (the amendment touches a test region nothing else edits) →
`main` = **`dc48b83` → `c80e5a6` → `7971ccc` → `1c891e0`**; citations 850 OK; full suite on
`1c891e0`: **10328 passed / 12 skipped** (361 s). Pushed 2026-09-08 09:5x KST; `main` CI run
34171328513. The amended #230 branch's own run is 34170874815.

### Amended #230 branch CI
run 34170874815 (`dc48b83`): **6/6** — windows py3.11 10217 / 68 / 0, py3.12 10217 / 68 / 0;
`run_contained abort in the exit drain: 0.312–0.343s total, 0.004–0.005s past the abort on
win32`. The smoke-test timeout did not recur.

### `main` CI
- run 34171328513 (`1c891e0`): **5/6** — windows py3.12 (job 101892045423) `1 failed / 10271
  passed`: `tests/tools/test_bash_tool_containment.py::test_a_holder_that_never_falls_idle_hits_
  the_drain_cap[deadline-1s]` → `ExecExitResult(exit_code=None, timed_out=True)`: the root (a
  pwsh startup, 0.5–0.7 s on the runner, plus a python child) did not exit inside the arm's
  1.0 s deadline under load, so `exec` timed the ROOT out; the same leg had passed on the #232
  branch run and on py3.11 (`drain cap: timeout=1.0 elapsed=1.000s`). Everything else green,
  incl. #227's three real-shell warnings on both windows legs and #230's abort-drain case
  (0.001–0.004 s past the abort).
- Fix: **`58f3bbb`** `test(tools): #232's deadline arm gives the win32 root time to start` —
  `_DEADLINE_ARM_SECONDS = 2.0 if win32 else 1.0` (the term still bites: `exit < timeout <
  exit + DRAIN_CAP_SECONDS`), a win32 ceiling of deadline + one grace + slack; the mutant
  discrimination (2.08 s vs 1.00 s) stays the POSIX arm's. Test-only; local: both ids pass
  (`timeout=1.0 elapsed=1.002s`), ruff clean, citations 850, drift 22. A second commit for
  #232's issue — recorded here as the deviation from one-issue-one-commit that it is.
- run 34172371153 (`58f3bbb`): **6/6** — windows py3.11 10272 / 68 / 0 (job 101895001629), py3.12
  10272 / 68 / 0 (job 101895001495); `drain cap: timeout=None 2.485–2.516s ticks=41`, `timeout=2.0
  elapsed=2.000–2.016s ticks=31–32` — the deadline term bites at the deadline on win32 too.

### Closed
#230, #232, #227, #238 closed 2026-09-08 10:5x KST with run/job ids, live results, "not measured"
and the owner questions in their comments; board Done ×4 (automatic). Remote and local branches
and the four worktrees removed. `main` = `58f3bbb`.
