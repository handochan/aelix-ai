# #202 — process-tree containment: the six lanes' progress logs

Branch `fix/202-process-tree-containment`, worktree `/tmp/wt-202`, cut from
`main` 39549b9. Spec: `.omc/specs/202-design-2026-09-05.md` (revision 2).
Concatenated 2026-09-05 by the integration lane; under `.omc/`, never committed.

Lanes, in the order they ran: foundation (the primitive), then the four
site lanes in parallel (rpc, hooks, resolve_config, docs), then integration
(spec §I: citations, gates, full suite, the fails-on-main demonstration).


---

# Lane: foundation

# Lane: foundation (#202) — progress log

Worktree: /tmp/wt-202  branch fix/202-process-tree-containment

## 1. Module written — packages/aelix-ai/src/aelix_ai/utils/_process_tree.py

Struct sizes verified with ctypes on this LP64 box (matches spec B.3):
```
$ .venv/bin/python -c "<struct defs>"
BASIC 64 LimitFlags off 16
IOC 48
EXT 144
```

Deviation logged: `os.environ.get("SYSTEMROOT")`, not `"SystemRoot"` — ruff
SIM112 is a gate and rejects the mixed-case spelling. Windows upper-cases every
`os.environ` key, so it is the same variable there; the tests set the uppercase
name on POSIX.

Deviation logged: `_KernelApi` carries class-level `_kernel32: Any` /
`_last_error: Callable[[], int]` declarations. Without them pyright under
`--pythonplatform Linux` narrows the `sys.platform != "win32"` guard to
"always raises", makes the assignments unreachable, and reports 9 errors.

```
$ .venv/bin/ruff check packages/aelix-ai/src/aelix_ai/utils/_process_tree.py
All checks passed!
$ .venv/bin/ruff format --check packages/aelix-ai/src/aelix_ai/utils/_process_tree.py
1 file already formatted
$ .venv/bin/pyright --pythonplatform Linux  .../_process_tree.py -> 0 errors
$ .venv/bin/pyright --pythonplatform Windows .../_process_tree.py -> 0 errors
```

## 2. Shim written — packages/aelix-coding-agent/.../tools/_process_tree.py
ruff check / format --check / pyright (Linux + Windows): all clean, 0 errors.

## 3. Tests written

- tests/process_tree/__init__.py (empty, matches tests/tools/__init__.py)
- tests/process_tree/test_process_tree_api.py  (D.1 — 29 cases incl. one extra:
  `test_win32_an_abandoned_tree_still_releases_its_handle`, added after the
  weakref.finalize fired mid-test and broke an assertion — the handle leak it
  guards is real)
- tests/process_tree/test_process_tree_real_processes.py (D.2 — 7 cases)
- tests/tools/test_process_tree_win32.py rewritten (D.1 last para — 11 cases)

Dropped from my first draft: a `test_win32_refuses_a_non_positive_pid` in the
tools file. The shim re-exports the SAME function object (the delegates cases
assert identity), so D.1's `test_a_non_positive_pid_is_refused_everywhere`
already covers it. Pure duplication.

## 4. Measurements taken on this box (darwin 25.4.0)

posix/F4 confirmed — `os.getpgid()` DOES raise for a zombie leader here:
```
$ .venv/bin/python scratchpad/darwin_zombie_getpgid.py
platform: darwin
leader state: zombie  grandchild: alive
getpgid(leader) RAISED ProcessLookupError [Errno 3] No such process
after killpg(pid): grandchild -> gone
```

The bug D.2 case 1 guards, reproduced — a root-only teardown leaves the
grandchild, the tree kill ends it:
```
$ .venv/bin/python scratchpad/root_only_demo.py
grandchild before: alive
root already reaped; a root-only kill has nothing left to aim at
after root-only kill: alive
after tree.hard_kill(): gone
```

`_retained_handle` on both spawn shapes (the seam the site lanes import):
```
Popen handle: None            # POSIX: no handle to have
contained: True job: None
asyncio handle: None
contained: True job: None
non-process object: None      # never raises
transport extra 'subprocess' -> subprocess.Popen   # traversal reaches the
has _handle: False | this platform: darwin         # object that carries _handle on win32
```

ctypes struct layout (spec B.3): BASIC 64 / LimitFlags at 16, IOC 48, EXT 144.

## 5. Final gates

```
$ .venv/bin/ruff check <module> <shim> tests/process_tree tests/tools/test_process_tree_win32.py
All checks passed!
$ .venv/bin/ruff format --check <module> tests/process_tree
4 files already formatted
$ .venv/bin/ruff format --diff tests/tools/test_process_tree_win32.py   # read-only, not in the scoped list
1 file already formatted
$ .venv/bin/pyright --pythonplatform Linux   <module> <shim>   -> 0 errors, 0 warnings
$ .venv/bin/pyright --pythonplatform Windows <module> <shim>   -> 0 errors, 0 warnings
$ .venv/bin/python -m pytest -p no:cacheprovider -q tests/process_tree tests/tools tests/agents_ext/test_reaper_kill_signal_win32.py
280 passed in 3.96s
$ .venv/bin/python -m pytest -p no:cacheprovider -q tests/process_tree   (x3, flakiness)
36 passed in 1.40s / 1.40s / 1.42s
```

git status: only the 4 lane paths are dirty/new. No repo-wide ruff format was
run; no git add/commit/checkout/stash.


---

# Lane: rpc

# Lane: rpc — progress log (issue #202)

## Setup / reading (2026-09-05)

- Read spec `.omc/specs/202-design-2026-09-05.md` in full (revision 2).
- Worktree `/tmp/wt-202` at `39549b9`; foundation lane already landed
  `packages/aelix-ai/src/aelix_ai/utils/_process_tree.py` (untracked) and
  `tests/process_tree/`, and modified `tools/_process_tree.py` +
  `tests/tools/test_process_tree_win32.py`.
- Foundation API matches spec §B and additionally exports `_retained_handle`
  (spec §C.3 says it lives in the tree module) — used as-is.
- `AGENTS.md` is gitignored (`.gitignore:65`) so it is absent from the worktree;
  read from the main checkout instead (read-only).

## Files changed (lane rpc)

1. `packages/aelix-coding-agent/src/aelix_coding_agent/rpc/rpc_client.py` (§C.1)
   - import `ProcessTree, _retained_handle, containment_spawn_kwargs, kill_process_tree`
   - module docstring divergence §2 rewritten (Windows twin + `kill_on_close=True`)
   - `self._tree: ProcessTree | None = None` in `__init__`
   - `spawn_kwargs.update(containment_spawn_kwargs(new_session=True))`; the literal
     `start_new_session=True` line deleted, the AELIX-ORIGINAL comment moved onto it
   - `ProcessTree.attach(..., kill_on_close=True, handle=_retained_handle(self._proc))`
     in the same statement group as `_proc`; premature-exit branch closes the tree
   - `stop()` per the §C.1 snippet: `soft_kill` guarded by `returncode is None`,
     `hard_kill` (or `kill_process_tree`) on grace expiry, `close()` at the end
2. `packages/aelix-coding-agent/src/aelix_coding_agent/rpc/rpc_mode.py` (§C.2)
   - `SIGBREAK` handler under `if sys.platform == "win32":`
   - restore loop split into two suppressed steps, `previous is not None`
3. `tests/rpc/test_rpc_client_shutdown.py` — #207 skipif removed; breadcrumb handler
   stub; `test_stop_escalates_to_the_hard_kill_when_the_soft_one_is_survived`;
   NEW `test_a_cooperative_child_exits_under_the_grace`
4. `tests/agents/test_rpc_sprint_pins.py` — #207 skipif removed; breadcrumb handler
   stub; `test_the_real_rpc_child_answers_the_client_it_ships_with` gains the
   `elapsed < grace` + `returncode == 0` stop() assertions
5. `tests/rpc/test_rpc_client_containment.py` — `test_the_child_gets_its_own_process_group`
   loses its skipif and gains the win32 `IsProcessInJob` arm; NEW
   `test_stop_ends_a_descendant_the_child_left_behind`

## Commands run, with output tails

All from `cd /tmp/wt-202`, python is `.venv/bin/python` (3.12.13, darwin).
No `uv sync` / `uv run`, no `git add|commit|checkout|stash`, no `ruff format`.

### Lint

```
$ .venv/bin/ruff check packages/aelix-coding-agent/src/aelix_coding_agent/rpc/rpc_client.py \
    packages/aelix-coding-agent/src/aelix_coding_agent/rpc/rpc_mode.py \
    tests/rpc/test_rpc_client_shutdown.py tests/rpc/test_rpc_client_containment.py \
    tests/agents/test_rpc_sprint_pins.py
All checks passed!
```

(One intermediate failure worth recording: ruff's isort treats `aelix_ai` as a
separate section from `aelix_coding_agent` in this package, so the new import
block needs a blank line after it — same shape `rpc_mode.py:154-156` already
had. Fixed by hand, not by `--fix`.)

### Types — both platforms, on the two product files

```
$ .venv/bin/pyright <rpc_client.py> <rpc_mode.py>
0 errors, 0 warnings, 0 informations
$ .venv/bin/pyright --pythonplatform Windows <rpc_client.py> <rpc_mode.py>
0 errors, 0 warnings, 0 informations
$ .venv/bin/pyright --pythonplatform Linux <rpc_client.py> <rpc_mode.py>
0 errors, 0 warnings, 0 informations
```

`signal.SIGBREAK` sits under `if sys.platform == "win32":` and needs no ignore
on any of the three.

### The lane's test set

```
$ .venv/bin/python -m pytest -p no:cacheprovider -q tests/rpc \
    tests/agents/test_rpc_sprint_pins.py tests/agents_ext/test_spawn_preexec_platform.py \
    tests/pi_parity/test_phase_4_4_strict_superset.py tests/agents_ext/test_rpc_channel.py \
    tests/agents_ext/test_child_argv_contract.py tests/agents_ext/test_child_context_files_inherit.py \
    tests/process_tree tests/tools/test_process_tree_win32.py --durations=8
...
374 passed, 2 skipped in 31.58s
```

(The two skips are pre-existing and unrelated: `tests/rpc/test_rpc_mode_deferred.py:123`
"got empty parameter set for (cmd_type)" and one in `test_spawn_preexec_platform.py`.)

`tests/agents_ext/test_rpc_channel*.py` — `grep -rln rpc_channel tests/` matched
`test_rpc_channel.py`, `test_child_argv_contract.py`, `test_child_context_files_inherit.py`;
all three are in the run above.

### Blast radius beyond the lane

```
$ .venv/bin/python -m pytest -p no:cacheprovider -q tests/agents_ext tests/agents
1677 passed, 11 skipped in 47.62s

$ .venv/bin/python -m pytest -p no:cacheprovider -q tests/server tests/pi_parity
440 passed in 0.60s
```

(`grep -rln 'RpcClient\|run_rpc_mode' tests/` → tests/agents, tests/agents_ext,
tests/pi_parity, tests/rpc, tests/server. All run.)

### Timing the task asked for

Cooperative-exit test (`elapsed < grace` is its subject):

```
$ .venv/bin/python -m pytest -p no:cacheprovider -q \
    "tests/rpc/test_rpc_client_shutdown.py::test_a_cooperative_child_exits_under_the_grace" \
    -vv --durations=0
tests/rpc/test_rpc_client_shutdown.py::test_a_cooperative_child_exits_under_the_grace PASSED [100%]

============================== slowest durations ===============================
0.15s call     tests/rpc/test_rpc_client_shutdown.py::test_a_cooperative_child_exits_under_the_grace

(2 durations < 0.005s hidden.  Use -vv to show these durations.)
============================== 1 passed in 0.18s ===============================
```

0.15 s against the 1.000 s grace, and `proc.returncode == 3` — the child left
through its own handler.

`test_stop_ends_a_descendant_the_child_left_behind`:

```
1.15s call     tests/rpc/test_rpc_client_containment.py::test_stop_ends_a_descendant_the_child_left_behind
```

1.15 s = the full 1.000 s soft-kill grace (the stub survives it) + the escalation
and the probe. Its neighbours for scale:

```
1.14s call     tests/agents/test_rpc_sprint_pins.py::test_stop_is_bounded_by_the_documented_worst_case
0.41s call     tests/rpc/test_rpc_client_shutdown.py::test_stop_escalates_to_the_hard_kill_when_the_soft_one_is_survived
0.37s call     tests/agents/test_rpc_sprint_pins.py::test_the_real_rpc_child_answers_the_client_it_ships_with
```

### It fails on main (doctrine/F10, done without a second worktree)

Instead of `git worktree add /tmp/wt-202-main` (outside this lane's file list), a
scratchpad pytest plugin re-imposes `main` 39549b9's `stop()` body verbatim
(`proc.terminate()` → grace → `proc.kill()`, root-only) over the branch's:

```
$ PYTHONPATH=<scratchpad> .venv/bin/python -m pytest -p no:cacheprovider \
    -p mainstop_plugin -q \
    "tests/rpc/test_rpc_client_containment.py::test_stop_ends_a_descendant_the_child_left_behind"
E           AssertionError: the grandchild is alive five seconds after stop() — the kill reached the child and stopped there, which is the #202 defect
E           assert 'alive' in ('gone', 'zombie')

tests/rpc/test_rpc_client_containment.py:367: AssertionError
=========================== short test summary info ============================
FAILED tests/rpc/test_rpc_client_containment.py::test_stop_ends_a_descendant_the_child_left_behind
1 failed in 6.19s
```

Plugin: `<scratchpad>/mainstop_plugin.py`. The integration lane should still run
the real `/tmp/wt-202-main` demonstration §I asks for; this is the same evidence
by a cheaper route.

### The real-child `returncode == 0` assertion is not vacuous

Mutated it to `== 999`, ran, reverted (file restored from a scratchpad copy and
`grep` confirmed `returncode == 0` back at line 266):

```
E       AssertionError: the real child exited 0; the shutdown path did not run its teardown
E       assert 0 == 999
E        +  where 0 = <Process 73511>.returncode
```

So the real `--mode rpc` child does answer the soft signal and return 0 on
darwin. The windows leg is what measures the new `SIGBREAK` handler.

### Locked citation anchors

Not touched, only shifted. Anchor TEXTS re-grepped and all still present
verbatim, so `check_citations.py --fix` can relocate rather than refuse:
`rpc_client.py:868-871` (`def _listener(...)`), `:952-952`
(`argv = [sys.executable, "-m", "aelix", "--mode", "rpc"]`), `rpc_mode.py:1475`,
`:2043-2046`, `:2156-2158`. `subprocess_hooks.py` and `print_channel.py` are not
in this lane and were not opened for writing.

### Strays

```
$ ps -eo pid,ppid,pgid,command | grep -i "time.sleep(60)\|time.sleep(30)" | grep -v grep
(no output)
```

### Final gate run (after the last comment refinements)

```
$ .venv/bin/ruff check <the 5 files>
All checks passed!

$ .venv/bin/pyright --pythonplatform Windows <rpc_client.py> <rpc_mode.py>
0 errors, 0 warnings, 0 informations
$ .venv/bin/pyright --pythonplatform Linux <rpc_client.py> <rpc_mode.py>
0 errors, 0 warnings, 0 informations

$ .venv/bin/python -m pytest -p no:cacheprovider -q tests/rpc \
    tests/agents/test_rpc_sprint_pins.py tests/agents_ext/test_spawn_preexec_platform.py \
    tests/pi_parity/test_phase_4_4_strict_superset.py tests/agents_ext/test_rpc_channel.py \
    tests/agents_ext/test_child_argv_contract.py tests/agents_ext/test_child_context_files_inherit.py
327 passed, 2 skipped in 29.98s
```

## Not done in this lane (deliberately)

- `check_citations.py --fix/--check`: anchors only SHIFTED, texts intact; the
  integration lane owns the relocation and the `git add` of the lock.
- The real `git worktree add /tmp/wt-202-main` demonstration (§I). Substituted by
  the plugin above; the integration lane should still do the real one.
- Windows-leg measurements. Everything here is darwin (Python 3.12.13). The
  `SIGBREAK` handler, `CTRL_BREAK_EVENT` delivery, `IsProcessInJob` and the job
  arms of these tests are UNMEASURED on this box by construction.
- The live rpc-delegation run CLAUDE.md rule 10 asks for.


---

# Lane: hooks

# lane: hooks — #202 progress log

Worktree /tmp/wt-202, branch fix/202-process-tree-containment.
Files: packages/aelix-coding-agent/src/aelix_coding_agent/extensions/subprocess_hooks.py,
       tests/subprocess_hooks/test_subprocess_hooks.py

## reads
- spec .omc/specs/202-design-2026-09-05.md (rev 2) — §0, §C.3, §D.4, §F
- foundation module packages/aelix-ai/src/aelix_ai/utils/_process_tree.py landed
  (exports ProcessTree, _retained_handle, containment_spawn_kwargs, kill_process_tree)
- tests/process_probe.py — is_dead_or_zombie / await_dead_or_zombie(timeout=, interval=)
- tests/process_tree/test_process_tree_real_processes.py — style reference

## commands run (all in /tmp/wt-202)

1. `.venv/bin/ruff check packages/aelix-coding-agent/src/aelix_coding_agent/extensions/subprocess_hooks.py`
   -> `All checks passed!`
2. `.venv/bin/pyright <product file>` -> `0 errors, 0 warnings, 0 informations`
3. `.venv/bin/pyright --pythonplatform Windows <product file>` -> `0 errors, 0 warnings, 0 informations`
4. `.venv/bin/ruff check tests/subprocess_hooks/test_subprocess_hooks.py` -> `All checks passed!`
5. `.venv/bin/python -m pytest -p no:cacheprovider -q tests/subprocess_hooks` -> `29 passed in 0.93s`
6. `... -q tests/subprocess_hooks -k "orphan or backgrounds" -v` -> `2 passed, 27 deselected in 0.60s`
7. non-vacuity probe (scratchpad/main_shape.py replicates main's terminate/kill-the-shell
   teardown) -> `MAIN-SHAPE grandchild 54273 state after teardown: alive`
8. `... -q tests/subprocess_hooks tests/extensions tests/agents/test_p2_band_boundaries.py`
   -> `282 passed in 6.72s`

## anchor
`grep -n "proc = await asyncio.create_subprocess_shell($"` -> line 163, byte-identical.

## timeout widening (deviation from spec §D.4's `timeout_ms=500`)

Measured the hook's setup latency (shell -> interpreter startup -> grandchild
Popen -> pid file) with scratchpad/measure_setup.py: `21, 19, 19, 19, 16 ms` on
the owner's box.  500 ms leaves only a ~1.4x margin once the windows leg's much
larger interpreter startup is paid, and a miss there fails the case for a reason
unrelated to containment (the pid file would not exist).  Raised to 1500 ms; the
child sleeps 60 s so the teardown under test is identical.

## final gates

- `.venv/bin/ruff check <product> <test>` -> `All checks passed!`
- `.venv/bin/pyright <product>` -> `0 errors, 0 warnings, 0 informations`
- `.venv/bin/pyright --pythonplatform Windows <product>` -> `0 errors, 0 warnings, 0 informations`
- `.venv/bin/python -m pytest -p no:cacheprovider -q tests/subprocess_hooks` -> `29 passed in 1.93s`
- `... -q tests/subprocess_hooks tests/extensions tests/agents/test_p2_band_boundaries.py` -> `282 passed in 6.72s`
- `... -q tests/pi_parity` -> `423 passed in 0.64s`
- `ps -eo pid,ppid,command | grep "time.sleep(60)"` -> empty (no strays leaked)


---

# Lane: resolve_config

# lane: resolve_config — #202 progress log

Worktree /tmp/wt-202, branch fix/202-process-tree-containment (from main 39549b9).

## reading
- `cat` spec `.omc/specs/202-design-2026-09-05.md` (main checkout, read-only) — ok
- `cat -n packages/aelix-ai/src/aelix_ai/oauth/_resolve_config.py` — ok
- `cat -n packages/aelix-ai/src/aelix_ai/utils/_process_tree.py` (foundation lane, untracked) — ok
- `cat -n tests/oauth/test_resolve_config.py` — ok
- `sed -n tests/process_probe.py` — `is_dead_or_zombie(pid)` sync, `await_dead_or_zombie` async
- `git status --short` -> foundation lane files only; my two files untouched by others

## edits
- `packages/aelix-ai/src/aelix_ai/oauth/_resolve_config.py`
  - import `ProcessTree, _retained_handle, containment_spawn_kwargs` from `aelix_ai.utils._process_tree`
  - `_run_shell_command` docstring gains the CONTAINMENT text (#202/ADR-0238): why the tree, and why `process_group=0` not `setsid` (the `/dev/tty` credential helpers)
  - `Popen(..., **containment_spawn_kwargs())`; `ProcessTree.attach(proc.pid, handle=_retained_handle(proc))` (kill_on_close default False)
  - both `proc.kill()` sites -> `tree.hard_kill()`; body wrapped in `try/finally: tree.close()`
  - `cast("subprocess.Popen[bytes]", ...)` — see the pyright note below
- `tests/oauth/test_resolve_config.py` — existing cases untouched; added
  `test_a_timed_out_command_does_not_orphan_its_pipeline`,
  `test_resolve_config_spawns_a_new_group_in_the_same_session`, plus
  `_reap` / `_await_dead` / `_PIPELINE_SOURCE` helpers.

## commands

$ .venv/bin/ruff check packages/aelix-ai/src/aelix_ai/oauth/_resolve_config.py tests/oauth/test_resolve_config.py
All checks passed!

$ .venv/bin/python -m pytest -p no:cacheprovider -q tests/oauth
236 passed in 7.66s

$ .venv/bin/pyright packages/aelix-ai/src/aelix_ai/oauth/_resolve_config.py    # FIRST attempt, before the cast
  _resolve_config.py:101:31 - error: Argument of type "str" cannot be assigned to parameter "object" of type "bytes" in function "append" (reportArgumentType)
1 error
  -> cause measured in scratchpad/probe_overload.py: unpacking a dict[str, Any]
     costs pyright the Popen text/bytes overload discrimination; it settles on
     Popen[str]. `text=False` does NOT steer it back (reveal_type: Popen[str]);
     a declared `proc: subprocess.Popen[bytes] = ...` fails invariance.
     Fixed with `cast("subprocess.Popen[bytes]", ...)` + a WHY comment.

$ .venv/bin/pyright packages/aelix-ai/src/aelix_ai/oauth/_resolve_config.py
0 errors, 0 warnings, 0 informations

$ .venv/bin/pyright --pythonplatform Windows packages/aelix-ai/src/aelix_ai/oauth/_resolve_config.py
0 errors, 0 warnings, 0 informations

$ .venv/bin/python -m pytest -p no:cacheprovider -q tests/oauth tests/model_registry
349 passed in 7.80s

## vacuity check (the new test must bite)
$ python - <<'X'  # temporarily replaced both `tree.hard_kill()` with `proc.kill()`
$ .venv/bin/python -m pytest -p no:cacheprovider -q tests/oauth/test_resolve_config.py::test_a_timed_out_command_does_not_orphan_its_pipeline
E   AssertionError: the ``!command`` pipeline outlived its kill (state=alive)
FAILED tests/oauth/test_resolve_config.py::test_a_timed_out_command_does_not_orphan_its_pipeline
1 failed in 7.09s
  -> product file restored from scratchpad/_resolve_config.py.bak; `grep -c "tree.hard_kill()"` == 2

## flakiness
$ for i in 1 2 3; do pytest -q -k "orphan or new_group"; done
2 passed, 19 deselected in 2.05s  (x3)

$ ps -A | grep "time.sleep(60)"   -> no output; no leaked children

## deviations from the spec (§C.4 / §D.5)
1. `_COMMAND_TIMEOUT` patched to **2.0**, not 0.5. The 0.5 s clock starts when
   `Popen` returns and has to cover sh startup + interpreter startup + the
   grandchild spawn + the pid-file write BEFORE the pid file exists; two
   interpreter startups on the windows leg do not reliably fit. A miss there is
   a red test about runner latency, not about the kill. The case also asserts
   `pidfile.exists()` with that exact message so the failure says which it was.
2. The `_run_shell_command` docstring gains TWO paragraphs, not one — same
   content (why the tree; why `process_group` and not `setsid`), split for
   readability.
3. `cast("subprocess.Popen[bytes]", ...)` is not in §C.4's sketch; the type gate
   forced it (see the pyright evidence above). Commented as a checker-only fact.

## citations
$ grep -n "_resolve_config" citations.lock.json  -> no matches; no locked anchor
  lives in either lane file, and no anchor cites them.


---

# Lane: docs

# Lane: docs (#202) — progress log

Worktree: /tmp/wt-202  branch fix/202-process-tree-containment

## 0. Evidence gathering

The brief pointed at `scratchpad/critique-digest.md`. **It does not exist** —
`ls` shows no `crit*`/`digest*`/`review*` file in the scratchpad. So every
measured number quoted in the ADR is re-derived from a source I can name:

- `scratchpad/progress-foundation.md` (the foundation lane's own log) for the
  darwin zombie-leader `getpgid` run and the root-only-teardown demo.
- `scratchpad/pty_driver.py` + `tty_test.py`, re-run by me just now (below).
- the spec's §0/§1 tables for the Pi facts and the site table.
- the brief itself for `grandchild alive after stop(): True`.

```
$ (cd scratchpad && /tmp/wt-202/.venv/bin/python pty_driver.py)
parent ctty check: parent HAS ctty
start_new_session= False -> HAVE_TTY | err:
start_new_session= True -> NO_TTY | err: sh: /dev/tty: Device not configured
```

Darwin zombie-leader measurement, from `progress-foundation.md` (the same
`scratchpad/darwin_zombie_getpgid.py` run):

```
platform: darwin
leader state: zombie  grandchild: alive
getpgid(leader) RAISED ProcessLookupError [Errno 3] No such process
after killpg(pid): grandchild -> gone
```

Root-only teardown demo, same source (`scratchpad/root_only_demo.py`):

```
grandchild before: alive
root already reaped; a root-only kill has nothing left to aim at
after root-only kill: alive
after tree.hard_kill(): gone
```

## 1. Files written

- `docs/decisions/0238-the-kill-reached-the-child-and-the-tree-is-what-had-to-die.md`
  (new, 321 lines — under the ~350 bound). Header block modelled on 0237
  (Status / Date / Supersedes-relates / Issue / Design spec). Sections: what was
  broken (the §1 table + the `grandchild alive after stop(): True` headline),
  the two Pi facts with `7af2d27d` and #9129's 70 minutes, Decision (§A
  verbatim incl. the asymmetry, `close()` as release, `kill_on_close` per site,
  the never-`killpg`-a-stranger rule), Windows semantics stated plainly
  (TerminateProcess uncatchable; a group is a console-event address;
  `os.kill(pid, 0)` is a Ctrl+C; KILL_ON_JOB_CLOSE fires on parent death),
  POSIX consequences (the pty measurement quoted; the `setsid` gap reconciled
  with ADR-0197 finding I2 / `reaper.py:33-38`), the accepted pid/pgid hazard
  and the assignment window (0.3 / 1.05 / 1.65 ms vs ~2 ms to first fork),
  what the tests pin, what stays open (#220 #221 #222), Consequences.
- `docs/decisions/README.md` — one row inserted after 0237 (line 144), same
  column shape, Korean summary at the density of 0235-0237.
- `README.md` / `README.ko.md` — the #202 bullet is now a split verdict (rpc /
  hooks / `!command` contained, print channel + reaper still orphan → #220),
  ADR-0238 linked exactly as ADR-0237 is; the closing sentence keeps
  "process-group cleanup" but scopes it to the print channel.
- `SLICE-STATUS.md` — header note (#202 closed 2026-09-05), W3 row now points
  at `packages/aelix-ai/src/aelix_ai/utils/_process_tree.py`, "Two facts"
  conclusion becomes "a Job Object, with `taskkill /T /F` as the fallback
  (#202)" plus the #9129 reason, Remaining §2 amended IN PLACE (→ #220),
  Remaining §10 gets a **Superseded (2026-09-05)** paragraph.
- `CHANGELOG.md` — new first entry in the existing `[Unreleased] → Fixed`
  subsection (the section already existed; none was created).
- `reaper.py` — both "That is #202, not this" sentences rewritten to §G.7's
  wording with #220. `DEFAULT_GRACE_SECONDS = 5.0` at line 80 untouched, and
  both edits are BELOW it so the only locked anchor in that file (`:80-80`)
  does not shift.
- `print_channel.py` — lines 971-973 replaced by exactly three lines; line 970
  (`# ``None`` on Windows, where subprocess REJECTS the`) is byte-identical and
  `preexec_fn=` is still at 974, so `:1106-1113`, `:1194-1197` and `:1218-1220`
  do not move.

## 2. Gates

```
$ .venv/bin/ruff check packages/aelix-coding-agent/src/aelix_agents/reaper.py \
    packages/aelix-coding-agent/src/aelix_agents/print_channel.py
All checks passed!

$ .venv/bin/pyright <reaper.py> <print_channel.py>
0 errors, 0 warnings, 0 informations
$ .venv/bin/pyright --pythonplatform Windows <reaper.py> <print_channel.py>
0 errors, 0 warnings, 0 informations

$ .venv/bin/python -m pytest -p no:cacheprovider -q tests/docs \
    tests/test_docs_bundle_sync.py tests/agents_ext/test_reaper_kill_signal_win32.py \
    tests/agents_ext/test_print_channel_spawn.py -x
92 passed, 10 skipped in 19.87s
```

No `ruff format` was run on anything (ADR-0152; none of my files are in the
spec's scoped format list). No `git add` / `commit` / `checkout` / `stash`.
`git status --porcelain` shows my 7 lane paths plus files owned by the
foundation / rpc / sites lanes, which I did not touch.


---

# Lane: integration

# integration lane — #202 (spec §I)

## step 0: git status --short in /tmp/wt-202

```
 M CHANGELOG.md
 M README.ko.md
 M README.md
 M SLICE-STATUS.md
 M docs/decisions/README.md
 M packages/aelix-ai/src/aelix_ai/oauth/_resolve_config.py
 M packages/aelix-coding-agent/src/aelix_agents/print_channel.py
 M packages/aelix-coding-agent/src/aelix_agents/reaper.py
 M packages/aelix-coding-agent/src/aelix_coding_agent/extensions/subprocess_hooks.py
 M packages/aelix-coding-agent/src/aelix_coding_agent/rpc/rpc_client.py
 M packages/aelix-coding-agent/src/aelix_coding_agent/rpc/rpc_mode.py
 M packages/aelix-coding-agent/src/aelix_coding_agent/tools/_process_tree.py
 M tests/agents/test_rpc_sprint_pins.py
 M tests/oauth/test_resolve_config.py
 M tests/rpc/test_rpc_client_containment.py
 M tests/rpc/test_rpc_client_shutdown.py
 M tests/subprocess_hooks/test_subprocess_hooks.py
 M tests/tools/test_process_tree_win32.py
?? docs/decisions/0238-the-kill-reached-the-child-and-the-tree-is-what-had-to-die.md
?? packages/aelix-ai/src/aelix_ai/utils/_process_tree.py
?? tests/process_tree/
```

## step 1: citations

```
$ git add packages tests docs README.md README.ko.md SLICE-STATUS.md CHANGELOG.md citations.lock.json
$ .venv/bin/python scripts/check_citations.py --fix
relocated 5 citation(s) across 5 file(s).

1 new citation(s) had no anchor; they are locked as written.
locked 486 anchor(s) over 850 gated citation(s) (32 ungated, 0 out of range).

$ .venv/bin/python scripts/check_citations.py --check   # EXIT=0
citations OK — 850 gated, none drifted.
```

Relocated: subprocess_hooks.py 149->163 (anchor text byte-identical),
rpc_client.py 868-871->944-947 and 952->1028, rpc_mode.py 2156-2158->2179-2181.
New (locked as written): bash.py:300-330 (cited by the new module / ADR).
No anchor was "stuck"; print_channel.py:970-970 and the other three
print_channel anchors did not move at all.

## steps 2-4: gates

```
$ .venv/bin/ruff check .
All checks passed!

$ .venv/bin/ruff format --check packages/aelix-ai/src/aelix_ai/utils/_process_tree.py tests/process_tree
4 files already formatted

$ .venv/bin/python scripts/check_types.py      # EXIT=0
OK  gate: 277 files analysed (floor 220)
OK  gate: 0 errors across 277 files
OK  spike: all 3 inverse assertions still error (narrowing alive)
PASS: type gate clean, narrowing spike still inverted

$ .venv/bin/pyright --pythonplatform Windows <10 changed .py under packages/>
0 errors, 0 warnings, 0 informations
```

check_citations.py --fix also rewrote two citing files that were NOT in the
original status (pure line-number relocations, no other change):
packages/aelix-coding-agent/src/aelix_agents/rpc_channel.py (rpc_client.py:868-871 -> :944-947)
packages/aelix-server/src/aelix_server/rpc_ws.py (rpc_mode.py:2156-2158 -> :2179-2181)

## step 5: pytest

```
$ .venv/bin/python -m pytest -p no:cacheprovider -q tests/process_tree \
    tests/tools/test_process_tree_win32.py tests/rpc tests/agents/test_rpc_sprint_pins.py \
    tests/subprocess_hooks tests/oauth/test_resolve_config.py \
    tests/agents_ext/test_reaper_kill_signal_win32.py tests/agents_ext/test_spawn_preexec_platform.py \
    tests/pi_parity/test_phase_4_4_strict_superset.py         # EXIT=0
348 passed, 1 skipped in 15.74s

$ .venv/bin/python -m pytest -p no:cacheprovider -q            # EXIT=0
10085 passed, 12 skipped, 10 warnings in 265.70s (0:04:25)
```

## step 6: the "fails on main" demonstration (spec §I, doctrine/F10)

`git worktree add /tmp/wt-202-main main` was refused — `main` is already checked
out at /Users/handochan/dev/aelix-ai — so the worktree was created **detached at
the same SHA** the branch was cut from:

```
$ cd /tmp/wt-202 && git worktree add --detach /tmp/wt-202-main 39549b9
Preparing worktree (detached HEAD 39549b9)
HEAD is now at 39549b9 docs(specs): handoff — #110 definition ①②③ closed (#200 #106 #204), next is #202
```

Copied in from the branch: `tests/rpc/test_rpc_client_containment.py`,
`tests/process_probe.py`, and — because the test file imports
`from aelix_ai.utils._process_tree import _retained_handle, kill_process_tree`
at module level and that module does not exist on main —
`packages/aelix-ai/src/aelix_ai/utils/_process_tree.py`. That module is purely
additive there: `aelix_ai/utils/__init__.py` is untouched by this change, and
nothing in main's product code imports it, so `RpcClient.stop()` under test is
still main's. Confirmed:

```
$ grep -n "async def stop" -A 45 .../rpc_client.py | grep -E "terminate|\.kill\(|ProcessTree"
476-            proc.terminate()
486-                proc.kill()
```

The venv's editable installs are plain-path `.pth` files (no meta-path finder),
so PYTHONPATH wins. Verified before running:

```
$ PYTHONPATH=/tmp/wt-202-main:/tmp/wt-202-main/src:/tmp/wt-202-main/packages/*/src \
  /tmp/wt-202/.venv/bin/python -c "import aelix_coding_agent, aelix_ai, aelix_agent_core; ..."
/tmp/wt-202-main/packages/aelix-coding-agent/src/aelix_coding_agent/__init__.py
/tmp/wt-202-main/packages/aelix-ai/src/aelix_ai/__init__.py
/tmp/wt-202-main/packages/aelix-agent-core/src/aelix_agent_core/__init__.py
/tmp/wt-202-main/packages/aelix-coding-agent/src/aelix_coding_agent/rpc/rpc_client.py   # inspect.getsourcefile(RpcClient.stop)
```

Run (EXIT=1):

```
$ cd /tmp/wt-202-main && PYTHONPATH=... /tmp/wt-202/.venv/bin/python -m pytest -p no:cacheprovider -q \
    "tests/rpc/test_rpc_client_containment.py::test_stop_ends_a_descendant_the_child_left_behind"
F                                                                        [100%]
=================================== FAILURES ===================================
______________ test_stop_ends_a_descendant_the_child_left_behind _______________
            state = await await_dead_or_zombie(grandchild, timeout=5.0)
>           assert state in (STATE_GONE, STATE_ZOMBIE), (
                f"the grandchild is {state} five seconds after stop() — the kill "
                "reached the child and stopped there, which is the #202 defect"
            )
E           AssertionError: the grandchild is alive five seconds after stop() — the kill reached the child and stopped there, which is the #202 defect
E           assert 'alive' in ('gone', 'zombie')

tests/rpc/test_rpc_client_containment.py:368: AssertionError
=========================== short test summary info ============================
FAILED tests/rpc/test_rpc_client_containment.py::test_stop_ends_a_descendant_the_child_left_behind
1 failed in 6.22s
```

Same node id on the branch:

```
$ cd /tmp/wt-202 && .venv/bin/python -m pytest -p no:cacheprovider -q \
    "tests/rpc/test_rpc_client_containment.py::test_stop_ends_a_descendant_the_child_left_behind"
.                                                                        [100%]
1 passed in 1.17s
```

`pgrep -fl "sleep(60)"` after both runs: nothing — no sleeper leaked.
Worktree removed: `git worktree remove --force /tmp/wt-202-main` (verified gone).

## §F anchor check (locked lines byte-identical)

- `subprocess_hooks.py` `proc = await asyncio.create_subprocess_shell(` — text
  unchanged, line 149 -> 163, relocated by `--fix`.
- `print_channel.py:970` `# ``None`` on Windows, where subprocess REJECTS the` —
  unchanged in `git diff 39549b9`; only 971-973 were rewritten, and the four
  print_channel anchors did not move at all.

## step 7 + final state

```
$ .venv/bin/python scripts/check_citations.py --check      # EXIT=0 (re-run after everything)
citations OK — 850 gated, none drifted.

$ .venv/bin/python -m pytest -p no:cacheprovider -q -rs tests/process_tree \
    tests/tools/test_process_tree_win32.py tests/rpc tests/subprocess_hooks tests/oauth/test_resolve_config.py
SKIPPED [1] tests/rpc/test_rpc_mode_deferred.py:123: got empty parameter set for (cmd_type)
313 passed, 1 skipped in 14.17s
```

The one skip in the touched area predates #202 (an empty parametrize set), not a
platform gate. `test_preexec_fn_runs_in_the_child`'s `skipif(os.name != "posix")`
survives and is within doctrine — `tests/conftest.py:112-119` allows a skip when
the OS makes the defence *unreachable* rather than merely untested, and
`preexec_fn` does not exist on win32 at all.

Final `git status --short` (staged only so `check_citations.py` could see the new
files; NOTHING committed):

```
M  CHANGELOG.md
M  README.ko.md
M  README.md
M  SLICE-STATUS.md
M  citations.lock.json
A  docs/decisions/0238-the-kill-reached-the-child-and-the-tree-is-what-had-to-die.md
M  docs/decisions/README.md
M  packages/aelix-ai/src/aelix_ai/oauth/_resolve_config.py
A  packages/aelix-ai/src/aelix_ai/utils/_process_tree.py
M  packages/aelix-coding-agent/src/aelix_agents/print_channel.py
M  packages/aelix-coding-agent/src/aelix_agents/reaper.py
M  packages/aelix-coding-agent/src/aelix_agents/rpc_channel.py          <- citations --fix only
M  packages/aelix-coding-agent/src/aelix_coding_agent/extensions/subprocess_hooks.py
M  packages/aelix-coding-agent/src/aelix_coding_agent/rpc/rpc_client.py
M  packages/aelix-coding-agent/src/aelix_coding_agent/rpc/rpc_mode.py
M  packages/aelix-coding-agent/src/aelix_coding_agent/tools/_process_tree.py
M  packages/aelix-server/src/aelix_server/rpc_ws.py                     <- citations --fix only
M  tests/agents/test_p2_band_boundaries.py                              <- citations --fix only
M  tests/agents/test_rpc_sprint_pins.py
M  tests/agents_ext/test_child_argv_contract.py                         <- citations --fix only
M  tests/oauth/test_resolve_config.py
A  tests/process_tree/__init__.py
A  tests/process_tree/test_process_tree_api.py
A  tests/process_tree/test_process_tree_real_processes.py
M  tests/rpc/test_rpc_client_containment.py
M  tests/rpc/test_rpc_client_shutdown.py
M  tests/subprocess_hooks/test_subprocess_hooks.py
M  tests/tools/test_process_tree_win32.py
?? .omc/specs/202-progress-2026-09-05.md                                <- not staged, never committed
```

Four files not in any lane's list were touched by `check_citations.py --fix`
alone (line-number relocations of `rpc_client.py:868-871`/`:952`,
`rpc_mode.py:2156-2158`, `subprocess_hooks.py:149`); every diff hunk there is a
single number inside a comment or docstring.

Placeholder sweep: no `<new issue>` / `NEW ISSUE` string survives in
packages/tests/docs/README*/SLICE-STATUS/CHANGELOG; #220/#221/#222 appear in
`tools/_process_tree.py`, `reaper.py` ×2, `print_channel.py`, ADR-0238,
`docs/decisions/README.md`, `SLICE-STATUS.md`, both READMEs and `CHANGELOG.md`.

NOT done by this lane (spec §I's last paragraph): the live CLAUDE.md-rule-10
runs (a real-model delegation aborted mid-turn, and a timing-out hook with a
pipeline) — they need provider credentials and were not in this lane's step
list. And the windows-latest leg has not run for this branch at all.



---

# Review fixes (round 2)

Four fix lanes (primitive, rpc, hooks+resolve_config, docs) applied the round-2
review findings on top of cb300dc, then one integration lane re-ran the gates.
Each lane's own progress log follows verbatim, integration last.


---

## Lane log: fix-progress-primitive.md

# fix lane: primitive (#202 review fixes)

Worktree /tmp/wt-202, base cb300dc. python = /tmp/wt-202/.venv/bin/python.

Files in lane:
- packages/aelix-ai/src/aelix_ai/utils/_process_tree.py
- packages/aelix-coding-agent/src/aelix_coding_agent/tools/_process_tree.py (docstring only)
- tests/process_tree/test_process_tree_api.py
- tests/process_tree/test_process_tree_real_processes.py
- tests/tools/test_process_tree_win32.py

## Log

### Changes applied

1. `ProcessTree.soft_kill(self, *, whole_group: bool = False) -> bool` — True when the
   soft signal was actually SENT (os.kill/os.killpg returned, or ctrl_break returned
   without raising); False when it raised (no console on win32 / ESRCH on POSIX) or the
   tree is closed. Docstring says a caller getting False has nothing to wait for and
   should go straight to hard_kill (win-leg/F2 remedy (a)). Both production callers
   (subprocess_hooks.py:204, rpc_client.py:533) ignore the value today — consuming it is
   another lane's edit.
2. `kill_process_tree` POSIX: `getpgid(pid) != pid` -> root-only `os.kill(pid, SIGKILL)`
   and return; never killpg a group whose leader is not pid (posix2/F5). The
   ProcessLookupError -> killpg(pid) fallback is unchanged.
3. `_taskkill_tree`: `subprocess.run(..., timeout=5)`, `except (OSError,
   subprocess.SubprocessError)` (win-leg/F6). Docstring states the escalation blocks the
   caller (a coroutine at every win32 site) for the spawn, bounded at 5 s.
4. Wording: module docstring THE ASYMMETRY (hook/!command shells removed from the setsid
   list, process_group=0 sentence added — D1); THE PID/PGID HAZARD (returncode-is-None is
   not proof of unreaped; the honest bound is watcher-reap -> loop-callback latency +
   grace; the group is what bounds it — posix2/F4); kill_process_tree docstring (bash.py
   before the reap, _subprocess.py after it, third caller rpc_client.stop() named —
   posix2/F1, D9).
5. tests/process_tree/test_process_tree_real_processes.py: same wording fix in
   test_kill_process_tree_reaches_the_group_of_a_zombie_leader; `_reap` resolves
   `%SYSTEMROOT%\System32\taskkill.exe` with the bare-name retry, root-only /F /PID kept
   (win-leg/F8); soft_kill's return asserted in test_soft_kill_is_delivered_and_survivable
   so a console-less windows runner says so directly. Same wording fix in
   tests/tools/test_process_tree_win32.py and tests/process_tree/test_process_tree_api.py.
6. Shim docstring: "one of those (!command, aelix_ai/oauth/_resolve_config.py) lives in
   aelix-ai" (D4).

Test edits: api file gained test_win32_soft_kill_reports_a_console_event_it_could_not_send
and test_posix_soft_kill_reports_a_signal_it_could_not_send[False|True]; every existing
soft_kill call now asserts its return. win32 file: test_posix_still_kills_the_process_group
RENAMED/split into test_posix_kills_the_process_group_of_a_child_that_leads_one (leader ->
killpg(pid)) and test_posix_never_killpgs_a_group_the_pid_does_not_lead (getpgid -> pid+1000
-> root-only os.kill, killpg fails the test); new
test_win32_survives_a_taskkill_that_never_returns pins timeout=5 + the SubprocessError catch.
The api spies' fake_run now asserts `timeout == 5`.

### Commands

$ cd /tmp/wt-202 && .venv/bin/python -m pytest -p no:cacheprovider -q tests/process_tree tests/tools tests/agents_ext/test_reaper_kill_signal_win32.py
285 passed in 4.00s                                                        (exit 0)

$ .venv/bin/ruff check .
All checks passed!                                                         (exit 0)

$ .venv/bin/pyright --pythonplatform Windows packages/aelix-ai/src/aelix_ai/utils/_process_tree.py packages/aelix-coding-agent/src/aelix_coding_agent/tools/_process_tree.py
0 errors, 0 warnings, 0 informations                                       (exit 0)

$ .venv/bin/pyright --pythonplatform Linux <same two files>
0 errors, 0 warnings, 0 informations                                       (exit 0)

$ .venv/bin/ruff format --check packages/aelix-ai/src/aelix_ai/utils/_process_tree.py tests/process_tree
4 files already formatted                                                  (exit 0)

$ .venv/bin/ruff format --diff tests/tools/test_process_tree_win32.py packages/.../tools/_process_tree.py
2 files already formatted   (read-only check on the two MODIFIED files; no reformat run)

Ripple check (not in the required set, adjacent suites that call the changed API):
$ .venv/bin/python -m pytest -p no:cacheprovider -q tests/rpc tests/subprocess_hooks tests/oauth/test_resolve_config.py tests/agents/test_rpc_sprint_pins.py
272 passed, 1 skipped in 14.21s                                            (exit 0)

### Non-vacuity (sabotage, each reverted immediately)

A. delete the `pgid != pid` guard  -> FAILED test_posix_never_killpgs_a_group_the_pid_does_not_lead
   ("that group is ours, not the child's"), 1 failed / 280 passed.
B. win32 soft_kill back to suppress+return True -> 8 failed, incl.
   test_win32_soft_kill_reports_a_console_event_it_could_not_send and test_never_raises[0..2].
C. drop `timeout=5` (applied with B) -> FAILED test_win32_survives_a_taskkill_that_never_returns
   and the four api taskkill/argv cases.
D. POSIX soft_kill back to swallow+return True -> 5 failed, incl. both
   test_posix_soft_kill_reports_a_signal_it_could_not_send params.
Restored file verified: 285 passed again.

### Not done / out of lane
- Consuming soft_kill's False at rpc_client.stop()/subprocess_hooks (win-leg/F2 remedy (a)
  second half) — those files are not in this lane.
- The identical `_reap` bare-`taskkill` in tests/oauth/test_resolve_config.py:228 and
  tests/subprocess_hooks/test_subprocess_hooks.py:117 (win-leg/F8 names all three) — not in
  this lane; the resolver here is written so it can be copied verbatim.
- No citations run (citations.lock.json has zero anchors in any lane file: `grep -c
  _process_tree citations.lock.json` -> 0). No git add/commit.

---

## Lane log: fix-progress-rpc.md

# LANE rpc — fix progress (#202 review round 2)

Worktree /tmp/wt-202, branch fix/202-process-tree-containment, base cb300dc.

## Findings in scope
- win-leg/F2 (remedy (a)): stop() uses soft_kill()'s new bool; a refused soft signal skips the grace.
- posix2/F2: rpc_client module docstring §2 overstates POSIX ("now killpg rather than a signal at the root").
- posix2/F8 / MUT-8: stop() docstring must own transport.close()'s Popen.poll()/kill().
- posix2/F4 (COMMON): the "50 ms poll" bound is not the honest bound.
- MUT-1: escalation test asserts the grace, never the hard kill.
- win-leg/F9: the CPython console-handler paragraph is uncited.
- win-leg/F4: the real-child stop() bound borrows the product constant.
- posix2/F7: _PIPE_HOLDER_STUB leaks a 30 s sleeper per case.
- MUT-5 / MUT-6: attach-before-first-await and kill_on_close=True at the rpc site are unpinned.
- win-leg/F5 / MUT-2: rpc_mode's signal install/restore has zero coverage.

## Commands

### Product edits (rpc_client.py only; rpc_mode.py NOT touched — no seam needed)
- module docstring §2 (:47-68) — POSIX claim scoped: SIGTERM at the ROOT, killpg is the
  escalation, a cooperative child's non-setsid leftovers survive on POSIX (posix2/F2).
- stop() docstring — killpg scoped to escalation; transport.close() clause added (posix2/F8, MUT-8).
- stop() body — `soft_kill_refused = not tree.soft_kill()`; the escalation branch is
  `if soft_kill_refused or not await self._await_exit(...)` (win-leg/F2 remedy (a)).
- stop() escalation comment — the "last 50 ms poll" bound replaced by the group bound
  (POSIX.1 §3.293 / attach_pid(PIDTYPE_PGID); empty group -> ESRCH) (posix2/F4).

### Commands + output tails

$ cd /tmp/wt-202 && .venv/bin/python -m pytest -p no:cacheprovider -q tests/rpc/test_rpc_client_shutdown.py
  6 passed in 1.61s

$ # MUTATION M8 (tree.hard_kill() -> pass in stop()), then the same file
  FAILED test_stop_escalates_to_the_hard_kill_when_the_soft_one_is_survived
  FAILED test_a_soft_kill_that_was_never_sent_does_not_buy_a_grace
    AssertionError: stop() took 5.001s against a 0.300s grace it had no reason to pay
  2 failed, 4 passed in 21.57s        [M8 reverted, file restored, 6 passed in 1.60s]

$ # MUTATION M10 (rpc site kill_on_close=False)
  FAILED test_the_rpc_site_asks_for_kill_on_close
    assert False is True ... {'handle': None, 'kill_on_close': False}
  1 failed, 16 passed in 3.18s        [reverted]

$ # MUTATION M9 (ProcessTree.attach moved past the 100 ms startup-grace await)
  FAILED test_the_tree_is_attached_before_the_first_await
    AssertionError: the tree was attached after a task was scheduled ...
    <Task pending name='Task-46' coro=<pump_jsonl_lines() ...>> is None
  1 failed, 16 passed in 3.19s        [reverted; git diff --stat back to 63 insertions/18 deletions]

$ # MUTATION M17 (rpc_mode restore loop back to one try + callable(previous))
  FAILED test_the_win32_arm_installs_a_sigbreak_handler_and_restores_it
    AssertionError: run_rpc_mode kept its SIGBREAK disposition after returning;
    getsignal now answers <function run_rpc_mode.<locals>.<lambda>>
  1 failed, 2 passed in 0.06s         [reverted, rpc_mode.py git diff == 0 lines]

$ # MUTATION M16 (the `if sys.platform == "win32":` SIGBREAK block disabled)
  FAILED test_the_win32_arm_installs_a_sigbreak_handler_and_restores_it
    AssertionError: the win32 arm never installed a SIGBREAK handler
  1 failed, 2 passed in 5.95s         [reverted]

$ .venv/bin/python -m pytest -p no:cacheprovider -q tests/rpc tests/agents/test_rpc_sprint_pins.py \
    tests/agents_ext/test_spawn_preexec_platform.py tests/agents_ext/test_rpc_channel.py \
    tests/pi_parity/test_phase_4_4_strict_superset.py
  284 passed, 2 skipped in 29.75s

$ .venv/bin/python -m pytest -p no:cacheprovider -q tests/tui tests/agents     # signal-handler contamination check
  1645 passed in 88.72s

$ .venv/bin/ruff check
  All checks passed!

$ .venv/bin/pyright --pythonplatform Linux   .../rpc/rpc_client.py .../rpc/rpc_mode.py
  0 errors, 0 warnings, 0 informations
$ .venv/bin/pyright --pythonplatform Windows .../rpc/rpc_client.py .../rpc/rpc_mode.py
  0 errors, 0 warnings, 0 informations

$ ps -eo pid,ppid,pgid,command | grep -E 'sleep\(30\)|sleep\(60\)' | grep -v grep
  (empty, exit 1)   # checked immediately after the lane run, twice

$ .venv/bin/python scripts/check_citations.py --check
  DRIFT, RELOCATION ONLY, from this lane's line shifts in rpc_client.py:
    print_channel.py:472            `rpc/rpc_client.py:1028`   -> moved to 1073
    rpc_channel.py:333              `rpc_client.py:944-947`    -> moved to 989-992
    tests/agents_ext/test_child_argv_contract.py:80 `rpc/rpc_client.py:1028` -> 1073
  (a 4th, subprocess_hooks.py:163, belongs to another lane.)
  NOT fixed here: --fix runs once, in the integration pass, per spec §F/§I.

## Not done / deviations
- rpc_mode.py needed no seam; the win32 arm is reachable by injecting sys.platform,
  signal.SIGBREAK and (for the restore half) loop.remove_signal_handler.
- On POSIX the two SIGTERM cases do NOT distinguish the #202 restore fix from the old
  `callable(previous)` shape (remove_signal_handler resets to SIG_DFL either way);
  the win32 arm is what catches it, which is why it also injects the
  NotImplementedError the proactor loop raises. Stated in the test's docstring.

---

## Lane log: fix-progress-hooks.md

# Lane: hooks + resolve_config — review-fix progress

Worktree /tmp/wt-202 @ cb300dc. python = /tmp/wt-202/.venv/bin/python.

## Files in this lane
- packages/aelix-coding-agent/src/aelix_coding_agent/extensions/subprocess_hooks.py
- tests/subprocess_hooks/test_subprocess_hooks.py
- packages/aelix-ai/src/aelix_ai/oauth/_resolve_config.py
- tests/oauth/test_resolve_config.py

## Read first
- primitive lane already landed `soft_kill() -> bool` (docstring says False => nothing to wait for). Confirmed by reading _process_tree.py:574-618.

## Commands

### Product edits
1. subprocess_hooks.py — teardown restructured: outer `try/finally(close)` >
   middle `try ... except asyncio.CancelledError: tree.hard_kill(); raise` >
   inner `try communicate() except TimeoutError: <soft->wait->hard->wait>`.
   The CancelledError handler now covers the teardown's 1.0 s / 5.0 s waits
   (posix2/F6). Docstring updated (cancellation anywhere in the ladder).
2. subprocess_hooks.py — `escalate = not tree.soft_kill(whole_group=True)`;
   a soft signal that was not SENT skips the grace and hard-kills at once
   (win-leg/F2 (a)).
3. subprocess_hooks.py — "the pid cannot have been reused" comment replaced by
   the honest asyncio bound (watcher waitpid on its own thread -> later loop
   callback; non-empty group's id is not recyclable; empty group -> ESRCH;
   residual case accepted, ADR-0238).
4. test_subprocess_hooks.py:~389 — "<order of magnitude>" -> "~75x the 16-21 ms
   measured on darwin, sized for two interpreter startups behind a cmd.exe".
9. _resolve_config.py — both post-kill reaps bounded:
   `with contextlib.suppress(subprocess.TimeoutExpired): proc.wait(timeout=5.0)`
   (win-leg/F3); `import contextlib` added.

### Test edits
5. test_a_timed_out_hook_does_not_orphan_what_it_spawned: run as a TASK, await
   the pid file, `assert probe_state(grandchild) == STATE_ALIVE` BEFORE the
   teardown. NOTE the deviation from posix2/F9's literal wording: probing after
   the await is impossible — the teardown has already killed the grandchild by
   then, so the alive probe had to move before it.
6. new test_a_cancelled_hook_does_not_leave_its_tree_behind (MUT-4) and
   test_a_hook_cancelled_inside_the_timeout_teardown_still_loses_its_tree
   (posix2/F6). The second uses one portable child, no /bin/sh: python handlers
   for SIGTERM AND SIGBREAK that keep running (_SIGNAL_SURVIVING_SOURCE), which
   makes the root survive the soft signal so the grace is a real window.
7. new test_a_hook_shell_gets_a_new_group_in_the_same_session (MUT-7, spy on
   asyncio.create_subprocess_shell) and
   test_the_hook_tree_is_not_attached_with_kill_on_close (MUT-6, spy on
   ProcessTree.attach). Plus an extra:
   test_a_soft_signal_that_could_not_be_sent_skips_the_grace pins change (2).
8. hooks `_reap` + oauth `_reap` resolve taskkill.exe from %SystemRoot%\System32
   with the bare-name retry (win-leg/F8).
10. _PIPELINE_SOURCE writes the pid file .part + os.replace (win-leg/F7).
11. new test_a_command_that_closes_stdout_and_lingers_still_loses_its_tree
    (MUT-3). _COMMAND_TIMEOUT untouched.
12. new test_the_command_tree_is_not_attached_with_kill_on_close (MUT-6).

### Measurements
- `sys.stdout.close()` does NOT close fd 1 (measured: EOF only at process exit,
  5.02 s); `os.close(1)` does (0.01 s). The MUT-3 case uses os.close(1) — with
  the first spelling it silently took the reader-timeout path (10.05 s).

### Commands (all in /tmp/wt-202)
- `.venv/bin/python -m pytest -p no:cacheprovider -q tests/subprocess_hooks` -> 34 passed in 4.24s
- `... tests/subprocess_hooks tests/oauth tests/model_registry tests/extensions` -> 631 passed in 17.30s
- `... tests/subprocess_hooks tests/oauth` x3 -> 271 passed in 12.51/12.53/12.53s
- `.venv/bin/ruff check <4 lane files>` -> All checks passed!
- `.venv/bin/pyright --pythonplatform Windows|Linux <2 product files>` -> 0 errors both
- `ps -eo pid,command | grep 'time.sleep(60)'` -> NO STRAYS (after every batch)

### Mutation checks (each restored immediately)
- product file reverted to cb300dc -> test_a_hook_cancelled_inside_the_timeout_teardown_still_loses_its_tree FAILS ("the hook root (80193) outlived a cancellation inside the teardown: alive"); the MUT-4 case passes there (that branch already existed).
- CancelledError handler body -> `pass` -> BOTH cancellation cases FAIL.
- `containment_spawn_kwargs(new_session=True)` + `kill_on_close=True` at the hook site -> both new spy cases FAIL.
- `escalate = not tree.soft_kill(...)` -> `tree.soft_kill(...)` -> grace-skip case FAILS (1.31 s >= 1.0).
- _resolve_config second kill site -> `proc.kill()` -> MUT-3 case FAILS ("the descendant outlived the EOF-then-linger kill (state=alive)").
- _resolve_config `kill_on_close=True` -> MUT-6 case FAILS.

### Open for the final-gate lane
- `scripts/check_citations.py --check` reports the locked anchor
  `subprocess_hooks.py:163-163` MOVED to 170 (text byte-identical, verified with
  a byte compare against cb300dc). Needs `--fix` before the final gates. Not run
  here: citations.lock.json is outside this lane.

---

## Lane log: fix-progress-docs.md

# fix-progress-docs (#202 review-2 fixes, docs lane)

Worktree /tmp/wt-202, branch fix/202-process-tree-containment on cb300dc.
Files touched (docs only): docs/decisions/0238-….md, SLICE-STATUS.md, README.md,
README.ko.md, CHANGELOG.md. docs/decisions/README.md NOT touched — its 0238 row
repeats none of the corrected claims (checked: it scopes "조용히 무시" to
`start_new_session=True`, its setsid sentence names no hook/`!command`, it cites
Pi's `stop()` with no line range, and it carries no "unreaped" claim).

## Findings applied

- ADR D1 (:183-189) — the setsid list no longer contains hook/`!command` shells.
  New text names tool children (`bash.py:278`, `_subprocess.py:78`) and MCP
  stdio servers (`mcp/client/stdio/__init__.py:256`), then states the real
  reason the two new sites are still outside the rpc child's group:
  `process_group=0` is `setpgid(0,0)` (own group, same session, tty kept), not
  `setsid`.
- ADR D6 (:65-68) — Pi provenance: the stale clone made the FIRST REVIEW's
  finding wrong; the fact was re-verified against real `origin/main` 47236c8 and
  stands. No claim of a wrong earlier ADR draft.
- ADR D7 (:83-85) — `rpc-client.ts:143-165` → `rpc-client.ts` `stop()` with
  "lines 145-167 at 47236c8; the line numbers move, the shape does not".
- ADR posix2/F1 (:219-230) — the "every caller calls while the child is
  unreaped" sentence replaced: `bash.py`'s kill paths do call before any wait;
  `tools/_subprocess.py::run_cancellable` was measured reaching it AFTER
  asyncio's watcher reaped the child (up to the caller's 30 s timeout); the
  fallback still only names the group the spawn asked for, that id stays pinned
  while any member lives (POSIX.1 §3.293 / `attach_pid(PIDTYPE_PGID)`), and an
  empty group gives ESRCH.
- ADR posix2/F4 (:241-248) — the "last 50 ms poll" bound replaced with the
  honest asyncio one: `returncode is None` does not prove the pid is unreaped
  (watcher waitpid()s on its own thread; the loop sets returncode from a later
  callback — measured with a loop blocked 1.5 s), so the bound is
  watcher→callback latency + the grace. The `!command` synchronous-`Popen`
  sentence is kept verbatim in substance.
- ADR win-leg/F2(b) (Consequences, :339-353) — the contingency bullet now names
  all five tests a console-less runner turns red
  (test_soft_kill_is_delivered_and_survivable,
  test_stop_escalates_to_the_hard_kill_when_the_soft_one_is_survived,
  test_stop_is_bounded_by_the_documented_worst_case,
  test_a_cooperative_child_exits_under_the_grace,
  test_the_real_rpc_child_answers_the_client_it_ships_with) and records why
  test_stop_does_not_wait_out_the_grace_when_a_descendant_holds_the_pipes keeps
  its timing (stop() skips the grace when the event was not delivered). The
  Decision's Windows paragraph (:105-108) states the same: soft_kill reports
  delivery, False routes straight to the hard kill.
- SLICE-STATUS D2 (§Remaining 2) — "which Windows silently declines (#202)" →
  "Windows silently declines `start_new_session`; it does **not** decline a job
  object (#202 / #220)". "Nothing in this item is wrong" replaced by the
  requalification (the taskkill-after-a-dead-root reasoning stands; the
  isolation clause did not and is corrected in place).
- SLICE-STATUS posix2/F3 (§Remaining 10 superseding note) — "Both now soft-kill
  the group or send CTRL_BREAK_EVENT, then end the tree" replaced with per-site
  truth: subprocess_hooks soft-kills the group; rpc_client.stop() soft-kills the
  child and escalates to the group only if it survives the grace (POSIX;
  CTRL_BREAK_EVENT is group-wide on Windows); `!command` goes straight to the
  hard kill.
- README.md / README.ko.md D3 — both split verdicts gain "on Windows more
  completely than on POSIX" plus the setsid clause (a job holds every
  descendant; a POSIX group does not hold a descendant that made its own
  session — every tool child, every MCP server — and those remain the reaper's
  job). Clause-for-clause parity between the two languages.
- CHANGELOG D5 — the /dev/tty sentence no longer attributes the measurement to
  gpg/pass/pinentry: "a child of a new session that opens the terminal gets
  `sh: /dev/tty: Device not configured` — measured with a shell under a real
  pty, and `!command` is the site where gpg / pass and pinentry run."

## Commands

$ cd /tmp/wt-202 && .venv/bin/python -m pytest -p no:cacheprovider -q tests/docs tests/test_docs_bundle_sync.py
..................                                                       [100%]
18 passed in 0.03s
(run twice — before and after the final wording pass; identical both times)

$ grep -rn 'session leader' README.md README.ko.md docs/decisions/0238*.md SLICE-STATUS.md CHANGELOG.md
docs/decisions/0238-…md:190:of the rpc child is a session leader of its own (`bash.py:278`,
-> one hit, about tool children. No hook / `!command` hit remains.

$ grep -rn 'silently declin' README.md README.ko.md docs/decisions/0238*.md SLICE-STATUS.md CHANGELOG.md
SLICE-STATUS.md:79:   signal in the reaper. Windows silently declines `start_new_session`; it does
-> scoped, as required.

$ .venv/bin/python scripts/check_citations.py --check
1 drifted, 0 unlocked.
tests/agents/test_p2_band_boundaries.py:70: `extensions/subprocess_hooks.py:163` no longer points at what it cited.
    cited     : proc = await asyncio.create_subprocess_shell(
    now there : proc_env: dict[str, str] = dict(os.environ)
    moved to  : 170
-> NOT from this lane (docs-only edits). The locked anchor LINE text is intact and
   merely moved (163 -> 170) by the hooks lane's edit to subprocess_hooks.py, so
   `--fix` relocates it. Left for the integration/hooks lane; no git add here.

No ruff/pyright run: this lane changed no Python.

---

## Lane log: fix-progress-integration.md

# LANE integration — #202 review-fix round 2, final gates

Worktree /tmp/wt-202, branch fix/202-process-tree-containment, base cb300dc
(four fix lanes landed uncommitted on top). python = /tmp/wt-202/.venv/bin/python.
No uv. No commit.

## Step 1 — stage + citations

 M CHANGELOG.md
 M README.ko.md
 M README.md
 M SLICE-STATUS.md
 M docs/decisions/0238-the-kill-reached-the-child-and-the-tree-is-what-had-to-die.md
 M packages/aelix-ai/src/aelix_ai/oauth/_resolve_config.py
 M packages/aelix-ai/src/aelix_ai/utils/_process_tree.py
 M packages/aelix-coding-agent/src/aelix_coding_agent/extensions/subprocess_hooks.py
 M packages/aelix-coding-agent/src/aelix_coding_agent/rpc/rpc_client.py
 M packages/aelix-coding-agent/src/aelix_coding_agent/tools/_process_tree.py
 M tests/agents/test_rpc_sprint_pins.py
 M tests/oauth/test_resolve_config.py
 M tests/process_tree/test_process_tree_api.py
 M tests/process_tree/test_process_tree_real_processes.py
 M tests/rpc/test_rpc_client_containment.py
 M tests/rpc/test_rpc_client_shutdown.py
 M tests/subprocess_hooks/test_subprocess_hooks.py
 M tests/tools/test_process_tree_win32.py
?? .omc/specs/202-progress-2026-09-05.md
?? tests/rpc/test_rpc_mode_signal_handlers.py

$ git add packages tests docs README.md README.ko.md SLICE-STATUS.md CHANGELOG.md citations.lock.json
  exit 0

$ .venv/bin/python scripts/check_citations.py --fix
  relocated 4 citation(s) across 4 file(s).
  locked 486 anchor(s) over 850 gated citation(s) (32 ungated, 0 out of range).

  Relocations (all pure line-number rewrites, verified by reading the diff and the target lines):
    print_channel.py:472                    `rpc/rpc_client.py:1028`     -> 1073
    tests/agents_ext/test_child_argv_contract.py:80  same                -> 1073
    rpc_channel.py:333                      `rpc_client.py:944-947`      -> 989-992
    tests/agents/test_p2_band_boundaries.py:70 `subprocess_hooks.py:163` -> 170
  Target lines verified:
    rpc_client.py:1073  = `argv = [sys.executable, "-m", "aelix", "--mode", "rpc"]`
    rpc_client.py:989   = `def _listener(event: dict[str, Any]) -> None:`
    subprocess_hooks.py:170 = `proc = await asyncio.create_subprocess_shell(`
  Locked-anchor LINE byte-identity vs cb300dc: BOTH hold.
    subprocess_hooks anchor text `proc = await asyncio.create_subprocess_shell(` — identical (was :163, now :170)
    print_channel.py:970 `# ``None`` on Windows, where subprocess REJECTS the` — identical, same line number

$ .venv/bin/python scripts/check_citations.py --check
  citations OK — 850 gated, none drifted.

$ git add <same pathspecs>   # re-staged after --fix
$ .venv/bin/python scripts/check_citations.py --check
  citations OK — 850 gated, none drifted.

Nothing was "stuck"; no hand re-derivation needed.

## Step 2 — gates

$ .venv/bin/ruff check .
  All checks passed!

$ .venv/bin/ruff format --check packages/aelix-ai/src/aelix_ai/utils/_process_tree.py tests/process_tree
  4 files already formatted

$ .venv/bin/python scripts/check_types.py
  OK  gate: 277 files analysed (floor 220)
  OK  gate: 0 errors across 277 files
  OK  spike: all 3 inverse assertions still error (narrowing alive)
  PASS: type gate clean, narrowing spike still inverted

$ git diff --name-only 39549b9 -- packages | grep '\.py$'
  _resolve_config.py, utils/_process_tree.py, print_channel.py, reaper.py, rpc_channel.py,
  subprocess_hooks.py, rpc_client.py, rpc_mode.py, tools/_process_tree.py, rpc_ws.py  (10 files)

$ .venv/bin/pyright --pythonplatform Windows <those 10>
  0 errors, 0 warnings, 0 informations
$ .venv/bin/pyright --pythonplatform Linux <those 10>
  0 errors, 0 warnings, 0 informations

## Step 3 — targeted x3, then full suite

Targeted set (11 paths):
  tests/process_tree tests/tools tests/rpc tests/agents/test_rpc_sprint_pins.py
  tests/subprocess_hooks tests/oauth tests/agents_ext/test_reaper_kill_signal_win32.py
  tests/agents_ext/test_spawn_preexec_platform.py tests/agents_ext/test_rpc_channel.py
  tests/pi_parity/test_phase_4_4_strict_superset.py tests/docs

$ .venv/bin/python -m pytest -p no:cacheprovider -q <targeted>     # run 1
  849 passed, 2 skipped in 46.94s
$ ... run 2
  849 passed, 2 skipped in 47.00s
$ ... run 3
  849 passed, 2 skipped in 46.83s
  -> no flake across three consecutive runs; identical counts.

$ .venv/bin/python -m pytest -p no:cacheprovider -q            # FULL suite
  10103 passed, 12 skipped, 10 warnings in 262.93s (0:04:22)      exit 0
  (the 10 warnings are the pre-existing pathspec GitWildMatchPattern DeprecationWarning
   from tests/harness/test_skills*.py — unrelated to #202)

Nothing broke. No fixes were needed in step 3.

## Step 4 — strays

$ ps -eo pid,ppid,pgid,command | grep -E 'sleep\(30\)|sleep\(60\)|sleep 6' | grep -v grep
  (no output, exit 1)  == EMPTY, as required
$ ps -eo pid,ppid,pgid,command | grep -E 'time\.sleep|python -c' | grep -v grep
  (no output)  broader probe, also empty

## Step 5 — wording audit (grep 'session leader' / 'unreaped' / '50 ms poll')

$ grep -rn -e 'session leader' -e 'unreaped' -e '50 ms poll' \
    packages/aelix-ai/src/aelix_ai/utils/_process_tree.py \
    packages/aelix-coding-agent/src/aelix_coding_agent/rpc/rpc_client.py \
    packages/aelix-coding-agent/src/aelix_coding_agent/extensions/subprocess_hooks.py \
    docs/decisions/0238*.md README.md README.ko.md SLICE-STATUS.md CHANGELOG.md \
    tests/process_tree tests/tools/test_process_tree_win32.py tests/rpc

11 hits. README.md, README.ko.md, SLICE-STATUS.md, CHANGELOG.md,
tests/tools/test_process_tree_win32.py and tests/rpc/** carry ZERO hits.

1. _process_tree.py:33  "A job holds every descendant, including one whose parent has
   already exited and one that made itself a session leader."
   CONSISTENT. About the win32 job, not about hook/!command shells.
2. _process_tree.py:39  "...so it is outside an rpc child's group as well, WITHOUT being
   a session leader. So Windows containment here is strictly STRONGER than POSIX's."
   CONSISTENT with COMMON fact 1 — the preceding two lines say hook and !command shells
   are spawned with process_group=0, own group, SAME session, tty kept, and explicitly
   removes them from the setsid list (which now names only tool children + MCP stdio).
   This is the D1 fix.
3. _process_tree.py:68  "on the two asyncio sites (rpc, hooks) ``returncode is None``
   does NOT prove the pid is unreaped."  CONSISTENT with COMMON fact 2.
4. _process_tree.py:73  "...the honest window there is (watcher reap -> loop callback
   latency) plus the grace, not a 50 ms poll quantum."  CONSISTENT with COMMON fact 2,
   including the group-id-pinning / ESRCH / accepted-residual clauses that follow.
5. rpc_client.py:576  "An older comment here claimed 'the last 50 ms poll saw
   returncode is None', which is not a bound: ..."  CONSISTENT — it names the old claim
   as wrong and replaces it with the watcher->callback bound plus POSIX.1 §3.293 /
   attach_pid(PIDTYPE_PGID) / empty-group-ESRCH, residual accepted, ADR-0238 cited.
6. subprocess_hooks.py:214  "unreaped: asyncio's child watcher calls waitpid on its own
   thread and a LATER loop callback copies the status into returncode (measured — review
   posix2/F4)."  CONSISTENT with COMMON fact 2.
7. ADR:190  "Every tool child of the rpc child is a session leader of its own
   (bash.py:278, _subprocess.py:78), and so is every MCP stdio server ... A hook shell
   and a !command shell are NOT."  CONSISTENT with COMMON fact 1 (D1 fix).
8. ADR:223  "...they do not all call while the child is unreaped. bash.py's kill paths
   do, before anything has waited; tools/_subprocess.py::run_cancellable was measured
   reaching it AFTER asyncio's watcher had already reaped the child."
   CONSISTENT with COMMON fact 3 (posix2/F1 fix).
9. ADR:242  "returncode is None does NOT prove the pid is unreaped ... so the honest
   bound is that watcher-to-callback latency plus the grace ... and not the 50 ms of a
   last poll."  CONSISTENT with COMMON fact 2.
10. tests/process_tree/test_process_tree_real_processes.py:244  "A grandchild that made
    itself a session leader has left our process group, so killpg cannot reach it."
    CONSISTENT — about a deliberately setsid grandchild in that test's own fixture.
11. tests/process_tree/test_process_tree_real_processes.py:343  "Deliberately no
    proc.wait(): the leader must still be unreaped."  CONSISTENT — a statement about
    that test's own synchronous Popen setup, not about the asyncio sites.

Cross-checks on the other COMMON facts (all read, none contradicted):
- rpc_client.py:531-535 (stop() docstring) names transport.close() as reaching
  Popen.poll()/Popen.kill() and calls it "asyncio's own last resort rather than this
  method's escalation". MATCHES COMMON fact 6.
- rpc_client.py:558-560 soft-kills the ROOT (tree.soft_kill(), whole_group defaults
  False); killpg only inside the escalation branch; :617-623 documents close() as a
  release that signals nothing on POSIX. MATCHES COMMON fact 4.
- ADR:148-150 "os.kill(pid, 0) is a Ctrl+C on Windows, not a liveness probe — signal 0
  is not special-cased". UNCHANGED, MATCHES COMMON fact 5.
- All four test `_reap` helpers (process_tree, oauth, subprocess_hooks, rpc containment)
  now resolve %SystemRoot%\System32\taskkill.exe with the bare name only as the retry —
  win-leg/F8 is closed across all three files the finding named plus the rpc one.
- win-leg/F2 remedy (a) is now closed at BOTH consumers: subprocess_hooks.py:230
  `escalate = not tree.soft_kill(whole_group=True)` and rpc_client.py:559
  `soft_kill_refused = not tree.soft_kill()`, so the ADR's Decision/contingency wording
  written by the docs lane matches shipped behaviour.

## Residuals the integration pass did NOT change (reported, not fixed)

1. tests/rpc/test_rpc_client_containment.py:489 — the pre-existing `finally` cleanup of
   test_stop_ends_a_descendant_the_child_left_behind still calls `kill_process_tree(grandchild)`
   while the same file's own `_reap` is deliberately root-only. It runs only after the
   assertion, so it cannot mask a failure, and on POSIX the primitive lane's new
   `getpgid(pid) == pid` guard already degrades it to a root-only `os.kill` for that
   grandchild (it does not lead a group). It is a cosmetic inconsistency, not breakage;
   changing it also means dropping the now-unused import, so I left both alone rather
   than make an unforced edit outside a finding. Flagged by the rpc lane as optional.
2. The 5 s `_taskkill_tree` timeout is a chosen bound, not a measured one (primitive
   lane's own note). taskkill.exe has still never been timed on the windows leg.
3. win-leg/F6's second half — wrapping the synchronous win32 hard kill in
   `asyncio.to_thread` at the two async call sites — was not adopted by any lane. The
   blocking is bounded at 5 s and documented in `_taskkill_tree`'s docstring; it is
   a design decision the reviews left open, not a regression.
4. Everything else the four lanes listed as "open" is now closed by another lane:
   soft_kill's bool is consumed at both consumers; all four `_reap` helpers resolve
   taskkill.exe; the ADR contingency bullet matches shipped behaviour; citations are
   fixed and re-locked.

## Final state

Staged (git add, explicit pathspecs, never -A) and NOT committed:
  CHANGELOG.md README.md README.ko.md SLICE-STATUS.md citations.lock.json
  docs/decisions/0238-….md
  packages/aelix-ai/.../oauth/_resolve_config.py, .../utils/_process_tree.py
  packages/aelix-coding-agent/.../aelix_agents/print_channel.py, rpc_channel.py   (citation relocation only)
  packages/aelix-coding-agent/.../extensions/subprocess_hooks.py, rpc/rpc_client.py, tools/_process_tree.py
  tests/agents/test_p2_band_boundaries.py (citation relocation only), tests/agents/test_rpc_sprint_pins.py
  tests/agents_ext/test_child_argv_contract.py (citation relocation only)
  tests/oauth/test_resolve_config.py, tests/process_tree/{api,real_processes}
  tests/rpc/{test_rpc_client_containment,test_rpc_client_shutdown}.py
  tests/rpc/test_rpc_mode_signal_handlers.py (NEW, staged as A)
  tests/subprocess_hooks/test_subprocess_hooks.py, tests/tools/test_process_tree_win32.py
Untracked and deliberately unstaged: .omc/specs/202-progress-2026-09-05.md (never committed).

## Live checks (CLAUDE.md rule 10) — run by the orchestrator, darwin, 2026-09-05

Both with a real model (`openrouter` / `qwen/qwen3.6-35b-a3b`, key from the main checkout's `.env`).

### A — subprocess hook whose pipeline outlives its timeout, end to end through `aelix -p`

Project-local extension `.aelix/extensions/slowhook/aelix-plugin.toml`:
`[[contributes.hooks]] event = "tool_call" command = "sleep 60 | cat" timeout_ms = 500`, `shell_exec = true`.

```
$ cd $SCR/live-hook && /tmp/wt-202/.venv/bin/python -m aelix_coding_agent -a --no-agents --no-session \
    --permission-mode auto --provider openrouter --model qwen/qwen3.6-35b-a3b \
    -p "Use the read tool to read README-live.txt in the current directory and reply with its first line only."
The first line of README-live.txt is:
**LIVE-202-MARKER first line**
exit=0 elapsed=4s
stderr: subprocess hook timed out (fail-open): <hook timed out after 500ms>
ps AFTER (sleep 60 / cat): (none)
```

Same run against main 39549b9's venv (`has ProcessTree: False`):

```
exit=0 elapsed=4s
stderr: subprocess hook timed out (fail-open): <hook timed out after 500ms>
ps AFTER on MAIN:
51544     1 51454 sleep 60
51545     1 51454 cat            <- orphaned, reparented to launchd, pgid = the parent aelix's group
```

### B — a real `--mode rpc` child, a real model, bash `sleep 300`, `stop()` mid-turn (`$SCR/live_rpc.py`)

```
child pid=52178 pgid=52178 started in 0.1s
get_state ok: model={'id': 'qwen/qwen3.6-35b-a3b', ...}
tool call with 'sleep 300' observed in events: True
ps BEFORE stop():
   52178 52176 52178 .../python -m aelix_coding_agent --mode rpc --no-session --no-extensions --no-agents --permission-mode yolo --provider openrouter --model qwen/qwen3.6-35b-a3b
   52263 52178 52263 sleep 300          <- the bash tool's child, its own group (setsid) — the documented POSIX limit
stop() took 0.104s; child returncode=0   <- cooperative SIGTERM, the child's own dispose reaped its tool
ps AFTER stop(): (none)
leftover check (sleep 300 | mode rpc): (none)
```
