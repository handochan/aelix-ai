# Batch plan — #260/#261 · #313 · #315 · #321 — 2026-09-24 (준비만 됨, 아직 실행 안 함)

컴팩트 직전에 적은 재개 지점이다. **워크플로우는 아직 띄우지 않았다.**

## 기준점 (2026-09-24 02:58 KST 확인)

- `origin/main` = `02f98560`. 마지막 코드 커밋은 `a8325c04`, 그 위는 핸드오프·`docs/05` 문서 커밋뿐
  (`git log --format='' --name-only a8325c04..origin/main | sort | uniq -c`로 확인).
- 원격 브랜치는 `main` 하나. pi HEAD `6671c6047` (2026-09-16).
- 다음 빈 ADR 번호는 **0249** — 레인이 새 ADR을 쓰면 A=0249, D=0250, C=0251, B=0252로 **미리 배정**(지난 배치에서 세 레인이 0246을 동시에 썼다).
- Codex 교차 리뷰: ChatGPT 한도 해제 예고가 **2026-09-24 23:48** — 그 전엔 안 돈다. 끝에 시도하고, 안 되면 "돌렸으나 한도"로 기록.

## 준비해 둔 것

| 레인 | 이슈 | 워크트리 | 브랜치 | 범위 |
| --- | --- | --- | --- | --- |
| A | #260 + #261 | `/tmp/wt-260` | `fix/260-drain-asks-the-pipe` | bash 성공 경로 drain(`tools/bash.py` `_drain_after_the_exit`, `_process_tree.py` reader). #260=기전, #261=증상 → 한 커밋으로 둘 다 닫는다. `.env` 복사해 둠(라이브 확인용, 커밋 금지) |
| B | #313 | `/tmp/wt-313` | `fix/313-process-tree-clock-floor` | `tests/process_tree/`만 — 0.2 s 하한을 사건 대기로, 같은 디렉터리 스윕 |
| C | #315 | `/tmp/wt-315` | `fix/315-tui-wallclock-sweep` | `tests/tui/`만 — 벽시계 대기 전수 스윕. 다른 디렉터리 발견은 follow-up |
| D | #321 | `/tmp/wt-321` | `fix/321-prompt-cancel-resets-phase` | `harness/core.py` `prompt()`의 `except Exception` → 취소돼도 phase 복구. `.env` 복사해 둠 |

- 네 워크트리 모두 `origin/main`(`02f98560`)에서 만들고 `uv sync` 끝.
- 보드: #260 · #261 · #313 · #315 · #321 → **In progress**로 옮겨 둠.
- **#325는 이 배치에서 뺐다** — 레인 A와 같은 `_drain_after_the_exit`·같은 테스트 파일이라 A 머지 뒤에 순차로.

## 레인에 줄 제약 (지난 배치에서 값이 난 것 + 새로 배운 것)

- 구현 → 독립 리뷰(**고치지 말고 보고만**) → 수정 → 독립 재검증. 새 테스트가 수정 전 코드에서 빨개지는지 재측정, 한 글자 사보타주.
- **pi를 먼저 본다**(오너 2026-09-24, CLAUDE.md 11): A는 pi `utils/child-process.ts::waitForChildProcess`와 bash 도구 drain, D는 pi agent-harness의 prompt 취소 처리. pi SHA를 기록에 남긴다.
- 스크래치는 레인마다 `/tmp/<issue>-work/` — 공유 스크래치패드 금지.
- 게이트는 워크트리 안에서 상대경로. `AGENTS.md`는 절대경로(`/Users/handochan/dev/aelix-ai/AGENTS.md`).
- A·B·C는 타이밍 민감 테스트를 다루고 네 레인이 한 머신에서 동시에 돈다 — 무관한 파일의 타이밍 실패는 부하일 가능성이 크니 단독 재실행 후 판단.
- 라이브 확인(CLAUDE.md 10): A(bash 도구 런타임)와 D(prompt 런타임)는 실제 모델로. 서브에이전트도 pty/라이브를 돌릴 수 있다(forkpty). B·C는 테스트만 바꾸므로 해당 없음 — C가 TUI 코드를 바꾸게 되면 pty 확인.
- 커밋 trailer: `Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>` + `Claude-Session: https://claude.ai/code/session_01CXR483KFsP1pG7ppxULd3E`.
- 머지는 메인 루프가 스택으로(각 브랜치 tip = main의 미래 커밋), 리베이스 뒤엔 **전체 스위트**와 커밋 메시지 수치를 다시 잰다.

## 재개할 때

1. 기준점 재확인(위 명령) — main이 움직였으면 워크트리를 새 main 위로 다시 만든다.
2. `git worktree list`로 네 워크트리가 그대로인지 확인.
3. 워크플로우를 띄운다(레인 4개, pipeline).

## 실행 기록 (2026-09-24 03:1x KST~)

- 기준점 재확인 03:03 — `origin/main` 그대로 `02f98560`, 네 워크트리 그대로.
- pi 업스트림이 148커밋 앞서 있었다(`6671c6047` → `a328aa89a`, 2026-09-23). 오너 클론은 건드리지 않고
  `git archive origin/main`으로 **`/tmp/pi-a328aa89a`** 스냅샷을 떠서 레인에 줬다.
- **워크플로우 `wf_74e80610-584` 실행 중** — 스크립트
  `~/.claude/projects/-Users-handochan-dev-aelix-ai/d09ee278-695b-49af-98e5-75a17c82683f/workflows/scripts/batch-260-313-315-321-wf_74e80610-584.js`.
  모양: A = design → critic → implement → review(보고만) → fix(지적 있을 때) → verify(항상);
  B·C·D = implement → review → fix → verify(fix가 있을 때만). 모든 agent `model: 'opus'`, B·C는 effort high.
  레인이 죽으면: `workflow-lane-dies-mid-write-resume` 메모리대로 반쯤 쓴 트리를 감사 → 그 레인 프롬프트만 패치 →
  `Workflow({scriptPath, resumeFromRunId: "wf_74e80610-584"})`.
- **교차 리뷰 상태**: Codex는 03:0x에 재확인해도 한도(“try again at 11:48 PM”). Gemini CLI는 인증은 되지만
  모델이 503 과부하였다. → 레인 A(P0, 위험)는 교차 리뷰 없이 머지하지 않는 쪽으로 판단 예정 — 워크플로우 뒤에 다시 시도.
- 레인 D에 넣은 함정: `prompt()`의 재시도 백오프 동안 `_run`의 finally가 이미 phase를 `idle`로 돌려놓아 두 번째
  `prompt()`가 가드를 통과할 수 있다 — `except`를 넓히면 남의 턴을 풀 수 있으니 소유권을 확인하라고 지시했다.

## 1차 결과 (워크플로우 `wf_74e80610-584`, 03:1x → 10:55 KST, 18 agents, 0 error)

| 레인 | 브랜치 tip | 결과 | 남은 것 |
| --- | --- | --- | --- |
| B #313 | `0ad95e11` (tests/process_tree 4파일) | review 1 must-fix → fix → **verify pass** | 11:1x push, 브랜치 CI 대기 |
| C #315 | `ce02a4a7` (tests/tui 16파일 + ADR-0107 정정) | review 1 must + 1 should → fix → **verify pass** | 11:1x push, 브랜치 CI 대기 |
| A #260·#261 | `2e56a285` (15파일, `_process_tree.py` +444, `bash.py` +169, 새 테스트 2파일) | review 2 must + 2 should + 6 nit → fix → **verify fail** | 막힌 1: win32 분기 `drained()`의 `mark[3] == 0`을 `>= 0`으로 바꿔도 새 테스트 40개 통과. 비차단: parked grace(to-do 1)·`run_contained`의 `pin_after` 미고정, pi #2630 서술 |
| D #321 | `9c0a620a` (50파일 — 41개는 인용 줄번호만) | review 1 must + 1 should → fix → **verify fail** | fail 사유는 문서: CHANGELOG "`-p`에선 이 창에서 취소 안 됨"이 거짓(첫 ^C가 main task 취소 → base 3번, 수정 1번). 도달 불가 SHA `1bca2856`이 ADR·테스트 docstring에. ARM 18(겹친 두 턴 상태에서 base 대비 회귀)은 **거래로 수용**, idle-tail 이슈로 닫는다 |

- **2차 라운드 `wf_686c8e03-ef6`** (A·D 각각 fix2 → verify2) 11:2x 시작.
- 설계(A): POSIX는 reader가 non-blocking fd + poll, 읽기 전 phase 홀수·넘긴 뒤 짝수 → P1 `drained()`(짝수 + FIONREAD 0이 exact), P2 `caught_up()`(exit 뒤 첫 look의 pin까지 handed_on). 증명 없으면 grace 한 번 더(루프 park), hard cap = exit+2.0 s / kill+1.0 s, hard cap에서 증명 없으면 `output_unconfirmed` + 모델용 notice. win32는 reader가 read1 전에 자기 핸들을 PeekNamedPipe(루프는 절대 안 봄 — libuv win/pipe.c 주석). win32 잔여: 마지막 빈 look 뒤 바이트 + reader가 grace 내내 굶으면 조용히 손실(pin됨).
- 오너 판단 후보(A가 올린 것, 권고와 함께 보고할 것): notice 문구 / starved reader가 `timeout=1.0` 호출을 exit+2.0 s까지 붙잡는 거래 / win32 잔여를 지금 이슈로 / P0라 Codex 뒤에 머지(→ 권고: 예).
- C의 open question(`asyncio.Queue._getters` 비공개 속성을 테스트가 읽음, 없으면 크게 실패) → 메인 루프가 수용 판단.
- 후속 이슈 후보: tests/ 전체 벽시계 스윕(tui·process_tree 밖, B+C 목록) · tests/tui negative window · `_process_tree._drain` 시계 seam · prompt() idle tail(ARM 18-20) · 취소된 재시도 백오프의 `_retry_attempt`/`auto_retry_end`(ARM 11) · core.py 게이트 밖 맨 줄번호 · REPL/RPC가 `output_unconfirmed` 미표시 · win32 잔여(overlapped read).

## 머지·2차 라운드 (11:2x → 13:1x KST)

- **main = `a9805d03`** — #313 `0ad95e11`(브랜치 CI 35945632317 6/6 green) → #315 `a9805d03`(B 위로 스택, CI 35945866506 6/6). 둘 다 ff push로 머지, 이슈 닫음(자동 Done), 워크트리·로컬 브랜치 삭제. **원격 브랜치 삭제는 오너 몫(분류기)** — 끝에 한 줄 명령으로.
- 후속 이슈: **#330**(tests/ 나머지 벽시계, P1) · **#331**(tests/tui negative window, P2) · **#332**(`_drain` 시계 seam, P2 — #260 머지 뒤).
- 2차 `wf_686c8e03-ef6`: **A `3e4d89df` verify pass**(win32 분기 테스트 2개, parked grace·`run_contained` pin 고정, pi #2630 서술 — 44개 새 테스트). **D `26245a86` verify fail** — ARM 18 범위를 "mid-turn"으로 좁게 적음; 실제는 두 번째 prompt가 **진행 중이기만 하면**(input/before_agent_start 훅 안 포함). 메인 루프가 `arm18b.py`로 직접 재확인 후 문구 4곳(ADR-0023·README 0023 행·CHANGELOG·커밋 메시지 THIRD REVIEW) 수정.
- A 커밋 메시지의 도달 불가 SHA `dec0624c` 2곳 → 서술로.
- **스택**: main `a9805d03` → **D `2c69aa2d`**(깨끗한 리베이스, 게이트 3개 + 468 passed, push → CI 대기) → **A `f610ea18`**(충돌 없이 자동 병합 — CHANGELOG 두 항목·README 두 행 각 1회 확인, lock 552 anchors, `--fix` 없이 950 gated none drifted, 258 passed, push → CI 대기 — **win32 분기의 첫 실측**).
- 교차 리뷰: **Gemini가 12:3x부터 응답**(503 풀림) → D에 read-only(`--approval-mode plan`) 리뷰 실행 중, 이어서 A. Codex는 23:48 이후 A에 추가.
- 메인 루프 수정분(D 문구·A 메시지)은 독립 에이전트가 검증 중.

## Windows CI가 잡은 것 (12:5x KST)

- **D `2c69aa2d` CI 35952181466**: windows 두 레그 모두 `test_p2_band_boundaries.py::test_kernel_untouched_vs_merge_base` 1건 실패 — `_git()`이 `text=True`에 인코딩 없이 `git show`로 **파일 내용**을 읽어 Windows에선 cp1252로 디코드, `_extension_runner.py`의 "Sprint 6h₁"(₁ = E2 82 81, cp1252에 0x81 없음)에서 reader 스레드가 죽고 stdout None → `ast.parse(None)` TypeError. D는 그 파일의 인용 줄번호만 바꿨을 뿐(허용 목록 밖 커널 파일 → prose-only 판정 경로가 처음 열림). → **#333** 파일, `fix/333-band-test-reads-utf8` `50bc181a`(메인 루프 작성: `encoding="utf-8"` + cp1252 러너를 흉내 내는 가드, 수정 없으면 CI와 같은 TypeError로 RED, 있으면 8 passed). D를 그 위로 재스택 → **`2e9b3ba2`**, 둘 다 CI 대기.
- **A `f610ea18` CI 35952321924**: windows 두 레그 각 4 실패 = 위 밴드 테스트 1 + **A 자신의 새 테스트 3**(`test_a_pin_is_caught_up…[native]`/`[win32]`, `test_a_look_is_dated_before_it_asks_the_pipe` — `pin_after(time.monotonic())` 직후 `pinned is None`이 `0`). 진단: Windows py≤3.12의 `monotonic()`은 GetTickCount64(~15.6 ms 눈금) → pin 직전 같은 눈금의 look이 `>=`로 "pin 이후"로 인정 → 오래된 pin → **P2 증명이 거짓으로 성립해 명령 자신의 바이트를 조용히 잃을 수 있다**(두 번째 테스트 docstring이 바로 그 시나리오). 제품 결함 — ubuntu는 초록.
- → **A 3차 `wf_fa93f754-1aa`**(fix3: 진단 확인·동순위 시계에서 건전한 순서 비교·양자화 시계 결정적 테스트 → verify3) + #333 독립 검증을 같이 실행 중.
- 교훈(메모리 후보): 시각 **순서**로 증명하는 설계는 동순위(tie)가 흔한 시계(Windows py≤3.12 monotonic)에서 반드시 따져야 한다 — 로컬(darwin ns 시계)·Linux Docker로는 절대 안 보인다.

## 3차·재스택 (13:1x → 15:1x KST)

- #333 첫 버전 `50bc181a` CI 6/6 green, D `2e9b3ba2`(그 위) CI 6/6 green — Windows에서 밴드 테스트 통과 확인.
- **오너 지시(14:4x)**: "codex 사용량 초기화 되기 전에 gemini 말고 우선은 클로드코드 자체에서 독립 컨텍스트 리뷰로" → Gemini 중단(무료 flash가 40분 할당량 재시도에 묶임), D·A에 **독립 컨텍스트 opus 리뷰**(레인 주장 없이 이슈·커밋·알려진 거래만) 실행. 메모리 `cross-review-fallback-is-independent-claude`.
- A 3차 `wf_fa93f754-1aa`: fix3 **엄격 비교**(`_look`의 `looked_at > pin_after`, `pin_after`의 `looked_at > instant`) + 양자화 시계 결정적 테스트 + 제품 e2e 동순위 테스트(수정 전 6/1006 B 21~25/30, 수정 후 0/30) → **verify3 pass**. 기각: 인과 순서(재설계), perf_counter(동순위 가능성만 줄임).
- #333 verify **fail(문구)**: "파일 내용을 읽는 헬퍼는 하나"가 거짓 — `tests/test_env_sandbox_windows.py`의 `git grep` 두 곳도 소스 줄을 로케일로 읽음(잠복) → 그 둘에도 `encoding="utf-8"`, 메시지·주석 정정, 테스트명 `test_prose_only_survives_a_cp1252_locale` → **`9b535076`**.
- A 메시지의 도달 불가 SHA(d02f749d, d1dd16dd — 다른 레인의 amend된 head) → "(lane)" + 설명.
- **재스택**: main `a9805d03` → #333 **`9b535076`** → D **`9c87c80f`** → A **`bce35834`**. 셋 다 push, CI 순차 대기(b91dwrmjy). A·D 독립 리뷰 실행 중.
- 초안 준비됨(scratchpad/issues): d1 idle tail · d2 취소된 재시도 · d3 abort pre-run(메인 루프가 probe로 확인: abort 후에도 provider 호출 1회, main에서도 같음 — 선존) · a1 output_unconfirmed 표면 · a2 win32 잔여.

## 15:2x KST

- CI(순차 대기): #333 `9b535076` 6/6 · D `9c87c80f` 6/6 · **A `bce35834` 6/6 — windows 두 레그에서 엄격 비교 수정 통과(첫 실측)**.
- **main = `9b535076`** (#333 ff 머지, 이슈 닫음, 워크트리·로컬 브랜치 삭제). 원격 브랜치 삭제 대기 목록: fix/313-process-tree-clock-floor, fix/315-tui-wallclock-sweep, fix/333-band-test-reads-utf8 (+ 이후 fix/321…, fix/260…).
- **D 독립 리뷰(opus, 레인 주장 없이) → must-fix F1**: 첫 `_run`의 flip이 claim 없이 일어나 겹침 상태에서 오래된 claim이 reset을 건너뜀 → 하네스 영구 busy(X1r, 예외 경로에서 base 대비 회귀; X1c; X2). + F2(ADR·CHANGELOG·주석이 "회귀는 ARM 18 하나", "모든 flip에서 claim"이라 적음 — 거짓). → **D 4차 `wf_725bd810-39a`**(claim을 flip 자리에서, X1r/X1c 테스트, 기록 정정, InputHandled reset도 owner 규칙) 실행 중.
- A 독립 리뷰 실행 중 — 끝나면 A를 #333 위로 직접 재스택(D 아래로)해 먼저 머지할지 판단.
- **A 독립 리뷰(15:4x)**: must-fix 없음, POSIX 조용한 손실 없음(P1 스트레스 22,699 증명 0 위반, identity 제거 뮤턴트 205 위반 — 프로브가 볼 수 있음). should-fix S1(win32 잔여 pin 테스트가 HOLD=0.3 타이머 게이트에 의존 — 0.3 s 정지면 12/16 실패), S2(drain이 exit 뒤 2 s 넘게 늦게 돌면 hard cap 판정을 곧바로 — 완전한 출력에 거짓 notice; pin을 `_wait_and_stamp`에서 미리). nit N1(REPL/RPC 미표시 = a1 이슈), N2(probe 없는 reader가 phase만으로 증명), N3(pty FIONREAD 부정확 — docstring). → **A 4차 `wf_822e8bdf-b58`** 실행 중.

## 16:3x KST — #321 머지

- D 4차 `wf_725bd810-39a`: fix4 `e97226de` → verify4 **pass** → 메인 루프가 문구 nit(arm I가 바꾸는 것은 ARM 21의 **결과**, ARM 13의 진단 줄도 다름) 정정 → **`4df8d884`**, CI 35968400483 6/6 green → **main = `4df8d884`**, #321 닫음.
  - claim을 flip 자리에서(`_run(owner=…)`), InputHandled도 owner 규칙, X1 계열·ARM 22·24 해결. 받아들인 회귀: 겹침 상태의 ARM 18/18h, 23c(→ #334가 닫음).
- 후속 이슈: **#334** idle tail(P1) · **#335** 취소된 재시도(P2) · **#336** 턴 전 abort 무시(P2, main에서도 재현) · **#337** core.py 맨 줄번호(P2).
- 남은 것: A 4차 `wf_822e8bdf-b58` → main(`4df8d884`) 위로 재스택 → CI → 머지 → #260·#261 닫기 → a1·a2 이슈 → 전체 스위트(main) → docs/05 행 → 핸드오프.

## 18:4x → 22:0x KST — A 4·5차와 재스택

- A 4차 `wf_822e8bdf-b58` → `316e5bf1`: S1(잔여 pin을 Gate(None)=호출 반환 후 개방), **S2 part1**(`_wait_and_stamp`·`_mark_the_kill`에서 pin_after), **S2 part2 채택**(늦은 look이 cap 앞 grace 안이나 뒤면 no-proof 끝을 그 look + grace로, 드레인당 1회, 두 드레인 모두 — 문서화된 상한이 "2.0 s 또는 그런 첫 늦은 look 뒤 0.1 s 중 늦은 쪽"으로 바뀜), N2, N3 → verify4 **fail**(정지 주입에서 deadline 케이스·새 exit-tick 케이스가 타이머 의존; 메시지 "58 passed under the stop" 거짓).
- A 5차 `wf_f42cc48c-868` → `d53f4379`: 두 케이스를 DrainLog(드레인 자신의 look/ask 순서)로 판정, exit-tick은 첫 ask로 개방, 늦은-루프 쌍도 사건 대기 → verify5 **fail(blocking 1)**: 새 판정이 "첫 yes가 끝냈다"를 확인 안 함(사보타주 Q1 통과). 검증자가 `assert not any(ask[3] for ask in asks[:-1])`로 35/35 측정.
- **메인 루프 2차 패스**: 그 단언 + README 행·`_unconfirmed_notice`·`WHAT IS LEFT` 문구 정확화 + 메시지 → Q1이 새 단언에서 RED 확인, 정지 4설정×3회 green → `5ceb78dd` → main 위로 재스택 **`66c5a0a4`**(충돌 없음, 게이트, 284 passed) → push, CI 대기. 2차 패스는 독립 검증 중, 4차 제품 변경(A·B·C)은 **독립 교차 리뷰 2회차** 중(Codex 전 오너 지시 방식).

## 22:3x KST — A 6차

- A `66c5a0a4`(main 위) CI 36000198338: **windows 두 레그 실패 — 테스트 전제만**: `…one_grace[twice]`의 `again - back_at >= 0.3`가 0.297, `…far_from_the_hard_cap…`의 첫 ask 0.296 < 0.3 (제품은 1000/1000 B, 1.000 s로 옳음). #313 모양(거친 시계 하한)이 새 테스트에 다시. 같은 모양이 bash 파일 :968·:1091에도.
- 2차 패스 독립 검증 **pass**(Q1 3/3 red, 정지 58/58) — 문구 nit 2(docstring 두 곳에 "드레인당 한 번" 없음, 메시지의 "5차는 제품 코드 무변경" 문장 4개가 2차 패스 뒤 거짓).
- 4차 제품 delta 독립 교차 리뷰 2회차: **must/should 없음**(pin 경합 300/300 건전, 늦은-look 11,424 가상시계 스케줄 위반 0), **nit N1**: win32+RPC abort에서 exit 스탬프 pin이 kill 스탬프보다 먼저 잡혀 root 뒤에 쓰던 job 멤버의 마지막 바이트를 무표시로 잃을 수 있음(모델링 10/10 손실, 이전 0/10) — 4차가 만든 좁은 회귀.
- → **A 6차 `wf_5a5e6f96-89d`**: 하한 전제에 한 눈금 여유/필요한 것만, N1(pin_after가 가장 늦은 시각 유지·앞선 look의 pin 폐기), 문구·메시지. 그 뒤 23:48 이후 가능하면 **Codex로 최종 A 교차 리뷰**(오너: 초기화 전까지는 Claude 독립 리뷰).

## 2026-09-25 00:1x KST — A 6차 끝, CI·Codex

- A 6차 `wf_5a5e6f96-89d` → `94915084` → verify6 **pass**: 하한 전제 4곳을 드레인 자신의 읽기로(15.625/16 ms 양자화 시계에서 64/64), N1 "늦은 시각이 이긴다"(pin_after가 최댓값 유지·락, `pinned`는 look이 그 시각 뒤일 때만) — 모델링한 win32 kill 순서에서 이전 10/10 **무표시** 손실 → 이제 10/10 **표시**, 대가: reader가 kill 시각을 가로질러 kill cap 내내 멈추면 완전한 출력도 표시(POSIX 순서 모델 10/10). 문구 nit(README 행에 대가 누락) → 메인 루프가 고쳐 **`b13c546c`** → push, CI 대기.
- **Codex 한도 해제 확인(00:0x "OK")** → Codex 교차 리뷰를 A 최종본에 실행 중(`/tmp/260-work/codex-tree` 임시 워크트리, effort high, 출력 `/tmp/260-work/codex-review.out`).
- 새 후속 초안 a3: windows py3.11에서 hard cap 직전 park가 0.11 s에 250~461회 ask로 돈다(판정 무관, CPU).
