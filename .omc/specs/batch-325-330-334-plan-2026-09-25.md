# Batch plan — #325 · #330 · #334 — 2026-09-25

`handoff-flake-cluster-2026-09-24.md`의 "바로 시작할 것"(#325 → #334)에 이후 순서 1번(#330)을 더한 배치다.
세 이슈는 파일이 겹치지 않는다(#330은 이슈 본문에서 `test_bash_tool_containment.py`를 #325 몫으로 뺐고,
#330 대상 파일 중 `core.py`를 인용하는 파일은 0개 — `grep -c core\.py`로 확인).

## 기준점 (2026-09-25 02:1x KST 확인)

- `origin/main` = 로컬 `main` = `f014fb46`. 마지막 코드 커밋은 `b13c546c`, 그 위는 문서 커밋뿐
  (`git log --format='' --name-only b13c546c..origin/main | sort | uniq -c` → `.omc/specs` 두 파일 + `docs/05`).
- 다음 빈 ADR 번호 **0249** — 미리 배정: #334 = 0249, #330 = 0250, #325 = 0251(새 ADR이 필요할 때만).
- pi: 스냅샷 `/tmp/pi-a328aa89a`(origin/main `a328aa89a`, 2026-09-23) 그대로. 오너 클론 HEAD `6671c6047`.
- Codex: 02:1x에 `codex exec "Reply with exactly: OK"` → `OK`(토큰 12,011). 쿼터 살아 있음.
- 보드: #325 · #334 · #330 → In progress.
- 공용 플러그인: `/tmp/flake-plugins/`(`coarse_clock_plugin.py` = #260 round3 `coarse260.py`,
  `stall_plugin.py` = #260 xreview) — 재부팅하면 사라진다. 원본은 `/tmp/260-work/`.

## 레인

| 레인 | 이슈 | 워크트리 | 브랜치 | 워크플로 |
| --- | --- | --- | --- | --- |
| A | #334 (P1) | `/tmp/wt-334` | `fix/334-prompt-holds-the-turn-through-its-tail` | `wf_5efe4af7-397` — design → critic → implement → review → fix? → verify, 전부 opus |
| B | #330 (P1) | `/tmp/wt-330` | `fix/330-tests-wallclock-sweep` | `wf_0fc44975-c3c` — implement → review → fix? → verify |
| C | #325 | `/tmp/wt-325` | `fix/325-abort-exit-drain-windows` | `wf_0fc44975-c3c` (B와 같은 워크플로, 병렬) |

- 스크래치: `/tmp/334-work/`, `/tmp/330-work/`, `/tmp/325-work/`. `.env`는 `/tmp/wt-334`에만 복사(라이브용, gitignore 확인).
- 워크플로 스크립트: `~/.claude/projects/-Users-handochan-dev-aelix-ai/c4c37442-a3c8-4a43-9695-d3402bca33c7/workflows/scripts/`
  의 `lane-334-prompt-holds-the-turn-wf_5efe4af7-397.js`, `lanes-330-325-test-verdicts-wf_0fc44975-c3c.js`.
  레인이 죽으면 `workflow-lane-dies-mid-write-resume` 메모리대로: 트리 감사 → 그 레인 프롬프트만 패치 →
  `Workflow({scriptPath, resumeFromRunId})`.
- 교차 리뷰 계획: #334(core, P1)는 워크플로 뒤 메인 루프가 Codex(`codex exec -C /tmp/wt-334 -s read-only … < /dev/null`,
  run_in_background 하나). #325·#330은 테스트만 바꾸므로 내부 리뷰·검증으로.

## #325 — 새 drain에서 재현 먼저 (핸드오프 지시)

- 진단 커밋 `1d105b2d`(`fix/325-abort-exit-drain-windows`, **머지 대상 아님** — `tests/tools/test_325_diag.py` + CI를
  진단만 돌리게 바꾼 `ci.yml`) → origin push → **CI 36033034335**.
- 설계: 이 케이스 그대로(루트 모양·`tick=0.05`·0.3 s 뒤 abort) 20회 quiet + 20회 `os.cpu_count()`개 CPU 버너 부하,
  레그 4개. tail이 쓸 때마다 자기 시계(`perf_counter`)를 stdout과 **사이드 파일**에 찍고(반환 뒤 쓰기까지 보인다),
  `_PipeReader.pin_after`/`proven` 스파이로 드레인 끝 종류(idle-proof / hard-cap / eof-or-reader)를 가른다.
- 결과: **160회 중 조기 종료 0**(windows 80회 포함). 전부 `end=hard-cap`, `unconfirmed=False`, `exit_code=0`.
  - windows tail의 최대 쓰기 간격: quiet 0.051-0.053 s, 부하 0.067-0.080 s (grace 0.1 s). `tail_gaps_over_grace=0` 전부.
  - 루트(pwsh) exit 스탬프: quiet 0.36-0.41 s, 부하 0.94-1.53 s. abort는 exit 뒤 0.1-0.3 s.
  - 원자료 `/tmp/325-work/diag-ci-36033034335.txt`, 진단 테스트 `/tmp/325-work/test_325_diag.py`.
- 판독: #325의 `1.375 s`는 `exit_code == 0`(먼저 단언됨)이 통과했으므로 watcher가 아니라 드레인 스스로의 조기 종료.
  새 drain에서 재현 안 됨. 코드상 남는 기전은 win32 잔여(`_PipeReader` WHAT IS LEFT — 빈 look 뒤 reader가 grace 동안
  굶으면 idle 규칙이 증명과 함께 끝남, #339)와, 모든 플랫폼에서 tail이 실제로 grace만큼 멈추는 경우(설계상 정상 종료).
  → 레인 C: win32 강제 분기 + held reader로 결정적 재현, 판정을 드레인 자신의 기록으로, 전제 실패는 스스로 설명하게.
- 로컬 브랜치는 `main`으로 리셋했다. 원격의 진단 커밋은 레인 C의 커밋을 **force-push**로 덮어 지운다.

## 실행 기록

- 02:2x KST — 두 워크플로 실행 시작.

## 1차 결과 (05:0x KST)

| 레인 | 워크플로 | 브랜치 tip | 결과 |
| --- | --- | --- | --- |
| #325 | `wf_0fc44975-c3c` | `c9fd7b11` (테스트 1파일) | review must 1 · should 2 → fix → **verify pass** (비차단 4: 메시지 "6 passed"→7, case 10 docstring 타이밍, 라벨 한 반복 창, `cap` 문구) |
| #330 | `wf_0fc44975-c3c` | `76b06bb6` (20파일, 제품은 주석 1줄) | review must 1 · should 6 → fix → **verify FAIL** — B1 `test_a_non_terminal_stop…`이 "timeout을 치렀다"를 더는 판정 안 함(옛 `>= 1.0`은 건전했다: win32에선 안 돌고 제품과 같은 시계), B2 ADR-0238 Status 줄 "no product code changed" 거짓 |
| #334 | `wf_5efe4af7-397` | `aeea1074` | design → critic(revise: must 1, should 5) → implement → review(must 2 문서 수치, should 3) → fix → **verify pass** (비차단: 낡은 주석 1줄, silent-overflow 경로의 claim 무테스트(M4b 생존), CHANGELOG에 "턴 abort 뒤 threshold compaction 안 함" 누락, ARM 7 TIMEOUT은 wedge 아님) |

- **#325가 뒤집은 전제(메인 루프 브리프가 틀렸다)**: "POSIX는 같은 hold에서 조기 종료 안 한다"·"#339가 남은 조기 종료를 맡는다" 둘 다 거짓. idle 규칙은 reader가 넘길 때 찍는 `last_chunk_at`으로 무장하므로 grace 동안 아무것도 안 넘기는 reader(정지한 writer, 굶은 reader, 프로세스 전체 정지)면 **모든 플랫폼에서** idle로 끝나고, #260 이후엔 `caught_up()` 증명과 함께 끝난다(명령 자신의 바이트는 안전, helper 꼬리만 잘림 — 설계대로). 강제 native/win32 분기 각 3/3 조기 종료 재현. → 테스트가 드레인 기록(`_DrainRecord`)으로 판정하고 idle 전제 실패는 최대 3회 재시도, 3회 모두면 이유를 말하며 실패.
- **#330이 주장한 제품 결함("flood 중 delegation 취소가 `cancelling`으로 3 s+ 멈춤, 6/6")은 메인 루프가 재현 못 함**: main 제품 코드로 `/tmp/flood-cancel-probe/test_flood_cancel_probe.py` 6/6, `…probe2.py`(레인과 같은 `wait_until` 전제 + 0~0.3 s) 15/15 모두 ≤ 0.055 s에 끝남. 이슈 등록 보류, 2차 라운드에 재측정·정리 지시.
- 공용 플러그인 추가: `/tmp/flake-plugins/stall_plugin_serial.py`(#325 레인 — 겹친 정지가 프로세스를 영구 SIGSTOP에 두던 결함 수정), `astall_plugin.py`(#330 레인 — asyncio spawn에서 무장, SIGCONT는 posix_spawn). 원래 `stall_plugin`은 Popen.wait만 무장해 asyncio 사다리 케이스는 한 번도 안 멈췄다(344개 중 30개만).

## 05:0x KST — 병렬 진행

- 세 브랜치 push → CI: #325 `c9fd7b11` **36051990669**(원격의 진단 커밋 `1d105b2d`는 force-with-lease로 덮음), #334 `aeea1074` **36052006149**, #330 `76b06bb6` **36052010894**. 뒤의 amend push가 진행 중인 run을 cancel할 수 있다(concurrency per-ref).
- **Codex 교차 리뷰 #334** 실행 중: `/tmp/334-work/codex-tree`(aeea1074 detached, 임시 워크트리), `-s workspace-write -c model_reasoning_effort=high`, 프롬프트 `/tmp/334-work/codex-prompt.txt`, 출력 `/tmp/334-work/codex-review.out`.
- **2차 라운드 `wf_526bf3b8-4bc`**: #330 fix r2(B1·B2 + N1 record_stop 경합("timed out, exit") + N2 flood 주장 재측정/정리·`await task` 상한 + N3·N4(print_channel 1.4 하한의 정지 내성)) → verify r2, #325 fix r2(문구 W1-W5) → verify r2.
- #334의 비차단 4건 + Codex 결과는 Codex가 끝나면 한 번의 fix 라운드로.

## 05:3x KST — Codex·후속 이슈·#334 3차

- **Codex(#334 `aeea1074`, effort high, 98,892 토큰, 한도 안 걸림)**: 1·3·5·6 결함 없음. **4번 새 결함**: release flush가 세션 append를 await하는 동안 `prompt()`를 취소하면 release는 제대로 되지만(phase idle) 앞선 쓰기가 큐에 남는다 → idle에서 새로 한 쓰기가 먼저 기록(`thinking writes ['low', 'medium']`, resume이 옛 값 복원), `dispose()`는 큐를 남긴 채 반환(`persisted_thinking []`). pi의 release flush는 동기라 이 취소 지점이 없다. 7번: `await harness.abort()`가 phase `turn`인 채 돌아온다(pi `abort()`는 `waitForIdle`) — ADR에 명시할 divergence. probe `/tmp/334-work/codex-probes/review334.py`, 전문 `/tmp/334-work/codex-review.out`.
- **후속 이슈 등록**: #344(P1 — OPENROUTER 키가 있으면 `--model <models.json 프로바이더>/<id>`도 OpenRouter로 감, 메인 루프 재현: 키 없으면 127.0.0.1로, 있으면 OpenRouter 400; pi는 알려진 프로바이더 접두 우선. #334 레인의 "diag_retry_abort_live.py 전제 실패"의 원인), #345 꼬리에 쌓인 입력이 다음 prompt까지 대기(pi는 `hasQueuedMessages()`면 이어 감), #346 RPC settled 이벤트 없음·abort가 idle 안 기다림, #347 abort가 중첩 compaction 요약을 안 끊음, #348 #330 밖 남은 벽시계 모양, #349 플레이크 플러그인을 저장소로, #350 굶은 reader가 idle 규칙을 발동(POSIX `drained()` 요구할지 — 오너 판단). #345-#350 P2.
- **#334 3차 `wf_0dea5c62-cd1`**: C4(Codex 결함) + C7(abort divergence 명시) + V1-V4(검증 비차단) + I1(ADR·메시지에 #345-#347·#335·#336 번호) → verify r3.
- **#334 CI 36052006149(aeea1074): ubuntu py3.11 1 실패** — `test_the_threshold_compaction_runs_inside_the_claim` `KeyError: 'waiter'`(11245 passed). 테스트 순서 경합: `_start`가 `watched.parked`를 기다리는 동안 즉답 provider의 prompt가 threshold compaction의 `session_compact` 훅까지 가서, 테스트가 `holder["waiter"]`를 넣기 전에 읽는다. py3.11의 `wait_for`는 내부 task를 하나 더 만들어 루프를 더 돈다. **로컬 py3.11로 3/3 재현**(`/tmp/334-work/py311-tree`, aeea1074 detached, `uv sync --all-packages --python 3.11`). 로컬 검증은 전부 py3.12였다 → 3차 뒤 4차 라운드에서 고치고, py3.11로도 돌린다.

## 06:0x KST — 브랜치 CI 결과 (첫 Windows 측정)

- **#325 `c9fd7b11` CI 36051990669: 6/6 green** (windows 두 레그 포함).
- **#334 `aeea1074` CI 36052006149: py3.11 두 레그(ubuntu·windows)만 실패**, 같은 한 테스트 `test_the_threshold_compaction_runs_inside_the_claim` `KeyError: 'waiter'`(windows py3.11: 1 failed, 11168 passed). py3.12 두 레그 green. → 3차 뒤 수정.
- **#330 `76b06bb6` CI 36052010894: windows 두 레그 실패**, 같은 한 테스트 `tests/oauth/test_resolve_config.py::test_a_command_that_closes_stdout_and_lingers_still_loses_its_tree` — 기록이 `[('hard_kill',)]`뿐이라 어느 사다리와도 안 맞음(1 failed, 11145 passed). 원인: 레인의 새 도우미 `_record_the_reap_ladder`가 `needle not in str(self.args)`로 거르는데, win32 argv 리스트의 `str()`은 경로 역슬래시를 이중으로 써서(`C:\\Users\\…`) pidfile 경로가 부분 문자열이 아니다 → `Popen.wait`가 하나도 안 잡힘. 테스트 도우미 결함, windows에서 결정적. 수정: `attach(pid)`가 아는 pid로 거른다. ubuntu 두 레그 green.

## 06:3x KST — #334 3차 결과, 4차 시작

- 3차 `wf_0dea5c62-cd1` → **`000bc8f5`**: C4는 `_drain_pending_session_writes()`(락 + 빌 때까지 flush)로 고쳤다. prompt() 진입, 공개 compact()/navigate_tree(), idle `set_thinking_level`, `dispose()`가 남은 큐를 먼저 비운다. C4 probe: 이전 `on disk ['low','medium'] restored medium` → `['medium','low'] restored low`, `dispose()` 이전 `pending [...] on disk []` → `pending [] on disk ['medium']`. C7(`harness.abort()`가 idle 전에 반환)은 ADR에 알고 택한 divergence로 적고 #346으로 넘겼다. V1-V4 처리, I1 이슈 번호 기입. 새 테스트는 aeea1074에서 6 red, 트리에서 43 passed, sabotage 146개 24행.
- **verify r3 FAIL(차단 1)**: release 루프는 `_drain_lock`을 안 잡는데, 그 근거("진입 drain이 in-flight drain을 기다렸다")가 **진입 drain 대기 중 취소되면** 거짓이다. release cancel 뒤 idle drain이 [medium, low]를 쥐고 append 중일 때, #2가 진입에서 락을 기다리다 취소되면 #2의 release가 'x'를 먼저 쓴다(`['x','medium','low'] live x restored low`). 도달성은 낮다(in-tree storage append는 yield 안 함 + 취소 두 번). 한 줄 수정(release를 locked drain으로) 측정 green. 비차단: fast path의 락 검사 무테스트(M3 생존), "모든 writer" 문구 과장(직접 writer 6곳), README 0023 행 미갱신, S4 행 설명 누락.
- **4차 `wf_7220ae76-bb2`**: R1 release를 locked drain으로(두 번째 취소에도 release) + R2 fast path 테스트(M3) + R3 문구 범위 + R4 README 행 + R5 S4 + **R6 py3.11 경합**(`holder["waiter"]`를 첫 await 전에; 같은 모양 전수 감사; `/tmp/334-work/py311-tree`에서 3.11로 3회). fix만 — 끝나면 메인 루프가 verify r4와 Codex(드레인 집중)를 병렬로.
- 오너 판단 후보(3차): (a) C7 divergence 수용·#346으로 연기, (b) 공개 compaction 중 `append_message()`가 다음 턴 turn_end flush가 아니라 **다음 prompt 진입에서, 그 prompt 메시지보다 먼저** 기록됨(pi의 "before the new prompt" 순서) — 권고: 둘 다 수용.

## 07:0x KST — 2차 라운드 결과 (#325 통과, #330 3차로)

- **#325 fix r2 → `53e80343`, verify r2 PASS**(비차단: `cap` 문구의 재무장 hard cap 경계, 공용 stall에서 late-tail 케이스가 전체 실행 1/3로 실패 — 이 변경과 무관, main 파일에서도 8a/8b floor가 매번 실패). force-push → CI: 36058758866 53e80343.
- **#330 fix r2 → `6d2313ab`, verify r2 FAIL(차단 1: flood 1.0 s 창의 이유가 틀림 — backlog가 아니라 (a) any-snapshot 전제는 spawn 전이라 아무것도 안 잼, (b) first-reduced-line 전제는 취소가 삼켜지는 턴에 떨어짐)**. B1(floor 복원, 건전성 주석)·B2·N1(`settled()`/`EXITED`)·N3·N4(드레인 자신의 두 시계 읽기) 닫힘.
- **N2가 찾은 제품 결함 → #351(P1)**: `RpcClient._await_terminator`의 finally `with suppress(BaseException): await death`가 `done`이 풀린 그 턴에 온 바깥 취소를 삼킨다 → 취소된 delegation이 턴 예산(600 s)까지 자식을 살려 둔 채 대기. **메인 루프가 main 코드로 재현: 12회 중 4회(모두 스냅샷 292개째 취소) 5 s·+10 s 뒤에도 미완료**, 나머지 0.054-0.058 s. #234의 bash watcher와 같은 모양(`asyncio.wait([task])`로 고친 전례). 앞서 "재현 못 함"은 내 probe가 그 한 턴을 비켜 갔기 때문 — 반증된 것에 적을 것.
- **#330 3차 `wf_3352d667-115`**: W1(Windows CI의 needle 필터 → pid 기반 + 같은 부류 전수 감사) + V1(창 이유 정정) + N1(#351) + N2 문구 + N3(pipe-holder 원시 기록 잔여) + N4 + N5(#348·#349 번호) → verify r3.

## 07:3x KST — #325 머지

- **main = `53e80343`** (#325, ff). 브랜치 CI 36058758866 6/6 green. 측정 쌍둥이: 네 케이스 모두 모든 레그에서 `end=cap attempt=1/3`(windows py3.11 abort 케이스 2.437 s). #325 닫음(코멘트에 재현·기전·수정 요약), 워크트리·로컬 브랜치 삭제. 원격 브랜치 `fix/325-abort-exit-drain-windows`는 오너 몫(분류기) — 끝에 한 줄 명령.
- #330·#334는 f014fb46 위라 새 main 위로 재스택 필요(파일 안 겹침 — #325는 `test_bash_tool_containment.py` 한 파일).

## 07:5x KST — #334 4차 결과, 재스택, 검증·Codex·CI 병렬

- 4차 `wf_7220ae76-bb2` → `f25e5248`: R1 release를 locked drain으로(P1 probe `['medium','low','x'] restored x`, 두 번째 취소에도 release), R2 fast path 테스트 2개(S20=M3, 4 red), R3 문구, R4 README 행, R5 S4 설명(15), **R6 `_start`를 `_begin`/`_start`로 나눠 holder를 첫 await 전에** — py3.11: 000bc8f5 3/3 red → 트리 150 passed ×3. sabotage 150개 26행. 라이브·TUI·Ctrl+C 재확인.
- **재스택**: main `53e80343` 위로 → **`e384ca4a`**(충돌 없음, citations OK 950, ruff OK). force-push → CI: 36061654576 e384ca4a.
- **병렬**: verify r4 `wf_5809a8f5-762`(opus xhigh, py3.11 포함) + **Codex 2차**(`/tmp/334-work/codex-tree` → e384ca4a, 프롬프트 `codex-prompt-r4.txt` — 드레인의 순서·유실·교착·happy path, 출력 `codex-review-r4.out`).
- **Codex 2차(e384ca4a, effort high, 87,971 토큰)**: (1) **순서 결함**: idle `set_thinking_level("medium")`의 직접 append가 진행 중(yield하는 storage)이면 락을 안 잡으므로, 이어진 prompt가 턴 중 큐잉한 "low"를 release drain이 먼저 쓴다 → `['low','medium'] restored medium`. 코드상 main에도 같은 모양(idle 직접 append + 턴 중 큐잉 → `_run` finally flush) = 기존 #314 계열로 판단. (2) 진행 중 append가 취소되면 그 항목만 유실 — 문서화된 #301 선택. (3) 조건부 교착: storage가 append 안에서 idle setter를 다시 부르면 자기 락을 기다림. (4) happy path 결함 없음. (5) 테스트 공백: drain 시작에 `await asyncio.sleep(0)` 사보타주 → 47 passed(빈 fast path의 새 suspension을 못 잡음). (6) ADR이 (1)의 공백을 안 적음. probe `/tmp/334-work/codex-probes-r4/{drain_probe,sabotage_probe}.py`.
- 방침(권고): (1)은 #334에서 알고 남긴 구멍으로 ADR에 적고 #314에 사례 추가, (3)은 storage 계약으로 ADR에, (5)는 "빈 fast path는 suspension 없음" 테스트로 고정 — verify r4 결과와 함께 5차 한 라운드로.

## 08:1x KST — #330 3차 통과, 재스택·CI / #334 1번 판정

- **#330 3차 `wf_3352d667-115` → `145214dd`, verify r3 PASS**: W1 `_record_the_reap_ladder`가 argv 텍스트 대신 `ProcessTree.attach`가 받은 pid로 거른다(probe: 옛 필터 `needle in str(args) = False`, 새 필터는 Windows argv에서 사다리를 기록), 같은 부류 전수 감사 — 다른 매치 없음. V1 창 이유 정정(any-snapshot=spawn 전 / first-reduced-line=#351의 삼키는 턴), N1 #351, N3 pipe-holder는 원시 기록 유지 + 잔여 위험 명시(오너 판단 후보), N4, N5(#348·#349). 비차단: astall도 SIGCONT를 잃음(→ #349 코멘트), resolve_config는 serial 정지 아래 통째 실행 시 손대지 않은 Popen 계수 케이스가 1-4개 실패(base도 같음).
- **재스택**: main `53e80343` 위로 → **`b805bcd2`**(충돌 없음, citations OK, drift 29 passed, ruff OK) → force-push → CI **36062300836**.
- **Codex 2차 1번은 기존 결함으로 판정**: `/tmp/drain-order-probe/probe.py`(실제 `_run`)로 main `53e80343`과 #334 `e384ca4a` 모두 `['low','medium'] live='low' restores='medium'` — 동일. #314에 사례로 코멘트. #334 5차에서 ADR에 "알고 남긴 구멍"으로 적는다.

## 08:4x KST — #334 4차 검증 통과, 5차(문서·테스트)

- **#334 `e384ca4a`: CI 36061654576 6/6 green**(py3.11 두 레그 포함 — 앞서 실패했던 것), **verify r4 PASS**(차단 0). 비차단: (T1) 자기 큐가 빈 release의 락 대기 무테스트(M10 생존, H6 probe로 동작 차이 확인), (D3) 한 번의 취소는 끝나지 않는 drain 뒤에서 release 못 함 — storage append가 영영 안 돌아오면 `turn`에 남음(두 번째 취소는 release; 선택한 거래지만 결과 미기재), (D4) "락 쥔 drain이 쓴다" 문구가 한 모서리에서 과함(다음 drain이 순서대로 씀, 유실·역순 없음).
- **5차 `wf_4c300b41-a47`**(제품 동작 변경 없음): T1(M10 red 테스트) + T2(Codex 5: 빈 fast path suspension 없음 고정) + D1(Codex 1·6: idle 직접 append 순서 공백 = 기존 #314, ADR에 알고 남긴 구멍) + D2(Codex 3: storage 재진입 계약) + D3 + D4 → verify r5.

## 09:0x KST — #330 머지

- **main = `b805bcd2`** (#330, ff, main `53e80343` 위). 브랜치 CI 36062300836 6/6 green. Windows 두 레그 모두 lingers 케이스가 **reader-timeout 사다리**(경고) — pwsh가 exec 안 함, 두 번째 kill 자리는 POSIX에서만 잰다. #330 닫음, 워크트리·로컬 브랜치 삭제. 원격 `fix/330-tests-wallclock-sweep`는 오너 몫.
- 남은 것: #334 5차 `wf_4c300b41-a47` → main `b805bcd2` 위로 재스택(충돌 후보: `citations.lock.json`, `docs/decisions/README.md` — #330은 0238 행, #334는 0011·0023 행) → CI → 머지 → 전체 스위트(main) → 핸드오프.

## 09:4x KST — #334 5차 통과, 재스택, 최종 CI

- 5차 `wf_4c300b41-a47` → `a306d705`, **verify r5 PASS**(차단 0): T1 `test_a_release_with_nothing_queued_waits_for_the_drain_appending_its_write`(S21=M10 red), T2 `test_the_drain_returns_without_suspending_when_nothing_is_queued_or_in_flight`(코루틴 수동 `send(None)` → StopIteration; S22=`sleep(0)` red), D1-D4를 ADR·README 행·CHANGELOG·주석·메시지에. 제품 코드는 주석만. py3.11 59 passed ×3, sabotage 152개 28행.
- 비차단 1: CHANGELOG의 storage 계약 문장 "waits … for ever"가 모든 경우를 말함 → 검증자 제안 그대로 "can wait … for ever"로(메인 루프, 2줄).
- **재스택**: main `b805bcd2` 위로 충돌 없이 → `db5a17af` → 문구 amend → **`aa193ed1`**(db5a17af와 차이는 CHANGELOG 2줄뿐; citations OK 950, ruff, types, 관련 99 tests). force-push → CI **36066285610**.

## 10:3x KST — #334 머지, main 전체 스위트

- **main = `aa193ed1`** (#334, ff, main `b805bcd2` 위). 브랜치 CI 36066285610 6/6 green(py3.11 포함). #334 닫음, 워크트리 셋(`/tmp/wt-334`, codex-tree, py311-tree)·로컬 브랜치 삭제. main CI: 53e80343 success(36061171011), b805bcd2 success(36064306673), aa193ed1 36068218996 진행 중.
- **main checkout 전체 스위트: 10 failed, 11238 passed, 23 skipped**(419.64 s) — 전부 `tests/cli/test_entry_router.py`(8)·`test_first_run_onboarding.py`(2), `'_FakeTTYStdin' object has no attribute 'fileno'`. 원인: main checkout 루트의 추적 안 되는 `.aelix/extensions/hello.py`(09-14부터) → 격리된 빈 신뢰 저장소 + cwd=저장소 루트 → 신뢰 프롬프트(prompt_toolkit) → 가짜 stdin. **이번 배치와 무관**: `.aelix` 없는 워크트리에서 f014fb46·53e80343·b805bcd2·aa193ed1 모두 61 passed, `.aelix`를 복사하면 f014fb46·b13c546c도 13 failed. → **#352**(P2). 깨끗한 워크트리(`/tmp/fullsuite-base`, aa193ed1)에서 전체 스위트 재측정 중.
- 이슈 코멘트: #336(재실행 `build_context` 중 abort 무시 R1/R5, Codex input-hook 재확인, T10은 #334가 닫음), #335(`_turn_state` 곁가지는 #334가 닫음), #341(`test_a_successful_root_returns…` 정지 플러그인 아래 실패).

## 11:0x KST — 마감

- **깨끗한 워크트리 전체 스위트(main `aa193ed1`): 11248 passed, 23 skipped, 97 warnings in 433.93 s** — 실패 0.
- **main CI `aa193ed1` 36068218996: 6/6 green.** 배치의 세 머지 모두 main CI green.
- 핸드오프: `.omc/specs/handoff-flake-batch-325-330-334-2026-09-25.md`. 후속 이슈 9건 #344-#352(#344·#351 P1).
- 남은 오너 몫: 원격 브랜치 3개 삭제(분류기) — 핸드오프 오너 판단 9의 한 줄 명령.
- **원격 브랜치 정리 완료**(오너가 실행): `fix/325-abort-exit-drain-windows`·`fix/330-tests-wallclock-sweep`·`fix/334-prompt-holds-the-turn-through-its-tail` 삭제, `git fetch --prune` 뒤 원격은 `main` 하나. 핸드오프 커밋 `2fec8551`의 main CI 36069984167 green.
