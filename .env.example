# Copy to `.env` and fill in for live LLM tests (Phase 2+).
# Phase 1.1 runs with a mock stream function and does not need any keys.

# OpenRouter
OPENROUTER_API_KEY=
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
